# CRM 功能设计方案 — F-01 / F-02 / F-03 / F-04

> 验收人：07 | 设计/研发：AI | 最后更新：2026-03-26

---

## 修改追踪表

| 编号 | 功能模块 | 问题描述 | 修改内容 | 涉及文件 | 状态 |
|------|----------|----------|----------|----------|------|
| F-01.1 | 数据中心 campaigns API | `take: 200` 导致总花费/MCC 费用不完整 | 去掉 200 行限制，总览与 MCC 费用改为全量去重 Campaign 聚合；显示行仍限 200 行，返回 `rowMeta` 告知前端 | `crm-mvp/src/app/api/user/data-center/campaigns/route.ts` | 已完成 |
| F-01.2 | 数据中心 campaigns API | `cnyDailyStats` SQL 日期范围用 `<=` 导致多算一天 | 修改为 `< endExclusive`，统一左闭右开区间 | 同上 | 已完成 |
| F-01.3 | 数据中心前端 | 佣金明细弹窗只有"按平台账号"维度，与 MCC 花费口径不一致 | 新增"按商家"维度 Tab；API 同时返回 `byAccount` 和 `byMerchant` | `crm-mvp/src/app/user/data-center/page.tsx`、`commission-by-account/route.ts` | 已完成 |
| F-01.4 | 数据中心前端 | 总览与表格行的口径差异无提示 | 统计卡片下方增加口径说明文字；当 `rowMeta.isLimited` 时显示行数限制提示 | `crm-mvp/src/app/user/data-center/page.tsx` | 已完成 |
| F-02.1 | 广告提交 → 文章图片同步 | `ad_creatives.image_urls`（广告选图）与 `articles.images` 完全独立，用户选的广告图不会写入文章 | 广告提交成功后，将 `image_urls` 同步写入关联文章的 `images` 字段 | `crm-mvp/src/app/api/user/ad-creation/submit/route.ts` | 已完成 |
| F-02.2 | 商家文章生成 | `triggerArticleGeneration` 始终用爬虫图覆盖 `articles.images`，即使用户已通过广告选图 | 新增判断：若文章已有 images 则不覆盖 | `crm-mvp/src/app/api/user/merchants/route.ts` | 已完成 |
| F-02.3 | AI 文章生成 | `ensureImagesPresent` + `redistributeImages` 为独立的图片注入/重排逻辑，与共享布局不一致 | 改用共享的 `article-image-layout.ts` 中的布局工具 | `crm-mvp/src/lib/article-gen.ts` | 已完成 |
| F-02.4 | 共享图片布局工具 | 缺少统一的图片布局管理模块 | 新建 `article-image-layout.ts`，提供布局解析/推断/构建/规范化/重建等纯函数 | `crm-mvp/src/lib/article-image-layout.ts`（新建） | 已完成 |
| F-03.1 | 文章发布页（从广告跳转） | 从广告提交页跳转过来时，加载文章但不使用 `articles.images` 重建内容 | 加载文章时读取 `images` 字段，用 `buildDefaultArticleImageLayout` + `rebuildArticleContentWithLayout` 重建 content | `crm-mvp/src/app/user/articles/publish/page.tsx` | 已完成 |
| F-03.2 | 文章发布页 | 保存草稿/发布前不保存最终 content | 保存草稿和发布前均先 PUT 保存 title + content 到数据库 | 同上 | 已完成 |
| F-03.3 | 远程发布器 | `ensurePublishContentImages` 在 content 无图时自动补图，违反"预览即最终"原则 | 移除 `ensurePublishContentImages` 调用，发布器只做路径规范化+本地化 | `crm-mvp/src/lib/remote-publisher.ts` | 已完成 |
| F-03.4 | 远程发布器 | `syncArticleImages` 不处理 `/api/` 开头的内部上传图片路径 | 扩展 `syncArticleImages` 识别 `/api/user/ad-creation/upload-image/` 路径，读取本地文件后上传到远程站点 | 同上 + `crm-mvp/src/lib/upload-image-reader.ts`（新建） | 已完成 |
| F-04.1 | 数据中心表格 | `#` 序号列显示自增行号，不是广告系列业务序号，且无实际意义 | 移除 `#` 序号列；后端排序改为状态优先 + 广告系列名称中的序号（如 `013`）升序 | `crm-mvp/src/app/user/data-center/page.tsx`、`crm-mvp/src/app/api/user/data-center/campaigns/route.ts` | 已完成 |
| F-04.2 | 图片上传到广告系列 | `campaign_asset_operation` + `MARKETING_IMAGE` 不兼容搜索广告，导致图片上传 100% 失败 | 改为 `field_type: "AD_IMAGE"`，兼容搜索广告系列 | `submit/route.ts` | 已完成 |
| F-04.3 | AI 文案质量分优化 | AI prompt 过于通用，生成的文案质量分低（广告效力"较差"） | 采用07提示词重构 prompt：折扣+物流优先展示、第一条品牌关联、描述中有且仅有一条含折扣+物流、控制大写字符、字数核查、符合 Google Ads 平台规范 | `ai-service.ts` | 已完成 |
| F-04.4 | 关键词匹配类型 | 所有关键词硬编码为 PHRASE，无法选择匹配类型 | 每个关键词 Tag 旁增加下拉选择器（广泛/词组/完全），新增关键词时可选匹配类型 | `ad-preview/[id]/page.tsx` | 已完成 |
| F-04.5 | 促销扩展 (Promotion) | 图2中的促销素材不可选 | 新增促销扩展模块：促销内容、折扣类型（百分比/金额）、促销代码、促销场合、货币、落地页 URL；通过 `promotion_asset` 提交 | `ad-preview/[id]/page.tsx`、`submit/route.ts` | 已完成 |
| F-04.6 | 价格扩展 (Price) | 图2中的价格素材不可选 | 新增价格扩展模块：价格类型（品牌/活动/产品类别等）、多个价格项（标题+描述+价格+货币+URL）；通过 `price_asset` 提交 | 同上 | 已完成 |
| F-04.7 | 致电扩展 (Call) | 图2中的致电素材不可选 | 新增致电扩展模块：国家代码+电话号码；通过 `call_asset` 提交 | 同上 | 已完成 |
| F-04.8 | 结构化摘要 (Structured Snippet) | 图2中的结构化摘要不可选 | 新增结构化摘要模块：标题类型（品牌/课程/目的地等13种）+ 多个值（3~10个）；通过 `structured_snippet_asset` 提交 | 同上 | 已完成 |
| F-04.9 | 图片 OCR 文字检测 | 图片可能含文字，不符合广告图片要求 | 新增 OCR 检测 API（tesseract.js），图片上传后自动检测文字，含文字图片标红警告；图片区增加品牌关联+无文字要求提示 | `check-image/route.ts`（新建）、`ad-preview/[id]/page.tsx` | 已完成 |
| F-04.10 | 扩展本地化链路 | DE 广告出现英语促销与 USD 货币，扩展默认值未按国家收口 | 新增 `ad-market` 国家配置贯穿扩展抓取、前端默认值与 Google Ads 提交；促销/价格/致电/结构化摘要按国家自动回填语言、币种、国家码与兼容 header | `crm-mvp/src/lib/ad-market.ts`、`crm-mvp/src/app/api/user/ad-creation/generate-extensions/route.ts`、`crm-mvp/src/app/api/user/ad-creation/submit/route.ts`、`crm-mvp/src/app/user/ad-preview/[id]/page.tsx` | 已完成 |
| F-04.11 | 卖点扩展 (Callout) | 免费配送、免费退货被拆成两条，且不同语言下易重复 | 新增多语言 shipping/return 识别、AI 结果归一化、卖点合并与去重；若同时命中配送和退货政策，优先压缩为一条 ≤25 字文案 | `crm-mvp/src/app/api/user/ad-creation/generate-extensions/route.ts` | 已完成 |
| F-04.12 | AI 文案净化 | 存在过期文案、重复文案、无意义文案，且标题/描述商业吸引力不足 | 在 `ai-service.ts` 增加过期/重复/无意义过滤、商业意图校验、本地化 fallback 与更强提示词；调用链改为“关键词 + 参考样本”辅助生成 | `crm-mvp/src/lib/ai-service.ts`、`crm-mvp/src/app/api/user/merchants/route.ts`、`crm-mvp/src/app/api/user/ad-creation/status/route.ts`、`crm-mvp/src/app/api/user/ad-creation/generate-more/route.ts` | 已完成 |
| F-04.13 | 商家页 AI 设定（硬规则） | 用户无法在商家页集中维护 `ai_rule_profile`，与 Semrush 优选/文案链路脱节 | 顶部四卡 `md=6`；外层 `Form`（`component={false}`）统一绑定 `adForm`；「节日营销」右侧新增「AI 设定（硬规则）」卡：分块字段 + 偏好/禁止词（tags）+ 政策自检开关 + 完整提示词；支持 `.txt/.md` 上传读入 `prompt_text`；与「广告投放设置」共用保存（PUT `/api/user/ad-settings`） | `crm-mvp/src/app/user/merchants/page.tsx` | 已完成 |
| F-04.14 | 扩展生成 AI 规则注入 | 站内链接描述、卖点、促销/价格 AI 提取未读取用户硬规则 | `POST generate-extensions` 读取 `ad_default_settings.ai_rule_profile`；`generateSitelinks` / `generateCallouts` / `extractPromotionWithAI` / `extractPriceWithAI` 在提示词末尾拼接 `buildAiRulePrompt`（按 section：sitelinks / ad_copy / compliance） | `crm-mvp/src/app/api/user/ad-creation/generate-extensions/route.ts` | 已完成 |
| F-04.15 | RSA 广告效力对齐（Google 官方口径） | 界面常提示「描述不够独特」「站内链接不足」；显示路径为空 | 依据 [Google Ads Help：RSA 广告效力](https://support.google.com/google-ads/answer/9921843)：① 描述生成 prompt 明确「相对标题的独特性」+ 描述间 Jaccard 更严 + 与标题 token 重叠过滤；② 文案入库时若 path1/path2 为空则 `suggestDisplayPaths`（关键词/品牌 slug）；③ 预览页在启用站内链接且有效条数 &lt;6 时提示「至少 6 条」；`generate-more` 描述接口传 `headlines_for_uniqueness` | `crm-mvp/src/lib/ai-service.ts`、`status/route.ts`、`merchants/route.ts`、`generate-more/route.ts`、`ad-preview/[id]/page.tsx` | 已完成 |
| F-04.16 | 去掉首条标题固定 + 站内链接强校验 | pinned_field 减少 RSA 组合数→降分；站内链接 404 在 Google Ads 被警告 | ① RSA 提交不再设置 `pinned_field: "HEADLINE_1"`，让 Google 自由组合所有 15 条标题；② 后端 submit 在组装 operations 之前逐条 HEAD 探测站内链接，404/410/超时直接 `apiError` 拦截；③ 前端 `handleSubmit` 改为先自动批量验证所有 `urlStatus` 非 valid 的链接，全部 valid 后才继续，无效则拦截并提示修正 | `submit/route.ts`、`ad-preview/[id]/page.tsx` | 已完成 |
| F-04.17 | 站内链接自愈——后端验证 + 补充探测 + 前端自动过滤 | 爬取的站内链接可能无效（404/软404/超时），用户需要手动排查 | ① `generateSitelinks` 完成后新增 `verifySitelinkCandidates`：用 `verifyLinks` 逐条 HEAD 验证，踢掉无效链接；不足 6 条时用 `getExtraProbePaths`（3 组备用路径）探测补充，最多 3 轮，直到 6 条或穷尽；② 前端收到站内链接后自动调 `check-url` API 逐条验证，无效的**自动移除**而非保留标红，仅保留通过验证的链接 | `generate-extensions/route.ts`、`ad-preview/[id]/page.tsx` | 已完成 |

---

## 设计原则

### F-01 数据统计口径
- **总览指标（总花费/总佣金/ROI）** = 全部去重 Campaign 聚合，不受表格行数限制
- **表格行** = 最多 200 行，按状态排序（启用优先），合计行数据与总览一致
- 前端增加口径说明，当行数被截断时显示提示
- 佣金明细提供"按商家"和"按平台账号"两个维度，均为独立聚合

### F-02 广告图片 → 文章图片同步
- `ad_creatives.image_urls` 是广告最终选图的权威来源
- 广告提交成功时同步到 `articles.images`
- 文章 AI 生成阶段仅在文章无图时填入爬虫图（初始占位）
- 共享布局工具 (`article-image-layout.ts`) 统一管理图片在 HTML 中的位置

### F-03 发布一致性
- **预览即最终**：文章发布页预览的 HTML 就是最终发布到站点的内容
- 发布器只允许：路径规范化 + 图片本地化（下载外链/读取本地上传图到远程站点）
- 发布器不允许：自动补图、重排图片位置、改变图片内容或顺序
- 发布前先保存最终 content 到数据库，确保发布器读到的是用户确认的版本

---

## 数据流示意

```
创建商家 → triggerArticleGeneration
  ├─ 爬虫图 → ad_creatives.image_urls (初始)
  └─ 爬虫图 → articles.images (仅初始，无图时)
        ↓
用户在广告预览页选图/调整
  └─ 最终选图 → ad_creatives.image_urls (更新)
        ↓
提交广告 (ad-creation/submit)
  ├─ ad_creatives.image_urls 保存
  └─ 同步到 articles.images ← 关键同步点
        ↓
文章发布页加载 (articles/publish)
  ├─ 读取 articles.images
  ├─ rebuildArticleContentWithLayout → 重建 HTML
  └─ 预览展示
        ↓
发布 / 保存草稿
  ├─ PUT 保存最终 content 到数据库
  └─ publish-to-site → 路径规范化 + 图片本地化（不改内容）
```

---

## F-04 广告创建优化设计原则

### 图片上传修复
- 搜索广告系列（SEARCH Campaign）必须使用 `AD_IMAGE` field_type
- `MARKETING_IMAGE` 仅适用于展示广告/Performance Max，搜索广告会报 `FIELD_TYPE_INCOMPATIBLE_WITH_CAMPAIGN_TYPE`
- 图片仍通过 `campaign_asset_operation` 挂载到 Campaign 级别

### AI 文案质量分优化
- 采用07提供的三步提示词策略重构 AI prompt
- 标题规则：第一条品牌关联、折扣和物流各一条并优先展示、控制大写字符、≤30字符严格核查
- 描述规则：有且仅有一条同时包含折扣+物流信息、50-90字符范围、品牌特色语言
- 所有文案必须符合 Google Ads 平台规范，不得欺骗误导消费者

### 关键词匹配类型
- 支持三种匹配类型：广泛匹配(BROAD)、词组匹配(PHRASE)、完全匹配(EXACT)
- 每个关键词 Tag 旁有独立的下拉选择器，可随时切换
- 新增关键词时可预选匹配类型，默认词组匹配
- 后端 submit 已支持读取 matchType，无需额外修改

### 广告扩展（新增4种）
- **促销扩展 (Promotion)**：支持百分比/金额折扣、促销代码、促销场合（黑五/圣诞等15种）、货币选择
- **价格扩展 (Price)**：支持9种价格类型、最多8个价格项（标题+描述+价格+URL）
- **致电扩展 (Call)**：支持8个国家代码 + 电话号码
- **结构化摘要 (Structured Snippet)**：支持13种标题类型、3~10个值
- 所有扩展通过 `campaign_asset_operation` 挂载到 Campaign 级别

### 二轮验收修正（本地化 + 质量）
- 促销/价格/致电/结构化摘要的默认提交字段必须按目标国家自动兜底，禁止再次回落到固定 `en`、`USD`、`US`
- 结构化摘要 header 内部保持 Google Ads 兼容标准值，前端只做展示层本地化，避免因本地化字符串导致提交不兼容
- Callout 若同时命中配送与退货政策，优先合并为一条 ≤25 字文案，并在 AI 输出后再次做去重与归一化
- 标题与描述必须过滤显式过期日期、重复语义与无意义填充语，且保留商业吸引力与品牌相关性

### 图片 OCR 文字检测
- 使用 tesseract.js（纯 JS OCR 引擎）检测图片中的文字
- 图片上传后自动触发 OCR 检测，含文字图片标红警告
- 过滤低置信度噪声（confidence < 50），至少2个有意义词才判定为含文字
- 图片区域增加明确提示：需与品牌强关联、不要有文字

### MCC 费用同步修复（2026-03-26）
- **问题根因**：`getExchangeRate` 在汇率获取全部失败时 fallback 返回 `1.0`，导致 CNY 原币被当 USD 存储，费用虚高 ~6.9 倍
- **修复 1**：`exchange-rate.ts` — 汇率不可用时返回 `0`，调用方跳过该记录不写入
- **修复 2**：`daily-sync/route.ts` — cron Sheet 同步范围从"昨天+今天"扩大到"近 7 天"，历史错误可被后续同步覆盖修正
- **修复 3**：`sync/route.ts` — Sheet 同步、API 同步、今日实时同步三条写入路径均增加 `rate <= 0` 跳过保护
- **数据修复**：对 wj01 的 czsmcc11241（CNY）和 ZWJMCC0226（USD）执行 force_full_sync，07 确认修复后数据准确
