---
description: 
alwaysApply: true
---

# 部署架构

- 全栈 Next.js 应用部署在阿里云服务器（47.239.193.33），使用密码登录
- 代码推送至 GitHub（https://github.com/starrats111/Google-Data-Analysis），通过 GitHub Actions 自动 SSH 部署到服务器
- Cloudflare 作为 CDN 代理（域名：google-data-analysis.top）
- 宝塔面板运行在亚马逊服务器（52.74.221.16），使用密钥登录
- 自动部署流程：push main → GitHub Actions → SSH → git pull → build → pm2 restart

# 服务器资源约束（极其重要）

服务器配置极低：**2核 CPU / 2G 内存 / 40G 磁盘**，非常容易崩溃，必须严格遵守以下原则：

- **禁止**在服务器上执行高内存操作（如 `npm install` 全量安装、大规模构建等）
- **禁止**同时运行多个耗资源的进程
- **禁止**在服务器上运行 `prisma generate` 或 `prisma migrate` 等重操作时不先确认资源状况
- 所有服务器操作必须**逐步执行**，每步确认无误后再进行下一步
- 安装依赖或部署前，先检查磁盘和内存使用情况（`df -h` / `free -m`）
- 遇到服务器无响应时，**不要反复重试**，先等待恢复
- 日志文件需定期关注大小，防止磁盘被日志撑满
