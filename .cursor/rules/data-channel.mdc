---
alwaysApply: true
---

# 数据通道铁律（07 2026-08-21 拍板，D-264 成文）

## 核心原则

**读走 Sheet，写走 API。没有第三条路。**

- **读数据**（花费/点击/展示/预算/名字/状态/IS/QS/出价……一切"从 Google 拿回来看"的东西）：
  一律走 MCC Google Sheet（统一 Ads Script 落表 → CRM `sheet-sync` 同步入库）。
- **写操作**（建广告、启停、调预算、调出价等 mutate）：必须走 Google Ads API（Sheet 干不了写），
  且必须经过 Token 池（`pickCredential` 轮换 + 持久化无权限标记 + 429 换凭证重试）。

## API 读取豁免清单（封闭列表，新增必须 07 拍板并记入设计方案）

| 豁免项 | 理由 | 状态 |
|---|---|---|
| mutate 后的即时反查确认（如 toggle 停后 1.5s 反查） | 写操作的一部分，验证写结果 | 永久豁免 |
| CID 清单同步（customer_client 列表） | Sheet 无此数据，且低频、人工触发为主 | 永久豁免 |
| TokenProbe 凭证体检 | 池子健康检查本身 | 永久豁免 |
| D-238 ads-metrics-sync（IS/QS/MaxCpc 每日拉取） | 历史决策；07 2026-08-21 已定调**违背原则，要迁回 Sheet**（D-264） | 过渡期兜底，迁完即关 |
| Sheet 被 Google 封禁的 MCC 走 API 应急补数（D-239/D-253 模式） | 主通道断了的降级路径 | 应急豁免，恢复即停 |

## 禁止行为

- ❌ 任何新功能用 API 拉取指标类数据（哪怕"只拉一次""量很小"）——先问：Sheet 能不能给？能给就改脚本加列
- ❌ 绕过 Token 池直接使用单一凭证调 API（脚本、cron、路由一律禁止）
- ❌ 在设计方案里不写明数据通道就开工

## 方案确立质量闸（07 2026-08-21 要求：禁止"讨论完方案还遗留 BUG"）

任何功能设计/修改，**方案给 07 确认之前**必须完成以下自查并在设计方案表格中体现：

1. **数据通道声明**：本功能读什么走 Sheet、写什么走 API，逐项列明；
2. **字段口径核对**：涉及的每个库字段，列出**全部现有写入点**（grep 齐全），确认单位/币种/时区语义一致——不一致必须先解决口径冲突再开工（教训：D-263 `daily_budget` 美元/人民币混写，D-244 与 HM-D50 各写各的）；
3. **失败与不确定路径显式设计**：探测/请求失败、超时、限流等"不确定态"必须有明确分支，禁止把"不确定"折叠进"否"（教训：D-261 探测二态误报 CID 问题）；
4. **消费方盘点**：改动字段/接口的所有读取方（前端列、报表、cron、Hermes）列全，逐一确认不被破坏；
5. **与既有 D 条目对撞检查**：新方案与设计方案中已有结论是否冲突，冲突处显式标注并请 07 裁决；
6. **回滚路径**：写明改动文件清单与回滚方式。

上线后验收同样按此清单复核一遍，缺一项不得报"完成"。
