# 审核报告-设计（第十三轮：迭代 1.8 设计方案复核）

> 审核人员：AI 审核官  
> 验收人员：07  
> 审核对象：`设计方案.md`（迭代 1.8，含 CR-017）  
> 交叉核对范围：`backend/app`、`frontend/src`、`backend/alembic`  
> 审核日期：2026-02-28  
> 结论：⚠️ 有条件通过（2 项严重 + 3 项中等需先闭环）

---

## 一、审核结论（给07的直结论）

本轮设计方案文档层面已经完成第十一轮口径修订（S-11/S-12、M-18~M-20、L-11/L-12），结构与编号整体清晰，且 API 路径文字口径已与目标实现统一。

但按“设计承诺 vs 现网实现”复核，当前仍存在阻塞项，主要是**文档已修、代码未落地**：

1. **S-11R（严重）**：`user_feedback` 类型级隐私过滤未在 `notifications.py` 落地；  
2. **S-12R（严重）**：脚本模式前端引导仍写“编辑者”，与设计“查看者”冲突。

此外还有 3 项中等一致性问题（OPT-008/OPT-009 的实现状态与文档表述耦合不清、平台标准化能力与实际函数差距）。

---

## 二、问题分级清单

### A. 严重问题（阻塞终审）

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| S-11R | 反馈隐私边界“文档已写”但“代码未落地” | 设计要求 `type=user_feedback` 仅 wj07 + manager 可见（`设计方案.md:1088`）；现网 `notifications.py` 仅按 `_visible_user_ids` 过滤，未做类型级特殊规则（`backend/app/api/notifications.py:21-30,41-47`） | wj07 与 leader 同组时，leader 可能看到反馈，触发隐私越权 | 在通知查询层加 `user_feedback` 类型级过滤分支；并补 manager/leader/member/wj07 四类验收用例 |
| S-12R | 脚本模式共享权限引导仍为“编辑者” | 设计要求“查看者”（`设计方案.md:934,940`）；前端页面仍写“知道链接的任何人—编辑者”（`frontend/src/pages/MccAccounts.jsx:734,749`） | 用户按界面配置会放开公开写权限，Sheet 可被篡改，影响 ROI/报表链路 | 立即改前端文案为“查看者”，并保留“脚本执行主体单独授予编辑权限”说明 |

### B. 中等问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| M-21 | OPT-008 在文档写了接口与服务方法，但现网尚未出现对应实现 | 文档写 `/api/bids/toggle-status`、`/batch-toggle-status` 与 `toggle_campaign_status`；现网 `bid_management.py` 未见两个接口定义，相关服务方法未检索到 | 非技术验收容易误判为“功能可用” | 在“审核确认”中明确“设计通过，代码待开发”；验收必须附接口联调证据 |
| M-22 | OPT-009 文件变更清单包含多项目标文件，现网尚未出现 | 文档列 `merchant_platform_sync.py`、`crypto.py`、`merchant_account_relationship.py`、`009_opt009_merchant_platform_sync.py`；现网未检索到 | 易造成实施阶段误排期（以为可联调） | 文件清单标题增加“目标变更（待开发）”，避免被当作已完成清单 |
| M-23 | 平台标准化要求 URL 变体，但现网 `normalize_platform_code` 仍仅覆盖代码/后缀数字 | 文档要求 URL 映射（`设计方案.md:1354-1358`）；现网函数主要处理 `PM1→PM`、`CG1→CG`，不处理 URL（`api_analysis_service.py:25-51`） | 迁移/同步时 URL 平台名无法归一，平台统计会继续分裂 | 明确“迁移脚本内联映射 + 函数扩展 URL 解析”双保险，再进入 OPT-009 开发 |

### C. 轻微问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| L-14 | 代码注释口径仍有“查看/编辑”残留 | 设计要求禁止“查看/编辑”术语（L-11）；`google_sheet_sync.py` 文件头仍写“查看/编辑”（`backend/app/services/google_sheet_sync.py:6`） | 误导实施人员按高权限共享 | 将注释统一为“公开可查看（只读共享）” |

---

## 三、本轮已对齐项（正向确认）

1. 文档中 OPT-008 路径已统一为 `/api/bids/*`。  
2. 文档中联盟账号更新路径已统一为 `PUT /api/affiliate/accounts/{id}`。  
3. 文档已补 URL 变体映射要求与迁移命名 `009_opt009_merchant_platform_sync.py`。  
4. 审核问题修正对照表与 CR-017 记录可互相印证。  
5. 章节结构（十～十八）当前无错位问题。

---

## 四、给07的执行建议（优先级）

### 第一批（阻塞项）

1. 先关 **S-11R**：通知权限层实现 `user_feedback` 类型级过滤并完成角色用例验收。  
2. 先关 **S-12R**：前端脚本模式引导从“编辑者”改为“查看者”，并复测引导弹窗与表单帮助文案一致性。

### 第二批（实施前口径）

3. 关闭 **M-21/M-22**：在设计文档中显式标注 OPT-008/009 为“设计完成、代码待开发”。  
4. 关闭 **M-23**：补充 URL 解析实现方案（函数 + 迁移脚本）后再排期开发。

### 第三批（文案清洁）

5. 关闭 **L-14**：清理 `google_sheet_sync.py` 的“查看/编辑”注释残留。

---

## 五、最终判定

- **可继续提审**：是  
- **可直接终审归档**：否  
- **综合结论**：⚠️ 有条件通过（先关闭 2 严重 + 3 中等问题）

---

> 审核签字：AI 审核官  
> 提交对象：07  
> 备注：本报告仅做审查结论，不涉及代码改动。
