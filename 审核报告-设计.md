# 审核报告-设计（第十轮：计划方案复核）

> 审核人员：AI 审核官  
> 验收人员：07  
> 审核对象：`设计方案.md`（当前迭代 1.6，含 CR-013）  
> 交叉核对范围：`backend/app`、`frontend/src`、`backend/alembic`  
> 审核日期：2026-02-28  
> 结论：⚠️ 有条件通过（2 项严重问题需先关闭）

---

## 一、审核结论（给07的直结论）

本轮相比上轮已有明显改进：

- `team_id` 口径已从“强制已落地”修正为“现网可空 + 后续强制”；
- OPT-005 接口路径口径已统一到 `/api/mcc/accounts/{mcc_id}/...`；
- 问题反馈系统设计契约已补齐；
- OPT-003/004 状态与文件清单冲突已部分收敛。

但仍存在 2 项阻塞级问题（安全/隐私边界），暂不建议终审归档。

---

## 二、问题分级清单

### A. 严重问题（阻塞终审）

| 编号 | 问题 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| S-11 | 反馈可见范围“设计口径”与“实现能力”仍有潜在穿透 | 设计写 `leader/member/employee` 不可查看他人反馈（`设计方案.md:1063-1065`）；但通知权限实现为 leader 可见同组通知（`backend/app/api/notifications.py:26-31`），反馈通知写入 `wj07`（`backend/app/api/feedback.py:32, 60-67`） | 若 `wj07` 与 leader 同组，leader 可能看到反馈通知，隐私边界依赖数据分组而非规则本身 | 在通知权限层对 `type=user_feedback` 增加强约束（仅 `wj07` + manager 可见，或按最终制度明确） |
| S-12 | 脚本模式共享权限文档与前端实际引导不一致 | 设计已改“任何人可查看 + 脚本执行主体具备编辑权限”（`设计方案.md:928`）；但前端引导仍写“任何人—编辑者”（`frontend/src/pages/MccAccounts.jsx:721`） | 用户按页面操作会扩大公开写权限，Sheet 被篡改后会污染成本/ROI/预警等全链路数据 | 立即修正前端引导文案，保持与设计一致：公开仅查看，写权限只给执行主体 |

### B. 中等问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| M-15 | 优化项总览仍写“共6项（OPT-001~006）”，但正文新增 OPT-007 契约节 | 总览为 6 项（`设计方案.md:14`）；正文新增“问题反馈系统（OPT-007）”（`设计方案.md:1046`） | 版本范围边界不清，验收清单与实施清单不一致 | 二选一：A) OPT-007 纳入总览与状态表；B) 去掉 OPT-007 编号，改为“补充约束章节” |
| M-16 | 商家专项章节编号仍有错位 | 章节标题为“十六”，子标题仍为“15.1”（`设计方案.md:1307, 1311`）；“十七”章子标题为“16.x”（`设计方案.md:1403, 1407`） | 评审、提审引用时易混淆，影响追踪和问责 | 统一章节编号，确保标题号与子标题号一致 |
| M-17 | OPT-003/004“待开发”与“审核确认通过”的表述容易误读 | 实施状态为待开发（`设计方案.md:35-36`），审核确认表仍显示“方案设计通过”（`设计方案.md:1272-1273`） | 非技术方容易误判为“已开发通过” | 在审核确认表增加“仅设计通过，尚未开发”标记 |

### C. 轻微问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|---|---|---|---|---|
| L-11 | 代码注释术语仍残留“查看/编辑”混合口径 | `google_sheet_sync.py` 文件头仍写“查看/编辑”（`backend/app/services/google_sheet_sync.py:6`） | 团队成员按注释执行时可能回到高权限共享习惯 | 同步更新注释，统一到“公开可查看（只读共享）” |

---

## 三、本轮已对齐项（正向确认）

1. **`team_id` 口径**：已改为“现网可空 + 后续强制”，与当前实现一致。  
2. **OPT-005 接口路径**：设计与实现均为 `/api/mcc/accounts/{mcc_id}/...`。  
3. **反馈系统契约**：已有独立章节，字段、入口、回滚已说明。  
4. **OPT-003/004 状态**：主状态表已明确为待开发，且文件清单改为“待实现项”。

---

## 四、给07的执行建议（按优先级）

### 第一批（阻塞项，必须先关闭）

1. 关闭 S-11：把 `user_feedback` 的可见范围做成代码规则，而不是依赖人员分组。  
2. 关闭 S-12：修正前端脚本模式共享权限文案为“查看者”。

### 第二批（并行关闭）

3. 关闭 M-15：统一 OPT 编号边界（6项 vs 7项）。  
4. 关闭 M-16：修复章节编号错位。  
5. 关闭 M-17：给“设计通过但未开发”加醒目标识。

---

## 五、最终判定

- **可继续提审**：是  
- **可直接终审归档**：否  
- **综合结论**：⚠️ 有条件通过（先完成 2 项严重问题闭环）

---

> 审核签字：AI 审核官  
> 提交对象：07  
> 备注：本报告仅做审查结论，不涉及代码改动。