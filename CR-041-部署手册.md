# CR-041 部署手册（在服务器上执行）

## 快速部署步骤

在服务器上（47.239.193.33）执行以下命令：

### 步骤1：拉取最新代码
```bash
cd ~/Google-Data-Analysis
git pull origin main
```

### 步骤2：停止旧服务
```bash
pkill -f 'uvicorn.*app.main' || true
sleep 2
```

### 步骤3：进入后端目录并激活虚拟环境
```bash
cd ~/Google-Data-Analysis/backend
source venv/bin/activate
```

### 步骤4：启动新服务
```bash
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
sleep 3
```

### 步骤5：验证服务健康状态
```bash
curl -s http://127.0.0.1:8000/health
```

预期输出：`{"status":"ok"}` 或类似的健康检查响应

### 步骤6：运行诊断脚本验证数据准确性
```bash
cd ~/Google-Data-Analysis/backend
python scripts/diagnose_l7d_data_accuracy.py
```

### 步骤7：查看服务日志
```bash
tail -50 /tmp/backend.log
```

---

## 完整部署脚本（一键执行）

将以下内容保存为 `deploy_cr041.sh`，然后执行 `bash deploy_cr041.sh`：

```bash
#!/bin/bash

set -e

echo "=========================================="
echo "CR-041 部署和验证流程"
echo "=========================================="

# 1. 拉取最新代码
echo ""
echo "📥 步骤1：拉取最新代码..."
cd ~/Google-Data-Analysis
git pull origin main
echo "✅ 代码拉取完成"

# 2. 进入后端目录
echo ""
echo "📂 步骤2：进入后端目录..."
cd backend
echo "✅ 已进入 backend 目录"

# 3. 激活虚拟环境
echo ""
echo "🐍 步骤3：激活虚拟环境..."
source venv/bin/activate
echo "✅ 虚拟环境已激活"

# 4. 停止旧服务
echo ""
echo "🛑 步骤4：停止旧服务..."
pkill -f 'uvicorn.*app.main' || true
sleep 2
echo "✅ 旧服务已停止"

# 5. 启动新服务
echo ""
echo "🚀 步骤5：启动新服务..."
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
sleep 3
echo "✅ 新服务已启动"

# 6. 验证服务健康状态
echo ""
echo "🏥 步骤6：验证服务健康状态..."
HEALTH=$(curl -s http://127.0.0.1:8000/health || echo "failed")
if [[ $HEALTH == *"ok"* ]] || [[ $HEALTH == *"healthy"* ]]; then
    echo "✅ 服务健康状态正常"
else
    echo "⚠️  服务健康检查响应: $HEALTH"
fi

# 7. 运行诊断脚本
echo ""
echo "🔍 步骤7：运行 L7D 数据准确性诊断..."
python scripts/diagnose_l7d_data_accuracy.py
echo "✅ 诊断完成"

# 8. 检查服务日志
echo ""
echo "📋 步骤8：检查服务日志（最后30行）..."
tail -30 /tmp/backend.log
echo "✅ 日志检查完成"

echo ""
echo "=========================================="
echo "✅ 部署和验证流程完成！"
echo "=========================================="
echo ""
echo "后续步骤："
echo "1. 在前端验证 L7D 数据是否与 Google Ads 一致"
echo "2. 检查费用、CPC、预算等数据的准确性"
echo "3. 确认所有标黄数据无误"
echo ""
```

---

## 验证清单

部署完成后，请按照以下清单进行验证：

### 后端验证
- [ ] 代码已从 GitHub 拉取
- [ ] 旧服务已停止
- [ ] 新服务已启动
- [ ] 健康检查通过
- [ ] 诊断脚本运行成功
- [ ] 日志中无错误信息

### 前端验证（在 Cloudflare 部署的前端上）
- [ ] 打开数据中心 → L7D 分析
- [ ] 检查标黄的 L7D 数据（佣金、订单数、出单天数）
- [ ] 与 Google Ads 数据对比：
  - [ ] 费用（Cost）准确
  - [ ] CPC 准确
  - [ ] 预算（Budget）准确
  - [ ] 点击数（Clicks）准确
  - [ ] 展示数（Impressions）准确
- [ ] 确认所有数据与 Google Ads 显示一致
- [ ] 无数据遗漏或重复

### 数据准确性验证
- [ ] L7D 佣金数据与数据中心一致
- [ ] L7D 订单数与数据中心一致
- [ ] L7D 出单天数准确
- [ ] 平台代码匹配 100% 成功
- [ ] 商家ID匹配 100% 成功

---

## 故障排查

### 问题1：服务启动失败
```bash
# 查看详细日志
tail -100 /tmp/backend.log

# 检查端口是否被占用
lsof -i :8000

# 强制杀死占用端口的进程
fuser -k 8000/tcp
```

### 问题2：诊断脚本报错
```bash
# 检查数据库连接
cd ~/Google-Data-Analysis/backend
python -c "from app.database import SessionLocal; db = SessionLocal(); print('数据库连接成功')"

# 检查模型导入
python -c "from app.models.user import User; print('模型导入成功')"
```

### 问题3：数据不准确
```bash
# 查看 Google Ads 数据
cd ~/Google-Data-Analysis/backend
python -c "
from app.database import SessionLocal
from app.models.google_ads_api_data import GoogleAdsApiData
from datetime import date, timedelta

db = SessionLocal()
end_date = date.today() - timedelta(days=1)
begin_date = end_date - timedelta(days=6)

data = db.query(GoogleAdsApiData).filter(
    GoogleAdsApiData.date >= begin_date,
    GoogleAdsApiData.date <= end_date
).all()

print(f'总记录数: {len(data)}')
for d in data[:5]:
    print(f'日期: {d.date}, 费用: {d.cost}, CPC: {d.cpc}, 预算: {d.budget}')
"
```

---

## 部署完成后的确认

部署完成后，请告诉我：

1. ✅ 服务是否成功启动？
2. ✅ 诊断脚本是否运行成功？
3. ✅ 前端 L7D 数据是否与 Google Ads 一致？
4. ✅ 费用、CPC、预算等数据是否准确？
5. ✅ 是否有任何错误或异常？

