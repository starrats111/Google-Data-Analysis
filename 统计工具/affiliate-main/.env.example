# 服务器配置
PORT=3000
NODE_ENV=production

# JWT密钥（生产环境必须修改为随机字符串）
# 生成方法: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-REQUIRED

# 加密密钥（用于平台账号密码加密，必须是32字符）
# 生成方法: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
ENCRYPTION_KEY=your-32-character-encryption-key

# 数据库配置（本项目使用SQLite，无需配置PostgreSQL）
# SQLite数据库文件会自动创建在项目根目录: database.db
