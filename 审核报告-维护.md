# 审核报告-维护（第七轮：v1.2b 维护方案复核）

> 审核人员：AI 审核官  
> 验收人员：07  
> 审核对象：`维护方案.md`（v1.2b）  
> 交叉核对范围：`backend/app`、`frontend/src`、`backend/alembic`、`backend/scripts`  
> 审核日期：2026-02-28  
> 结论：⚠️ 有条件通过（1 项严重 + 1 项中等需先闭环）

---

## 一、审核结论（给07的直结论）

维护方案 v1.2b 相比 v1.2a 已完成明显修订，文档层面已对上轮问题作出回应：

- 已补写 M-008 的隐私边界条款（MS-01）；
- 已将 M-002 从 P1 降级为 P3（MM-02）；
- 已补充 M-008 的 `sender_id` 迁移与历史兼容策略（MM-03）；
- 回滚段落已改为“建议 revert，不建议 reset --hard”。

但本轮按“文档承诺 vs 现网代码”复核后，仍有两类未闭环风险：

1. **MS-01R（严重）**：`user_feedback` 类型级隐私过滤在现网代码仍未落地；
2. **MM-01R（中等）**：M-007 宣称的 `_create_client()` 目标行为（优先 service account）尚未在代码实现。

因此本轮结论为：**有条件通过**，需先关闭上述两项再进入实施验收。

---

## 二、问题分级清单

### A. 严重问题（阻塞）

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| MS-01R | M-008 已写隐私规则，但通知权限代码仍未做 `user_feedback` 类型级强过滤 | 维护方案要求 `user_feedback` 仅“提交人 + wj07 + manager”可见（`维护方案.md:388-394`）；现网 `notifications.py` 仍按通用 `_visible_user_ids` 过滤，未见按 `type=user_feedback` 特殊分支（`backend/app/api/notifications.py:21-30,41-47`） | 若 wj07 与 leader 同组，leader 仍可能看到反馈，隐私边界被穿透 | 将 M-008 拆成前置子任务：先改 `notifications.py` 做类型级过滤并补接口测试，再做反馈管理页与回复功能 |

### B. 中等问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| MM-01R | M-007 文档已纠偏，但现网 `_create_client()` 仍是 `use_service_account` 分支逻辑 | 方案写“需改造后达成 service account 优先”（`维护方案.md:333-347`）；现网仍是 `if use_service_account: ... else OAuth ...`（`backend/app/services/google_ads_service_account_sync.py:173-201`） | 若未先完成改造就验收“脚本模式可出价管理”，会出现结果不稳定或继续失败 | 把 M-007 验收拆两步：①先验 `_create_client` 逻辑改造；②再验 BidManagement 全链路可用 |

### C. 轻微问题

| 编号 | 问题 | 证据 | 风险 | 建议 |
|------|------|------|------|------|
| ML-02 | M-008 依赖文件（`FeedbackManage.jsx`、`sender_id` 迁移）在现网尚未出现，状态应明确“待开发” | 维护方案列为新增项，代码库尚未出现对应页面/字段/迁移文件 | 可能被非技术方误读为“已可上线” | 在 M-008 标题旁补“设计完成，待开发落地”，避免状态误解 |

---

## 三、逐项核查结果（M-001 ~ M-008）

| 编号 | 审核结论 | 说明 |
|------|----------|------|
| M-001 | ✅ 通过 | 方案口径清楚；现网 `google_ads_service_account_sync.py` 仅保留 1 处 `time.sleep`（错误退避），与“请求限速走限速器、退避保留 sleep”的目标一致 |
| M-002 | ✅ 通过 | 已按审核建议降级为 P3，符合“先确认调用路径再清理”原则 |
| M-003 | ✅ 通过 | backfill 05:00 与现网调度一致，参数降级合理 |
| M-004 | ✅ 通过 | `CUSTOMER_DELAY=2.5`、`MCC_COOLDOWN=60` 与维护口径一致 |
| M-005 | ✅ 通过 | 部署强调 `source venv/bin/activate` + `--workers 1`，符合 SQLite 约束 |
| M-006 | ✅ 通过 | 影响范围与修复后动作描述完整，口径比前版更严谨 |
| M-007 | ⚠️ 有条件通过 | 文档纠偏正确，但代码改造未落地前不能判定“恢复可用” |
| M-008 | ⚠️ 有条件通过 | 方案结构完整，但 MS-01R（类型级隐私过滤）未落地前不得上线回复功能 |

---

## 四、给07的执行建议（优先级）

### 第一批（阻塞）

1. 先关闭 **MS-01R**：在 `notifications.py` 落地 `user_feedback` 类型级过滤，并提交接口验收证据（manager/wj07/leader/member 四类账号各 1 组用例）。

### 第二批（实施前）

2. 关闭 **MM-01R**：按维护方案改造 `_create_client()` 为“service account 优先，OAuth 回退”，并给出脚本模式 MCC 的真实联调结果。  
3. M-008 进入开发时同步标注“待开发落地”，避免和“方案闭环”混淆。

---

## 五、最终判定

- **可继续提审**：是  
- **可直接实施全部项**：否  
- **综合结论**：⚠️ 有条件通过（先关闭 1 严重 + 1 中等）

---

> 审核签字：AI 审核官  
> 提交对象：07  
> 备注：本报告仅做审查结论，不涉及代码改动。
