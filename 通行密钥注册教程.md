# Google 通行密钥（Passkey）注册教程

> 适用机器：本机（Windows 10 专业版 22H2，ASUS 物理机）
> 排查与结论日期：2026-08-13

## 一、最终结论

**这台机器日常登录用的是 Windows 内置 Administrator 账户（SID 以 -500 结尾的那个应急账户）。
微软从设计上就禁止该账户使用 Windows Hello（PIN / 人脸 / 指纹），任何设置、策略、修复都绕不过去。**
这就是「设置 → 登录选项」里三个 Windows Hello 项全部显示"此选项当前不可用"的根本原因。

排查过程中确认过以下各项均正常（供以后参考）：

| 检查项 | 结果 |
|---|---|
| 会话类型 | Console 本机，非远程桌面 |
| TPM | 存在、已启用、就绪 |
| 系统 / Chrome | Win10 22H2 / Chrome 151，均支持通行密钥 |
| 组策略 / MDM / NoConnectedUser | 无拦截 |
| NGC 系统文件、CloudExperienceHost 组件 | 完整 |
| 依赖服务（NgcSvc、VaultSvc、KeyIso、TokenBroker 等） | 正常 |
| 蓝牙 | 无（手机扫码方式不可用） |

## 二、排查期间做过的系统改动

1. **Ngc 目录权限重建（保留，属真实修复）**：
   `C:\Windows\ServiceProfiles\LocalService\AppData\Local\Microsoft\Ngc` 的权限曾被某优化工具重置成继承上级权限（正常应仅 SYSTEM 可访问）。已删除空目录并由系统按正确权限自动重建。
2. `FilterAdministratorToken`（曾改为 1 排障）：**已回滚为 0**，恢复原状。
3. `AllowDomainPINLogon`（曾加为 1 排障）：**已删除**，恢复原状。

回滚项在下次重启后完全恢复原行为，无遗留影响。

## 三、注册 Google 通行密钥的可行方案

### 方案 A：在手机上创建（推荐，免费，2 分钟）

1. 手机（安卓或 iPhone）登录同一个 Google 账号（Gmail / Chrome App 均可）。
2. 手机浏览器打开 `myaccount.google.com` → 安全性 → 通行密钥 → 创建通行密钥。
3. 用手机指纹 / 面容 / 锁屏密码确认即可。
4. 之后电脑上登录 Google 时选密码或原有二步验证；通行密钥保存在手机上，需要时用手机验证。

注意：**不要**在电脑上选「使用其他设备扫二维码」——本机无蓝牙，扫码后会一直转圈连不上。

### 方案 B：买一把 FIDO2 硬件安全密钥

- 普通 U 盘**不行**（无安全芯片，Windows/Google 都不认）。
- 需要 FIDO2/U2F 专用密钥：CanoKey、飞天 ePass FIDO、YubiKey 等，几十到一百多元。
- 硬件密钥不依赖 Windows Hello，内置 Administrator 账户也能用：
  插上 USB → Google 通行密钥页面点「创建通行密钥」→ 选「安全密钥」→ 按提示触摸密钥并设置密钥 PIN。
- 「设置 → 登录选项 → 安全密钥」这一项在本机是可用的（未被灰掉），可佐证此路通。

### 方案 C：新建一个日常账户（治本但折腾，不急可不做）

新建一个普通管理员账户并迁移日常使用后，Windows Hello 的全部功能即恢复可用。
代价是所有软件配置、浏览器登录态等都要迁移，工作量大，仅在确有需要时考虑。

## 四、常见问题

- **Google 强制注册通行密钥吗？** 不强制，提示页可点「以其他方式登录 / 暂不」跳过。
- **通行密钥丢了会锁死账号吗？** 不会，Google 保留密码 + 原有二步验证方式。
- **手机换机/丢失？** 到 Google 账号安全设置删掉旧通行密钥、用新设备重建即可。
