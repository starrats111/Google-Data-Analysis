# 傻瓜式安装教程 - 一步一步来

## 📋 准备工作

### 检查1：确认 PowerShell 窗口已打开
- 看到蓝色背景的窗口
- 提示符类似：`PS C:\Users\Administrator>`

### 检查2：确认 Node.js 已安装
在 PowerShell 中输入：
```powershell
node --version
```
**应该显示**：`v24.13.0` 或类似版本号

如果显示错误，说明 Node.js 未安装，需要先安装。

---

## 🎯 第一部分：后端安装（Python）

### 步骤1：打开 PowerShell 窗口
- 按 `Win + X`
- 选择 "Windows PowerShell" 或 "终端"

### 步骤2：进入后端目录
**复制粘贴以下命令，然后按回车：**

```powershell
cd "D:\Google Analysis\backend"
```

**预期结果**：提示符变成 `PS D:\Google Analysis\backend>`

### 步骤3：创建虚拟环境（如果还没有）
**复制粘贴以下命令，然后按回车：**

```powershell
python -m venv venv
```

**预期结果**：没有错误信息，提示符回到 `PS D:\Google Analysis\backend>`

**等待时间**：约10-30秒

### 步骤4：激活虚拟环境
**复制粘贴以下命令，然后按回车：**

```powershell
.\venv\Scripts\Activate.ps1
```

**预期结果**：提示符前面出现 `(venv)`，变成：
```
(venv) PS D:\Google Analysis\backend>
```

**如果提示"无法加载脚本"**：
- 执行：`Set-ExecutionPolicy RemoteSigned -Scope CurrentUser`
- 输入 `Y` 确认
- 然后重新执行步骤4

### 步骤5：升级 pip
**复制粘贴以下命令，然后按回车：**

```powershell
python -m pip install --upgrade pip
```

**预期结果**：看到 "Successfully installed pip-xx.x.x" 或 "Requirement already satisfied"

**等待时间**：约10-20秒

### 步骤6：安装基础依赖包
**复制粘贴以下命令，然后按回车：**

```powershell
pip install fastapi uvicorn sqlalchemy python-jose passlib python-multipart openpyxl python-dotenv
```

**预期结果**：看到很多下载信息，最后显示 "Successfully installed ..."

**等待时间**：约2-5分钟（取决于网速）

**如果很慢**：可以使用国内镜像（复制粘贴）：
```powershell
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple fastapi uvicorn sqlalchemy python-jose passlib python-multipart openpyxl python-dotenv
```

### 步骤7：安装 pydantic（旧版本，避免编译问题）
**复制粘贴以下命令，然后按回车：**

```powershell
pip install pydantic==2.4.2
```

**等待时间**：约30秒-1分钟

**然后执行：**

```powershell
pip install pydantic-settings==2.0.3
```

**等待时间**：约30秒

### 步骤8：验证安装是否成功
**复制粘贴以下命令，然后按回车：**

```powershell
python -c "import fastapi; print('FastAPI: OK')"
```

**预期结果**：显示 `FastAPI: OK`

**然后执行：**

```powershell
python -c "import sqlalchemy; print('SQLAlchemy: OK')"
```

**预期结果**：显示 `SQLAlchemy: OK`

**然后执行：**

```powershell
python -c "import uvicorn; print('Uvicorn: OK')"
```

**预期结果**：显示 `Uvicorn: OK`

**然后执行：**

```powershell
python -c "import pydantic; print('Pydantic: OK')"
```

**预期结果**：显示 `Pydantic: OK`

**如果都显示 "OK"**：✅ 安装成功，继续下一步！

**如果有错误**：告诉我具体错误信息。

### 步骤9：初始化数据库
**复制粘贴以下命令，然后按回车：**

```powershell
python scripts/init_db.py
```

**预期结果**：显示 `数据库表创建完成！`

### 步骤10：初始化用户数据
**复制粘贴以下命令，然后按回车：**

```powershell
python scripts/init_users.py
```

**预期结果**：显示：
```
创建经理账户：manager / manager123
创建员工账户：employee1 / employee1123
...
用户初始化完成！
```

### 步骤11：初始化联盟平台
**复制粘贴以下命令，然后按回车：**

```powershell
python scripts/init_platforms.py
```

**预期结果**：显示：
```
创建平台：Amazon Associates
创建平台：Commission Junction
...
联盟平台初始化完成！
```

### 步骤12：启动后端服务
**复制粘贴以下命令，然后按回车：**

```powershell
uvicorn app.main:app --reload
```

**预期结果**：显示：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**✅ 后端服务已启动！**

**重要**：保持这个 PowerShell 窗口打开，不要关闭！

---

## 🎯 第二部分：前端安装（Node.js）

### 步骤1：打开新的 PowerShell 窗口
- 按 `Win + X`
- 选择 "Windows PowerShell" 或 "终端"
- **或者**：在任务栏右键点击 PowerShell，选择 "新建窗口"

**重要**：后端服务的窗口要保持打开！

### 步骤2：进入前端目录
**复制粘贴以下命令，然后按回车：**

```powershell
cd "D:\Google Analysis\frontend"
```

**预期结果**：提示符变成 `PS D:\Google Analysis\frontend>`

### 步骤3：安装前端依赖
**复制粘贴以下命令，然后按回车：**

```powershell
npm install
```

**预期结果**：看到很多下载信息，最后显示：
```
added XXX packages, and audited XXX packages in XXs
```

**等待时间**：约2-5分钟（取决于网速）

**如果很慢**：可以使用国内镜像（先执行）：
```powershell
npm config set registry https://registry.npmmirror.com
```
然后重新执行 `npm install`

### 步骤4：启动前端服务
**复制粘贴以下命令，然后按回车：**

```powershell
npm run dev
```

**预期结果**：显示：
```
VITE v5.x.x ready in XXX ms

➜  Local:   http://localhost:3000/
```

**✅ 前端服务已启动！**

**重要**：保持这个 PowerShell 窗口打开，不要关闭！

---

## ✅ 第三部分：验证和测试

### 验证1：检查后端服务
1. 打开浏览器
2. 访问：http://localhost:8000/docs
3. **应该看到**：FastAPI 的 API 文档页面（有交互式界面）

### 验证2：检查前端服务
1. 打开浏览器（或新标签页）
2. 访问：http://localhost:3000
3. **应该看到**：登录页面（白色卡片，有用户名和密码输入框）

### 测试登录
1. 在登录页面输入：
   - **用户名**：`manager`
   - **密码**：`manager123`
2. 点击"登录"按钮
3. **应该看到**：进入系统主页面

---

## 📝 完整命令清单（复制粘贴版）

### 后端（在一个 PowerShell 窗口）

```powershell
cd "D:\Google Analysis\backend"
python -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install fastapi uvicorn sqlalchemy python-jose passlib python-multipart openpyxl python-dotenv
pip install pydantic==2.4.2
pip install pydantic-settings==2.0.3
python -c "import fastapi; print('FastAPI: OK')"
python -c "import sqlalchemy; print('SQLAlchemy: OK')"
python -c "import uvicorn; print('Uvicorn: OK')"
python scripts/init_db.py
python scripts/init_users.py
python scripts/init_platforms.py
uvicorn app.main:app --reload
```

### 前端（在另一个 PowerShell 窗口）

```powershell
cd "D:\Google Analysis\frontend"
npm install
npm run dev
```

---

## ❓ 常见问题

### Q1: 提示"无法加载脚本"？
**A:** 执行：
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```
输入 `Y` 确认

### Q2: pip install 很慢？
**A:** 使用国内镜像：
```powershell
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple [包名]
```

### Q3: npm install 很慢？
**A:** 使用国内镜像：
```powershell
npm config set registry https://registry.npmmirror.com
```

### Q4: 提示"端口被占用"？
**A:** 检查是否有其他程序在使用 8000 或 3000 端口

### Q5: 安装失败，显示错误？
**A:** 把完整的错误信息告诉我，我会帮您解决

---

## 🎉 成功标志

✅ 后端服务运行：看到 `INFO: Uvicorn running on http://0.0.0.0:8000`
✅ 前端服务运行：看到 `Local: http://localhost:3000/`
✅ 可以访问：http://localhost:3000 看到登录页面
✅ 可以登录：使用 manager / manager123 成功登录

---

**按照以上步骤，一步一步执行，每一步都等待完成后再执行下一步！**















