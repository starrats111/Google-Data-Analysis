# 最简单安装方法 - 直接复制粘贴

## 🚀 方法1：使用一键安装脚本（最简单）

### 后端安装（在一个 PowerShell 窗口）

```powershell
cd "D:\Google Analysis"
.\一键安装后端.ps1
```

等待完成后，执行：
```powershell
uvicorn app.main:app --reload
```

### 前端安装（在新的 PowerShell 窗口）

```powershell
cd "D:\Google Analysis"
.\一键安装前端.ps1
```

等待完成后，执行：
```powershell
npm run dev
```

---

## 📋 方法2：手动复制粘贴命令

### 后端（在一个 PowerShell 窗口，按顺序执行）

```powershell
cd "D:\Google Analysis\backend"
python -m venv venv
.\venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install fastapi uvicorn sqlalchemy python-jose passlib python-multipart openpyxl python-dotenv
pip install pydantic==2.4.2
pip install pydantic-settings==2.0.3
python scripts/init_db.py
python scripts/init_users.py
python scripts/init_platforms.py
uvicorn app.main:app --reload
```

### 前端（在新的 PowerShell 窗口，按顺序执行）

```powershell
cd "D:\Google Analysis\frontend"
npm install
npm run dev
```

---

## ✅ 验证

1. 访问 http://localhost:8000/docs - 应该看到 API 文档
2. 访问 http://localhost:3000 - 应该看到登录页面
3. 登录：manager / manager123

---

## 💡 提示

- 每个命令复制粘贴后，按回车执行
- 等待命令完成后再执行下一个
- 后端和前端需要在不同的 PowerShell 窗口运行
- 保持两个窗口都打开，不要关闭








