# 01 系统交接手册

> 生成时间：2026-08-06，全部数据来自当天 SSH 实地勘察 4 台生产服务器，非文档推测。
> 机密（密码/密钥/Token）一律不写进本文件，统一存放在本机 `C:\Users\Administrator\.infra\服务器总账.md`（不进 git）。

---

## 第 0 课：你接手的到底是什么

07 留下的不是一套系统，是 **4 套独立代码 + 4 台生产服务器**，跑的是同一门生意：
**在 Google Ads 上买流量 → 导到联盟商家（affiliate）的落地页 → 赚佣金差价。**

赚钱公式：`利润 = 联盟佣金 − Google 广告花费`。所有系统的功能都在服务这条公式的某一环。

| 系统 | 一句话定位 | 服务器 | 域名 | 状态 |
|---|---|---|---|---|
| **CRM**（主系统） | 42 人团队用的投放工作台：领商家 → AI 生成广告 → 批量上 Google → 拉回佣金算 ROI | 43.156.142.141（腾讯云） | google-data-analysis.top | 在跑，日花费约 $2000 |
| **Hermes**（自动投放体） | 无人值守机器人：自己选商家、自己建广告、自己止损，通过飞书跟人对话 | 43.165.170.242（腾讯云海外） | 借 CRM 的 /hermes-approve | 在跑，日发布 12-46 条 |
| **kylink**（换链接引擎） | 生成/轮换联盟追踪后缀，防止链接被联盟风控，附带刷点击养链 | 43.106.49.28（阿里云新加坡） | xc.kyads.net | **核心功能已死 6 天** |
| **kyads**（对照中控） | 与 CRM 同目标、异逻辑的另一套上广告中控，作参考 | 216.67.230.8（hostingsg） | kyads.net | 在跑，代码 7/1 停更 |

四套都是 **Next.js + Prisma + MySQL/MariaDB**，代码互相独立，但共享同一批 Google MCC 账号、同一批联盟账号、同一个住宅代理账号。

**关键认知：这三套系统会互相打架。** 它们操作的是同一批 Google 广告账户。CRM 里手动暂停一个广告，Hermes 可能会把它续投回来（HM-D39「远端真值同步」就是为解决这个而写的）。改任何一套之前，先想清楚另外两套会不会跟着动。

---

## 第 1 课：怎么连上去（照抄即可）

四台机器的数据库**都只监听 127.0.0.1**，不能从本地直连。07 定的规矩（`.cursor/rules/deployment.mdc`）是：**排查问题直接 SSH 到生产服务器上查，不启本地环境、不开隧道。**

```powershell
# CRM（主系统）
ssh -i C:\Users\Administrator\.ssh\xlx0310.pem ubuntu@43.156.142.141

# Hermes（自动投放体）
ssh -i C:\Users\Administrator\yangma0612.pem ubuntu@43.165.170.242

# kylink（换链接）
ssh -i C:\Users\Administrator\.ssh\xc0224.pem root@43.106.49.28

# kyads（对照中控）
ssh -i C:\Users\Administrator\.ssh\xc0224.pem root@216.67.230.8
```

### PowerShell 有个坑，必须知道

Windows PowerShell 会吞掉传给 ssh 的引号，导致 SQL 语句在服务器上被截断报错。**正确做法**：把命令写成 `.sh` 或 `.sql` 文件，`scp` 上去，服务器上先 `tr -d '\r'` 去掉 Windows 换行符，再执行。

```powershell
scp -i <密钥> myquery.sh ubuntu@43.156.142.141:/tmp/q.sh
ssh -i <密钥> ubuntu@43.156.142.141 "tr -d '\r' < /tmp/q.sh > /tmp/qq.sh && bash /tmp/qq.sh"
```

`\r` 这件事害过人：Hermes 曾因为 `cron-env.sh` 被 Windows 以 CRLF 覆盖，`\r` 拼进 token，**全部定时任务 400 停摆 20 小时**（记录在总账 HM-D19）。

### 各系统的日常巡检命令

```bash
# CRM：进程 / 健康 / 日志 / 定时任务结果
pm2 list                                        # 进程名固定叫 ad-automation
curl -s localhost:20050/api/health              # 内存、DB 延迟
tail -f /home/ubuntu/.pm2/logs/ad-automation-error.log
ls -la /var/log/crm-cron/                       # 每个 cron 一个日志文件
crontab -l

# Hermes：服务 / 健康 / 日志
sudo systemctl status hermes-ads
curl -s localhost:8787/api/health                # 返回任务状态分布，最快的体检
sudo tail -f /var/log/hermes-ads/service.log

# kylink
sudo systemctl status kylink
sudo journalctl -u kylink -f

# kyads
pm2 list
tail -f /var/log/kyads-*.log
```

### 查数据库（在服务器上执行）

```bash
# CRM，密码从 .env 里取，不要手打
cd /home/ubuntu/Google-Data-Analysis/crm-mvp
DBP=$(grep -oP 'DATABASE_URL="mysql://crm:\K[^@]+' .env | head -1)
mysql -ucrm -p"$DBP" google-data-analysis --table -e "SELECT ..."

# Hermes 用的是 SQLite，且服务器没装 sqlite3 命令，要用 node 读
node --experimental-sqlite -e "
const {DatabaseSync}=require('node:sqlite');
const db=new DatabaseSync('/home/ubuntu/hermes-ads/data/hermes-ads.db',{readOnly:true});
console.table(db.prepare('SELECT status, COUNT(*) n FROM ad_runs GROUP BY status').all());
"
```

### 三条红线（07 立的，继续遵守）

1. **本地连的都是生产库**，禁止在本地跑 `prisma migrate` / `db push` / 任何 `purge:*` `cleanup:*` 脚本。
2. **改数据库结构必须走 Prisma Migration 流程**，不手写 DDL。
3. **写入数据库的数据必须来自真实 API 响应**，不许臆测、估算、填补空字段（`.cursor/rules/data-integrity.mdc` 里有真实教训案例）。

---

## 第 2 课：CRM —— 主系统（你 80% 的时间会在这里）

### 它替 42 个人干什么

服务器有 3 个组、42 个账号：**wj 新城组 / jy 交垟组 / yz 邮政组**，各有组长（`wjzu`/`jyzu`/`yzzu`），外加 `manager`、`admin` 两个管理员。业务量（2026-08-05 实数）：日花费 $1,837、日佣金 $4,857、1,679 单、32,692 次点击、**2,008 个在投广告系列**。

一个投手在 CRM 里的完整动线：

1. **认领商家** —— 从联盟平台同步来的商家库里挑一个（`user_merchants` 表 162 万行，是全库最大的表）
2. **建广告草稿** —— `campaigns` / `ad_groups` / `ad_creatives` 三张表
3. **AI 编排** —— 系统抓商家官网、SemRush 取关键词、AI 写标题描述和 sitelink（日志里的 `[Intellicenter]` `[SitelinkAI]` `[GenRunner]` 就是它）
4. **预览确认后提交** —— `POST /api/user/ad-creation/submit` 一次批量 Mutate 到 Google Ads
5. **数据回流** —— Google Ads Script 把花费写进 Google Sheet，CRM 读 Sheet；佣金从 11 个联盟平台的 API 拉回来
6. **看数据中心** —— 花费、佣金、订单、ROI、CPC、EPC 摆在一起看盈亏

### 支撑上面这条主线的配套功能

| 功能 | 干什么 | 在哪看 |
|---|---|---|
| 换链接（后缀池） | 联盟追踪链接会被风控，所以准备一池子后缀轮换着用。池里 23,161 条待用 | `suffix_pool` / `suffix_assignments` 表 |
| 刷点击（养链） | 主动给联盟链接刷点击，让链接看起来是活的 | `kyads_click_tasks` / `kyads_click_task_items` |
| 住宅代理 | 刷点击和爬取都走 kookeey 住宅代理，按国家出口 | `kyads_proxies` / `proxy_exit_ip_usage` |
| ATC 广告情报 | 抓 Google 广告透明中心，看竞争对手在投什么，OCR 识别广告图 | `user_atc_watchlist` / `ad_image_ocr_cache` |
| SEO 文章站群 | 自建内容站发软文，给联盟链接做落地页 | `articles`（4,486 篇）/ `publish_sites`（39 个站） |
| 每日亏损提醒 | 每天 09:30 把亏损系列推给系列归属人（**只提醒，不自动关**） | `/var/log/crm-cron/loss-digest.log` |
| 联盟对账 | 佣金、拒付、结算、银行流水 | `affiliate_transactions`（63 万行）/ `affiliate_payments` |

**一个必须记住的设计决定**：07 明确拍板 **「CRM 都是自己手动关停」**，所以 CRM 侧的自动止损（花费哨兵）已于 2026-07-27 停用（`.env` 里 `SPEND_GUARD_ENABLED=0`）。代码和 `spend_guard_actions` 表还在，想复活要改三处。**自动止损只在 Hermes 侧存在。** 这个决定的代价见 BUG 清单第 9 条。

### 19 个定时任务（`crontab -l` 可见）

高频的几个：`click-execute` 每 1 分钟、`job-sweeper` 每 2 分钟、`suffix-replenish` 每 5 分钟、`txn-quick-sync` / `today-merchants-sync` / `campaign-status-drift` / `kylink-sync` / `parent-network-backfill` / `proxy-health` 每 30 分钟。每日的：`daily-sync` 06:00、`atc-watchlist-scan` 08:00、`loss-digest` 09:30、`daily-merchant-check` 00:00。

**D-333 的 paid-carve（2026-09-14 已装，已实跑一次）**：

```
40 9 * * * (date '+\%Y-\%m-\%d \%H:\%M:\%S CST'; curl -s -m 1800 -H 'Authorization: Bearer <CRON_SECRET>' http://localhost:20050/api/cron/paid-carve; echo '') >> /var/log/crm-cron/paid-carve.log 2>&1
```

**注意 `-m 1800`，这条是全场唯一的例外，别"统一"回 280。** 首次全量实测 **1326s（22 分钟）**，
42 个用户串行调支付明细API、标记 77,772 笔。curl 断连不会中止服务端处理，所以 280s 的后果
不是"跑一半"，而是日志里只留一个空响应体、事后谁都判断不出跑没跑完 —— 就是 `daily-sync.log`
那个「只有时间戳、响应体为空」的坑。route 侧 `maxDuration` 同步给到 1800。

RW/LH/LB 的交易API 不返回打款状态，`paid` 桶只能靠支付明细API 剖分回填。这一步原先挂在 `daily-sync` 的 Step 3.6，紧跟交易同步 —— 而交易同步每天把堆撑爆（见 BUG 清单 OOM 条），进程被 pm2 重启，于是 Step 3.6 **一次都没跑到过**（pm2 日志：`Step 3` 14 次、`Step 3.6` 0 次）。后果是这三个平台的 `paid` 桶冻结半年，结算页「结算率」严重低估。09:40 这个点是刻意选的：`daily-sync` 06:00 起跑、OOM 稳定落在 07:51 前后，等它重启完再跑。

2026-09-14 首次实跑后的效果（paid 桶单独口径，all-time）：LB 1.83% → **17.96%**、
LH 30.96% → **40.41%**、RW 30.39% → **41.53%**；paid 行数 LB 1,916→14,196、
LH 18,916→63,531、RW 8,553→29,430。另有配套的白名单表 `affiliate_paid_signids`
（当日 backfill 29,385 行、跑完后 280,264 行）：它记住剖分算出来的 sign_id，
`txn-quick-sync`/`team-sync` 把 paid 冲回 pending 后，靠它一条 set-based UPDATE 就能还原，
不必重调外部 API。**这张表别当缓存删** —— 删了等下一次全量剖分才能重建。

所有 cron 都用 `Authorization: Bearer <CRON_SECRET>` 调本机 20050 端口的 HTTP 接口，日志分文件落在 `/var/log/crm-cron/`。**排查「某个自动化没生效」的第一步就是去这个目录 `tail` 对应日志。**

### 部署

推 `main` → GitHub Actions 自动 SSH 部署 → `git reset --hard origin/main` → `next build` → `prisma migrate deploy` → `pm2 restart ad-automation` → 健康检查 `/api/health`。

服务器只有 **2 核 / 3.7G 内存 / 60G 磁盘**，构建、迁移、同步这类重活必须串行，别并发。现在内存已经用掉 2.87G 并吃了 1.87G swap，很紧。

---

## 第 3 课：Hermes —— 自动投放机器人

CRM 是「给人用的工具」，Hermes 是「代替人干活的机器人」。你**通过飞书跟它说话**，它自己完成选商家、建广告、发布、监控、止损、复盘。

### 它的一天

| 时间 | 干什么 |
|---|---|
| 07:50 / 07:55 | 从 CRM 拉情报、拉团队已验证商家 |
| 08:00 | 同步联盟 offers（现有 37,321 个在线 offer）、重建候选队列（15,344 个待测） |
| 09:00 | 执行当日投放计划 |
| 09:10 | 全量对账佣金 |
| 09:30 | 发每日复盘卡片到飞书 |
| 每 5 分钟 | **任务泵**：推进状态机、换后缀、花费哨兵硬止损 |
| 每小时 :12 | **巡检**：拉 Google 真值、三阶段止损决策、自动调参、拒登检查 |
| 每小时 :45 | 待投名单补货（目标 100 个，低于 50 就补） |

### 它的止损逻辑（07 打磨最久的部分，值钱）

分三段，每轮实时算 `ROI =（佣金 − 花费）/ 花费`：

- **观察期**（3 天）：零出单且花费到线（现设 $6）就关；一出单预算自动翻倍，进等待期
- **等待期**（2 天）：只看净亏，超 $4 关
- **运营期**：ROI > 200% 本月豁免；净亏超线或 ROI < 0 立刻关；断流 ≥3 个在投日且 ROI < 100% 关
- **自动复活**：被自动止损关掉的，如果后来出了新单且 ROI 转正，自动续投（07 手动暂停的不碰）

配套还有：**拒登库**（广告被 Google 拒登后按域名整体规避，不再重复撞）、**CID 池**（一个 CID 只能有一条活广告，79 个 CID 是产能天花板）、**远端真值同步**（Google 上的实际状态为准，回写本地）。

### 它的数据在哪

SQLite 单文件 `~/hermes-ads/data/hermes-ads.db`（154MB，34 张表）。核心表：`ad_runs`（任务主表）、`cid_registry`（CID 池）、`offers`、`test_queue`、`ad_rejections`（拒登库）、`affiliate_transactions`、`app_settings`（在线可调的规则）。

**改规则不用改代码**：飞书里对它说「预算上限改成 $10」就写进 `app_settings` 生效。当前实际值：日预算默认 $2 / CPC $0.35 / 硬上限 $15 / 止损线 $6 / 观察期 3 天 / 待投名单 100。（注意：总账文档里记的是旧值 $9/2 天，已过期。）

---

## 第 4 课：kylink 与 kyads

**kylink（换链接引擎）** —— 5.7GB 数据库，19 张表。它的活是：通过 SOCKS5 代理访问联盟链接 → 追踪重定向链 → 从最终 URL 里抽出 query 参数当作 `final_url_suffix` → 存进后缀池 → Google Ads Script 来 lease 一条用。附带刷点击养链。**注意：CRM 已经自己实现了一套后缀池，两边功能重叠。** CRM 每 30 分钟调一次 kylink 同步，但实测恒为 0/0（见 BUG 第 12 条）。

**kyads（对照中控）** —— 3.6GB 数据库，43 张表。与 CRM 同目标不同实现，特色是：team 级 OAuth 账号池 + 配额治理、CID 三态管理、Brand Assessment（用 SerpApi 评估品牌词）、五阶段草稿泵 + 四阶段发布泵、AI 决策日志。**它是参考实现，不是生产依赖**，但它跟 CRM/Hermes 共享 Google MCC，所以别以为动它不影响别人。

---

## 第 5 课：07 留下的 BUG 与隐患清单

按「是否正在造成损失」排序。每条都有当天实地取到的证据。

### P0 —— 正在流血，建议今天就处理

**① Hermes 的 Google Ads API 版本已被 Google 封杀，止损系统事实上失明**

- 证据：`~/hermes-ads/src/lib/google-ads.js:9` 写死 `const API_VERSION = 'v21'`；服务日志近 24 小时报 26 次
  `UNSUPPORTED_VERSION: Version v21 is deprecated. Requests to this version will be blocked.`
- 连带失效的三个功能：远端真值同步（HM-D39）、CID 池与 MCC 对账（HM-D51）、花费临界直查（HM-D38 的止损兜底）
- **最要紧的后果是止损失明**：花费判断现在只剩 Google Sheet 一条路，而 Sheet 由 Google Ads Script 定时写、本身就有延迟和缺口（日志里「回流表格里没有 campaign xxx」3 天内报了 93 次）。原本设计了「花费接近止损线时直查 Ads API 取真值」当兜底，这条兜底现在整条废了 —— **Sheet 里查不到的广告，烧多少都不会被自动关**
- 第二个后果是 CID 占用无法核实：现在 79 个 CID 里 71 个被 71 个 `monitoring` 状态的任务持有（**这批是正常在投，不是僵尸**），91 个已批任务在排队等位。但因为拿不到 Google 真值，Hermes **无法发现「本地以为在投、Google 上其实早就停了」的情况** —— 这类僵尸占用会一天天累积，只是今天还没积起来。同理，MCC 里新增或注销子账号也不会再自动进出池
- 修法明确：CRM 用的是 `v23` 且工作正常（`crm-mvp/src/lib/google-ads/client.ts:21`）。把 Hermes 的常量从 v21 改成 v23，验证 GAQL 字段兼容后重启即可

**② kylink 的刷点击功能已经死了 6 天**

- 证据：`click_task_execute` 从 **2026-07-31 20:18** 起每分钟都报 `previous run still in progress`，7 天内跳过 7,999 次；数据库里 8,545 个 `ClickTask` 卡在 `running`（最后更新时间停在 2026-07-31 22:54），97,961 个 `ClickTaskItem` 卡在 `pending`
- 影响：养链停摆。同期 kylink 的补货任务也大面积失败（08-04 是 336 失败 / 7 成功）
- 处理方向：先确认 kylink 的刷点击是否还被业务依赖（CRM 自己也有 `click-execute`），如果依赖就清僵尸锁 + 把卡死的 running 任务收尾；如果不依赖，正式下线这条 cron，别让它一直报错掩盖真问题

**③ CRM 每天 06:00 的全量同步会把主进程内存打爆**

- 证据：`ad-automation-error.log` 2026-08-06 06:01:53 记录
  `FATAL ERROR: Reached heap limit Allocation failed - JavaScript heap out of memory`；PM2 日志同一分钟 `exited with code [134]`（SIGABRT）。`/var/log/crm-cron/daily-sync.log` 只有时间戳、响应体为空 —— 说明请求还没返回进程就死了。历史上 exit 134 共 11 次
- 影响：`daily-sync` 从未真正跑完；顺带把整个网站重启一次
- 处理方向：给 daily-sync 分页/分批，或改成独立进程跑（别占 web 进程的堆）。服务器只有 3.7G 内存，加 `--max-old-space-size` 只是拖延

**④ Hermes 的生产代码脱离了版本控制**

- 证据：服务器工作区 HEAD 停在 `20d8df4`（HM-D44，7/28），而 `origin/main` 已到 `864f5ce`（HM-D62，8/1），**落后 13 个提交**；同时工作区有 **26 个文件、2,929 行未提交改动**；`git diff origin/main -- src/` 显示 18 个源文件内容不一致
- 风险：现在跑的代码，git 里任何一个提交都不等于它。**一次 `git pull` 或 `git reset --hard` 就会覆盖掉正在跑的逻辑**，而且没人知道丢了什么
- 处理方向：动 Hermes 代码之前，第一件事是先把服务器工作区完整备份（`tar` 一份带时间戳），再逐文件比对决定以谁为准，最后提交一个「实际生产状态」基线

**⑤ Hermes 的飞书告警发不出去**

- 证据：`[feishu] 应用卡片发送失败，降级 webhook: {"code":230025,"msg":"The length of the message content reaches its limit."}`
- 双重故障：消息内容超过飞书长度上限所以主通道失败；而兜底的 webhook 已经被 07 手动清空（他要求「所有消息由智能助手发，不要 webhook 机器人身份」）→ **告警彻底静默**
- 影响：止损、拒登、故障这些需要人知道的事，你收不到

### P1 —— 数据失真或失控，本周处理

**⑥ 21 个 MCC 账号没有 API 凭据，等于「只能看不能动」**

57 个在用 MCC 里，21 个缺 `service_account_json`、20 个缺 `developer_token`（主要是 jy 组：蓝倩倩、包海倩、林念槐、徐文君、夏慧敏、陶韩阳子、陈若男、胡婷婷、齐荠、徐梦丽、林娜）。这些账户下的广告**无法通过程序暂停或调价**，只能人工进 Google 后台。总账里记的「87 个哨兵停不掉、涉及 $1,106」就是这个原因。

**⑦ 14 个 MCC 的花费数据根本拉不回来**

- 5 个 Sheet 权限没开（`701-617-7168`、`341-732-8198`、`229-873-4998`、`103-263-0703`，报「需设为知道链接的任何人都可以查看」）
- 1 个表格结构不对（`540-358-0329`，缺 `DailyData` tab）
- 9 个缺 `CampaignInfo` tab，需要在 Google Ads 后台跑 Script 生成（`902-969-6513`、`584-349-5386`、`322-069-8976`、`947-023-8186`、`123-617-6174`、`479-294-1574`、`183-752-7166`、`977-128-1080`、`796-104-2944`）
- 这个错误每 30 分钟刷一次日志，已经刷了几百次。**花费拉不到 = 盈亏算不准 = 止损判断失明**

**⑧ 11 个 CID 报无权限**

`7194606180`（65 次）、`6047393954`、`5593269517`、`3324133978`、`3063463927`、`4386362880`、`2714299133`、`8109932666`、`5185241319`、`5101991319`、`4679937523` 全部 `USER_PERMISSION_DENIED`。可能已脱离 MCC 或账号被注销（总账里 `5509319692` 就是查了半天发现是账号 CANCELED）。需要逐个核对后从池里摘掉，否则一直白报错。

**⑨ 270 个在投系列零出单但已烧掉 $5,845，没人自动关**

今天 09:30 的亏损提醒报：166 个亏损系列 + 269 个零出单系列，**累计净亏 -$8,509**。其中 jy 组占大头（jy06 -$2,479、jy09 -$977、jy08 -$958、jy05 -$868），大部分是「dormant」= 已停但历史亏损。当前还在活跃亏损的是 -$788。
这不是代码 bug，是 07 的产品决定（CRM 不自动止损，只提醒，让归属人自己关）。**但配合第 6 条（jy 组没有 API 凭据）就变成了：提醒发出去了，收到的人也没法用系统关，只能手动进后台。** 这是 01 需要尽早做决定的一件事。

**⑩ 13 个联盟连接全部不健康**

`connection-health` 昨夜跑出 `scanned:13, unhealthy:13`；`platform_connections` 表里 16 条处于 `error`（LB 4、LH 3、CF 2、PB 2、RW 2、CG/EV/MUI/PM 各 1）。连接坏了 = 佣金数据拉不回来 = ROI 算不准。

**⑪ 住宅代理的 IP 去重池已经耗尽，开始复用 IP**

日志：`[exit-ip] dedup 撞车耗尽(CA)：2 次探测均命中已用 IP/段（已用 IP=1584 段=1455），本次复用 23.233.14.26`。刷点击复用同一批出口 IP，**有被联盟判定为刷量作弊的风险**（这属于会掉佣金的那类风险，不只是技术问题）。kookeey 账号剩余流量 61.5GB，够用，问题在去重策略。

**⑫ CRM ↔ kylink 集成形同虚设**

`kylink-sync` 每 30 分钟跑一次，12 个用户，`totalSuccess: 0, totalFailed: 0`，kylink 侧 `CrmIntegration` 表也是 `todaySuccess=0, todayFailed=0`。跑了但什么都没同步。要么该修，要么该正式下线，现在这样只是每天白跑 48 次。

**⑬ 后缀池补货速度跟不上缺口**

每轮扫 599 个系列，231 个低库存，但一轮只补 1-4 个。按这个速度永远补不满。失败原因里能看到真实卡点：`tracker_forbidden`（联盟跳板返回 403，追踪链接已失效需人工重取）、`stuck_on_tracker`（`go.ultrainfluence.com`、`app.partnermatic.com` 跟不动，Hermes 侧 3 天内撞了 147 次）。

### P2 —— 技术债与安全隐患，排期处理

| # | 问题 | 证据 |
|---|---|---|
| ⑭ | **用户密码明文存库** | `users` 表有 `plain_password varchar(128)` 字段 |
| ⑮ | 生产库堆了 22 张临时备份表 | `_d048_bak_*`、`_restore_bak_*_20260630`、`ads_daily_stats_bak_d211` 等，占空间且干扰理解 |
| ⑯ | 数据库无清理策略，越滚越大 | CRM 3.0GB / kylink **5.7GB** / kyads 3.6GB。kylink 的 `Alert` 表 23.8 万行占 1.3GB，`SuffixStockItem` 107 万行占 1.67GB |
| ⑰ | CRM 磁盘有 790MB 废弃构建产物 | `crm-mvp/.next.bak_r04/`，磁盘已用 53% |
| ⑱ | kyads 内存濒临耗尽 | 3,489MB / 3,915MB + 1.6GB swap，`next-server` 单进程吃 2.2GB，已连续跑 29 天没重启 |
| ⑲ | 站内通知积压 13,435 条未读 | 没人看的告警等于没有告警 |
| ⑳ | CRM 生产不在 `main` 分支上 | 服务器 checkout 在 `deploy-r02-monthly-report`（内容与 `origin/main` 同为 `37db6610`，但分支名容易误导后续部署）；另有未提交的 `crm-mvp/tsconfig.json` 改动 |
| ㉑ | 总账文档已与实际不符 | 文档说有 `ai-insights` 和 `spend-guard` cron，实际 crontab 里都没有；Hermes 风控参数文档记 $9/2天，实际 $6/3天 |
| ㉒ | 安全待办 07 一直没做 | kyads 服务器 root 密码曾在聊天/终端明文出现未轮换；2026-06-20 用过的 GitHub PAT 未吊销；`F:\jy0207.pem` 用途至今不明 |
| ㉓ | kylink 代码停更 1.5 个月 | 最后提交 2026-06-23，Next.js 14.2.15；kyads 最后提交 2026-07-01 |
| ㉔ | 仓库根目录有 **1,000+ 个临时调试脚本** | `_07_*.sh`、`_c*_probe.sh`、`_d*.mjs` 等全部未提交也未清理，`git status` 已经没法用了 |

### 关于文档的一个坏消息

07 没留系统手册。他留的是 `设计方案.md`：**1MB、10,956 行、200 多个章节的时间顺序开发日志**（编号 C-001 到 D-219）。它是「某天为什么这么改」的流水账，不是「系统长什么样」的说明书。
查具体某个功能的来历时它极有价值（搜编号或关键词），但**不能靠它建立全局认知**。Hermes 另有一份 `Hermes设计方案.md`（320KB）。

---

## 第 6 课：建议的上手路线

**第 1-2 天：只看不动。** 四台机器各 SSH 进去一次，把第 1 课的巡检命令跑一遍，看懂日志在说什么。打开 google-data-analysis.top 用 `manager` 账号把每个页面点一遍，对照本文第 2 课的功能表。

**第 3 天：先止血 P0-①。** Hermes 的 v21 → v23，投入最小收益最大 —— 修完止损系统才重新长出眼睛。改之前先按 P0-④ 的办法完整备份服务器工作区。（注意：这个修复**不会**让 91 个排队任务立刻发出去，它们等的是真实空闲 CID；要提产能只能往 MCC 里加子账号。）

**第 4 天：把告警通道修通（P0-⑤）。** 在自动化系统里，通知瞎了比功能坏了更危险 —— 你会以为一切正常。

**第 5 天：做那个产品决策（P1-⑥ + ⑨）。** jy 组 21 个 MCC 没凭据、270 个系列零出单烧了 $5,845，两件事叠在一起是持续失血。要么补齐凭据让系统能管，要么明确改成人工兜底并定考核。这件事需要你以 01 的身份重新拍板，不能沿着 07 的旧决定滑下去。

**第 6-7 天：处理 P0-② 和 P0-③。** kylink 刷点击定去留；CRM daily-sync 的 OOM 修掉。

**之后：** 把 P1 剩下的按周清，P2 排进季度。同时补上 07 欠的那份系统手册 —— 本文可以当第一版底稿，每处理完一条就更新它。

---

## 附：本次勘察的产出文件

本次为取证生成的脚本都以 `_01_` 前缀留在仓库根目录（`_01_recon_*.sh`、`_01_probe*.sh`、`_01_crm_db*.sql`），可复跑复核。确认无需后可直接删除。
