# 02 同事加入清单（2026-09-23）

> 口径：**他加入协作，01 仍在**。所以**不轮换凭据、不回收权限**。
> 目标：让他有一份能跑起来的环境 + 知道坑在哪。
> 本文件不写任何密码/密钥/Token，机密仍在 `C:\Users\Administrator\.infra\`（不进 git）。

---

## 一、代码（他自己就能做完）

```bash
git clone https://github.com/starrats111/Google-Data-Analysis.git
cd Google-Data-Analysis/crm-mvp
npm install
```

工具链对齐本机：**Node v24.14.0 / npm 11.9.0**。

装完跑一次自检，它会直接报出还缺什么（只读，不连生产库、不打印密钥值）：

```bash
node scripts/onboard-check.mjs
```

查 6 类：工具链版本、`.env` 十个必需键（含 example 里没有的那 4 个）、4 把 SSH 私钥、
`~/.infra` 六份机密文件、8 条工作规则、依赖。全绿才算配好；缺项退出码 1。

四套系统代码互相独立、共享同一批 Google MCC / 联盟 / 住宅代理账号。CRM 在本仓库 `crm-mvp`；
Hermes / kylink / kyads 的位置见 `.cursor/rules/infra-registry.mdc`（已随仓库分发）。

## 二、只能你本人交的东西（clone 拿不到）

### 1. 机密目录 `C:\Users\Administrator\.infra\`

| 文件 | 内容 |
|---|---|
| `服务器总账.md`（47KB） | 全部服务器密码 / Token，**最关键的一份** |
| `部署总览.md` | 部署流程 |
| `三系统架构与集成.md` | 跨系统集成 |
| `kyads-sa.json` | kyads 的 Service Account，广告开关的每小时任务攥在它手里 |
| `crm-prod-db.env` | 生产库连接 |
| `crm-proxies.tsv` | 住宅代理清单 |

### 2. SSH 私钥 4 把（`C:\Users\Administrator\.ssh\`）

| 密钥 | 用途 |
|---|---|
| `xlx0310.pem` | CRM 43.156.142.141 |
| `yangma0612.pem` | Hermes 43.165.170.242 |
| `xc0224.pem` | kylink 43.106.49.28 + kyads 216.67.230.8（两台共用） |
| `id_ed25519` | 通用 |

注：`xlx0310.pem` / `yangma0612.pem` 在 `.ssh\` 和用户根目录各有一份副本，给他一份即可。

### 3. `.env`（被 .gitignore 挡住，照 example 建会缺键）

`crm-mvp/.env` 有 4 个键**连 `.env.example` 都没有**，必须单独给：

- `DATABASE_URL`
- `SHADOW_DATABASE_URL`
- `GOOGLE_ADS_DEVELOPER_TOKEN`
- `BACKEND_API_URL`

`hermes-ads/.env` 与其 example 键一致，照抄值即可。
站群新机 `47.237.139.53` 的凭据**在库里不在 .env**，要单独讲。

### 4. 踩坑记忆库（最省他时间的东西）

整目录拷给他：
`C:\Users\Administrator\.claude\projects\D--Program-Files--x86--wz-Google-Analysis\memory\`

23 条已验证的坑 + 索引 `MEMORY.md`。含 MCC 封控敞口、脚本冻住不报故障、报表丢已删 MCC 花费
（全年 $6,277）、共享 Key 吞佣金、广告花费真值只从 2026-02 起、本机没装 iconv 等。

**传输方式**：以上四项别走微信 / IM —— 发出去就等于公开。用加密压缩包线下给，或当面拷。

## 三、要动生产机的（你本人操作）

- 他的 SSH 公钥加到 4 台机器的 `authorized_keys`（4 台，不是 1 台）
- Google MCC 账号、联盟账号、住宅代理账号的登录权
- Hermes 靠飞书跟人对话，机器人那边加他
- GitHub 仓库 `starrats111/Google-Data-Analysis` 协作者权限

## 四、当面必须讲的三件事

1. **三套系统会互相打架**：操作同一批 Google 广告账户。CRM 里手动暂停一个广告，Hermes 可能
   续投回来（HM-D39 就是为此写的）。改任何一套前先想另外两套会不会跟着动。
2. **工作区有并行会话**：多人同时改文件、切分支、抢 D 号。提交和追加设计方案前逐行核对。
   别人可能在你工作期间 `stash -u` + `reset` 把工作区清空（不是丢失，但未跟踪文件会集体消失）。
3. **开工确认卡制度**（`.cursor/rules/pre-work-confirmation.mdc`）：任何开发动工前先发确认卡，
   获批才写代码。历史翻车的共同根因就是拿到需求闷头干。

## 五、旧手册已过期的地方

`01_系统交接手册.md` 写的是 2026-08-06 状态，到今天 6 周半，至少这些变了：

- 站群服务器 9-03 迁阿里云 `47.237.139.53`（旧 AWS 机死掉、44 站离线）
- kylink「核心功能已死 6 天」早已过期
- P0/P1 清单里 D-316/D-317/D-322/D-324/D-328/D-330 等已修掉

让他照旧手册排查会踩空。建议按本清单 + `设计方案.md` 末尾那条「交接说明」对照看。

## 六、代码交接状态（2026-09-23 已清点完）

- 33 个本机独有提交**已全部推 origin**，零残留
- 4 个 stash 已各自重做成 `handover/*` 分支推上远端，**原 stash 未删**（并行会话还能 pop）
- `handover/stash-d349-wip` 里**四摊活儿混在一起**（D-349 QUK/BA、D-348 supersedes_id、
  puppeteer 信号量、一摊未归类），**接手前必须先拆**，别整条合
- `设计方案.md` 缺 D-334～D-349 共 16 条条目，末尾已补一条交接说明写清各号落点；
  报案原话与拍板结论仍缺，**等 01 定要不要回填**
- Temp 里那两个 worktree（`d337work` / `mergecheck`）内容已确认在远端有着落，可弃
