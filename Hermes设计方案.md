# Hermes 独立智能投放体 — 设计方案

> **文档规范**
> - 验收人：07
> - 设计人：AI 设计研发
> - 创建日期：2026-07-13
> - 版本：v1.22（2026-08-07 **07 要求「查过的联盟品类回写 CRM 主营业务」，新增 12.16/HM-D75**。先否掉了想当然的做法：Hermes 的联盟品类 53% 也是 `Others` 打头，与 CRM 的 71% 同源同套，互抄等于没做；真正新增的知识只有三层闸查出的品类结论，374 条里 **276 条**落在「联盟只给 Others、我们查出真品类」的交集。回写分两层——主营业务只补空（写了打 `category_manual=1` 防被平台同步冲回），政策标记按 `blocked→prohibited`/`review→restricted`，**刻意不用 CRM 的 `restriction_level`**（它把成人/金融/博彩只算 restricted、领取仅弹警告，而被封账号里就有一个踩成人内容）。金融按命中原词细分 Insurance/Crypto/Finance。**顺手堵掉一个会推翻 07 裁定的闭环**：CRM 的商家情报接口会把 restricted/prohibited 导回来变 `crm_policy_flags`，而 `sop-screen.js` 对它是无条件淘汰——不改的话 07 手动放行的商家会被自己种的标签重新拦住。**实测**：主营业务写入 6,190 条与事前预测 6,198 差的正好是 8 行已有真分类的；抽查抓出 Acmejoy（致账号封停那个）在 BSH 另一个 MID 上漏写，开域名传播后 `prohibited` 由 1,257 行涨到 **14,848 行/978 商家**。⚠️ 剩两件待拍：同品牌不同 TLD（`acmejoy.nl/.it/.fr`）仍未覆盖；8 条组员广告正在投刚被标禁投的保险/烟草商家）
>
> - 上一版：v1.21（2026-08-06 深夜 **07 拍板「LH 这张表压过其他联盟先跑完」并要求其余五个账号用各自平台的 8 月推荐表，新增 12.15/HM-D73/HM-D74**。五个平台的表形态各不相同：PM（261）、RW（190）各自独立，而 **`CZ 8月推荐商家列表.xlsx` 一张表里塞了三个平台**（首列 BU 为 `chuizhan_ui`/`chuizhan_cg`/`chuizhan_ev`，对应 MUI 302 / CG 208 / EV 26），必须按 BU 拆开再匹配，否则会把 MUI 的商家算到 CG 账号头上；这五个平台没有 LH 那种可直接对 CRM `merchant_id` 的 MID，只能按标准化域名精确匹配。**LH 档内加成**新增设置 `rec_lead_network`，加 5000 分——这个数是挑过的：推荐表档分数为 `20000 − rank`、rank 封顶 9000，加完落 24001–25000，仍低于白名单档最低的 30001，**三档区间照旧不重叠**。**但上线后正式重建队列，推荐表命中从干跑的 130 掉到 9、队首一条 LH 都没有**，顺着差异查出一个已经静默吃掉商家半个月的 BUG（HM-D74）：写库时只清 `queued` 状态，而唯一键 `(domain, country)` 管的是全表，于是任何非 `queued` 的旧行都让该商家在此后**每一次**建队列里被跳过。`sop_dropped` 被挡是对的，但 `suffix_failed`（后缀池空）和 `copy_failed`（AI 文案失败）**是系统自己的临时故障**，却和永久淘汰同等待遇——全库 **147 条**卡在这两个状态、最早的到 7/20，其中 **11 个正是 07 点名要跑的 LH 商家**（JB Tools rank 33、PETstock AU 63、MGM Resorts 154、Nutricost 192…），**没有任何日志或告警**。修法是建队列前放行冷却 12 小时外的这两类行，用新表 `queue_retry_ledger` 记次数最多放行 3 次防止无限重试烧 SerpApi。实测放行 147 条，推荐表命中 **9 → 53**，队首 8 条 LH 连成一段压在所有其他联盟前面。**顺带把 12.14 的一个数说准了**：原「104 个池子里根本没有」是只按域名比 `offers` 得到的，改成 `merchant_id OR domain` 并扣掉已建过广告的后逐个归因——真正只差「加入联盟」的是 **49 个**，另有 **19 个已加入但 offer 全部下线**（申请表解决不了、要联盟重新上架），57 个在池里被闸拦（团队黑名单 23、拒付率过高 4 等）。拒付率拦下的四个值得单看：MSC Cruises **57.81%**（91 笔 / $6254 基数）、Secret Flight Club 65.08%、AG Jeans 34.06%、Drivalia 33.53%——**推荐表推的商家不等于能赚钱的商家**。⚠️ 生效时点：候补名单当前 61 条 > 低水位 50，LH 要等候补掉到 50 以下那一轮才真正开始建。见 12.15）；v1.20（2026-08-06 深夜 **07 选定 LinkHaitao 8 月推荐表要求按顺序跑并按团队要求出跟进表 + 申请表，另拍板金融连保险一并禁、放行厨刀等四类误伤，新增 12.14/HM-D72**。交付两类文件：wj07 两个 LH 账号各一张跟进表（原表列序不动、右侧加申请/拒付/备注，另附口径说明页）与一张未加入审核申请表（`wenjun3` 待申请 99 个、`novanest` 159 个）。**口径都是实测出来的**：199 个 MID 打到 CRM 全库，LH 命中 169（85%）、PM 33、LB 22——`MID` 就是 LinkHaitao 自己的商家 ID、`联盟` 列只是上游来源；「申请」列三值 CRM 只给得出 `join`，因为各平台同步全带 `relationship=Joined`，`pending`/`rejected` 结构上拿不到。**最该防的一类**：77 个已加入里 **17 个属淘汰品类**（Merrell/Saucony/alibaba 在黑名单、`mycapnos.com` 电子烟、`lolivier.fr` 保险），关系列如实写 `join` 但备注加 `⛔ 已加入但不投`，否则看表的人会直接投出去。**「按顺序跑」的机制早就在**：`monthly_recommendations` 里这张表 199 条 rank 1-199 已在库、三档优先级也在跑，卡的是商家关系——199 个只有 95 个在 Hermes 商家池、**104 个池子里根本没有**，没加入就没有 tracking 链接；也就是说申请表交上去才是让推荐表跑完的前提。**金融收紧时抓到自己一个漏洞**：只加英语 `insurance` 等于只拦英美站，`unipol.it`（意语 `assicurazioni`）已经是 `approved` 待上线状态且词表零命中，补齐七个语种后在线池金融硬拦 182 个，并当场停掉两条在投（`freely.me` 已投 $4.63、`unipol.it` 未发布直接取消省下一个 CID）。**放行四类误伤也没有照说的批量改**，要求落地页给正证据 ≥3 次且反证据为 0，当场挡住 `kasumi-knives.com`（folding/hunting/edc 都在，不是纯厨刀店）。⚠️ 待 07 拍：推荐表档是 6 个联盟混排、LH 的 #1 与 MUI 的 #1 优先级相同，若要这张表先跑完需给 LH 档内加成。见 12.14）；v1.19（2026-08-06 傍晚 **07 核实七个封号后指出其中一个的封停理由是「违反色情内容政策」，并给出 `ADFLY-广告禁投品类.pdf` 要求禁投品类一律不投，新增 12.13/HM-D71 禁投品类闸**。追到具体商家：`761-316-6644` 名下唯一品类拒登来自 **`acmejoy.com`**（成人情趣用品），8/03 05:15 发布、**58 分钟后**被判 `SEXUALLY_EXPLICIT_CONTENT`（PROHIBITED），Hermes 当场自动暂停——**账号仍在次日被封**；同形态 `shopduer.com` 带走 `795-035-8060`。**唯一结论：对 PROHIBITED 品类，事后暂停救不了账号，只能事前不投。** 原来三个缺口各自独立：CRM 情报库 910 条**只覆盖大麻/酒精/医疗保健，成人、武器、博彩、仿冒、烟草一条都没有**（恰好就是出事的）；联盟品类字段 65% 是 `Others>Others` 且高危商家全落在这个值上，只能平反不能定罪；关键词也不行——出事的三个商家域名与名字里一个敏感词都没有。落地三层：L1 词表 → L2 落地页词频 → L3 拒登回灌永久拉黑，接在 SOP 深查之前。**跑真数时抓出自己四个错误并全部修掉**：正文整段 flatten 会跨词拼假词（葡语「por Nome」拼成 `porn`，把一个旅游险站判成成人内容，原以为「≥8 次才定性」兜得住、实测兜不住）、`xxx` 是卡号打码占位符、`insurance` 不在 PDF 的高风险清单里（一口气挂了 15 个保险大牌）、`armour`/`axe` 太脏（Under Armour 两站被误杀）。**还补了个漏网方向的洞**：落地页只看硬词会把词表的 soft 怀疑无条件洗清，`buckknives.com` 卖猎刀却被洗清，改为定向数怀疑品类的 soft 词频，挂回 35 个。最终 **188 blocked / 91 review / 79 cleared**，在投任务实查 0 条命中。⚠️ 待 07 拍：待复核队列里厨刀、成人纸尿裤、运动装备清洁剂、主题积木这几类明显能放，按「判不了就挂起」现在安全但在少赚钱。见 12.13）；v1.18（2026-08-06 下午 **07 问「359-711-5243、833-741-4380 属于 SUSPENDED 吗」，从这个小问题里岔出一个能让全系统失能的事故，新增 12.12/HM-D70**。答案本身很简单：不属于，两条独立路径实查都是 ENABLED（被封的是另外 8 个）。但为解释这两个账号名下广告为何零花费去实查系列状态时，一次查询报 `UNSUPPORTED_VERSION`、紧挨着的同样查询却成功——翻那批一直被归类成「Google Ads 报错（未归类）」的失败，完整报文是 **`Version v21 is deprecated. Requests to this version will be blocked.`**。Google 已在**分批**拒 v21，8/06 一个上午 14 条广告踩到、日志里躺着 9 条，而**没有任何告警**，是手工翻日志才发现的。**严重性不在少发广告**：止损暂停与发布走同一条 `mutate` 通道，全量阻断后 Hermes 会同时失去发布与关广告的能力——前者只是少赚，**后者是一边烧钱一边把广告记成已暂停**。处置：v22–v25 实测均可用、v26 尚不存在；在 v22 与 v25 上各验 9 项核心操作加一次 63-operation 完整发布干验（写操作全走 `validateOnly`），**两版结果完全一致**，故取离下次弃用最远的 **v25**。同时按 07 拍板加**系统级失能告警**（版本弃用 / 令牌池耗尽 / CID 池归零，6 小时静默），用废弃 v20 端到端触发验证，第一次就抓到文案缺陷——Google 把有用的话埋在 `details` 里、外层 `message` 永远是没信息量的那句 `Request contains an invalid argument.`，已改为提取真实消息。⚠️ **顺带纠正我上午两个说过头的判断**：`budgetExceeded: 93` 是每轮**时间**预算不是钱的闸门，近 7 天真实发布量 24–46 条/天、产能正常，「CID 是唯一硬瓶颈、产能见顶」说重了，该盯的是一周退役 7 个账号的流失趋势；38 条 monitoring 零花费全是 8/05–8/06 发的、无一超 24 小时，实查是智能出价 `LEARNING` 期未起量，不是故障。见 12.12）；v1.17（2026-08-06 上午 **07 发来 CRM 数据中心截图「今日投放数 0 条」，并要求「按昨天说的规则改造 Hermes」。先排障后改造，新增 12.10/HM-D68 与 12.11/HM-D69**。**故障（HM-D68）**：`advanceApproved()` 把上限校验排在了定稿前面——`assertLimits(run)` 校验的是建任务时盖的旧章，而按发布时默认值定稿要到下一步才发生，于是 8/05 把 `hard_cpc_usd` $5→$1 之后，存量任务身上 $2 的旧章撞上新硬顶抛异常，**而 CID 已在上一步预占、这个异常没有任何一层接**，任务永久停在 `cid_reserved`：不发布、不失败、不归还。pump 每 5 分钟捞一条就吃掉一个 CID，5 条依次卡死后 71 个可用账号被占满、**空闲归零**、98 条堵在门口。**这个 BUG 是我 8/05 收紧硬顶时引入的**，教训写成了可复用的检查清单：`hard_*` 这类 Owner 上限会回溯作用于所有中间态任务，收紧前必须扫存量旧值会不会撞线、撞线后的异常有没有人接。修完解卡 7 条，**全部发布成功**。**改造（HM-D69）**：A 盈亏比改用 CRM 系列级真数（实测 #46 合并口径 R=16.98、系列级实际 10.25，按公式反推出价差 66%）；B 新广告基准 $1→$0.35（**推翻我当天上午基于 HM-A08 给出的提醒**——实付 <$0.35 那 133 条整体盈亏比 3.82，≥$0.35 那 124 条只有 0.37 且净亏 $570，根因是贵的那批还没攒够样本就被止损关掉）；C 抬价样本门槛按 AOV 分档（AOV ≥$10 的广告 1–2 单就能算出 R=25，一笔运气单能把 2 倍说成 25 倍）。干跑 0 条动作，三条高 AOV 少订单的广告被新门槛正确挡住。⚠️ **CID 已是唯一硬瓶颈且在净减少**：修完立刻又被占满，且一周内退役 7 个账号（1 CANCELED + 6 SUSPENDED），池子共 80 个——产能已不取决于名单/审批/规则，需 07 加号。见 12.10、12.11）；v1.16（2026-08-05 傍晚 **07 要求实地核查生产环境「真实情况和我们预想的一样吗？会不会有 BUG」，查出一个已经吃掉真金白银的 BUG，新增 12.9 与 HM-D67**：`adjustRun` 用 `queue_source === 'external'` 判断一条广告是不是手动出价，而这 5 条接管广告在 Google 侧**全部是 TARGET_SPEND**，于是每次调价都去改广告组与关键词的 `cpc_bid_micros`——正是 HM-D57 白纸黑字警告过、在智能出价下**被 Google 完全忽略**的字段。代价是 `#46 mmlafleur` 的三次抬价（7/30 `$0.25→$0.29`、7/31 `$0.29→$0.33`、8/05 HM-D66 首轮 `$0.33→$0.66`）**全部落空**，真实上限三个月纹丝不动停在 **$0.50**，库内记的是从未生效的幻影值（#47/#48 同样对不上）。**这解释了 HM-D66 为什么"生效"了却看不到量变**。顺带挖到一个比规则本身更有分量的旁证：#46 近 7 天 `searchRankLostImpressionShare` **73%–86%**、而预算丢失率只有 0–10%，**"该抢没抢"从此有硬数据、不再是推测**。修法是**不再用建单来源当运行时状态的代理指标**，每次调价前现问 Google `bidding_strategy_type` 再决定动哪个旋钮。07 拍板 **#46 小步到 $0.75**（不直接跳 auto-tune 算出的 $1.00）、**存量 72 条 $2 保持不动**（"上限不等于花费，且 mmlafleur 刚证明压低上限会丢七八成展示"）。已实调确认 $0.75 真的落到 Google。见 12.9 与 HM-D67）；v1.15（2026-08-05 傍晚 **07 划清「上线前统一标准 / 出单后进决策池」两段分工，决策池的规则首次落地，见 HM-D66**：新商家一律 $2 预算 + 基准上限、交给止损阀淘汰，**有出单才进观察决策池**；后半段此前没有规则，实测的代价是——全系统最赚钱的 `#46 mmlafleur`（花 $319 收 $5,523、盈利比 17.3）**出价上限被焊在 $0.33、而预算早在 $15 顶满**，它一直被出价卡量不是被预算卡量，同时一批亏损广告的上限却是 $2，**上限与赚钱能力完全脱钩**。根因是 HM-D57 移除 CPC 规则时把「被智能出价忽略的关键词层出价」和「真正生效的 `cpc_bid_ceiling`」一起停了。新规则 `cpc_up`：**新上限 = 实付均价 × (盈利比 ÷ 目标比)**，乘实付价而非现上限（上限 $2 的广告实付才 $0.19，按上限翻倍会跳到 $4）；07 拍板拒付率「抬高门槛而不是一刀切」，目标比 = 2 × (1 + 拒付率)，口径与 CRM 结算中心一致。配套 `tune_profit_ratio_up` 1.2→1.5、`hard_cpc_usd` $5→$1。已上线并首轮生效：`#46 $0.33 → $0.66`（想出 $1.07，被单次封 2 倍压住，下轮再爬）。**同时记下一次被 07 当场纠正的口径错误**：我拿「已裁决样本 100% 拒付」判 `mmlafleur` 危险，而已裁决只占 7.4%、approved 平均要等 70.6 天，它最老的单才 59 天——按 CRM 口径它拒付率只有 6.8%；07 的定义是 **净利润 = 佣金 − 拒付 − 成本**，拒付是已发生的事实、显式扣掉即可，不该拿它去折算没发生的事。**⚠️ 确认率暂不入规则**：全局确认率 0.1%、结算率 0%，是系统才跑两个半月所致，现在用它做权重会把所有商家一起判死。见 HM-D66）；v1.14（2026-08-05 下午 **排查 07 报案「为什么在跑广告没有补上」，查出一次没有任何报警的空转，新增 12.8 与 HM-D65**：在投两天内从 74 掉到 30、44 个 CID 空转，但**四个环节各自都在按设计正常工作**——名单补货看到「名单 52 > 低水位 50」跳过（`waitlistCount()` 把**待批也算库存**）、发卡看到「这批已发过」不再发（`approval_msg_sent=1` 后不进候选，HM-D29 防轰炸）、任务泵看到 `approved` 为空无事可做、止损照常关停。**根因是名单水位的口径**：名单管的是「仓库里有没有货」，能不能上广告取决于「货有没有出库许可」，两件事被算成一个数——仓库 52 件、出库许可 0 张，系统看到 52 就安心了。**一个反直觉的推论**：那两天就算补货触发了也救不回来，新建的任务同样是待批，只会把 52 堆到 100，`approved` 依然是 0；补货解决的是「没货可批」，不是「批了没人点」，故修法不是调水位口径而是补一条独立的出货口检测。**HM-D65 落地**：加列 `approval_reminded_at` 节流 + 新设置项 `approval_remind_hours`（默认 12h）+ `dispatchPendingApprovals()` 空分支转入新增的 `remindStalePendingApprovals()`，先发一条**文字**消息说清代价（等了多久 / 在投多少 / 多少 CID 空转）再重推卡片——只重推卡片不够，那张卡跟两天前长得一模一样，很容易被当成已看过的。**本次事故已解除**：手动置回 `approval_msg_sent=0` 重推批量卡，07 于 14:09:28 一次批完 52 条，14:30 在投回到 54 条、另有 26 条排队。⚠️ HM-D65 只覆盖「待批无人处理」，07 批了之后因 CID 不足或发布连续失败的空转仍无告警（07 未选「盯在投数量」那项）。见 12.8 / HM-D65；v1.13 **按商家定出价上限已全量上线（HM-D64 第二批），规则相对 12.5f ⑥ 实质改写**：干跑推翻 ⑥ 的两个参数后 07 重新拍板 **#23 探测下限 = $2**（不是 $0.30）、**#24 天花板按档给**（不是统一收 $2），最终规则是「**$2 基准线 + 有证据才加价**」——`上限 = max(base, min(盈亏平衡 × 档位系数, 档位天花板))`，`base = $2`、`trusted` × 0.5 封顶 $5、`inflated` × 0.25 封顶 $2（等于 base 故实际不加价）、其余档一律 $2。实现上 `max` 必须在最外层，写成 `min(max(raw, base), hard)` 的话 07 未来调低 `cpc_hard_inflated` 就会击穿基准线。**落地动作**：`effectiveBidding()` 接入 `cpcCeilingFor()`（发布与审批卡共用）、新增三个设置项（`cpc_safety_trusted`/`cpc_safety_inflated`/`cpc_hard_inflated`，07 可在设置界面直接调）、`hard_cpc_usd` 由形同虚设的 100 收到 **$5** 真正充当 `trusted` 档天花板。**上线前干跑全库 452 条 `ad_runs`：降价 0 条**（07 要的「基准线不下移」在全库成立）、**待发布 52 条一字未变**、全快照 3,104 家里能加价的只有 **14 家（0.5%）**，落在 $2.13–$4.39（`caracaranyc.com` 平衡 $8.771 → $4.39 居首）。**上线后实调四条代表路径**确认新逻辑真在跑：可信档能加价的 → $4.39、可信档但平衡值低的 `tedbaker.com`（$0.0712）→ 维持 $2 **不降价**、虚高档 `mmlafleur.com`（$54.97）→ 封在 $2、快照外 → $2。第二行是本次拍板最要紧的行为：`trusted` 不等于「按平衡值定价」，低于基准线时一律维持基准线——**这顺带消掉了 ⑥ 原本会压住的 35 个商家**（含 `PupLabs` 平衡 $0.4205、旧规则会压到 $0.21）。另修正 HM-A08 ① 的一处判断：「只封顶不设下界」是针对 ⑥ 的 $0.30 说的，下限定回 $2 后它反而会让平衡值近 $0 的商家掉到 $0.01，与「基准线不下移」冲突，故未采纳。**分两批上线这个做法本身值得留档**——第一批只落数据通路、刻意不接决策逻辑，正是这个干跑窗口推翻了 ⑥ 的两个参数；若一次性上线，$0.30 下限会直接掉死 57.6% 的广告。见 12.5f-1 ⑦ 与 HM-D64 / HM-A08；v1.12 **Hermes 侧数据通路已打通并干跑，12.5f ⑥ 的两个参数必须回调——新增 12.5f-1**：服务器侧 **HM-D64** 上线本地缓存表 `crm_team_verified` + 同步任务（首跑 20 秒 / 3,104 条）+ 独立 cron，**但四档定价逻辑刻意未接入 `effectiveBidding()`，线上行为一字未变**。干跑拿到三个决定性事实：① **待发布 52 条能按盈亏平衡定价的是 0 条**（43 条快照里根本没有、6 条 `no_ad_data`、3 条 `thin_sample`）——不是匹配 bug（域名模糊匹配命中 0、反向抽查口径一致），而是两池天然不相交：快照收「团队已出过单」的商家、队列排「还没投过」的新商家，天花板＝`offers` 21,831 域名与快照重叠仅 **1,092（5.0%）**；② **探测下限 $0.30 建立在口径错配上**——⑥ 用「全库 77.8% 成交 ≤$0.30」论证，那是含已跑出量老系列的口径，而 **Hermes 自己 171 条有花费广告实际 CPC 中位 $0.3250、≤$0.30 仅 45.0%**，与 12.5a 已证的测试期 CPC $0.3686 吻合，属 12.5a 踩过的「加权错觉」同类错误；代价是 17 条真正出过单的广告有 **8 条**实际 CPC >$0.30（含净赚的 `BudgetVetCare` $0.403 / `Calecim` $0.532）；③ **上限压低不省钱只是拿不到量**——$0.8 组 72 条平均仅 10.7 点击、**0 条跑到 50 点击**、实际均价被顶到 $0.6634，而 $2 组 212 条平均 38.6 点击、实际均价反而只 **$0.3283**（二价竞价下空间给足才能挑便宜机会成交，这正是服务器侧 HM-D57 在 7/31 把上限从 $0.5 提到 $2 的原因）。**故建议把规则从「分档降价」改成「有证据才加价」**：上限 = `max($2 基准, trusted 的平衡值 × 0.5)`，其余档一律维持 $2；`hard` 不宜统一抬到 $5（会连带放开 31 个平衡值虚高的 `inflated` 商家），应按档给天花板。另修正一处实现偏离（初版误加了 ⑥ 已警告过的 `clamp` 下界，改为只封顶）、顺带证实 12.5e ④（$6 实际只够买约 18 个点击，**「50 点击无单即 pass」在 $6 下是虚设**）。新增待确认项 **#23**（探测下限 $0.30 vs $2）/ **#24**（`hard` 统一抬还是按档给）。见 12.5f-1 与 HM-D64 / HM-A08；v1.11 **12.5c 的 CRM 只读接口已落地并实跑，暴露 12.5f 公式适用面远小于预期**：`GET /api/hermes/team-verified-merchants` 上线（CRM 侧登记为 D-214），实际契约三处与原约定不同（路径归入既有 `/api/hermes/*`、联盟字段是 `platform` 非 `network`、CRM 无 `normalized_domain` 列须从 `merchant_url` 运行时解析）。**拿到三个此前不知道的事实**：① 团队已验证商家 **3,103 家**（46.9 万单 / $179.8 万佣金），`team_verified` 布尔信号覆盖面很好；② **但只有 611 家（19.7%）在 CRM 有广告点击、算得出 `breakeven_cpc`**，其余八成只能走探测下限；③ **这 611 家里又有 408 家（67%）不可直接用**——179 家佣金含非广告流量（`Coldwater Creek` **2,002 单只有 58 个点击**算出 $185.71），229 家点击 <30 样本失真（`LH/86428` 是 **1 单 5 点击 $272 佣金**算出 $54.46；点击 <10 那批平均 $4.51，而点击 ≥200 的平均只 $0.873）。**真正可直接乘系数的只剩 203 家，占已验证商家的 6.5%——12.5f ⑥ 的「探测下限」从兜底变成了覆盖 87% 的常态。** 原打算用「仅可归因佣金」修正偏高问题，实查 **`affiliate_transactions.campaign_id` 全库 46.9 万笔只填 1 笔（0.0%）**，CRM 做不了系列级归因。故如实标注而非估算填补，可信度用**四档枚举 `breakeven_quality`**（`trusted`/`inflated`/`thin_sample`/`no_ad_data`）——因为**样本不足与数值偏高是两回事**：前者乘任何系数都是假精确、只能走下限，后者才适合压系数。**07 拍板 #22：偏高档取 0.25**，实测对 inflated 179 家里的 **118 家真正生效**、另 61 家乘完仍撞 $2 硬顶。**另有一个比限额规则本身更要紧的发现**：三档商家的**实际成交 CPC 只用掉各自盈亏平衡值的 1.6%–10%**（`trusted` 档 $0.0572 vs $0.5622）且三档全部大幅盈利——成交价由 Google 二价竞价定、上限只是天花板，**故 12.5f 的定位修正为「给每个商家一个自己不会亏的天花板」而非「限制超额付费」**。另修正两条证据边界（原「上线前重算绝对值」已查明无法重算、原「80% 无团队数据」的空档比记载更大且接口无力补齐）。**实现中踩的两个坑**：`user_merchants` 有 162 万行，Prisma 的 `distinct` 是应用层去重会把整表搬进 3.7G 内存的 Node（改按 MID 走索引，全接口 23 秒）；验证增量部署时判据取错（「产物目录存在＋接口 401」第一版就满足），对着旧进程调了一次。见 12.5c-1；v1.10 2026-08-05 晚 **编号治理 + 补记服务器侧九笔已生效改动**：审计发现 Hermes 服务器代码自行编号**已到 HM-D63**，与本文档 8/04 起新增的 D55～D61 **七个号全部重叠**；07 拍板方案 A——`HM-D` 专指已落地代码改动、**编号以服务器为准**，`HM-A` 专指分析与待实现设计、由本文档编号，原 D55～D61 重编为 **HM-A01～A07**（映射见 13.1），并**补齐服务器侧 D55～D63 九笔**（见 13.2）。**这不只是编号问题**——正因文档缺了这九笔，12.5f 初稿把 7/31 已撤除的 `cpc_up`/`cpc_down`/`cpc_up_epc` 当成现行规则，配套建议打在了不存在的东西上，已撤回重写；同时发现服务器侧 HM-D57 的「净亏线按单笔佣金中位数分档」与「买不起就别测/`merchant_budget_skips`」**正是 12.5f 建议的同源先例**，07 选的「照投不拦靠闸门兜」已有现成实现，只需把判定点击数扩到 50。另落定 **#18 月搜门槛 = 2 万**（「单笔佣金 ≥$1.5」同步删除）、**#19 日预算 = $2**。见 13.1/13.2；v1.9 **07 指出「CPC 要有限额，Hermes 完全搞反了——贵的商家要去抢才用高 CPC，不是直接拉 2 刀」——审计确认成立，新增 12.5f，并修正 12.5d/12.5e 的「选便宜 CPC」口径**：`default_cpc_usd = 2` 是全局默认、`hard_cpc_usd = 100` 形同虚设，代价是在投 34 条有 **24 条花超 $6 却只 10 条攒到 50 点击**、14 条卡在判决无效区烧掉 $172（tinyland $8.94 买 9 个点击）；正确依据＝**该商家的盈亏平衡 CPC**（商家间差 **328 倍**，付超的 361 条净亏 $2,447 vs 未付超的 727 条净赚 **$270,714**）；**07 的「贵的要抢」有硬数据且方向与我原结论相反**——ROI 随 CPC 单调上升（≥$0.60 档 **1,897%**，那批单笔佣金 $20-103）；在投 86 个 MID **69 个(80%)团队无数据**，Noyz 盈亏平衡 $0.0232 却付 $0.403(**17 倍**)、Oxiline $2.90 该抢没抢；新增待确认项 #20（服务器编号撞号）/#21（限额规则），#17 作废。见 HM-A07；v1.8 **07 澄清「总预算就是 6 刀」——新增 12.5e 按总预算口径重做全部验证**：$6 够花（全库出单系列首单时只花 $2.67/20.5 点击，误杀 **14.3%**，**修正 12.5a 旧表的 41.3%**）、50 点击只漏 11%、**真正的矛盾是 $6 能否买到 50 点击且完全由 CPC 决定**（CPC<$0.08 → 97.4% 达标，≥$0.35 → 0%）；yz04 有 44.4% 的系列总花费 <$6 且恰是最贵档，证明「$6 判决」就是他的实操；日预算建议 $2-3；$11-14 降级为补救额度。见 HM-A06；v1.7 **07 定 HM-D48 选品门槛完全更换，拉 yz04 Google Ads 关键词/出价真值定出替换物：新增 12.5d「品牌词月搜索量 ≥2 万」**——四个事前判别器只有月搜索量有预测力（Spearman −0.578，竞争度指数仅 0.146 且同 `HIGH` 标签内 CPC 差 92 倍），2 万这条线与 12.5a 的 $0.12 CPC 线独立重合；同时实证他出价上限统一 $0.50 只用掉 27.8%、日预算中位 $4 且 38/52 条 ≤$6（**印证 07 的 $6**），CPC 差异根因结案为商家品牌词竞争；新增待确认项 #18，见 HM-A05；v1.6 **07 拍板判决口径，新增 12.5a 判决矩阵**「未验证 $6 / 已验证 $11-14 + 累计点击 ≥50 零订单即 pass」，并校正「CPC 差 3.4 倍」为加权错觉、否掉「调低出价上限换点击」；新增 12.5c 团队验证信号通道（CRM 只读 API）与待确认项 #16/#17，见 HM-A04；v1.5.2 按 07 质疑修正 `is_deleted` 软删除误算：**Hermes 并未漏接池**，扩池的真实约束是联盟账号数量；12.5b 重写、待确认项 #13 重写，跨账号扩池核心数字经严格口径复核不变，见 HM-A03；v1.5.1 按 07 纠正重写 12.6 的「复投」口径为**跨联盟账号扩池**，新增待确认项 #14/#15，见 HM-A02；v1.5 新增第十二章「阈值的外部证据基线」，变更记录顺延为第十三章；v1.4 为 2026-07-20 全联盟入池；v1.3 为 2026-07-20 P2 上线；v1.2 为 2026-07-17 共享表格接入；v1.1 为 2026-07-16 第二轮需求澄清定稿；v1.0 为 2026-07-13 初稿）
> - 本文档是 Hermes 系统的**专属设计文档**；CRM 主设计文档（`设计方案.md`）中 HM-01 章节仅保留索引。
> - ⚠️ 本文档不写任何明文机密（token/密钥/密码），机密统一见 `C:\Users\Administrator\.infra\服务器总账.md`。

---

## 一、系统定位

Hermes 是**独立于 CRM 和 kyads 之外的第三套系统**：一个飞书交互的数字员工，自动完成联盟 offer 情报整理 → 筛选排队 → 创建投放任务 → 飞书审批 → CID 预占 → Google Ads 发布 → 监控与安全调整 → 每日复盘的全链路。

独立性边界（v1.1 修订）：

| 维度 | 独立方式 |
|---|---|
| 发布通道 | 自己直连 Google Ads API。凭证为 **wj07 在 CRM 绑定的 MCC**（`google_mcc_accounts` user_id=8 名下活跃行：MCC 941-949-6301「zwj0123」，行内自带 SA JSON + dev token）的**本地副本**，运行时不调 CRM/kyads 服务。注：号码与 kyads MCC 相同，但凭证来源以 CRM 该行为准 |
| 数据存储 | 服务器本地 SQLite，不写 CRM/kyads 库；团队去重/黑名单走**共享谷歌表格**（见第 5.4 节） |
| 联盟凭证 | wj07 的**两条** LH api_key 都**复制一份**存 Hermes 本地（CRM 生产库 platform_connections id=17「wenjun3/LH1」、id=277「novanest/LH2」，均 connected），视作两个独立 offer 池 |
| 团队去重 | 读写**共享谷歌表格**（已跑记录 + 黑名单，全员可编辑，各员工 Hermes 自动登记）；替代 v1.0 的 SSH 隧道读库方案。表格暂不可达时用本地缓存快照继续运行 |
| 交互 | Hermes Agent 原生飞书机器人（复用旧飞书应用凭证，websocket 长连，无公网回调）；全部走 07 私聊，不建群 |
| 多员工 | **一个 Hermes 对应一个员工**，本期只做 wj07 这套；代码按可复制部署设计（换员工凭证/配置即可再起一套） |

## 二、需求结论

### 2.1 第一轮（07 已确认，2026-07-13）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 总目标 | 重建 43.165.170.242 上的 Hermes，全环节（offer 管道/发布链路/监控/交互/稳定性） |
| 2 | 定位 | 独立第三系统，完全不依赖 CRM/kyads 运行 |
| 3 | Google Ads 凭证 | ~~复制 kyads 那套~~ → **v1.1 修订**：用 wj07 在 CRM 绑定的 MCC 凭证本地副本（见 2.2 #4） |
| 4 | 投放后端 | 全新重写（旧 hermes-crm 只参考思路，不恢复） |
| 5 | 交互 | Hermes Agent 原生飞书机器人 + 旧飞书应用凭证 |
| 6 | 审批方式 | **纯飞书内完成审批**，不恢复公网审批页（fengdu-ads.top/hermes-approve 保持下线） |
| 7 | 联盟 | 第一期只接 LH（用 CRM 的 wj07 凭证），架构预留多联盟抽象 |
| 8 | 存储 | 自己的 SQLite |
| 9 | 去重 | ~~SSH 隧道只读同步~~ → **v1.1 修订**：共享谷歌表格（见 2.2 #6） |
| 10 | LLM | gemai 中转（OpenAI 兼容，claude 系） |
| 11 | 风控 | 按 SOP 第一周权限：全部 approval_required + 硬限单日预算≤$5、CPC≤$0.3、每日新建≤5 |

### 2.2 第二轮（07 已确认，2026-07-16）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 方案基调 | 以 v1.0 为基础继续，不推翻；核心价值四合一：自动测 offer 流水线、飞书数字员工、情报整理、投放安全监控；不急上线，先聊透 |
| 2 | 多员工模型 | 一个 Hermes 对应一个员工，该员工名下所有数据（MCC/CID、联盟连接）Hermes 均有使用权；本期先把 wj07 这套完整跑通，代码可复制部署 |
| 3 | 范围 | 先 07（wj07）一套；换员工凭证/配置即可再起一套 |
| 4 | CID 来源 | wj07 在 CRM 绑定的 MCC/CID（实查：MCC 941-949-6301「zwj0123」活跃，名下 39 个 CID；791-461-8254 已删除不用） |
| 5 | LH 双池 | id=17（wenjun3/LH1）+ id=277（novanest/LH2）都接，独立 offer 池；A 无 B 有→用 B；两池都有→选佣金率高的池；A 池高 ROI→用 B 再投一份（双开，走复测队列+审批，理由「高 ROI 扩池」） |
| 6 | 团队去重 | 共享谷歌表格（人人可编辑）：登记「团队已跑记录（域名+国家+结果）」+「域名黑名单」两类；各员工 Hermes 测完自动登记、建队列前先读表；**替代** SSH 读库方案；表格开工时新建，结构见 5.4 |
| 7 | 黑名单初始 | 从零开始，靠每日复盘 + 人工拉黑累积 |
| 8 | 建广告参照 | 广告结构/关键词参照 kyads 现有实现（已读代码，规格见第 5.5 节）；**落地 URL 例外**：final URL 必须是商家域名，不照抄 kyads（07 修正，见 HM-D04） |
| 9 | 文案 | **默认 filter 模式（07 纠正，2026-07-20，对齐 kyads 生产默认）**：不 AI 生成——SerpApi 抓品牌词真实在投广告，LLM 只当「筛选官」原文照搬挑选组装 RSA；关键词=域名品牌词；预算/CPC 固定 $2/$0.3。AI 生成（ai_generate）仅 07 明确要求时用。审批卡片逐条展示给 07；成熟后免逐条审 |
| 10 | 测试国家 | 前一天指定「xx 国家多少个」按计划执行；未指定则跟 offer 可投国家，优先美国，一任务一国 |
| 11 | 每日计划 | 每晚 Hermes 主动飞书问「明天测什么」+ 07 随时一句话改计划，两者都要 |
| 12 | 默认预算/CPC | **固定 $2/天、CPC $0.3（07 纠正 2026-07-20，对齐 kyads 默认，不用 AI 建议）**；07 可在建任务时显式指定（仍受硬限 ≤$5 / ≤$0.3）。ai_generate 模式下才用 LLM 建议 |
| 13 | 效果闭环 | Hermes 自己定时拉 LH 转化/佣金报表算每任务 ROI |
| 14 | 归因 | 追踪链接不能带标识；靠**广告系列命名**归因，参照 CRM 实现（规格见第 5.6 节） |
| 15 | 测试周期 | 天数上限 + 花费止损双卡控（具体数值开工时拟一版给 07 审） |
| 16 | 监控 | 每小时巡检；触发止损条件直接自动暂停 + 飞书报告 |
| 17 | 放量 | 前期 Hermes 主动提案（ROI 达标发审批卡片建议加预算，每次加幅有上限），07 批准才执行；经验/学习样本足够后过渡到 Hermes 自主决策 |
| 18 | 每日复盘 | 以在投任务总览为主：每任务花费/点击/转化/佣金/ROI |
| 19 | 交互渠道 | 全部走 07 与 Hermes 的飞书私聊，不建群 |
| 20 | 代码管理 | 只服务器本地 git + 本机克隆备份，不上 GitHub |

## 三、现状基线（2026-07-13 SSH 实测）

- 服务器 43.165.170.242（腾讯云海外，2核/3.7G/59G，磁盘用 25%）。
- 仅剩全新 Hermes Agent v0.18.2（git 安装 upstream 56a8e81d，`~/.hermes` + `~/.hermes/hermes-agent`）；网关进程空转：无消息平台、无可用模型（config 指向 OpenRouter 报 payment error）。
- 旧 hermes-crm/网关 7 月 9 日已整体下线，备份：服务器 `~/hermes-reset-backup-20260709/` + 本地 `.infra` 副本；归档目录 `~/.hermes.old-20260709`（2.2G）、`~/hermes-crm.old-20260709`（414M）。
- 无 systemd hermes 服务、无 crontab 任务。

## 四、总体架构

```
43.165.170.242
├─ Hermes Agent v0.18.2（大脑 + 交互，systemd 用户服务 hermes-gateway）
│   ├─ 模型：gemai 中转 claude 系（config.yaml base_url 指向 gemai）
│   ├─ 飞书原生接入：旧应用凭证，websocket 长连，仅白名单用户/群可用
│   └─ skills（自然语言指令 → 调 hermes-ads API）：
│       sync-offers / build-queues / create-runs / pump-runs / daily-review
└─ hermes-ads（全新投放后端，Node 22 + SQLite，监听 127.0.0.1:8787，Bearer 鉴权）
    ├─ 联盟适配层（NetworkAdapter 接口，第一期实现 LHAdapter × 2 实例：LH1/LH2 双池）
    ├─ offer 管道（标准化 → 过滤 → 黑名单 → 团队去重 → 队列）
    ├─ 团队登记同步器（读写共享谷歌表格：已跑记录 + 黑名单，本地缓存快照兜底）
    ├─ 任务状态机（create → 审批 → CID 预占 → 发布 → 监控）
    ├─ Google Ads 直连（google-ads-api，wj07 的 MCC 凭证本地副本）
    ├─ LH 报表同步器（拉转化/佣金 → 按系列名归因 → 算 ROI）
    ├─ 风控硬限（代码层强制，见第八节）
    └─ SQLite：/home/ubuntu/hermes-ads/data/hermes-ads.db
```

- hermes-ads 以 systemd **系统服务** `hermes-ads.service` 运行，只绑回环，公网零暴露。
- Nginx 不做任何新配置（纯飞书审批，无公网页面）。
- 代码管理：hermes-ads 建独立 git 仓库，**服务器本地裸库 + 本机克隆备份，不推 GitHub**（07 已确认，2026-07-16）。

## 五、offer 管道设计（对应 SOP 第 3-6 节）

### 5.1 联盟适配层

```
interface NetworkAdapter {
  network: string                          // 'LH' | 'RW' | ...
  fetchAllOffers(): RawOffer[]             // 全量 offer 明细
  fetchStatuses(): OfferStatus[]           // 在线/合作状态校验
}
```

**v1.4（2026-07-20 HM-D15）：全联盟入池** —— 07 明确「每个联盟都拉取 API 存入池子」。wj07 名下 7 个联盟账号全部接入（key 复制自 CRM `platform_connections`）：

| 池 | 联盟 | 账号 | 适配器 | 接口形态 |
|---|---|---|---|---|
| LH1 / LH2 | LinkHaitao | wenjun3 / novanest | `adapters/lh.js` | post_form merchantBasicList3，200/页 |
| CG1 | CollabGlow | allurahub | `adapters/generic.js` | post_json monetization，2000/页 |
| EV1 | EngageVantage | mevora | 同上 | 同上 |
| MUI1 | UltraInfluence | allurahub | 同上 | 同上 |
| PM1 | PartnerMatic | keymint | 同上 | 同上 |
| RW1 | Rewardoo | parcelandplate | 同上 | post_form merchant_details，200/页（1000 必 504），慢（CRM 有 504 前科） |

- 端点/分页/限流参数照抄 CRM 生产 `platform-api.ts` 在跑配置；monetization 系传 `relationship:"Joined"` 只回已加入商家。
- 各联盟跳板域名（collabglow/engagevantage/ultrainfluence/partnermatic/rewardoo/creatorflare）已加入后缀跟链 `TRACKER_HOST_PATTERNS`，终态停跳板=没跟到落地页的判定对新联盟同样生效。
- CRM 还有 CF（7 商家）连接但 wj07 无 api_key 记录的不接；RW 连接在 CRM 侧 status=error（连续失败 3 次），Hermes 侧同步失败会在 sync_logs 里体现、不影响其他池。

历史（v1.1 双池版）：
- 池 A = CRM platform_connections id=17（account_name「wenjun3」，LH1）；池 B = id=277（「novanest」，LH2）。api_key 一次性从 CRM 库复制到本地 `.env`。
- 每池独立拉取全量明细，落 `offers` 表（带 `pool`/`network` 字段）。
- **选池规则**（建任务时定）：
  1. 只有一个池有该 offer → 用有的那个池。
  2. 两池都有（普通新测）→ 选佣金率高的池。
  3. 某池已验证高 ROI 可盈利 → 用另一个池再投一份（双开，占两个 CID）；走复测队列 + 飞书审批，理由固定「高 ROI 扩池」。

### 5.2 域名标准化（SOP 第 5 节，全库统一函数）

小写 → 去协议 → 去路径/参数/锚点 → 去 `www.` → 去尾斜杠 → 子域名提取主域名（`m.x.com`/`shop.x.com` → `x.com`）。所有比较（黑名单/已跑/队列）只用 `normalized_domain`。

### 5.3 筛选流水线（SOP 第 6 节，每步落库可追溯）

1. **可测池**：Online + Joined + tracking_url 非空 + siteUrl 可解析域名 + 推广限制不禁目标国家。
2. **排黑名单**：normalized_domain 命中 `domain_blacklist`(status=active)、restricted_keywords 明确禁 PPC/SEM/brand bidding、promotion_area 禁目标国家 → 排除。
3. **团队去重**：`normalized_domain + country_code` 已在共享表格「已跑记录」（本地缓存表 `team_tested_offer_history`）→ 默认不进新测队列。
4. **新测队列** `test_queue`：未跑过的，按佣金高、国家明确、规则清晰、支持 deeplink 排优先级。
5. **复测队列** `retest_queue`：仅限 ROI 好放量 / 人工标记复测 / 上次失败可恢复 / 规则重大变化 / 扩国家 五种理由，理由必填。

### 5.4 团队登记：共享谷歌表格（v1.2，2026-07-17 接入 07 提供的团队现有表格）

- **表格已定**（07 提供，2026-07-17）：`https://docs.google.com/spreadsheets/d/1wQE3ieaVJhkDPvbpn2y6ldThh6gtowQuhW0Tzlm6p7Q`，**任何有链接的人可编辑**（无需逐个授权）。
- **读通道**：公开 CSV 导出（gviz），**不依赖 Sheets API / 服务账号**，SA 的 GCP 项目 API 未启用也照常工作（已实测）。
- **写通道**：仍需 Sheets API（当前未启用）。写失败的行落本地 `pending_sheet_writes` 表兜底 + 本地缓存立即生效，API 启用后可补写。
- 团队表格现有工作表（实测结构，与原草案不同，按团队现状适配）：

**「黑名单」（gid=0，团队既有，约 4800 行）**

| 列 | 说明 | Hermes 用法 |
|---|---|---|
| 商家名称 / 商家平台 | 原始名称与联盟平台 | 留档（note/added_by 辅助） |
| 商家域名 | 域名 | **标准化后作黑名单主键**（5.2 规则） |
| 下架时间 / 备注原因 / 名单来源 | 时间、原因、登记人 | 映射到本地 `domain_blacklist` 的 added_at / reason / added_by |

**「白名单」（gid=874482181，即「推荐商家表」，团队既有，约 390 行）**

| 列 | 说明 | Hermes 用法 |
|---|---|---|
| 商家名称 | 原始名称 | 清洗为 match_key（小写去符号）与 offer 商家名匹配 |
| ROI参考 / 佣金率 / 结算率 | 团队实测参考 | 留档展示 |
| 标记 | 重点分享 / 普通分享 | **入队严格优先（HM-D15 改）**：重点 +1200 / 普通 +1000，白名单必然排在所有非白名单前（非白名单打分上限约 150） |
| 分享时间 / 备注 | 月份、说明 | 留档 |

**「已测记录」（gid=1885280150，已建，Hermes 登记用）**：列结构十列（normalized_domain / country_code / network / network_account / owner / source / tested_at / result / roi / note）。
- **登记时机（HM-D15 定）**：每发布一个广告（状态机进 monitoring）即自动 `registerTestedOffer` 登记一行（result=observing），同时写进本地 `team_tested_offer_history` 立即参与团队去重。
- **写通道（07 拍板 2026-07-20：不开 Sheets API）**：双通道自动降级——① Sheets API（哪天启用了自动生效，最快）→ ② **无头浏览器匿名编辑**（`src/lib/sheet-anon-write.js`：表格「任何有链接可编辑」，匿名 Chrome 会话直接写；公开 CSV 数行定位下一行 → Name Box 跳格 → 逐格键入 → Enter，写完重读 CSV 复核该行真的落表才算成功，全局串行，约 20s/行）→ ③ 两条都失败落 `pending_sheet_writes` 兜底，每日团队表同步时自动补写（`flushPendingSheetWrites`）。每日同步重建缓存时 pending 未补写的行会回填缓存，去重不丢。**服务器实测**：run#14 那行经浏览器通道 19s 写入成功，公开 CSV 复核在表 ✅。

- 同步节奏：建队列前全量读表刷新本地缓存（`domain_blacklist` / `team_whitelist` / `team_tested_offer_history` 三张 SQLite 表）；任务出结果后 Hermes **自动追加**已测记录行（写失败落 pending 兜底）。表格不可达 → 用本地最后快照继续跑 + 飞书告警。
- 实测效果（2026-07-17 服务器）：黑名单同步 4802 条、白名单 387 条；队列重建 9220 候选中 **黑名单排除 695**、白名单命中加权 16，新测队列 4313。

### 5.5 建广告规格（v1.1，参照 kyads 实现，2026-07-16 已读代码提炼）

> 参照来源：`C:\Users\Administrator\Desktop\项目\kyads`（关键文件：`lib/ad-create/google-ads-publisher.ts`、`default-campaign-name.ts`、`prompts/generate.ts`、`final-url.ts`）。

| 项 | Hermes 规格（对齐 kyads） |
|---|---|
| 结构 | 一任务 = 1 SEARCH campaign + 1 ad group + 1 条 RSA |
| 网络 | 仅 Google 搜索（`target_google_search=true`，其余 false） |
| 出价 | Manual CPC（不开 eCPC）；ad group / keyword 设 `cpc_bid_micros` |
| 地理 | 一国定位，`positive_geo_target_type: PRESENCE` |
| 语言 | 按国家映射语言常量 |
| 设备 | 不设 device criterion（默认全设备） |
| 关键词 | **域名品牌词 1 个**（kyads 口径，如 `["tous"]`），**PHRASE** 匹配；不做 LLM 扩词 |
| 否定词 | campaign 级 PHRASE；公共否定词表 = kyads 生产 `team_google_ads_publish_settings` 同款 55 词（2026-07-20 取回），上限 200 |
| RSA 文案 | **默认 filter 模式（kyads 生产默认，07 拍板 2026-07-20）**：SerpApi 三路样本（google SERP desktop+mobile、google_ads 六变体、透明中心整域名）抓真实在投广告 → 标题按 `\|/–/—` 拆、描述按句号拆成候选池（去重、30/90 字符过滤）→ LLM 筛选官按 kyads `prompts/filter.ts` 规则原文挑选（合规红线/六维评分/品牌自投+0.5/阈值降级保 3标题2描述下限）→ **代码层强制校验输出必须原文存在于候选池**；LLM 挂了走本地兜底（品牌自投优先）。样本不足下限→任务创建失败如实报 07（严禁编造）。`ai_generate`（LLM 生成，prompt 参考 kyads `prompts/generate.ts`）仅 07 明确说「用 AI」时启用 |
| Final URL | **必须是商家域名**（07 修正，2026-07-16）：优先商家首页；offer 支持 deeplink 且有更合适页面时可用该商家域名下的内页。**不能**用联盟 click URL，也**不能**照抄 kyads 的「验证落地页当 final URL」做法 |
| 追踪后缀 | 解析 LH tracking_url 跳转到商家落地页后，抽取落地页 URL 的 **query 参数**作 campaign 级 `final_url_suffix`（完整联盟链接 = 商家域名 + ? + suffix）；不用 tracking template。维护采用**完整后缀池体系**（07 已定 A 方案，2026-07-17），见 5.7 |
| 预算/CPC | **filter 模式（默认）固定 $2/$0.3（kyads 同款默认值）**；ai_generate 模式才用 LLM 建议；硬限预算 ≤$5、CPC ≤$0.3 代码层校验 |

**广告系列命名（归因关键，采用 CRM 6 段格式而非 kyads 新 5 段）：**

```
{序号3位}-{平台段}-{商家名清洗}-{国家}-{MMDD}-{merchantId}
例：001-LH1-nike-US-0716-12345
```

- 平台段带池序号（LH1=wenjun3，LH2=novanest），末段必须是 LH 数字 MID——归因解析依赖这两段（见 5.6）。
- 序号按 Hermes 本地历史最大号 +1，撞名自动顺延。

### 5.6 效果归因与 ROI（v1.1，参照 CRM 实现）

> 参照来源：crm-mvp `src/lib/platform-api.ts`（LH cashback2 拉单）、`campaign-naming.ts`、`campaign-merchant-link.ts`、`daily-stats-commission.ts`。

- **拉单**：LH `api.php?mod=medium&op=cashback2`（token=api_key，单次窗口≤31 天，页大小≤2000，限频约 4s），两池各拉各的。
- **归因链（两跳，与 CRM 一致）**：订单 `mcid`（数字 MID）→ 商家；系列名「平台段 + 末段 MID」→ 同一商家。即收入按 **商家 + 联盟账号（池）** 维度对齐花费，不靠追踪链接带标识（LH 的 tagcode/sub id 不用于归因）。
- **ROI 口径**：`(佣金 − 拒付 − Google 花费) / 花费`；日期按 CST 切日两侧统一。
- Hermes 一 CID 一系列、一商家一国一任务，正常情况商家↔系列一一对应，归因比 CRM 场景更干净；双开（两池同商家）时按平台段（LH1/LH2）分开对齐，不串池。
- 同步频率：每小时随监控巡检拉近 14 天增量；每日复盘前全量对账一次。

### 5.7 追踪后缀池体系（v1.1，07 已定 A 方案：完整复刻 CRM 模式，2026-07-17）

> 参照来源：crm-mvp `src/lib/suffix-engine/`（`stock-producer.ts`、`suffix-generator.ts`、`config.ts`）、`/api/cron/suffix-replenish`、`/api/v1/suffix/lease|report`、MCC 脚本 `link-exchange-script-template.ts`。

原理：联盟后缀（clickid/token 类参数）由联盟按次签发、有时效，**点击增长就要换新后缀**，否则转化算不到头上。CRM 用「池子预生成 + 脚本换链」解决，Hermes 完整复刻，但做一处架构适配：

**与 CRM 的差异**：CRM 靠 MCC 里的 Google Ads 脚本（AdsApp）监点击、换后缀；Hermes 自己直连 Google Ads API，**不装 MCC 脚本**，由 hermes-ads 后端直接查点击增量 + mutate `final_url_suffix`，链路更短。

| 环节 | Hermes 规格（对齐 CRM 数值口径） |
|---|---|
| 后缀生成 | 住宅代理跟 LH tracking_url 跳转（支持 LH lhdeal 延迟 JS 跳转，需无头浏览器兜底，单条超时 55s），取落地页 query 串入池；落地被商家前端洗参时回溯跳转链「同根域名 + 带联盟追踪键」那一跳取 query（CRM D-182 同款兜底） |
| 后缀前置验证（硬门槛） | **建任务时先跟链验证能拿到 suffix，再生成文案**（2026-07-20，07 反馈「suffix 跟踪链接没加上」后落地）：验证失败 → 该 offer 从队列排除（`test_queue.status=suffix_failed`）+ 飞书告知，不烧 SerpApi、不占每日额度；验证成功的后缀直接入池，审批卡片/确认页展示「追踪后缀 + 完整联盟链接」供 07 核对。⚠️ LH 部分 offer 走 **tatrck.com 跳板 cookie 归因**（lhdeal → tatrck `?url=&s=lh_xx` 302 → 商家落地**无任何参数**，tous/thriftbooks/airalo/canvasondemand/suppliesoutlet 实测均如此），这类 offer 无法用 final_url_suffix 追踪，只能排除 |
| 池表 | SQLite `suffix_pool`：campaign 维度，状态 `available / leased / consumed / expired` |
| 时效 | 入池 `expires_at = now + 36h`；过期回收；`leased` 超 6h 无回执回收 |
| 水位 | 目标库存 20 / 低水位 6 触发补货（起步任务少，可先按每系列 5/2 缩配，跑通后调） |
| 补货 cron | 每 5 分钟：先回收过期 → 低水位系列补货（带锁防并发、断链冷却） |
| 换链触发 | 每 5 分钟任务泵内查各在投系列**点击增量**（Google Ads API）；有增长 → 从池取最旧可用后缀 → mutate campaign `final_url_suffix` → 标记 consumed |
| 断链处理 | 跟链连续失败 ≥3 → 飞书告警 + 8h 冷却，**不自动停投**（广告继续跑旧后缀，对齐 CRM 策略）；由 07 决定处置 |
| 发布时 | 建系列前先为该商家生成首条后缀，发布时一并挂上；生成失败则任务 failed（发布前置校验） |
| 代理 | **复用 CRM 同款代理商 kookeey**（07 已定，2026-07-17）：网关 `gate.kookeey.info:1000`，socks5/http 同端口双协议，用户名模板带 `{COUNTRY}` + 8 位 session + life-5m（每会话 5 分钟出口轮换）；wj07 在 CRM 即绑 kookeey。凭证密码在 CRM 库 `kyads_proxies` id=1（AES 加密存储），P1 开工时解密复制一份到 Hermes 本地 `.env`，运行时不依赖 CRM |

## 六、任务状态机（对应 SOP 第 7 节）

```
created ──审批请求──▶ pending_approval ──07在飞书批准──▶ approved
   │                        │拒绝                        │
   │                        ▼                            ▼
   │                     rejected                  cid_reserved（一CID一系列）
   │                                                     │
   └───任何步骤失败───▶ failed（记录阶段+原因，停住不重试）│
                                                         ▼
                                                    publishing ──▶ monitoring
                                                                      │
                                                          止损触发→pause(自动，v1.1)
                                                          加预算/加CPC/扩词/扩国/双开(需审批)
```

关键规则（代码层强制，不靠提示词）：
1. 创建一律 `mode=approval_required`（第一周无 auto_publish）。
2. Hermes 机器身份**不能审批任何任务**（审批接口校验审批人身份 ≠ 机器）。
3. CID 预占：本地状态可用 + 远程实查无 enabled campaign，双验通过才占；失败即停。
4. 发布失败 → failed 并飞书报告，不自动换 CID 重试。
5. SOP 第 9 节 10 条停止条件全部实现为状态机的硬中断。
6. （v1.1）测试周期双卡控：天数上限 + 累计花费止损（具体数值 P2 开工时拟定给 07 审），到期/到线自动出结论并登记共享表格。
7. （v1.1）放量提案：monitoring 中 ROI 达标（阈值待拟）→ Hermes 主动发审批卡片建议加预算/加 CPC，每次加幅有上限（如 ×2 封顶）；07 批准才执行。经验样本足够后（07 另行批准）过渡到自主决策。
8. （v1.1）CID 来源：wj07 名下 MCC 941-949-6301（CRM google_mcc_accounts id=1「zwj0123」）的 39 个 CID；预占前本地状态 + 远程实查双验（沿用 v1.0 规则）。

### 飞书内审批流程（交互式卡片 + 指令双通道，07 私聊）

1. 任务进入 pending_approval 后，机器人向 **07 私聊**发**交互式卡片**（07 要求，2026-07-20）：蓝头卡片展示任务号、offer、域名、国家、池（LH1/LH2）、文案模式、预算、CPC、止损、关键词、RSA 文案逐条 + **「✅ 批准发布 / ❌ 拒绝」两个按钮**。卡片发送失败自动降级纯文本（含审批链接）。
2. 审批双通道任选：**① 点卡片按钮**——打开确认页（`https://google-data-analysis.top/hermes-approve/approve?rid=&act=&sig=`，HMAC-SHA256 签名链接、只发 07 私聊、GET 只展示确认页防预抓、POST 才执行、幂等防重复）；链路 = CRM nginx `/hermes-approve/` → 反向 SSH 隧道（CRM:18788 → Hermes 回环:8788，`hermes-approve-tunnel.service` 常驻，Hermes 安全组仅放行 22 故借道 CRM 入口，仅作哑 HTTPS 转发、无数据耦合）；**② 私聊回复指令**：`批准 #12 同意低预算测试` / `拒绝 #12 规则不清`。
3. 后端审批接口校验**审批人飞书 open_id 白名单**（初始仅 07 一人），并落审计日志（谁、何时、批/拒、备注）；机器人自身 id 硬编码禁止出现在白名单。
4. 同一任务提醒只发一次（去重表），07 可指令要求重发。
5. 每日复盘输出当日全部审批流水，供 07 复核（信任边界补偿：审批指令经由 Agent 转发，审计日志 + 日复盘保证可追责）。

## 七、数据模型（SQLite，建表 DDL 开工时定稿）

| 表 | 用途 | 对应 SOP Sheet |
|---|---|---|
| `offers` | 全量 offer 原始池 + 状态校验结果（含 `pool` 字段区分 LH1/LH2） | all_offers_raw + advertiser_status_raw |
| `domain_blacklist` | 域名黑名单**本地缓存**（源=共享谷歌表格 Sheet2） | domain_blacklist |
| `team_tested_offer_history` | 团队已跑记录**本地缓存**（源=共享谷歌表格 Sheet1，任务出结果后回写表格） | team_tested_offer_history |
| `daily_plans` | 每日测试计划（国家×数量，飞书指令写入） | — |
| `test_queue` / `retest_queue` | 新测/复测队列（含 priority/test_decision/reason） | agent_offer_test_queue / retest_queue |
| `ad_runs` | 投放任务主表（状态机） | agent_run_log 主体 |
| `run_events` | 任务事件流水（每次状态迁移/API调用/失败原因） | agent_run_log 明细 |
| `cid_registry` / `cid_reservations` | CID 池与预占记录（唯一约束保证一 CID 一系列） | — |
| `approvals` | 审批流水（审批人 open_id/动作/备注/时间） | — |
| `daily_reports` | 每日复盘快照 | daily_report |
| `suffix_pool` | 追踪后缀池（campaign 维度，available/leased/consumed/expired，36h 过期） | — |
| `sync_logs` | 联盟同步/去重同步/后缀补货日志 | — |

## 八、风控硬限（代码层，写死不可被提示词绕过）

| 限制 | 值 | 实现位置 |
|---|---|---|
| 单任务日预算 | ≤ $5 | 创建接口校验 + 发布前二次校验 |
| 单任务 CPC | ≤ $0.3 | 同上 |
| 每日新建任务数 | ≤ 5 | 创建接口按日计数 |
| 一 CID 一广告系列 | 唯一约束 + 远程实查 | cid_reservations + Google Ads 查询 |
| 自动调整范围 | 仅 pause/keep：每小时巡检，触发止损条件**直接自动暂停**+飞书报告（v1.1 简化，不做自动降预算/降CPC 中间档） | monitor 执行器白名单 |
| 加预算/加CPC/扩词/扩国 | 必须飞书审批 | 状态机 |
| 机器不能自审 | 审批人白名单排除机器 id | 审批接口 |
| 停止条件 | SOP 第 9 节 10 条 | 状态机硬中断 + 飞书报告 |

第一周结束后由 07 复盘决定是否开放小额 auto_publish（另立变更记录）。

## 九、每日运转（cron 编排）

| 时间 | 任务 | 方式 |
|---|---|---|
| 08:00 | 联盟全量同步（双池）+ 重建队列 | crontab curl `POST /api/sync` + `/api/queues/rebuild` |
| 08:30 | 共享表格同步（已跑记录 + 黑名单刷新本地缓存） | crontab curl `POST /api/team-history/sync` |
| 09:00 | 按当日计划（`daily_plans`）批量建任务 → 发审批卡片 | crontab curl `POST /api/runs/plan-execute` |
| 每 5 分钟 | 任务泵（推进状态机到下一卡点 + 新审批提醒 + 点击增量换后缀） | crontab curl `GET /api/cron/pump?notify=true` |
| 每 5 分钟 | 后缀池补货 + 过期回收 | crontab curl `POST /api/cron/suffix-replenish` |
| 每小时（:12） | 监控巡检：花费/点击回流落 `run_daily_stats` + 止损双卡控自动暂停 + 换后缀健康/回流断流告警（同类 6h 去重） | crontab curl `POST /api/cron/monitor` |
| 每小时 | 监控巡检（Google 花费 + LH 近 14 天报表增量 → 止损判断 → 自动暂停/放量提案） | crontab curl `POST /api/cron/monitor` |
| 21:00 | 飞书私聊问 07「明天测什么（国家×数量）」，回复写入 `daily_plans`；07 全天可随时改 | Agent skill |
| 21:30 | 每日复盘生成（在投任务总览：花费/点击/转化/佣金/ROI）+ LH 全量对账 + 飞书推送 | crontab curl `POST /api/report/daily` |
| 全天 | 07 飞书指令（同步/建任务/审批/改计划/复盘），由 Agent skills 调 API | 飞书私聊 |

所有 cron 接口用 `CRON_SECRET` Bearer 鉴权；日志落 `/var/log/hermes-ads/`，按周轮转。

## 十、分期计划与验收

| 期 | 内容 | 验收标准（07 验收） |
|---|---|---|
| P0 | gemai 模型配置 + 飞书原生机器人上线 + 白名单 | 飞书群 @Hermes 正常对话，非白名单用户被拒 |
| P1 | hermes-ads 骨架 + LHAdapter×2（双池）+ 筛选管道 + 共享谷歌表格（新建+同步器）+ 队列 + skills(sync/queues) | 飞书发「同步 offer / 生成队列」输出 SOP 7.1 规定格式的数量汇报（分池），黑名单/去重抽查零漏，表格自动登记可见 |
| P2 ✅（2026-07-20 交付，见 HM-D09/D10） | 任务状态机 + 每日计划（21:00 问询+指令改）+ 飞书审批 + CID 预占 + Google Ads 发布（5.5 规格）+ skills(create/pump) | 1 个 approval_required 任务全链路走通（含审批卡片展示文案）——**run#8 Tous（filter 模式，真实广告原文筛选）**已到 pending_approval、卡片已发，**发布段等 07 飞书批准后触发**（AI 版 run#7 已按 07 纠正删除，默认改 filter 见 HM-D10）；硬限实测（超 $5/超 0.3/超 5 个/机器自审 全部被拒）✅；测试周期/止损数值定稿→待确认项 #7（默认 7 天/$15 待 07 拍板）。21:00 主动问询属 Agent 侧定时，随 P3 cron 编排一起做 |
| P3 | 每小时 monitor（止损自动暂停 + 放量提案）+ LH 报表归因 ROI + 每日复盘 + cron 编排 | SOP 第 7/8 节每日流程完整跑一天，复盘含每任务花费/点击/转化/佣金/ROI。进度：monitor ✅（HM-D14）、每日复盘 ✅（HM-D19）、自动调参 ✅（HM-D25）、**佣金归因 ROI ✅（HM-D28，扩展为全联盟拉单不止 LH）**；余：放量提案 ROI 阈值（待确认项 #8） |

每期完成在本文档「变更记录」追加一行，07 验收后进下一期。

## 十一、待 07 确认项

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 1 | ~~审批页入口~~ | 纯飞书内审批，不恢复公网页 | ✅ 已确认（2026-07-13） |
| 2 | ~~LH 凭证~~ | 用 CRM wj07 的 LH 连接（id 17/277 已核实 connected） | ✅ 已确认（2026-07-13） |
| 3 | ~~黑名单初始数据~~ | 从零开始，共享谷歌表格累积（复盘建议 + 人工拉黑） | ✅ 已确认（2026-07-16） |
| 4 | ~~CID 池范围~~ | 起初是 wj07 名下 MCC 941-949-6301（zwj0123）的 39 个 CID；2026-07-28 加入 MCC 988-504-7597（gjc0324）。**HM-D51 起池子由巡检自动与两个 MCC 对账**（ENABLED 入池、注销停用摘掉），07 加子账号不必再通知 Hermes。预占前双验不变 | ✅ 已确认（2026-07-16，HM-D51 改为自动同步） |
| 5 | ~~wj07 两条 LH 连接用哪条~~ | 都用：双池模型（LH1=wenjun3、LH2=novanest），选池规则见 5.1 | ✅ 已确认（2026-07-16） |
| 6 | ~~hermes-ads 是否推 GitHub 私库~~ | 不推；服务器本地 git + 本机克隆备份 | ✅ 已确认（2026-07-16） |
| 7 | 测试周期/止损具体数值 | 演进：v1 拟 7 天/$15（2026-07-20）→ HM-D31 改 $9/3 天 → **HM-D32 现值：观察期 `test_days_cap=2` 天、`test_spend_cap_usd=9`、净亏线 `stop_net_loss_usd=4`、等待期 `wait_days=2` + `wait_roi_min=100%`、安全区 `roi_safe_threshold=200%`**。**2026-08-04 新增外部证据（第十二章 yz04 实证）表明现值偏紧**：2 天线回溯误杀 18.4% 的最终出单系列、$9 线误杀 28.9%（损失佣金 $26,976）；且「$2 预算 + CPC 硬限 $0.3 + 2 天」最多只能积累约 13 个点击，而首单前需要中位 32、p90 111 个点击——参数组合在数学上凑不出判决所需样本量。**2026-08-04 07 已拍板判决口径，最终值见 12.5a 判决矩阵**：主判据＝**累计点击 ≥50 且零订单 → pass**；硬顶按「团队是否已验证该商家」二分——**未验证 $6、已验证 $11-14**；未验证且实测 CPC ≥$0.12（$6 买不到 50 点击）到线即 pass 不追加。依据是四格矩阵里只有「未验证＋高 CPC」亏钱（ROI −10.6%），其余三格 ROI 50-131%。12.5 原八条建议降为推导过程 | **口径已拍板（12.5a）**；剩余待确认：① 已验证商家 CPC 上限放开幅度（暂拟 $1.0，见 12.5a 冲突说明）② 硬顶取 $11 还是 $14 ③ 天数上限是否仍保留 |
| 8 | 放量提案 ROI 阈值与加幅上限 | P3 开工时拟定 | 待拟定 |
| 13 | **扩池能力受限于联盟账号数量**（原表述「Hermes 少接了池、复制 api_key 即可」已作废，见 12.5b 更正说明） | 07 于 2026-08-04 指出 yz04 的核心动作是「同商家同联盟**不同账号**再开一条」，实测该组 ROI 421.9% vs 同账号再开 129.7%。**Hermes 已接入 wj07 全部 7 个有效连接，没有漏接**；但除 LH（2 个）外每个联盟只有 1 个账号，扩池无从执行。建议：① **先只在 LH 上启用扩池**（两池均在跑，$361.84 + $84.13），用它验证 #14 的触发线；② 若要推广，需 07 去联盟**新开第二个账号**，按 7 月花费排优先级 MUI($410.79) > EV($380.65) > PM($249.50) > RW($208.38) > CG($8.22) | 待 07 决定（是否新开联盟账号；LH 先行不阻塞） |
| 16 | **CRM 侧新增只读接口 `/api/team/verified-merchants`** | 12.5a 判决矩阵一半依赖「该商家是否已被团队其他人跑出订单」，而实查表明 Hermes 现有三个数据源都拿不到（「已测记录」表 374 行全是自己写的、`result` 全 `observing`、`roi` 全空；白名单表只有 389 家且无域名列）。**07 已拍板走 CRM 只读 API、Hermes 每日拉**，接口字段与匹配键约定见 12.5c。CRM 侧改动需同步登记进 CRM 自己的设计方案 | ✅ 通道方式已定（2026-08-04）；待实现 |
| 17 | ~~已验证商家的 CPC 上限放开幅度~~（**已被 12.5f 取代，不必再拍「幅度」这个数**） | 原问题：07 选了「CPC 硬限保持 $0.3」＋「已验证商家只卡总花费不卡 CPC」，两者在高 CPC 商家上冲突，暂按「未验证 $0.3 / 已验证放开到 $1.0」实现。**2026-08-05 作废**：07 指出 Hermes CPC 逻辑搞反，12.5f 实证限额不该是「给已验证商家一个拍脑袋的固定放开值」，而应等于**该商家自己的盈亏平衡 CPC × 安全系数**（商家间差 328 倍，$0.016 到 $5.35，任何单一数值都必然同时过松与过紧）。原「$1.0」与「$0.3」双方都不再采用。边界警告仍有效并已被 12.5f 解释：`231-MUI1-VisitorsCoverageInc` 与 wj02 三次全亏，属于付超盈亏平衡点的典型 | ✅ 已由 12.5f 结案；后续见 #21 |
| 20 | **编号治理：拆成 `HM-D`（已生效改动）与 `HM-A`（分析与待实现设计）两套** | 实地审计发现 Hermes 服务器代码/库里一直在自行编号，**已用到 HM-D63**，且 **D55～D61 七个号与本文档 8/04-8/05 新增的记录完全重叠**（服务器侧 D57 = 智能出价大改造、本文档原 D57 = 软删除修正，同号不同事）。根因是两边各自递增：服务器侧记的是**已生效的代码改动**，本文档 8/04 起记的是**分析结论与待实现的设计决策**，性质本就不同，单序列顺延只会再撞。**07 于 2026-08-05 拍板方案 A**：① `HM-D` 专指**已落地的代码改动**，编号**以服务器代码为准**，本文档只做记录、不自行分配；② `HM-A` 专指**分析、证据与待实现的设计决策**，由本文档独立编号；③ 本文档原 HM-A01～D61 七笔全部重编为 **HM-A01～A07**（见 13.1 映射表）；④ 服务器侧 D55～D63 九笔改动**全部补记进变更记录**（07 同时拍板，见 13.2）。**纪律**：以后若本文档要新增 `HM-D` 号，必须先 `grep -rho 'HM-D[0-9]\+' ~/hermes-ads/src` 取服务器侧最大号再往后接 | ✅ 已拍板并执行（2026-08-05） |
| 21 | **CPC 限额改为按商家盈亏平衡值计算（12.5f）** | 07 指出「贵的商家要去抢才用高 CPC，不是直接拉 2 刀」。实测确认 `default_cpc_usd = 2` 是全局默认、`hard_cpc_usd = 100` 形同虚设，代价是在投 34 条有 **24 条花超 $6 却只有 10 条攒到 50 点击**、14 条卡在判决无效区已烧 $172。两个方向的错同时在犯：Noyz 盈亏平衡 $0.0232 却付 $0.403（**17 倍**）、而 Slate Flosser（$4.68）/ Oxiline（$2.90）该抢没抢。**07 已四项全部拍板**：安全系数 **0.5**、无数据商家探测下限 **$0.30**、`hard_cpc_usd` **收回 $2**、低平衡值商家**照投不拦靠闸门兜**。规则效果已回溯验证：新上限压住的量**恰好是全部亏钱的量**（不受影响的 384 个商家 ROI 2,196.7% / 净赚 $475,732，被压住的 608 个 ROI −22.8%）。**第四项选择连带修正了公式**——原 `clamp(…, 探测下限, …)` 的下界会把 365 个低平衡值商家从 <$0.02 抬回 $0.30 使规则失效，探测下限改为只适用「团队无数据」分支。最终规则与两处配套改动见 12.5f ⑥ | ✅ 四项已拍板（2026-08-05）；**待实现**，实现时必须带上 12.5f ⑥ 配套第 2 条（`cpc_up` 提价规则加天花板，否则会把被压住的商家一路提回去、抵消整个限额） |
| 22 | **`breakeven_trusted=false` 的商家怎么定 CPC 上限** | 12.5c 的 CRM 接口已落地（D-214），实跑暴露 12.5f 公式的适用面比预想小得多：团队已验证 **3,103** 家里只有 **611 家（19.7%）**在 CRM 有广告点击、能算出 breakeven_cpc，其余八成只有「能出单」这个布尔信号；而这 611 家里又有 **345 家（56%）**的 breakeven 被高估（联盟订单含非广告流量、分母只有广告点击，179 家订单数甚至多于点击数）。原打算用「仅可归因佣金」修正，实查 `affiliate_transactions.campaign_id` 全库 46.9 万笔**只填了 1 笔（0.0%）**，此路不通。**07 定的 $2 硬顶恰好挡住了最脏的一档**（订单>点击那 179 家 breakeven $9.08，×0.5 撞顶）；真正受影响的是中间「订单>30%点击」那 **166 家**（breakeven $2.12 → 上限 $1.06，不撞顶）。**需 07 定这 166 家的处置**：(a) 安全系数从 0.5 压到 0.25；(b) 当作无数据、直接走 $0.30 探测下限；(c) 照现状不动，认这个偏差。详见 12.5c-1 | ✅ **07 于 2026-08-05 选 (a)：偏高档安全系数取 0.25**（保留区分度，同时把高估砍掉一半）。实测该系数**对 inflated 179 家里的 118 家真正生效**，另 61 家乘完仍撞 $2 硬顶。**上线核对时又发现第三类问题并已一并处理**：229 家点击 <30，breakeven 无统计意义（`LH/86428` 是 1 单 5 点击算出 $54.46），已加「点击 ≥30」门槛、可信度改四档枚举 `breakeven_quality`，样本不足的归探测下限。**真正可直接乘系数的只剩 203 家（占已验证商家 6.5%）。⚠️ `thin_sample` 这档 07 尚未拍板，暂按探测下限处置**（见 12.5c-1 末尾）。Hermes 侧公式见 12.5f ⑥，**待实现** |
| 14 | **扩池触发线数值** | 建议「单条系列累计净利 ≥$100 且订单 ≥50 → 自动进复测队列，理由『高 ROI 扩池』」。该值由 14 个已扩池商家的第一条成绩事后倒推（见 12.6 第 4 条），**属待验证假设**，上线后需回看命中情况再调 | 待 07 拍板 |
| 15 | **新联盟账号的冷启动策略** | 建议新接入账号的首批任务**只从已验证商家清单搬**、不做新 offer 探索（yz04 新 MUI 账号搬清单 ROI 195.4%，同联盟老账号做新探索仅 33.6%）。需在建队列逻辑里给「新池」加一个特殊分支 | 待 07 拍板 |
| 19 | **$6 为总预算口径下的两项待定** | 07 已澄清 $6 是**单条系列总预算**。12.5e 按此重做验证后需 07 再定两件：① **日预算取 $2 还是 $3**（$3 → $6 撑 2.7 天，$2 → 拖到 6.7 天；低于 2 天学习期不够）；② 是否接受 12.5e ⑦ 的简化规则——「$11-14 只在 CPC 超预期、$6 买不到 50 点击时才对已验证商家启用」，而非默认给已验证商家的额度。附带一条已修正的事实：**$6 的误杀率是 14.3%（全库）而非 12.5a 旧表的 41.3%**，故 $6 比原先判断的宽松 | ✅ **07 于 2026-08-05 定日预算 = $2**。**⚠️ 随之出现一个需要确认的约束**：日预算 $2 + 总预算 $6 意味着最快 3 天花完，落在 12.2 实测 ROI −32.7% 的「2-3 天档」。该相关性很可能是幸存者偏差（跑得久是因为在赚钱，不是跑久了才赚钱），但若 07 希望留出更长学习期，可把日预算降到 $1.5（$6 撑 4 天）。**12.5e ⑦ 的简化规则（$11-14 降级为补救额度）07 尚未表态，暂按简化版实现** |
| 18 | **HM-D48 选品门槛的替换物：品牌词月搜索量 ≥2 万** | 07 已定原 SOP 门槛（含「单笔佣金 ≥$1.5」）**完全更换**。实测四个事前可得判别器只有月搜索量有预测力（Spearman **−0.578**；预估首页出价 0.341、竞争度指数 **0.146** 基本无关，且同为 `HIGH` 标签内 CPC 差 92 倍）。2 万这条线上通过组 CPC $0.0396 / 拒绝组 $0.2145（**差 5.4 倍**），且**与 12.5a 独立算出的 $0.12 CPC 线重合**。取法必须按系列所有品牌词变体取 **max**（否则 MGM、jwpei 会被误杀）。API 已实跑通（`generateKeywordHistoricalMetrics`，78 词 8 国 16 秒）。详见 12.5d | ✅ **07 于 2026-08-05 定门槛 = 2 万**。「单笔佣金 ≥$1.5」**同步删除**（12.5f 给出了更强理由：盈亏平衡 CPC = 单笔佣金 × 转化率，已把单笔佣金作为因子包含，另设佣金门槛是重复约束；且 12.5f ④ 证明高佣金商家恰是最赚钱的一批，$1.5 这条线既筛不掉什么、方向还相反）。**⚠️ 落地时口径按 12.5f 修正**：月搜门槛管的是「$6 内买不买得到 50 个点击」，不是「挑便宜商家」 |
| **23** | **无数据商家的探测下限：$0.30 还是 $2** | 12.5f ⑥ 定的 $0.30 在 Hermes 侧干跑后站不住：支撑它的「全库 77.8% 成交 ≤$0.30」是**含已跑出量老系列的口径**，而探测下限只作用于无数据的新商家——**Hermes 自己 171 条有花费广告实际 CPC 中位 $0.3250、≤$0.30 仅 45.0%**，与 12.5a 已证的测试期 CPC $0.3686 吻合。代价已量化：17 条真正出过单的广告有 **8 条**实际 CPC >$0.30（含净赚的 `BudgetVetCare` $0.403、`Calecim` $0.532）；而 $0.8 上限那档 72 条广告平均只拿 10.7 点击、**0 条跑到 50 点击**。**这一档影响待发布 52 条的全部**（它们 100% 无数据）。详见 12.5f-1 ②③④ | ✅ **07 于 2026-08-05 定 = $2**，即维持现状、不设更低的探测下限。**已落地**：`base = min(default_cpc_usd, hard_cpc_usd) = $2` 作为全档基准线，`max()` 在最外层保证任何一档都不会低于它。干跑全库 452 条 `ad_runs`**降价 0 条**，待发布 52 条一字未变。见 12.5f-1 ⑦ |
| **24** | **`hard_cpc_usd` 怎么放开：统一抬还是按档给** | ⑥ 定的「收回 $2」会削平 14 个 `trusted` 商家（`caracaranyc` 平衡 $8.771 应得 $4.386、`mohawkgeneralstore` $4.179、`lunya.co` $4.163）——它们正是 07「贵商家去抢」的对象。但**统一抬到 $5 会连带放开 31 个 `inflated` 商家**（该档平均平衡值 $12.594，虚高源于 CRM `campaign_id` 零填充），基于不可信数据给 $3–5 比封在 $2 更危险；⑥ 自己那句「$2 硬顶恰好挡住最脏一档」在此成立。详见 12.5f-1 ⑤ | ✅ **07 于 2026-08-05 定「按档给天花板」**。**已落地**：`hard_cpc_usd` 由 100 收到 **$5** 充当 `trusted` 档天花板，新增设置项 `cpc_hard_inflated` = **$2** 把虚高档封死在基准线（该档系数刻意保留但当前不生效，CRM 修好归因后抬这个值即可放开，无需改代码）。实测能加价的仅 14 家、最高 $4.39，虚高档 179 家全部维持 $2。见 12.5f-1 ⑦ |
| 9 | ~~共享表格创建与授权~~ | 07 直接提供团队现有表格（公开编辑，ID `1wQE3…6p7Q`），Hermes 走公开 CSV 读，已接入并实测（见 5.4 v1.2） | ✅ 已确认（2026-07-17） |
| 10 | ~~后缀维护深度~~ | **A 方案**：完整复刻 CRM 后缀池体系（池+补货+点击增量换链），Hermes 用直连 API 替代 MCC 脚本，规格见 5.7 | ✅ 已确认（2026-07-17，07：A） |
| 11 | ~~跟链代理来源~~ | 复用 CRM 同款代理商 **kookeey**（gate.kookeey.info:1000，实查 CRM 库唯一 active 供应商，wj07 已绑）；凭证开工时从 CRM `kyads_proxies` 解密复制到 Hermes 本地 | ✅ 已确认（2026-07-17） |
| 12 | 共享表格的 Google API 权限 | 07 提供公开编辑表格后，**读已完全解决**（公开 CSV 通道，不需要 API）。残留：Hermes **自动写**「已测记录」仍需 Sheets API（kyads SA 的 GCP 项目未启用，实测 403）。P2 前不阻塞——写失败会落 `pending_sheet_writes` 兜底、去重走本地缓存。彻底解决仍是原二选一：07 到 GCP 控制台启用 Sheets API（项目 opportune-epoch-496313-i9），或后续换有 API 的 SA | 半开（仅写通道，不阻塞） |

## 十二、阈值的外部证据基线（yz04 实证，2026-08-04）

> 数据来自 **CRM 生产库**（`ads_daily_stats` 日粒度真值），对象是 CRM 侧两个员工 2026-07 的投放结果：
> **yz04（蓝永培，佣金最高）** 与 **wj07（朱文欣，Hermes 所服务的这位）**。
> 本章只做「阈值该设多少」的证据支撑，不改变 Hermes 的架构与流程；具体调参见待确认项 #7。
> ⚠️ 结论口径已做队列对齐（详见 12.1），未对齐前的版本会得出相反结论。
>
> **📖 本章阅读顺序（子节按「最新结论在前」排，不按字母序）**：`12.1-12.4` 是方法与原始证据 →
> `12.5a` 判决矩阵（07 已拍板的口径）→ `12.5`（推导过程，已被 12.5a 取代）→ `12.5b/12.5c`（扩池约束、
> 团队验证信号通道）→ **`12.5f` CPC 限额（最新，2026-08-05，并修正 12.5d/12.5e 的口径）** →
> `12.5e`（$6 总预算验证）→ `12.5d`（选品门槛）→ `12.6` 其他实证 → **`12.7` 证据边界（必读）**。
> **若各节冲突，以编号更靠后的日期为准**：12.5f > 12.5e > 12.5d > 12.5a。

### 12.1 方法论前提：必须按建立时间分队列

查 CRM 发现 yz04 名下 118 个 Google 账户的**最早系列建立时间全部落在 2026-06-26 13:34 同一分钟**——那是一次批量纳管，把 Google 上已跑一段时间的系列导进 CRM。wj07 的账户则从 3 月到 7 月陆续建立，7/28 一天新增 56 条。直接混着比等于拿「已通过筛选的老系列」比「还在测试期的新系列」。

按建立时间拆开后（7 月有花费的系列）：

| 队列 | yz04 系列 | yz04 净利 | yz04 ROI | wj07 系列 | wj07 净利 | wj07 ROI |
|---|---|---|---|---|---|---|
| 7 月前建（老） | 73 | $17,357 | 260.0% | 6 | $3,076 | 661.0% |
| 7 月新建（新） | 222 | $7,529 | 139.2% | 186 | −$825 | −66.7% |

两人都是「老系列赚钱、新系列亏钱」。**以下全部结论只用「7 月新建」队列**，确保与 Hermes 的场景（全是新建测试）可比。

### 12.2 核心证据：8 天是分水岭

7 月新建队列按「7 月内有花费的天数」分档：

| 有花费天数 | yz04 系列 | 出单率 | ROI | 每系列净利 | 加权 CPC |
|---|---|---|---|---|---|
| 1 天 | 8 | 12.5% | −54.3% | −$1.25 | $0.2699 |
| 2-3 天 | 51 | 52.9% | −32.7% | −$2.44 | $0.0962 |
| 4-7 天 | 107 | 72.0% | **11.3%** | $2.09 | $0.0955 |
| 8-14 天 | 42 | 97.6% | **138.4%** | $50.72 | $0.0815 |
| 15 天+ | 14 | 100% | 356.7% | $379.17 | $0.0476 |

- **4-7 天档 ROI 只有 11.3%**，等于白跑；8 天之后才进入产出区。
- 活过 8 天的 56 条系列贡献了 yz04 新建队列净利的 **98.8%**（$7,438 / $7,529）。
- wj07 同队列只有 **1 条**活过 8 天（占 0.5%），yz04 是 25.2%——差 50 倍。而 wj07 那唯一 1 条也是正 ROI。
- 这不是老系列存活偏差：本表已排除 7 月前建的系列。

**对 Hermes 的直接冲击**：HM-D32 定的观察期是 **2 天**（`test_days_cap=2`，零出单满 2 天即关）。按此表，2-3 天档的 ROI 是 −32.7%，Hermes 现在的节奏在数据上**根本到不了产出区就把系列关掉了**。

### 12.3 止损闸门的误杀回溯

拿 yz04 七月 **201 条「花过钱且最终出单」**的系列做回溯（此处用全队列以获得足够样本）：若在首单前按某条线止损，会砍掉多少、损失多少佣金。

| 闸门 | 误杀条数 | 误杀率 | 损失佣金 |
|---|---|---|---|
| 花费 $3 | 171/201 | 85.1% | $41,368 |
| 花费 $6 | 94/201 | 46.8% | $32,099 |
| **花费 $9（Hermes 现值）** | 58/201 | **28.9%** | **$26,976** |
| 花费 $15 | 20/201 | 10.0% | $14,050 |
| 天数 1 天 | 60/201 | 29.9% | $5,517 |
| **天数 2 天（Hermes 现值）** | 37/201 | **18.4%** | **$2,932** |
| 天数 3 天 | 19/201 | 9.5% | $637 |
| 天数 5 天 | 7/201 | 3.5% | $239 |

分布基础数据：
- **首单出现时点**：51.2% 在首次花费当天、18.9% 第 1 天、11.4% 第 2 天、12.4% 第 3-4 天、3.0% 第 5-7 天、3.0% 第 8 天以后。
- **首单前累计花费**：中位 $5.69、p75 $9.49、p90 $14.01、p95 $19.14、max $59.18（分布很宽）。
- **首单前累计点击**：中位 32、p75 62、p90 111（分布集中得多）。

**结论**：花费是分布很宽的量，拿它当硬闸门必然大量误伤；天数分布集中，代价小一个数量级。

### 12.4 为什么花费闸对 Hermes 尤其危险：参数组合自锁

CPC 差异会让「同一条花费线」变成完全不同的严厉程度。同队列实测 CPC：**yz04 $0.0722 / wj07 $0.2455**，差 3.4 倍。

据此推算观察期内能积累多少样本：

| 配置 | 日预算 | 实际 CPC | 每天点击 | 观察期 | 期内累计点击 |
|---|---|---|---|---|---|
| **Hermes 现值** | $2 | 若打满硬限 $0.3 | ≈6.7 | 2 天 | **≈13** |
| wj07 实测 | $2-5 | $0.2455 | ≈7 | 2 天（中位） | ≈20（63.5% 的系列 <20） |
| yz04 实测 | $3（中位） | $0.0722 | ≈41 | 5 天（中位） | ≈207 |

而 12.3 显示首单前需要中位 **32** 个点击、p90 **111** 个。

**Hermes 当前的「$2 日预算 + CPC 硬限 $0.3 + 观察期 2 天」在数学上凑不出出单所需的样本量**——最多 13 个点击就下判决。wj07 的真实结果印证了这一点：他 192 条花钱系列里 122 条（63.5%）累计点击不到 20 就停了，这一档出单率只有 12.3%。**Hermes 目前是在复刻 wj07 的失败模式，而不是 yz04 的成功模式。**

补充说明 $9 这条线为何看着「还行」：它隐含假设 CPC ≈ $0.09（$9 买 100 个点击）。yz04 实际 CPC $0.072，$9 能买 125 个点击，刚好够；但对 CPC $0.25 的系列，$9 只买到 36 个点击，远远不够。**同一条线对不同 CPC 的系列宽严不一，这是花费闸的结构性缺陷。**

### 12.5a 判决矩阵（07 已拍板，2026-08-04）—— 本章最终结论

07 定的判决口径是「**少量预算($6)换一定量(50 点击),换不到订单就 pass,严一点没关系**」，并补充「**如果这个商家被 CRM 其他人验证过是可出单商家,我们可以适当调高去做,如果是没人验证过的区域,那我们宁愿保守一点**」。这两句话经数据验证后落成一个二维矩阵。

**先说 $6/50 点击这组数字的可行性边界**（这是拍板前的关键校正）：

- 买到 50 个点击的**中位成本是 $9.77**（wj07 $9.52，两人几乎一样），$6 的实际购买力只有 20-24 个点击。
- 能在 $6 内买到 50 点击的比例：yz04 8.6%、wj07 2.2%。**连 yz04 自己都做不到**。
- 但按 CPC 分层后，$6/50 点击**在低 CPC 商家上完全成立**：CPC<$0.12 的 86 条系列里 89.5% 能攒到 50 点击、平均只用 2.1 天，其中 29.9% 真的在 $6 内做到；CPC≥$0.12 的 322 条只有 30.7% 能攒到，且 **$6 内做到的是 0 条**。

**再说 CPC 是选品属性而非出价技巧**（这条推翻了「调低出价上限换更多点击」的设想）：

| 实际 CPC | 系列数 | 日均点击 | 3 天内攒到 50 点击 |
|---|---|---|---|
| <$0.08 | 54 | 106.9 | 84.8% |
| $0.08-0.12 | 32 | 34.4 | 88.9% |
| $0.12-0.20 | 84 | 19.7 | 53.8% |
| $0.20-0.30 | 88 | 11.7 | 13.1% |
| $0.30-0.50 | 102 | 7.1 | 0.9% |
| >$0.50 | 48 | 5.4 | 0% |

单调关系：**CPC 越低，买到的量越多**，与「低出价买不到量」的直觉相反。原因是出价上限压根没触发——两组系列的出价上限分别是 $0.97 / $1.16（有值的那部分），而实际成交 CPC 是 $0.04-$0.70，**上限比成交价高一个数量级，CPC 由拍卖市场和质量得分决定**。yz04 8/4 那批低 CPC 系列的 CTR 是 Etsy 18.7% / MGM 29.1% / Gilt 40.8% / iHerb 53.6%，是**品牌词自投**的典型特征；同期预算利用率 80-155%，说明他在把预算跑满而非被出价卡住。

**判决矩阵（团队验证 × 实测 CPC，全库 7 月新建 2337 条系列实证）：**

| | **CPC < $0.12**（$6 能买到 50 点击） | **CPC ≥ $0.12**（$6 买不到 50 点击） |
|---|---|---|
| **已验证**（开投前已有团队其他人在该商家跑出订单） | 379 条，命中 **65.2%**，ROI **130.9%**<br>→ $6 硬顶 + 50 点击判决 | 884 条，命中 44.2%，ROI **50.2%**<br>→ **硬顶放宽到 $11-14** 拿满 50 点击再判 |
| **未验证** | 306 条，命中 44.8%，ROI **117.9%**<br>→ $6 硬顶 + 50 点击判决 | 768 条，命中 29.3%，ROI **−10.6%**<br>→ **$6 到线直接 pass，不追加** |

**「未验证 + 高 CPC」是四格里唯一亏钱的**（768 条花 $7,311 净亏 $772），其余三格 ROI 都在 50% 以上。这比「未验证就保守」更精确：**未验证但低 CPC 的 306 条 ROI 117.9%，不该砍**——低 CPC 本身就是保护，$6 就能凑够判决样本，试错成本极低。真正需要「已验证」这道背书的只有买不到量的高 CPC 商家。

「已验证」信号本身的强度（这是 07 直觉的量化）：

| 分组 | 系列数 | 命中率 | ROI |
|---|---|---|---|
| 开投前已被团队其他人跑出订单 | 1263 | **50.5%** | **81.0%** |
| 无人验证过 | 1074 | 33.7% | 38.2% |

命中率 1.5 倍、ROI 2.1 倍。按验证人数分档看**第一个验证人贡献最大**（0 人 33.7% → 1 人 46.1% → 2 人 45.4% → 3+ 人 56.0%），所以判定只需「有没有人跑出过单」这个布尔值，不必加权人数。

**闸门定价曲线**（决定为什么已验证商家的硬顶定在 $11-14）：

| 单条硬顶 | 约买到 | 误杀率（yz04 最终出单系列中首单前花费超线的比例） | 每多 $1 降低 |
|---|---|---|---|
| $6 | 20 点击 | 41.3% | — |
| $9 | 40 点击 | 26.3% | 5.0 pt |
| $10 | 50 点击 | 19.4% | 6.9 pt |
| **$11** | 55 点击 | **14.4%** | 5.0 pt |
| $14 | 80 点击 | 7.5% | 2.3 pt |
| $18 | 100+ | 5.0% | 0.6 pt |

**拐点在 $11**：$6→$11 每多花 1 美元能换回 5-7 个百分点的误杀率，过 $14 后只剩 0.6，再加钱不划算。点击闸单独看：20 点击误杀 58.1%、30 点击 45.6%、**50 点击 22.5%**、100 点击 9.4%。

> ⚠️ **上表已被 12.5e 修正（2026-08-05）**：该表是 yz04 **全期**口径。07 澄清「$6 是总预算」后按「7 月新建 + 排除零点击 + 按 `user_merchant_id` 归因首单」严格重算，**$6 的真实误杀率显著低于上表**——全库 1315 条 **14.3%**、yz04 163 条 27.6%、wj07 62 条 16.1%。**结论方向因此改变：$6 够不够花不是主要矛盾，$6 买到的点击够不够判决才是。** 见 12.5e。

**⚠️ 一处内部冲突待 07 确认**：07 同时选了「CPC 硬限保持 $0.3」与「已验证商家不卡 CPC、只卡总花费」。但高 CPC 商家的实际成交价常在 $0.3 以上（`231-MUI1-VisitorsCoverageInc` 实际 CPC $0.44-1.99，07 本人把该系列 `max_cpc_limit` 设成 **1.58**，全库其他人跑同商家都是 0.30），**若出价上限锁死 $0.3，已验证商家买不到量，$11-14 的预算根本花不出去，放宽花费闸等于空转**。故本方案按「未验证商家 CPC 上限 $0.3；已验证商家 CPC 上限放开（暂拟 $1.0），风险由 $11-14 总花费硬顶兜住」实现，待 07 确认放开幅度。同一条系列也给出了放开的边界警告：它 7 月下旬 CPC 飙到 $1.30-1.99，7/26 花 $9.98 只买到 6 个点击、其后连续零订单；同期 wj02 用 CPC 上限 $1.27-1.42 跑同一商家三次全亏（−$7.66 / −$8.50 / −$4.74）。**「已验证就能加价」必须有天花板，否则会退化成对老商家无限加价。**

### 12.5 建议的阈值改法（推导过程，最终值以 12.5a 为准）

| # | 现状 | 建议 | 依据 |
|---|---|---|---|
| 1 | 观察期主判据 = 天数 2 天 或 花费 $9 | **主判据改成累计点击 ≥ 100~120 且零出单**；天数/花费降级为兜底 | 12.3（点击分布集中，p90=111）+ 12.4（花费闸对不同 CPC 宽严不一） |
| 2 | `test_days_cap=2` | 放宽到 **不早于 5 天**（配合点击闸，谁先到看哪个更保守） | 12.2（2-3 天档 ROI −32.7%）+ 12.3（2 天误杀 18.4%） |
| 3 | `test_spend_cap_usd=9` | 保留但**只当硬顶**防单系列失控，不作为主判据 | 12.3（$9 误杀 28.9%、损失 $26,976） |
| 4 | 等待期考核 `wait_roi_min=100%`，`wait_days=2` | 考核时点后移到**有花费满 8 天之后** | 12.2（8 天前 ROI 都在 11% 以下，此时考核等于按噪声判死刑） |
| 5 | 日预算默认 $2 | 若观察期改点击闸，需保证**每天能买到 ≥20 个点击**；按 CPC 反推预算而非固定值 | 12.4（$2 + CPC $0.3 = 每天 6.7 个点击） |
| 6 | SOP 阶段三「单笔佣金 ≥$1.5」硬门槛（HM-D48） | **07 已定完全更换**：删掉该门槛，换成「**品牌词月搜索量 ≥2 万**」（按系列所有品牌词变体取 max）。见 12.5d | 12.6（单均佣金与成败无关，方向甚至相反）+ 12.5d（月搜索量 Spearman −0.578，是唯一有预测力的事前判别器） |
| 7 | 5.1 选池规则第 3 条「高 ROI 扩池」**机制与代码路径都已存在，缺的是触发数值**；而 wj07 除 LH 外每个联盟只有 1 个有效联盟账号，**扩池目前只能在 LH 上做** | ① 触发条件落死：单条系列**累计净利 ≥$100 且订单 ≥50** 自动进复测队列，理由「高 ROI 扩池」；② 先只在 LH 启用验证，其余联盟需 07 新开联盟账号（见 12.5b） | 12.6 第 3、4 条（跨账号扩池 ROI 421.9%，同账号再开仅 129.7%） |
| 8 | 新接入的联盟账号与老账号走同一套「探索新 offer」流程 | 新账号首批任务**只从已验证商家清单里搬**，不做新探索 | 12.6 第 5 条（新账号搬清单 ROI 195.4% vs 老账号新探索 33.6%） |

#### 12.5b 扩池的真实约束：wj07 除 LH 外每个联盟只有一个联盟账号

> **本节曾出错并已重写（2026-08-04，07 当场指出）**：初版称「wj07 名下有 CG 8 个 / RW 4 个 / PM 2 个 connected 连接，Hermes 只各接了 1 个，复制 api_key 即可补齐」。错因是查询只筛了 `status='connected'`，**漏掉 `platform_connections.is_deleted`**。按 `is_deleted=0` 重算，wj07 只有 **7 个**有效连接，**恰好就是 Hermes v1.4 池表里的那 7 个**——**Hermes 一个都没漏，不存在「少接了池」这回事**。原表里被我算成缺口的 15 个连接（CG ×7、RW ×4、PM ×1、CF ×2、AD ×1）7 月 `camps_spent_july` 全为 0、花费全为 $0.00，确系废弃记录。**教训与 `max_cpc_limit` 那次同类：查 CRM 任何主表前先 `SHOW COLUMNS` 确认软删除列。**

5.1 的「高 ROI 扩池」规则写于 v1.1 双池时期，「另一个池」指的正是**同联盟的另一个账号**（LH1=wenjun3 / LH2=novanest），与 yz04 的做法完全一致——**机制方向是对的，代码路径也在（复测队列 + 审批理由「高 ROI 扩池」）**。真正的约束是**联盟账号本身不够**：

| 联盟 | Hermes 现接池 | wj07 有效连接（`is_deleted=0`） | 7 月实际花费 | 可否扩池 |
|---|---|---|---|---|
| LH | LH1 + LH2 | **2**（wenjun3 #17、novanest #277） | $361.84 / $84.13 | ✅ **唯一现在就能做** |
| CG | CG1 = allurahub(#280) | 1 | $8.22 | ❌ 需新开账号 |
| EV | EV1 = mevora(#100) | 1 | $380.65 | ❌ 需新开账号 |
| MUI | MUI1 = allurahub(#83) | 1 | $410.79 | ❌ 需新开账号 |
| PM | PM1 = keymint(#171) | 1 | $249.50 | ❌ 需新开账号 |
| RW | RW1 = parcelandplate(#20) | 1 | $208.38 | ❌ 需新开账号 |

对照 yz04（同样按 `is_deleted=0`，10 个有效连接）：**MUI ×2**（haven-hearth #271 → fortunavita #291）、**RW ×2**（thekhanhnguyen #273 ↔ loreenhorn1 #279）、**LB ×2**（xukewei #270 → dominick #290），BSH/CG/LH/PM 各 1。他 7 月全部 11 次跨账号扩池就发生在这三对账号上，**且用到的账号全部是有效连接**——12.6 第 3~5 条的数据不受本次纠正影响。

所以这条建议的性质变了：**不是复制配置就能做，而是要 07 先去联盟申请第二个账号**（成本更高、周期更长）。落地顺序建议：

1. **立刻可做**：只在 LH 上启用扩池（两个池都在跑，$361.84 + $84.13），用它验证触发线数值和第二条的实际回报，样本攒够再谈推广。
2. **待 07 决定**：MUI 是 wj07 花费最大的池（$410.79，273 条系列），若要新开第二个联盟账号，优先级最高；EV（$380.65）次之。
3. **注意伸缩风险**：一旦某联盟池数超过 9，系列命名平台段会出现 `CG10` 这种两位数，而 5.6 的归因链依赖「平台段 + 末段 MID」解析——**加池前必须确认平台段解析支持两位以上序号**。目前每联盟 1-2 个池，暂不触发。

全库看，跨账号扩池几乎只有 yz04 在做：他 11 个商家做过（占其活跃商家 4.2%），wj07 5 个（2.9%），wj10 4 个，其余 15 名员工全部为 0。

**「跨账号」判定已按严格口径复核（`chk_expand_strict.sh`）**：初版用 `platform_connection_id` 是否不同来判定，而 yz04 有 5 组同名账号的新旧成对记录（LB/xukewei #177+#270、LH/lanshao #158+#267+#268、MUI/haven-hearth #264+#271、PM/yongpeilan #263+#269、RW/thekhanhnguyen #266+#273），存在把「同一账号的新旧两条记录」误计为跨账号的风险。改用 `平台 + TRIM(account_name)` 重算后**三组数字一字未变**（11/24/421.9%、16/33/129.7%、238/121.5%），误判商家 **0 个**——原因是那些 `is_deleted=1` 的旧记录 7 月一条系列都没跑过，yz04 7 月全部有花费的系列都挂在 10 个有效连接上。全库统计同样不变。**12.6 第 3~5 条的结论经此复核仍然成立。**

顺带一个未展开的观察：yz04 的多系列商家里除「同联盟换账号」外，还有少量**跨联盟**的（同一商家在两个联盟都有 offer，MID 不同）。例如 Camp Snap 同时在 MUI（haven-hearth $2,741 + fortunavita $242）和 CG（blivio $116）上跑，Coverland 同时在 MUI 和 LB 上跑。这些例子里赚钱的都是同联盟那条，跨联盟那条金额很小，**样本不足以判断跨联盟复制是否也有效**，暂不纳入建议。

#### 12.5c 实现前提：「团队已验证商家」这个信号 Hermes 现在拿不到

12.5a 的矩阵有一半依赖「该商家是否已被团队其他人跑出订单」。实查发现这个信号目前**不在 Hermes 的可达范围内**：

| 数据源 | 现状 | 能否支撑判定 |
|---|---|---|
| 共享表格「已测记录」(gid=1885280150) | 374 行，**全部 `source=hermes-auto` / `owner=wj07`**；`result` 372 行全是 `observing`，`roi` 填充率 **0%** | ❌ 双重不可用：只有 Hermes 自己在写（其余 19 名员工走 CRM，不登记此表）；且从不回写终态，判不出是否出单 |
| 共享表格「白名单/推荐商家表」(gid=874482181) | 389 行人工维护，列＝商家名称 / ROI参考(79.7%) / 佣金率(36.2%) / 结算率(50.1%) / 标记(69.2%) / 备注(79.7%)；备注质量高（「已经跑出来几万结算了」「跑了2-3年了，网盟多麦海淘、返利网都结算正常」）。Hermes 已在读（队列 priority 1200 层） | ⚠️ 可作补充：覆盖仅 389 家、靠人工更新、**只有商家名没有域名列**，匹配不准 |
| 共享表格「黑名单」(gid=0) | 4815 行，有域名列，字段完整 | ✅ 现有用途（拉黑）正常 |
| **CRM 生产库** | 12.5a 的全部实证（命中率 50.5% vs 33.7%）就是用 `user_merchants.merchant_id` + 各员工首个出单日期算出来的；覆盖 20 名员工全部商家、自动更新 | ✅ **唯一准确来源** |

**07 已拍板（2026-08-04）：CRM 侧开一个只读 API，Hermes 每日拉取。** 理由是信号最准、覆盖最全、自动更新，且走 HTTP 而非直连数据库，不违背 v1.2「共享表格替代 SSH 读库」的解耦初衷。

**接口已于 2026-08-05 在 CRM 侧落地（CRM 设计方案 D-214）。实际契约与最初约定有三处不同，以下为准：**

```
GET /api/hermes/team-verified-merchants        ← 不是 /api/team/…
Authorization: Bearer ${HERMES_API_TOKEN}      ← 复用既有 Hermes 鉴权（verifyHermesToken）
可选：?since=YYYY-MM-DD（默认全期）&verified_only=1

→ { generated_at, since, basis{…口径自述…}, counts{…}, merchants: [{
      merchant_id, platform, merchant_name, domain,
      team_verified, verified_by_count, first_order_date, last_order_date,
      total_orders, total_orders_incl_rejected,
      breakeven_cpc, breakeven_trusted, orders_per_click,
      weighted_cpc, total_clicks, total_cost,
      total_commission, rejected_commission, per_order_commission, avg_roi,
      campaigns_ran, users_ran }] }
```

三处更正的原因：

| 最初约定 | 实际 | 原因 |
|---|---|---|
| `/api/team/verified-merchants` | `/api/hermes/team-verified-merchants` | CRM 已有 `/api/hermes/*` 命名空间（publish-sites / crawl-images / publish-article / merchant-intelligence / campaign-state）与统一鉴权；`/api/user/team/*` 是组长 session 功能，同名易混 |
| `network` | `platform` | CRM 里联盟维度的字段名就叫 `platform`（VarChar(8)，如 RW/LH/CG/PM）。另有 `parent_network` 是上级联盟，不是同一回事 |
| `normalized_domain` | `domain` | CRM **没有**这一列，只有 `user_merchants.merchant_url`；接口内用与本方案 `normalizeDomain` 同口径的 `toDomain()` 运行时解析 |

- Hermes 侧挂进现有 08:30 的 `/api/team-history/sync` 时段，落本地缓存表，接口不可达时用上次快照（与黑名单/已跑记录同款兜底策略）。
- 匹配键用 `merchant_id`（数字 MID）+ `platform` 复合优先、`domain` 兜底。**注意 `merchant_id` 单独不唯一**——同一个 MID 在不同联盟是不同商家，CRM 侧聚合键就是 `platform + merchant_id`。
- 建队列时把该商家标记为 `team_verified: true/false`，直接驱动 12.5a 矩阵选择 $6 还是 $11-14 的硬顶。
- ⚠️ 全量约 17 秒（`user_merchants` 162 万行），**每天只拉一次**，不要高频轮询。CRM 是 2 核 3.7G。

**07 已定「已测记录」表的回写修复先不做**（`result` 全 observing、`roi` 全空这个坏掉的功能），等本通道落地后一并处理；期间去重仍照现状可用。

##### 12.5c-1 接口落地后的实测：12.5f 的 CPC 公式只覆盖得到 20% 的已验证商家

写完接口在生产库实跑，拿到三个此前不知道的事实，它们直接决定 12.5f 那条 `CPC上限 = breakeven_cpc × 0.5` 能用在多少商家身上：

| 实测 | 数字 | 对 12.5f 的影响 |
|---|---|---|
| 团队跑出过有效订单的商家 | **3,103** 家（46.9 万笔有效单、$179.8 万有效佣金、涉 3,322 个 platform+MID） | `team_verified` 布尔信号覆盖面很好，12.5a 矩阵可以放心用 |
| 其中在 CRM 有广告点击数据、**能算出 breakeven_cpc** 的 | 仅 **611** 家（**19.7%**），另 2,492 家为 `null` | ⚠️ **八成已验证商家给不出 breakeven**。12.5f 的公式对它们无从下手，只能走 $0.30 探测下限——即"有团队数据就用 breakeven"这个前提，实际只对 1/5 的商家成立 |
| 611 家里 breakeven **不可直接用**的 | **408** 家（**67%**）——179 家佣金含非广告流量、229 家点击样本不足 | 详见下面两小节。可直接用的只剩 **203 家（占已验证商家的 6.5%）** |

**第一类不可用：佣金含非广告流量（179 家，`inflated`）**

联盟订单是按联盟账号全量拉的，同一商家名下**不区分来源**；而 `ads_daily_stats` 只记 Google Ads 的点击。于是出现订单数逼近甚至数倍于点击数的组合：`Coldwater Creek` **2,002 单只有 58 个点击**、`New Balance Brazil` 1,041 单 / 39 点击、`Hoteles Xcaret UK` 1,458 单 / 2,236 点击，算出的 breakeven 是 $185.71 / $118.35 / $49.05。

本想改用「只算能归因到系列的佣金」修正，实查 **`affiliate_transactions.campaign_id` 全库 468,856 笔里只填了 1 笔（0.0%）**——系列级归因这条路在 CRM 侧根本不存在。这是 CRM 的独立缺陷，已记进 D-214 待办。

**第二类不可用：点击样本不足（229 家，`thin_sample`）——这一条是上线后核对返回值才发现的**

第一版接口只做了「订单/点击比」这一道判定，结果按 breakeven 降序排出来的头部全是小样本假象：$1,009 / $849 / $592。最极端的 `LH/86428 Inspiring Vacations` 是 **1 单、5 个点击、$272 佣金**，算出 breakeven **$54.46**——若当可信数据用，出价上限会给到 $27.23，而它实际 CPC 只有 $0.152。

样本量与失真程度的关系很干净：

| 点击区间 | 商家数 | 平均 breakeven | 最高 breakeven |
|---|---|---|---|
| ≥200 | 124 | $0.873 | $8.33 |
| 100-199 | 24 | $1.641 | $8.36 |
| 50-99 | 36 | $1.591 | $8.77 |
| 30-49 | 19 | $0.867 | $3.67 |
| 10-29 | 46 | $1.491 | **$19.79** |
| <10 | 17 | **$4.506** | **$54.46** |

故加入「点击 ≥30」门槛（与 12.5f ⑥ 效果验证、12.5e「首单 81.4% 落在 30 点击内」同口径），可信档上界从 $54.46 回落到 **$8.77**。**样本不足与数值偏高是两回事**：前者乘任何安全系数都是假精确，应直接走探测下限；后者才适合压低系数。因此接口的可信度标记从布尔改为四档枚举 `breakeven_quality`。

**⚠️ 一个比限额规则本身更重要的发现：现有出价离任何档位的盈亏平衡值都还很远**

| `breakeven_quality` | 商家数 | 点击 | **实际成交 CPC** | 盈亏平衡 CPC | 净利 | ROI |
|---|---|---|---|---|---|---|
| `trusted` | 203 | 321,204 | **$0.0572** | $0.5622 | $162,209 | 883.4% |
| `inflated` | 179 | 188,613 | **$0.0480** | $2.9128 | $540,342 | **5,970.3%** |
| `thin_sample` | 229 | 2,853 | **$0.2291** | $20.95 | $59,117 | 9,043.4% |
| `no_ad_data` | 2,492 | — | — | — | — | — |

三档的实际成交价**都只用掉盈亏平衡值的 1.6%–10%**，且三档全部大幅盈利。这说明**新上限规则对现有这批商家几乎不构成约束**——真实成交价是 Google 二价竞价定的，我们的上限只是天花板。这恰好解释了服务器侧 HM-D57 里 07 把上限提到 $2 的道理：天花板压低会买不到量（LAGOS #127 零展示），压高了大部分商家实际也只花 $0.05。

**所以 12.5f 的定位需要修正一句话**：它不是「限制超额付费」，而是**「给每个商家一个自己不会亏的天花板，让 Google 在天花板内自由竞价」**。$2 一刀切的真正问题不是普遍付高了，而是对 breakeven <$4 的商家，天花板架在了盈亏平衡点之上——竞争一加剧就会亏（cpc_rule_sim 实测付超的 361 条净亏 $2,447），同时对 breakeven 高的商家又封死了「该抢」的空间。

**07 于 2026-08-05 拍板（#22）：对佣金偏高的那档安全系数从 0.5 压到 0.25**（仍用它自己的 breakeven，不退回探测下限，保留区分度）。实测这个系数**对三分之二的商家真正生效**：inflated 179 家里 118 家（breakeven $0.08-7.92）×0.25 后低于 $2 硬顶、系数起作用，另 61 家（breakeven $8.39-185.71）乘完仍撞顶、与不压系数无差别。`trusted` 档同理：189 家 ×0.5 后生效、14 家撞顶。

于是 12.5f ⑥ 的安全系数按 `breakeven_quality` 分四档：

| `breakeven_quality` | 商家数 | 安全系数 | 依据 |
|---|---|---|---|
| `trusted` | 203 | **0.5** | 点击 ≥30 且订单/点击 ≤0.3，分子分母同源可信 |
| `inflated` | 179 | **0.25** | 点击够但佣金含非广告流量；118 家生效、61 家撞硬顶 |
| `thin_sample` | 229 | **走 $0.30 探测下限** | 点击 <30，breakeven 无统计意义，乘系数是假精确 |
| `no_ad_data` | 2,492 | **走 $0.30 探测下限** | CRM 无广告数据，只有 `team_verified` 布尔信号 |

最终上限 = `min(breakeven_cpc × 安全系数, $2 硬顶)`；后两档取 $0.30。

> `thin_sample` 这一档是上线核对时新发现的，07 拍 #22 时尚不存在。**这里按「样本不足则无从计算、只能探测」的逻辑归到探测下限**，与 `no_ad_data` 同处置。若 07 认为这 229 家（整体 ROI 9,043%、实际 CPC $0.2291 已是三档最高）值得给更高天花板去抢，需单独拍板。

#### 12.5f CPC 限额必须按商家的盈亏平衡值定（2026-08-05，07 指出「Hermes 完全搞反了」）

07 原话：「**CPC 要有限额，现在 Hermes 完全搞反了，我的意思是如果这个商家的 CPC 很贵，我们要去抢，才采用高 CPC，而不是直接拉 2 刀的 CPC**」。实地审计 Hermes 生产库与代码后确认这个批评成立，且**本章 12.5d/12.5e 里「选便宜 CPC」的表述需要修正**。

**① 现状：$2 出价上限是全局默认值，不分商家**

`app_settings` 实际值：`default_cpc_usd = 2`、`hard_cpc_usd = **100**`（风控上限形同虚设）、`test_spend_cap_usd = 6`。出价分布证实一刀切：`pending_approval` 52 条**全部 $2**，在投 34 条平均 $1.95，全库 `cpc_usd ≥$1` 的有 272 条。

这个值的来由在代码注释里有据（`src/lib/settings.js`）：

> 服务器侧 HM-D57，07 拍板 2026-07-31：「智能出价，策略是争取更多次数，写一个死上限 2 刀」。病例：LAGOS #127 手动出价 $0.80，Google 说首页要 $1.84，质量分 10/10 却零展示；而 `cpc_down` 规则按「均价只有出价的 38%」又把它砍到 $0.24，日点击从 33 掉到 8。

**当时的方向是对的**（治「出价太低拿不到量」），错在它被写成 `default_cpc_usd` 全局默认、且注释明确标注「不该被自动调参碰」，于是每条新系列不分商家一律 $2。

**② 代价已经发生：钱花超了，判决样本还不够**

Hermes 在投 34 条系列（86 条含待批）实测：

| 指标 | 数值 |
|---|---|
| 累计花费已超 $6 | **24/34 条（71%）** |
| 累计点击已达 50 | 仅 **10/34 条（29%）** |
| **花超 $6 但点击不足 50（判决无效区）** | **14 条，已烧 $172.24** |
| 在投合计实际 CPC | $0.2115 |

个案：`340-MUI1-tinyland` 花 **$8.94 只买到 9 个点击**（CPC $0.993）；`256-MUI1-wecreattechltd` $19.29 / 23 点击（$0.839）；`194-MUI1-nubeo` 花 **$25.79（预算 4 倍）才 49 个点击**。这些系列既超了 $6 硬顶、又攒不够 50 点击，**两头不着**——正是 12.5e ④ 预测的死区。

**③ 正确的限额依据：盈亏平衡 CPC**

$$\text{盈亏平衡 CPC} = \frac{\text{该商家历史有效佣金}}{\text{该商家历史点击}} = \text{单笔佣金} \times \text{点击→订单转化率} \times (1-\text{拒付率})$$

全库 7 月新建按商家聚合（点击 ≥30），**商家之间差 328 倍**：

| 商家的盈亏平衡 CPC | 商家数 | 点击 | 实际付的 CPC | 净利 |
|---|---|---|---|---|
| **<$0.05** | 829 | 191,549 | $0.0531（**付超 3.3 倍**） | **−$7,058** |
| $0.05-0.10 | 143 | 94,210 | $0.0387 | +$3,396 |
| $0.10-0.20 | 158 | 67,053 | $0.0700 | +$5,037 |
| $0.20-0.50 | 187 | 65,712 | $0.1029 | +$11,842 |
| $0.50-1.00 | 91 | 27,499 | $0.1499 | +$13,456 |
| **≥$1.00** | 112 | 22,023 | $0.2124（**只用了 4%**） | **+$113,153** |

直接按「有没有付超平衡点」分组，判据干净得没有歧义：

| 判定 | 系列数 | 花费 | 归因佣金 | 净利 |
|---|---|---|---|---|
| **实际 CPC > 该商家盈亏平衡 CPC** | 361 | $5,254 | $2,807 | **−$2,447** |
| **实际 CPC ≤ 盈亏平衡 CPC** | 727 | $22,495 | $293,209 | **+$270,714** |
| 该商家从未赚过钱 | 854 | $7,717 | $0 | −$7,717 |

**④ 07 的「贵的要去抢」有硬数据支撑——这修正了 12.5d/12.5e 的表述**

按实际 CPC 分层看每点击收益（EPC）与 ROI，**方向与我此前的结论相反**：

| 实际 CPC | 系列 | 点击 | 每点击收益 | ROI |
|---|---|---|---|---|
| <$0.08 | 435 | 316,747 | $0.1723 | 501% |
| $0.08-0.12 | 332 | 56,113 | $0.6274 | 543% |
| $0.12-0.20 | 687 | 65,292 | $1.2254 | 682% |
| $0.20-0.35 | 709 | 32,487 | $3.4234 | **1,242%** |
| $0.35-0.60 | 271 | 8,236 | $6.6106 | **1,422%** |
| **≥$0.60** | 210 | 2,451 | **$16.89** | **1,897%** |

**贵的点击是贵得有道理的**：那批商家单笔佣金极高（Hoteles Xcaret UK 单笔 $103.47、盈亏平衡 CPC **$47.55**；M.M.LaFleur $43.67 / $1.42；PandaDoc $59.22；PupLabs $20.35 / $30.55）。对它们来说 $2 上限甚至偏保守。

> ⚠️ **因此 12.5d 的「选便宜 CPC」必须改口径**：不是「选绝对便宜的商家」，而是「选**相对自己盈亏平衡值**便宜的商家」。月搜索量门槛（12.5d）解决的是「买不买得到量」，盈亏平衡 CPC 解决的是「买来的量亏不亏」——**两个不同维度，都要，不能互相替代**。

**⑤ Hermes 在投商家实测：两个方向的错同时在犯**

把在投 86 个 MID 对上 CRM 的团队真值：

| 该商家盈亏平衡 CPC | 商家数 | 团队实际 CPC | 问题 |
|---|---|---|---|
| **团队完全无数据** | **69（80%）** | $0.3514 | **拿 $2 上限投彻底未知的商家** |
| <$0.05 | 3 | $0.1317 | **付超 6 倍** |
| $0.05-0.15 | 3 | $0.1681 | 偏高 |
| $0.15-0.50 | 4 | $0.2208 | 合理 |
| $0.50-2.00 | 4 | $0.3725 | **偏低，可以加价抢** |
| ≥$2.00 | 2 | $0.2490 | **严重偏低，该抢没抢** |

两个方向的反面案例都在：

- **付超**：`Noyz` 盈亏平衡 **$0.0232**（3 个员工跑过、5 单、单笔 $4.10、883 点击），Hermes 用 $2 上限实际付 $0.403——**17 倍**；`ULANZI` 平衡 $0.0789、实际 $0.3973（**5 倍**）；`Ergomotion` 平衡 **$0**（1 单佣金为 0）仍在投。
- **该抢没抢**：`Slate Flosser` 平衡 **$4.68**、`Oxiline` 平衡 **$2.90**（Hermes 实际仅 $0.347）、`BudgetVetCare` $1.83、`M.M.LaFleur` $1.42（Hermes `228-EV1` 实际 $0.33）——这些完全有加价空间却没加。

**⑥ 限额规则（07 于 2026-08-05 拍板，四项全部选定）**

07 的四项选择：安全系数 **0.5**、无数据商家探测下限 **$0.30**、`hard_cpc_usd` **收回 $2**、低盈亏平衡商家 **照投不拦、靠 $6/50 点击闸门兜**。

> ⚠️ **第四项选择连带修正了公式**。我原先写成 `clamp(盈亏平衡CPC × 0.5, 探测下限, hard)`，**这个下界是错的**：它会把 Noyz（$0.0232 × 0.5 = $0.0116）抬回 $0.30，规则等于失效。实测有 **365 个商家的新上限会低于 $0.02**（7 月净亏 $4,399），全被下界抬起来则这批亏损原样保留。**探测下限只适用于「团队无数据」的商家，不能当全局下界。**

> ⚠️ **2026-08-05 接口落地后的第二次修正（#22）**：CRM 接口实跑发现「有团队数据」这一支要再分三档——已验证商家里只有 **19.7%** 算得出 `breakeven_cpc`，而这 611 家里又有 **408 家（67%）**不可直接用（179 家佣金含非广告流量、229 家点击 <30 样本不足）。**真正可直接乘系数的只剩 203 家，占已验证商家的 6.5%。** 07 已拍板对偏高那档取 **0.25**。详见 12.5c-1。

```
按 CRM 接口返回的 breakeven_quality 分四档：

① trusted     ：出价上限 = min( 盈亏平衡CPC × 0.5 , hard_cpc_usd )   ← 不设下界
② inflated    ：出价上限 = min( 盈亏平衡CPC × 0.25, hard_cpc_usd )   ← 佣金含非广告流量，砍半
③ thin_sample ：出价上限 = min( $0.30 , hard_cpc_usd )               ← 点击<30，数值无意义
④ no_ad_data  ：出价上限 = min( $0.30 , hard_cpc_usd )               ← 只有 team_verified 可用

  数据来源：GET /api/hermes/team-verified-merchants（已上线，字段名以该接口为准）
  ③④ 合计覆盖已验证商家的 87%，「探测下限」是常态而非兜底
  hard_cpc_usd：从现值 100 收回 **$2**（保留 07 在服务器侧 HM-D57 定的天花板，让它真正生效）
  default_cpc_usd：**取消全局默认**，$2 只作天花板，不再作为每条新系列的出价上限
```

> ⚠️ **实现时别误解这条规则的作用**：实测三档商家的实际成交 CPC 只用掉各自盈亏平衡值的 1.6%–10%（`trusted` 档 $0.0572 vs $0.5622），因为成交价由 Google 二价竞价决定、上限只是天花板。**所以这套规则主要不是「省钱」，而是「让天花板不架在盈亏平衡点之上、同时给该抢的商家留出空间」。** 收紧 `hard_cpc_usd` 到 $2 时要留意 HM-D57 的原始教训：天花板压太低会零展示（LAGOS #127）。

**规则效果实测（`cpc_rule_sim.sh`，全库 7 月新建、点击 ≥30 的 992 个商家）——压住的量恰好是全部亏钱的量：**

| 按新规则的影响 | 商家数 | 点击 | 花费 | 佣金 | 净利 | ROI |
|---|---|---|---|---|---|---|
| **新上限 ≥ 现实际成交价（不受影响）** | 384 | 310,668 | $21,657 | $497,389 | **+$475,732** | **2,196.7%** |
| **新上限 < 现实际成交价（量会掉）** | 608 | 160,302 | $14,036 | $10,837 | **−$3,199** | **−22.8%** |

赚钱的 384 个商家一个都不受影响；被压住的 608 个整体 ROI −22.8%。**这是对 07「按商家定限额」判断的最强验证。**

新上限的分布（决定了「照投靠闸门兜」是否自洽）：

| 新上限档 | 商家数 | 该档 7 月净利 |
|---|---|---|
| <$0.02（**几乎必然零展示**） | 365 | **−$4,399** |
| $0.02-0.08 | 172 | +$7,209 |
| $0.08-0.30（低于探测下限，故下界必须去掉） | 172 | +$28,244 |
| $0.30-1.00 | 140 | +$66,493 |
| **≥$1.00（可放开抢）** | 143 | **+$374,986** |

**07 选「照投不拦」是自洽的**：那 365 个低平衡值商家拿到 <$0.02 上限后必然零展示、零花费，Hermes 现有的零流量止损（实测样例：`零流量：投满 1 天累计花费 $0` → 自动暂停）会直接清掉它们，**不需要改建队列逻辑，市场自己淘汰**。探测下限 $0.30 也够用：全库 **77.8%** 的系列实际成交 ≤$0.30。

**⑦ 落地前必读：Hermes 已有两套同源机制，本节是「接上去」而不是「新建」**

> ⚠️ **先撤回我在本节初稿里写错的两条配套建议**。我曾建议「给 `cpc_up` 提价规则加天花板」并「让 `cpc_up_epc` 成为唯一上调路径」——**这两条规则都已不存在**。服务器侧 HM-D57（07 拍板 7/31）**移除了 `auto-tune.js` 里的全部 CPC 类规则**，我在 `run_events` 里看到的 `cpc_up` / `cpc_down` / `cpc_up_epc` 触发记录全是 7/31 之前的历史流水。撤除理由代码里写得很清楚且成立：智能出价下组层/关键词层的 `cpc_bid_micros` 被 Google 完全忽略，那正是原 CPC 规则在写的字段——**它们三天里一直在改一个不起作用的数**；且二价竞价下均价**永远**低于上限，`cpc_down` 依据的「均价只有出价的 38% ⇒ 竞争弱」本身是错的推断。

**`auto-tune.js` 当前实际只剩两条规则，且已经在用盈亏平衡思想**（服务器侧 HM-D55，07 拍板 7/30，把判据从「流量信号」换成「盈利信号」，因为实测 113 次加预算里 **101 次加在最终亏钱的广告上**、75 条零出单广告多烧 $539）：

```
保护类  减预算（盈利比 <1 即亏钱）→ ×0.7，地板 default_budget_usd
成长类  加预算（点击≥20 且订单≥2 且盈利比≥1.2 且佣金归自己且预算撞线）→ ×1.25
        盈利比 = EPC ÷ 实际均价 = 有效佣金 ÷ 花费
```

**「盈利比」与本节的「盈亏平衡 CPC」是同一个量的两种写法**：盈利比 ≥1 ⟺ 实际 CPC ≤ 盈亏平衡 CPC。差别只在作用对象——现规则调**预算**，本节要求调**出价上限**。

**已有机制一：净亏止损线已经按商家单笔佣金分档**（`sync-commissions.js` 的 `netLossCapFor()`，服务器侧 HM-D57）

```
净亏线 = clamp( 单笔佣金中位数 × stop_net_loss_scale , stop_net_loss_usd , stop_net_loss_cap_usd )
```

其立论与本节一字不差——代码注释原文：「$4 这条线跟客单价完全无关。evryjewels 单笔佣金 $0.25，$4 等于亏掉 16 单的利润才停；LAGOS 单笔 $105.78，$4 是一单利润的 4%，一出单反而立刻濒死」。**所以「按商家属性分档而非一刀切」这件事 07 在 7/31 已经做过一次，只是做在止损线上、没做在出价上限上。** 落地含义：① 有现成范式可循（`clamp(中位数 × scale, 底线, 硬顶)`）；② **`perOrderCommissionMedian()` 函数已存在可直接复用**，盈亏平衡 CPC = 单笔佣金中位数 × 转化率，分子已经有了，只缺转化率；③ 用中位数而非均值的理由同样适用（LAGOS 只出过 1 笔运气单，均值会授权两百多刀额度）。

**已有机制二：07 选的第四项「照投不拦、靠闸门兜」已经有实现**（`merchant_budget_skips` 表 + `merchant-gate.js` 闸 E，服务器侧 HM-D57）

```
reason = no_traffic（花不出钱）/ unaffordable（均价太贵）
cpc_ceiling_usd / spend_cap_usd  ← 记录判定当时的预算级别，现值 > 它即自动解除
设置开关 unaffordable_action: on=提前关停并标记 / alert=只发飞书 / off=关闭
```

判定依据与 12.5e ④ 完全同源，代码注释原文：「实测 31/72 条在投广告累计花费 $0；能读到 Google 首页出价估算的 14 条里，只有 4 条能在 $6 的花费闸内买够 12 个点击（schoolyardsnacks 首页价 $11.40，**$6 买 0.5 个点击**）」。**这比我原先建议的「建队列时按盈亏平衡值拦掉」更好**：它是**相对预算级别**的、可自动解除的（07 抬高上限或花费闸，这些商家自动恢复资格），而不是永久拉黑。**故 07 选「照投不拦」不需要新建任何拦截逻辑，只需把判定口径从「买不够 `tune_min_clicks` 个点击」扩充为「买不够 50 个点击」并接上盈亏平衡值。**

**⑧ 修正后的配套改动（仅两条）**

1. **`default_cpc_usd = 2` 取消全局默认**，改为按商家算（有团队数据走 `min(盈亏平衡×0.5, $2)`，无数据走 $0.30）；$2 只作 `hard_cpc_usd` 天花板。这是本节唯一的新增逻辑。
2. **上调出价的路径需要重新引入，但要挂在「盈利比」上而非旧的 `cpc_up`**。服务器侧 HM-D57 撤掉全部 CPC 规则后，**现在没有任何机制能给「值得抢」的商家加价**——这正是 12.5f ⑤ 里 `Slate Flosser`（盈亏平衡 $4.68）、`Oxiline`（$2.90）该抢没抢的直接原因。建议照 `auto-tune.js` 现有的成长类规则加一条对称项：**盈利比 ≥1.2 且出价上限已撞线 → 上限 ×1.25，天花板 = `min(盈亏平衡CPC × 安全系数, hard_cpc_usd)`**。撤旧规则的理由不适用于此：改的是 campaign 层的 `cpc_bid_ceiling`（`setCpcCeiling()` 已实现且是 Google 唯一认的旋钮），不是被忽略的 `cpc_bid_micros`；判据是盈利比而非「均价/出价比值」，不会重犯 LAGOS #127 的错。

> ⚠️ **编号约定（07 于 2026-08-05 拍板，详见待确认项 #20 与 13.1）**：本节提到的 **服务器侧 HM-Dxx** 一律指 Hermes 服务器代码里已生效的改动（编号以服务器为准）；本文档自己的分析与待实现设计改用 **HM-Axx** 前缀。审计发现服务器侧已用到 HM-D63，且 D55～D61 与本文档原编号完全重叠——**这也是本节初稿把已撤除的 `cpc_up` 规则当成现行规则的根源：文档缺了服务器侧 7/30-8/01 的九笔改动**，已按 13.2 补齐。

##### 12.5f-1 Hermes 侧落地：⑥ 的两个参数经干跑推翻后重新拍板，已全量上线（HM-D64 / HM-A08，2026-08-05）

> **本节结论先行**：数据通路（缓存表 + 同步任务 + cron）与定价规则**均已上线生效**。规则相对 12.5f ⑥ 有实质改动——⑥ 定的「$0.30 探测下限 + 分档降价」被干跑证据推翻，07 于 2026-08-05 重新拍板为「**$2 基准线 + 有证据才加价**」（待确认项 #23/#24）。落地后的净效果：**全库无一条降价，待发布 52 条一字未变，仅 14 个 `trusted` 商家可突破 $2 上探至 $4.39**。①–⑥ 是推翻 ⑥ 的证据，⑦ 是最终生效的规则。

**已生效部分（服务器侧 HM-D64）**

| 落地项 | 实测 |
|---|---|
| 本地缓存表 `crm_team_verified` | 已建，`(platform, merchant_id)` 主键 + `normalized_domain` / `merchant_id` 双索引 |
| 同步任务 `jobs/sync-crm-team-verified.js` | 首跑 **20 秒 / 3,104 条**落表，整表替换；拉失败或返回 0 条时**拒绝覆盖旧快照** |
| 路由 `GET /api/cron/crm-team-verified` | 独立 `withLock('crm-team-verified')`，与 `crm-intel` 隔离（该接口 26 秒，串在一起会把 2 秒的情报同步拖成半分钟） |
| crontab | `55 7 * * *`，与 `crm-intel`（`50 7`）错开 5 分钟 |
| `lib/cpc-ceiling.js` 四档定价 | **已接入 `effectiveBidding()`**（发布与审批卡两条路径共用），规则见 ⑦ |
| 三个新设置项 | `cpc_safety_trusted` = 0.5、`cpc_safety_inflated` = 0.25、`cpc_hard_inflated` = $2，均已进 `REGISTRY`（07 可在设置界面直接调） |
| `hard_cpc_usd` | **100 → $5**（走 `setSetting` 留流水）。改前形同虚设，现在真正充当 `trusted` 档天花板；改动当时旧代码仍在跑，`min(2, 100)` 与 `min(2, 5)` 同为 2，线上无感 |

匹配用「`merchant_id`+`domain` 双条件 → 仅 `domain` → `merchant_id` 全库唯一」逐级退让。三级都要：CRM 侧 3,104 条里 **885 条（29%）没有 domain**，只用域名会漏；而 `merchant_id` 是联盟内部编号、跨联盟同号段（`EV/8012692` 与 `MUI/8005348` 都是 7 位），只用它会张冠李戴，故末级要求唯一。

**⚠️ 顺带修正了一处实现偏离**：初版 `cpc-ceiling.js` 写成 `clamp(raw, floor, hard)`，正是 ⑥ 上方警告过的错误下界。已改为**只封顶不设下界**，仅保留 $0.01 的技术底（Google `cpc_bid_micros` 精度 10000）。修正后实测有 **8 个 trusted 商家**落到 <$0.02（`Splash Wines` 平衡值 $0 → $0.01），此前会被错误抬到 $0.30。

**① 覆盖率的真相：这套定价对当前待投队列一条都用不上**

| 待发布 52 条（`queued`/`pending_approval`/`approved`）的逐条匹配 | 条数 |
|---|---|
| 快照里没有这个商家（团队从没投过） | **43** |
| 有订单但 CRM 无广告数据（`no_ad_data`） | 6 |
| 点击 <30 的薄样本（`thin_sample`） | 3 |
| **可按盈亏平衡定价（`trusted`/`inflated`）** | **0** |

**不是匹配 bug**，专门验证过：域名模糊匹配（去 `www.` / 只比主体名）命中 **0 条**，说明两边归一化口径一致；反向抽查快照里的 MUI 商家，在 Hermes `offers` 里都能按域名找到。43 条是真的没有——它们的 MID 全在 `1865xxxx` 新号段。

**根因是两个池子天然不相交**：CRM 快照收的是团队**已经出过单**的商家，Hermes 待投队列排的是**还没投过**的新商家。天花板可以量化：`offers` 全库 **21,831** 个域名，与快照重叠仅 **1,092 个（5.0%）**；其中 `trusted` 203 个有 136 个在池内、`trusted`+`inflated` 382 个有 256 个在池内。**所以 12.5f ⑥ 的按商家定价要等 Hermes 投到那 256 个商家时才会生效，对当下队列完全够不着。**

**② 探测下限 $0.30 建立在口径错配上——支撑它的「77.8%」不是 Hermes 的口径**

⑥ 用「全库 77.8% 的系列实际成交 ≤$0.30」论证 $0.30 够用。但探测下限**只作用于无数据的新商家**，而新商家恰好处在测试期起点：

| 口径 | 样本 | 实际 CPC 中位 | ≤$0.30 占比 |
|---|---|---|---|
| **Hermes 自己已发布广告** | 171 条有花费 | **$0.3250** | **45.0%** |
| CRM 团队快照商家实付均价 | 611 个 | $0.1433 | 85.1% |

**这跟 12.5a 踩过的「加权错觉」是同一类错误**：CRM 全库含大量已跑出量的老系列（Etsy 单日 1571 点击 / CPC $0.0099），而 12.5a 本身已证明「低 CPC 是跑出量的结果不是原因」，且实测测试期系列 CPC 是 **$0.3686 vs $0.2824**——与 Hermes 这里的 $0.3250 高度吻合。**$0.30 这条线正好卡在测试期 CPC 的中位数上，等于对一半的新商家关掉竞价资格。**

**③ 上限压低不省钱，只是拿不到量（Hermes 自己 367 条已发布广告实证）**

| 发布时盖的上限 | 广告数 | 拿到花费 | 实际成交均价 | 平均点击 | 平均花费 | 跑到 50 点击 |
|---|---|---|---|---|---|---|
| $0.5 | 35 | 19（54%） | $0.3877 | 17.2 | $6.34 | 2 / 35 |
| **$0.8** | 72 | 47（65%） | **$0.6634** | **10.7** | $6.75 | **0 / 72** |
| **$2** | 212 | 127（60%） | **$0.3283** | **38.6** | $8.53 | 54 / 212 |

**反直觉但与 ⑥ 上方那句「成交价由二价竞价决定、上限只是天花板」完全一致**：给 $2 上限的实际均价（$0.3283）**反而比给 $0.8 的（$0.6634）更低**——空间给足了 Google 才能挑便宜的时段与位置成交；上限卡死则只能在少数昂贵机会里勉强成交。$0.8 那档是最坏的：72 条平均只拿到 10.7 个点击，**一条都没跑到 50 点击，判决全部无效**。这正是服务器侧 HM-D57 在 7/31 把上限从 $0.5 提到 $2 的原因，回调到 $0.30 等于走回头路还更远一步。

**④ 代价的具体形状：17 条真正出过单的广告里 8 条会被 $0.30 掐死**

| # | 商家 | 实际 CPC | 点击 | 花费 | 有效佣金 | 净额 |
|---|---|---|---|---|---|---|
| 331 | BudgetVetCare.com | **$0.403** | 75 | $30.24 | $39.28 | **+$9.04** |
| 303 | Calecim Professional | **$0.532** | 22 | $11.71 | $19.54 | **+$7.83** |
| 318 | Rapsodo | **$0.303** | 52 | $15.78 | $13.00 | −$2.78 |
| 186 | VSL#3 | **$0.828** | 27 | $22.36 | $12.86 | −$9.50 |
| 395 | Oxiline | **$0.368** | 23 | $8.47 | $10.66 | **+$2.19** |
| 410 | Onix Pickleball | **$0.318** | 55 | $17.49 | $7.01 | −$10.48 |

按佣金排前四名里就有两条在 $0.30 之外，且都是净赚的。这批广告的实际 CPC 中位 **$0.2707**、p75 **$0.4032**。**注意亏损上限本来就由 $6 花费闸兜住**，出价上限再压一道，管的不是风险而是「能不能参与竞价」。

**⑤ `hard_cpc_usd` 收到 $2 的两难：放宽会同时放开虚高档**

| `hard` 取值 | 撞顶商家数 | 构成 |
|---|---|---|
| **$2** | 75 | `trusted` 14 + `inflated` 61 |
| $5 | 30 | 全部是 `inflated`（平衡值 >$20 那批） |

收 $2 会把 14 个 `trusted` 商家削平——它们正是「该抢」的对象（`caracaranyc` 平衡 $8.771 → 应得 $4.386，`mohawkgeneralstore` $8.359 → $4.179，`lunya.co` $8.3258 → $4.163）。但**统一放宽到 $5 会连带放开 31 个 `inflated` 商家**（该档平均平衡值 **$12.594**，虚高来自 CRM 的 `campaign_id` 零填充），基于不可信数据给 $3–5 上限比封在 $2 更危险。**⑥ 原话「$2 硬顶恰好挡住了最脏的一档」在这里成立**——所以若要放开，应按档给天花板（`trusted` 放宽、`inflated` 仍封 $2），而不是抬全局 `hard`。

**⑥ 顺带发现：$6 预算下「50 点击」这道闸永远不会先触发**

实际花到 $5–7 的 15 条广告，平均只买到 **24.7 个点击**，跑到 50 的只有 1 条；按实际成交均价 $0.33 算，$6 大约只够买 **18 个点击**，凑满 50 点击需要约 **$16.5**。这不影响止损效果（$6 花费闸一定先到），但**「50 点击无单即 pass」在 $6 下是虚设**，与 12.5e ④「$6+50 点击是判决有效性规则」的推论一致：贵商家花完 $6 只有十几个点击，此时零订单证明不了商家不行。

**⑦ 拍板结果：从「分档降价」改成「有证据才加价」（07 于 2026-08-05 定 #23 = $2、#24 = 按档给，已落地）**

```
出价上限 = max( base , min( 盈亏平衡CPC × 该档系数 , 该档天花板 ) )

  base = min(default_cpc_usd, hard_cpc_usd) = $2      ← 基准线，任何一档都不会低于它
  trusted   ：× 0.5，天花板 hard_cpc_usd    = $5      ← 14 个商家可突破 $2 去抢
  inflated  ：× 0.25，天花板 cpc_hard_inflated = $2   ← 等于 base，故实际不加价
  thin_sample / no_ad_data / 快照无此商家：base
```

两处实现细节值得记一笔。**一是 `max` 必须在最外层**：写成 `min(max(raw, base), hard)` 的话，07 未来若把 `cpc_hard_inflated` 调到 $2 以下，虚高档就会掉到基准线以下，与「不降价」的拍板相悖。**二是虚高档的系数刻意留着没删**——当前被天花板压成恒等于 base、不起作用，但 CRM 修好 `campaign_id` 归因后只需把 `cpc_hard_inflated` 抬起来即可生效，不必改代码。

**上线前干跑，全库 452 条 `ad_runs` 逐条比对：**

| 状态 | 条数 | 加价 | 不变 | **降价** |
|---|---|---|---|---|
| `pending_approval`（待发布） | 52 | 0 | **52** | **0** |
| `monitoring`（在投） | 30 | 1 | 29 | **0** |
| `paused` / `cancelled` / `failed` / `rejected` | 385 | 197 | 188 | **0** |

降价 0 条，即 07 要的「基准线不下移」在全库范围内成立。历史行那 197 条「加价」是账面对比而非真实变化——它们的 `cpc_usd` 是 HM-D57 之前 $0.5/$0.8 时代的存量值，且这些状态不会再走发布路径。**待发布 52 条一条不变**，与 ① 的覆盖率结论互为印证。

**全快照 3,104 家假想分布：能加价的只有 14 家（0.5%）**，全部是 `trusted`，落在 $2.13–$4.39；其余 3,090 家一律 $2（`no_ad_data` 2,486 / `thin_sample` 229 / `trusted` 但平衡值低 189 / `inflated` 179 / 快照无记录 7）。加价幅度前五名：`caracaranyc.com` $4.39（平衡 $8.771，60 点击 14 单）、`mohawkgeneralstore.com` $4.18、`lunya.co` $4.16、`sunandsnow.pl` $4.15（427 点击 118 单）、`nomatic.com` $3.95。

**上线后实调四条代表路径**，确认新代码真在跑而非仍读旧逻辑：

| 场景 | 商家 | 结果 |
|---|---|---|
| 可信档能加价 | `caracaranyc.com` | 平衡 $8.771 × 0.5 → **$4.39** |
| 可信档但平衡值低 | `tedbaker.com` | 平衡 $0.0712 × 0.5 = $0.04 → **维持 $2**（不降价） |
| 虚高档 | `mmlafleur.com` | 平衡 $54.9723 × 0.25 = $13.74 → **封在 $2** |
| 快照外新商家 | 任意 | **$2** |

第二行是这次拍板最要紧的行为：`trusted` 不等于「按平衡值定价」，平衡值低于基准线时一律维持基准线。**这也顺带解决了本节末尾那条注里 35 个「被压住」的商家**——`PupLabs`（平衡 $0.4205、实付 $0.3274）在旧规则下会被压到 $0.21，现在维持 $2，实付不受影响。

**这仍然满足 07 的原始要求**：07 的原话是「商家 CPC 很贵我们要去抢才采用高 CPC，而不是一律拉 2 刀」；在 TARGET_SPEND 下 $2 上限对便宜商家实测并不造成浪费（实付 $0.15–0.33），真正的问题是**贵商家封顶在 $2 抢不动**。故「不一律 2 刀」的正解是给贵商家开口子，而不是给便宜商家降上限。

> ⚠️ 本节与 ⑥ 的 `cpc_rule_sim` 效果表（384 不受影响 / 608 被压住 ROI −22.8%）**不矛盾，作用范围不同**：那张表算的是**有 breakeven 数据**的商家按各自平衡值定价的效果（Hermes 本地快照口径复算：382 个有实付均价可比的商家里 347 个不受影响、35 个被压住），本节质疑的只是**无数据商家**该落在哪个探测下限上。**这 35 个被压住的商家在 ⑦ 拍板后已不复存在**——`max(base, …)` 使任何低于 $2 的计算结果都退回基准线，`PupLabs`（平衡 $0.4205、实付 $0.3274、旧规则新上限 $0.21）现在维持 $2。代价是 ⑥ 那张表预期的「压住虚高档省钱」也一并没有了，但 ③④ 已证明那份预期本身就不成立：压上限省不到钱，只会拿不到量。

#### 12.5e 「$6 是总预算」口径确认后的完整验证（2026-08-05，07 澄清）

07 澄清 **$6 指单条系列的总预算硬顶，不是日预算**。此前我曾拿它与 yz04 的日预算中位 $4 作印证，那是错的口径——他日预算 $3-4 但系列跑几十天，单条累计花费远超 $6（8/4 当天 `073-MUI1-puplabs` 一天就花 $26.31，该系列累计 $1,332.68）。按总预算口径重做全部验证，结论如下。

**① yz04 自己的系列累计花费分布——「$6 判决」其实就是他的实际做法：**

| 单条累计总花费 | 系列数 | 占比 | 合计点击 | 该档 CPC | 平均投放天数 |
|---|---|---|---|---|---|
| **<$6** | **397** | **44.4%** | 887 | **$0.1935** | 10.0 |
| $6-10 | 51 | 5.7% | 4,416 | $0.0990 | 4.5 |
| $10-20 | 208 | 23.3% | 28,708 | $0.1067 | 6.8 |
| $20-50 | 143 | 16.0% | 45,260 | $0.0926 | 10.9 |
| $50-200 | 77 | 8.6% | 115,421 | $0.0708 | 24.4 |
| ≥$200 | 18 | 2.0% | 130,825 | **$0.0677** | 44.3 |

**近一半系列（397/894）总花费不到 $6 就停了,而这批恰恰是 CPC 最贵的一档($0.1935)**;而 ≥$50 的 95 条系列 CPC 只有 $0.07,吃掉 74% 的点击。**他的实操就是「贵的早砍在 $6 以内、便宜的放它长跑」——与 07 的 $6 判决口径本质相同。**

**② $6 够不够跑到出单？够，而且很宽裕：**

全库 7 月新建的 1,448 条最终出单系列，**首单时累计只花了 $2.67、累计 20.5 个点击**。$6 这条线的误杀率：

| 口径 | 出单系列数 | 首单前平均花费 | $6 误杀率 | $11 误杀率 |
|---|---|---|---|---|
| **全库（7 月新建，排零点击）** | 1,315 | $2.94 | **14.3%** | — |
| yz04 | 163 | $4.48 | 27.6% | 6.6% |
| wj07 | 62 | $3.43 | 16.1% | 0% |

yz04 误杀率高出全库一倍,是因为**他的首单来得晚**（$4.48 vs $2.94）,不是口径错误。按最终 CPC 分层看误杀率反而各档接近（10.7%–17.5%）——**$6 这条线对贵商家和便宜商家一样宽,它不是区分器。**

**③ 50 点击够不够判决？够，只漏 11%：**

1,448 条出单系列的首单发生位置：**81.4% 在 30 个点击内、90.1% 在 50 个点击内、96.1% 在 100 个点击内**。所以「50 点击零订单 → pass」只会漏掉 **11.0%** 的成功系列，07 说的「严一点没关系」在数据上代价很小。

**④ 真正的矛盾在这里：$6 能不能买到那 50 个点击——完全由 CPC 决定：**

| 最终 CPC | 系列数 | 花到 $6 时累计点击 | 其中 ≥50 点击 | ≥30 点击 |
|---|---|---|---|---|
| **<$0.08** | 309 | **193.6** | **97.4%** | 99.4% |
| **$0.08-0.12** | 242 | 64.7 | **81.8%** | 98.3% |
| $0.12-0.20 | 460 | 43.1 | 23.5% | 92.6% |
| $0.20-0.35 | 451 | 28.4 | 1.8% | 34.1% |
| ≥$0.35 | 358 | 12.8 | **0%** | 0.8% |

**这张表是整个判决体系的地基。** CPC ≥$0.35 的系列花完 $6 只有 12.8 个点击——此时零订单**不能证明这个商家不行**，样本量根本不支持判决，pass 掉是在扔硬币。而 CPC <$0.08 的系列花完 $6 已有 193 个点击，零订单就是可信的否决。

**所以「$6 总预算 + 50 点击」不是一条止损规则，而是一条「判决有效性」规则**，它成立的前提是选品门槛把 CPC 压在 $0.12 以下——**这正是 12.5d 的月搜索量门槛存在的意义**（月搜 ≥2 万 → CPC $0.0396 → 落进 <$0.08 档 → 97.4% 能在 $6 内攒够 50 点击）。

**⑤ 组合规则的全库实测（7 月新建 3,028 条）：**

| 分组 | 系列数 | 总花费 | 命中率 | CPC |
|---|---|---|---|---|
| CPC <$0.12（通过） | 767 | $14,534 | **58.3%** | $0.0390 |
| CPC ≥$0.12（拒绝） | 2,261 | $24,155 | 44.3% | $0.2228 |

被拒绝的那批**吃掉 62% 的总花费($24,155),命中率却更低**。这是选品门槛的直接收益估算。

**⑥ 日预算怎么配（$6 总预算下的天数）：**

| 实际日均花费 | 系列数 | 花到 $6 用的天数 |
|---|---|---|
| ≤$2 | 59 | 6.7 天 |
| $2-3 | 142 | 2.7 天 |
| $3-4 | 92 | 2.3 天 |
| $4-6 | 60 | 2.0 天 |

**建议日预算设 $2-3**，让 $6 撑 2.7-3 天。理由：低于 2 天 Google 的学习期还没过（12.2 已证 2-3 天档 ROI −32.7%），而日预算 $4+ 会让 $6 在 2 天内烧完。日预算 $2 时实际会拖到 6.7 天，偏慢。

**⑦ 由此可以简化判决规则**（相比 12.5a 的四格矩阵）：

> 选品阶段用**月搜 ≥2 万**把 CPC 压到 $0.12 以下 → 单条日预算 **$2-3**、总预算 **$6** → 花到 $6 时若累计 ≥50 点击且零订单 → **pass**（此时判决可信）；若花到 $6 点击仍不足 50，说明该商家实际 CPC 超预期 → 未验证的直接 pass，**已验证的**才追加到 $11-14 买满 50 点击再判。

这样「$11-14」从「默认给已验证商家的额度」降级为「只在 CPC 超预期时才用的补救额度」，绝大多数系列只花 $6。

#### 12.5d 选品门槛的替换物：品牌词月搜索量（HM-D48「完全更换」的落点）

07 已定 HM-D48 那套 SOP 门槛（含「单笔佣金 ≥$1.5」）**完全更换**。12.5a 的矩阵把「实测 CPC 是否 <$0.12」作为一个轴，但那是**开投后才知道**的量，不能当选品门槛。这一节回答「开投前用什么判」，依据是 yz04 8/4 全量 Google Ads 真值（52 个 CID、1152 条关键词行、78 条当天有花费的关键词，`yz04_gads.mjs`）+ 关键词规划器事前预估（`yz04_kwplan.mjs`）。

> ⚠️ **读本节前必看（2026-08-05 12.5f 的修正）**：本节把低 CPC 当好事，这个口径**不完整**。12.5f 实证按实际 CPC 分层时 ROI 是**随 CPC 单调上升**的（≥$0.60 档 ROI 1,897%），因为贵商家的单笔佣金极高。所以月搜索量门槛的作用**不是「挑便宜商家」，而是「保证在 $6 内买得到 50 个点击、让判决有效」**；「这个 CPC 亏不亏」是另一个维度，由 12.5f 的**盈亏平衡 CPC** 判定。两者都要：月搜门槛管**买得到量**，盈亏平衡 CPC 管**买来不亏**。单独用本节会砍掉 Hoteles Xcaret UK（盈亏平衡 CPC $47.55）这类最赚钱的商家。

**先看他的出价配置——完全是统一模板，成本不由他控制：**

| 项目 | yz04 8/4 实测 | 含义 |
|---|---|---|
| 出价策略 | **52/52 全部 `TARGET_SPEND`**（最大化点击） | 无一条手动 CPC，也无 tCPA/tROAS |
| 关键词有效出价 | 中位 **$0.500**（统一模板值） | 不按商家调整 |
| 实际成交 CPC / 有效出价 | 中位 **0.278**，p10 仅 0.044，贴顶(>0.9)只 8/78 | **出价上限压根没触发**，与 12.5a 的结论一致 |
| 日预算 | 中位 **$4.00**，最常见 $3(16 条)/$4(10 条)，38/52 条 ≤$6 | ⚠️ **这是日预算，不可与 07 的 $6 总预算相提并论**（初版曾错误地拿它「印证 $6」，07 已澄清，改正见 12.5e）。按总预算口径他的系列累计花费从 $3.86 到 $1,332.68 |
| 关键词质量分 | 中位 **10**（满分），446 个有值的里 337 个是 10 分 | 纯品牌词特征，质量分无区分力 |
| 匹配类型（有花费的） | EXACT 59 / PHRASE 19 / BROAD 0 | 词形就是品牌名变体（`etsy`、`american eagle`+`ae`、`mgmresorts`+`mgm grand`+`mgm rewards`） |

同样的 $0.50 上限下，`etsy` 成交 $0.0099、`mitoredlight` 成交 $0.4550，**差 46 倍**。展示份额丢失归因给出机制：便宜那批「因排名不足丢失」接近 0%（Etsy 0%、American Eagle 0.2%——**没人跟他抢，他吃满了**），贵那批高达 90%（出价 $0.50 仍排不上）。**所以 CPC 是商家品牌词的市场属性，选品阶段就该筛掉贵的。**

**四个候选判别器的实测预测力**（对当天实际 CPC 的 Spearman 秩相关，n=73~74）：

| 候选（开投前可拿到） | Spearman | 可用性 |
|---|---|---|
| **品牌词月搜索量** | **−0.578** | ✅ **唯一有实用预测力**（搜索量越大 CPC 越低） |
| 规划器预估「首页低位出价」 | +0.341 | ❌ 太弱 |
| 规划器预估「首页高位出价」 | +0.253 | ❌ 太弱 |
| 规划器「竞争度指数」 | +0.146 | ❌ **基本无关** |

竞争度标签必须明确否掉：标为 `HIGH` 的 51 个词，CPC 从 $0.0091 到 $0.8400，**跨度 92 倍**；`etsy` 竞争度 HIGH(指数 91) 成交 $0.0099，`mito red light` 同为 HIGH(100) 成交 $0.3700。**同一标签内差 37 倍，不可作门槛。**

**月搜索量分档 → 成本与 $6 的实际购买力（分界线就在 2 万）：**

| 品牌词月搜索量 | 词数 | 实际 CPC | $6 能买到 | 对 $6/50 点击 |
|---|---|---|---|---|
| <5 千 | 10 | $0.2411 | 25 个点击 | ❌ 不可行 |
| 5 千 – 2 万 | 26 | $0.1642 | 37 个点击 | ❌ 不可行 |
| **2 万 – 10 万** | 27 | **$0.0620** | **97 个点击** | ✅ 可行 |
| **10 万 – 50 万** | 9 | **$0.0520** | **115 个点击** | ✅ 可行 |
| **≥50 万** | 2 | **$0.0097** | **617 个点击** | ✅ 极宽裕 |

这个分界线**恰好落在 12.5a 的 $0.12 CPC 线上**：月搜 2 万以下对应 CPC $0.16-0.24（>$0.12，$6 买不到 50 点击），2 万以上对应 $0.05-0.06（<$0.12）。**两条独立算出来的线重合，互为交叉验证。**

**系列级门槛实测**（把词聚回系列、取该系列最大月搜索量，52 个系列）：

| 门槛 | 通过 | 通过组 CPC | 拒绝 | 拒绝组 CPC | 成本差 |
|---|---|---|---|---|---|
| **月搜 ≥ 2 万** | **31 系列** | **$0.0396** | 21 系列 | $0.2145 | **5.4 倍** |
| 月搜 ≥ 5 万 | 16 系列 | $0.0273 | 36 系列 | $0.1337 | 4.9 倍 |
| 月搜 ≥ 10 万 | 10 系列 | $0.0256 | 42 系列 | $0.1180 | 4.6 倍 |

单日达量率（当天就攒到 ≥50 点击的比例）：2 万门槛下**通过组 17/31 = 54.8%，拒绝组仅 3/21 = 14.3%**。注意这是单日口径，实际判决允许多天累积，真实达量率高于此。**建议取 2 万而非 5 万/10 万**：更严的门槛成本收益几乎不再改善（$0.0396→$0.0256），但通过量从 31 条砍到 10 条，会把大量能赚钱的商家挡在外面。

**误判检查（2 万门槛）：**

- **假阳性**（月搜够但仍然贵）只 2 个：`pique` 月搜 11 万却 $0.2100、`rails` 月搜 6.05 万却 $0.1554。占通过组 6.5%，由 12.5a 的 $6/50 点击闸兜住即可。
- **假阴性**（月搜不够但很便宜）2 个：`mgmresorts` 月搜 4400 成交 $0.0148、`jwpei` 月搜 1.21 万成交 $0.0430。**但取「系列最大月搜」可以救回来**——MGM 那条系列还挂着 `mgm rewards`(月搜 9.05 万)，jwpei 那条还挂着 `jw pei`(7.4 万)，两条都能过 2 万线。**这是必须按系列取 max、而不是逐词判定的原因。**

**实现路径（已实测可调通）：**

```
POST /v23/customers/{MCC}:generateKeywordHistoricalMetrics
body: { keywords:[品牌词变体], geoTargetConstants:[国家], 
        keywordPlanNetwork: GOOGLE_SEARCH, language: languageConstants/1000 }
→ keywordMetrics.avgMonthlySearches   ← 唯一要用的字段
```

- 已用 yz04 的 MCC 凭证实跑通：78 个词、8 个国家、**16 秒**，US 覆盖率 57/61（93.4%），其余国家 100%。
- 落点在 SOP **阶段三**（原「单笔佣金 ≥$1.5」的位置），只对少数候选商家跑，配额压力小；结果按商家缓存 30 天（品牌词搜索量是慢变量）。
- 品牌词变体取法照 yz04 的实际做法：商家 slug 原样 + 加空格拆词（`jwpei`→`jw pei`、`withjoy`→`with joy`），**取所有变体的月搜索量最大值**。
- 查不到时按 HM-D48 既定原则「**查不到就放行并降分，不淘汰**」处理，不因 API 覆盖缺口让商家背锅。

### 12.6 其他可迁移的实证结论

1. **单均佣金不是选品标准，方向甚至相反**。同队列下 yz04 单均佣金 $3.60、wj07 $4.47——wj07 挑的商家单价更高却整体亏损。yz04 赚的是 3,962 单对 94 单。（注：全队列混看时是 $4.95 vs $21.60，那是 wj07 一条 M.M.LaFleur 老系列造成的错觉，已被队列对齐修正。）
2. **出单率是商家级属性，不是执行属性**。11 个共同商家上两人的点击→订单转化率几乎相同（**2.44% vs 2.33%**），但 CPC 差 2.1 倍、投放天数差 2.7 倍（7.9 vs 2.9）。说明 wj07 的转化能力不差，缺的是量。这支持 HM-D48 加强选品的方向，但选品指标应是「能否出量」而非「单笔佣金高低」。
3. **真正赚钱的不是「同商家多开」，而是「同商家换联盟账号再开」**（07 于 2026-08-04 当场纠正我的误读，原文：「同商家同联盟但是不同账号开……他的联盟账号也是不同的，只是联盟相同」）。把 yz04 的多系列商家按「第二条是否换了 `platform_connection_id`」拆开，两组表现完全不是一回事：

| 分组 | 商家数 | 系列数 | 花费 | 订单 | 净利 | ROI |
|---|---|---|---|---|---|---|
| **多系列 — 跨联盟账号** | 11 | 24 | $3,345 | 2,726 | **$14,114** | **421.9%** |
| 多系列 — 同一联盟账号 | 16 | 33 | $1,883 | 1,227 | $2,442 | 129.7% |
| 单系列商家 | 238 | 238 | $6,857 | 5,116 | $8,329 | 121.5% |

   **同一账号里再开一条（129.7%）和只开一条（121.5%）基本没有差别**——所谓「复投」本身不产生增益。增益全部来自换账号：11 个跨账号商家只吃掉 yz04 7 月花费的 27.7%，却产出净利的 56.7%。原先写的「27 个商家贡献 67% 净利」把两组混在一起，掩盖了这个分界，已作废。

   为什么换账号有效，数据只能给出边界不能给出机制：两人在共同商家上的点击→订单转化率几乎相同（12.6 第 2 条），说明差别不在商家侧；而同商家同联盟换账号后，联盟给的 offer 条件、追踪链路、Google 侧的 CID 与账号历史全都换了一套。Contiki 是最干净的反例——第一条在 `xukewei` 账号跑 6 天花 $23.92 零出单（按任何止损线都该关），7/10 换到 `dominickessaryiagdot` 账号重开，跑 21 天赚 **$1,056（ROI 1193%）**。**「这个商家不行」和「这个商家在这个账号上不行」是两个判断，Hermes 现在把它们当成一个。**

4. **扩池的触发线可以量化，且门槛比想象的高**。把 yz04 全部 487 个有花费商家按「后来是否做了跨账号扩池」分组，看第一条系列当时的成绩：

| 分组 | 商家数 | 第一条平均净利 | 平均花费 | 平均订单 | 净利>$500 | 净利>$100 | 净利≤0 |
|---|---|---|---|---|---|---|---|
| 后来做了跨账号扩池 | 14 | **$1,127** | $242 | 202.9 | 4 | 9 | 3 |
| 没扩池 | 473 | $37.6 | $36 | 21.9 | 10 | 43 | 306 |

   相差 30 倍。**他不是「盈利就加开」，而是「跑出量级」才加开**——9/14 是净利 $100 以上，且订单量普遍在 200 单以上。这个门槛可以直接做成复测队列的入队条件（建议：单条系列累计净利 ≥$100 且订单 ≥50，两者同时满足）。另外扩池不追求复制原成绩：12 组配对里第二条 8 赚 4 亏，但第二条平均只投 $44，最大亏损 $46、最大盈利 $1,056——**是一个投入受限、上限敞开的不对称赌注**，这也是它适合自动化的原因。

5. **新联盟账号的冷启动方式：不用来试新商家，用来搬已验证清单**。`fortunavita` 是 yz04 于 7/24 新开的第 3 个 MUI 账号，它 7 月只建了 5 条系列，**全部在 7/24 同一天建、全部是 MUI1 上已验证盈利的商家**（campsnap / puplabs / teepublic / coverland / londontheatredirect），零个新商家试探。结果 5 条里 4 条出单（80%），ROI 195.4%。对照同一联盟的老账号 `haven-hearth` 7 月新建 59 条系列：命中率 57.6%，ROI 仅 **33.6%**。新账号首月靠搬清单拿到的 ROI 是老账号做新探索的 5.8 倍。含义：**新账号最稀缺的是「已知能出单的确定性」，不是「新 offer 的可能性」。**
6. **预算要花满**。yz04 预算利用率中位 98.7%、p90 达 131.6%（超预算跑）；wj07 只有 88.8%。预算的作用是控单日风险，不是限制总投入。
7. **首日 CPC 不能当闸门**。试过用它预测成败，yz04 五档首日 CPC（<$0.08 / $0.08-0.15 / $0.15-0.25 / $0.25-0.40 / >$0.40）的最终出单率是 72.1%、83.7%、70.7%、69.0%、70.2%——基本持平。贵的流量照样出单，CPC 影响的是「能买到多少量」而非「转化能力」。用 $0.25 当门槛会砍掉 45.4% 的系列，只换来每系列净利从 $84 提到 $138，不划算。

### 12.7 证据边界（必须连同结论一起看）

| 项 | 说明 |
|---|---|
| **CPC 差异的根因已查清（此项原为「未查清」，2026-08-04 结案）** | 先后排除：出价策略（两人 100% MAXIMIZE_CLICKS）、网络设置、老系列学习期、质量分、追踪断链。**Google Ads 真值给出定论（12.5d）：CPC 由商家品牌词的市场竞争决定，不由投放动作决定**——yz04 出价上限统一 $0.50 且只用掉 27.8%，同一上限下不同商家成交价差 46 倍；便宜的那批「因排名不足丢失展示份额」≈0%（无人竞价），贵的那批 90%。可事前判定的代理变量＝**品牌词月搜索量**（Spearman −0.578）。 |
| **CRM 侧关键字段是空的（已绕过）** | `campaigns.max_cpc_limit` 在 yz04 名下 1,815 条系列**全为 NULL**（全表填充率仅 14.6%，11 个用户 100% 空），**不能据此推断出价行为**——此坑已踩过一次并撤回结论。同样为空：yz04 的 `keywords` 0 条、`ad_creatives` 0 条、`cookie_duration` 0/295。**关键词与文案策略在 CRM 侧无法分析**——已于 2026-08-04 改走 Google Ads API 直取（`yz04_gads.mjs`），12.5d 的全部结论来自 API 真值而非 CRM 字段。 |
| **12.5d 的样本只有一天、一个人** | 关键词真值是 yz04**单日（8/4）**52 个 CID 的快照：1152 条关键词行里当天有花费的仅 **78 条**，最粗的档位（月搜 ≥50 万）只有 2 个词。月搜索量→CPC 的负相关（−0.578）方向稳健，但**分档 CPC 的绝对值不可当预期值用**。另外月搜索量是**按国家**取的（geo 定向），同一品牌在不同国家的量差很大，判定必须带国家参数。**未做的验证：拿 wj07 的失败系列反向跑一遍月搜门槛**（会否也是月搜低的那批），这是上线前最该补的一步。 |
| **wj07 的新建队列极度偏新** | 186 条新建系列里超过 100 条建于 7/24 之后，到月底只跑了几天。其「活过 8 天率 0.5%」有一部分是时间不够，不全是被主动关停。 |
| **佣金归因到商家级** | CRM `affiliate_transactions.campaign_id` 仅 65/589,216 行有值，佣金按 `user_merchant_id` 归到商家再摊到系列，同商家多条系列时会串。 |
| **花费口径混合** | 日表 `data_source` 有 sheet / api 两种，Sheet 值历史上比 Google 真值低 $0.5–$2.31（HM-D38 同源问题）。ROI 绝对值略偏高，同口径对比不受影响。 |
| **拒付未落定** | 7 月账面拒付率 yz04 17.7%、wj07 4.2%，但大量订单仍 pending。yz04 历史全期拒付率是 29.1%，净利会被高估。**上表所有 ROI 均按账面拒付计算，实际会更低。** |
| **跨账号扩池样本仅 11 个商家** | 12.6 第 3 条的 421.9% ROI 建立在 11 个商家 / 24 条系列上，且**高度集中**：PupLabs 与 Camp Snap 两个商家就占了该组净利的 68%（$9,614/$14,114）。去掉这两个后该组 ROI 降到约 218%——仍高于同账号组的 129.7%，但倍数从 3.3 倍缩到 1.7 倍。**「换账号有效」的方向可信，效应量不可信。** |
| **换账号为何有效未定论** | 已确认的只是「换了 `platform_connection_id` 的那组表现更好」。混在里面无法拆开的变量至少有三个：换账号同时换了 Google CID（账号历史/学习状态不同）、联盟给不同账号的 offer 条件可能不同、扩池发生在月中（第二条平均只跑 7 天，赶上的流量环境与第一条不同）。Contiki 那个 1193% 的反例说明账号本身确实是变量，但单例不足以定性。 |
| **⚠️ 本文档曾落后于服务器代码九笔改动——分析 Hermes 行为前必须先读代码，不能只读本文档** | 2026-08-05 审计发现服务器侧 HM-D55～D63（7/30-8/01）九笔已生效改动本文档一笔未记，直接后果：12.5f 初稿把 7/31 已被 HM-D57 **彻底移除**的 `cpc_up`/`cpc_down`/`cpc_up_epc` 当成现行规则，据此给出的配套建议全部打空（已撤回重写）；同时漏看了 HM-D57 的「净亏线按单笔佣金分档」与「买不起就别测」——那正是本节建议的同源先例，`perOrderCommissionMedian()` 与 `merchant_budget_skips` 都可直接复用，等于差点重复造轮子。**根因**：本文档在本机维护、Hermes 在服务器上自行迭代并自行编号，两边无同步机制。**已立的纪律**（13.1）：新增 `HM-D` 号前必须 grep 服务器取最大号；**还应补的**：每轮分析开始前先 `git log` / grep 一遍 `~/hermes-ads/src` 的近期改动注释，再下结论。**这是本次分析第四次踩「拿过时认知当现状」的坑**（前三次：`max_cpc_limit` 全空被误读、队列不可比、`platform_connections` 软删除）。 |
| **`platform_connections` 有软删除列** | 该表有 `is_deleted`（tinyint，默认 0），**只筛 `status='connected'` 会把废弃记录一起算进来**——我据此得出「wj07 有 CG 8 个 / RW 4 个连接、Hermes 少接了池」的错误结论，07 当场指出（「你是不是把软删除也算上了？朱文欣没有这么多」）。真值：wj07 有效连接 7 个、yz04 10 个。已按 `is_deleted=0` 重算并复核跨账号判定（结论不变）。**这是本次分析第三次踩「CRM 字段语义」的坑（前两次是 `max_cpc_limit` 全空、队列不可比），查 CRM 主表前必须先 `SHOW COLUMNS`。** |
| **扩池触发线是事后拟合** | 12.6 第 4 条的「净利 ≥$100 且订单 ≥50」是从 14 个已扩池商家的第一条成绩里倒推的，yz04 未必按这个数值决策。它可作为 Hermes 的初始值，但**必须当作待验证假设，上线后回看命中情况再调**。 |
| **盈亏平衡 CPC 是同口径比值，绝对值有虚高（12.5f）** | 该值＝有效佣金 ÷ 点击，但两个分量口径不齐：**佣金是该商家全期的**（`affiliate_transactions` 无系列级归因，见上「佣金归因到商家级」），**点击只统计 7 月新建系列**。故全库基准那一行算出的「转化率 16.7%、平均盈亏平衡 CPC $0.98」**明显虚高，不可当预期值用**。但 12.5f 的核心结论只依赖**同一口径下 `实际CPC` 与 `盈亏平衡CPC` 的相对比较**（分子分母同样摊派，偏差同向抵消），以及**商家之间的排序**（$0.016 到 $5.35 的 328 倍差距远大于口径噪声），这两者成立。~~**上线前需补**：改用「该商家有点击期间的佣金」重算一遍绝对值，再定安全系数。~~ **2026-08-05 接口落地后已查清，且比原判断更严重**：CRM 接口两个分量都改用全期（口径已齐），但真正的病根不是时间窗错配，而是**联盟订单按账号全量拉、不区分流量来源**——611 个有点击的商家里 **345 个（56%）**的订单数超过点击数的 30%（179 个甚至多于点击数），佣金分子含非广告流量。原以为可用「仅可归因佣金」修正，实查 `affiliate_transactions.campaign_id` 全库 46.9 万笔**只填 1 笔（0.0%）**，CRM 做不了系列级归因。故绝对值**无法重算校准**，只能标注可信度（`breakeven_trusted`）并对不可信档把安全系数压到 0.25（#22）。详见 12.5c-1。 |
| **12.5f 的高 CPC 高 ROI 有生存者偏差** | 「ROI 随 CPC 单调上升」的分层里，≥$0.60 档只有 210 条系列、2,451 个点击（占总点击 0.6%），却摊到 $41,398 佣金——**这批是「贵但活下来了」的**，被贵死在测试期的系列因为没出单不进佣金侧。所以正确读法是「**高 CPC 商家里的成功者极其赚钱**」，不是「高 CPC 本身好」。这也正是限额要按**每个商家自己的**盈亏平衡值定、而非按 CPC 档位一刀切放开的原因。 |
| **在投商家 80% 无团队数据是当下最大的空档** | Hermes 在投/待批 86 个 MID 里 **69 个（80%）在 CRM 里查不到任何团队订单**，盈亏平衡 CPC **无从计算**。故 12.5f ⑥ 的规则对当前绝大多数系列**落不了地**，只能先吃「探测下限 $0.30」。这使待确认项 #16（CRM 只读 API）从「提升准确率」升级为**限额规则能否生效的前置依赖**。**2026-08-05 接口上线后复核：空档比这条原本记的更大，且不是接口能补上的。** 接口确实拿到了 3,103 个团队已验证商家（远多于 Hermes 本地所知），但其中**只有 611 个（19.7%）在 CRM 有广告点击、算得出盈亏平衡 CPC**——另外八成只有「能出单」这个布尔值。也就是说 12.5f ⑥ 的 ①② 两支合计只覆盖两成商家，**③「探测下限 $0.30」是常态而非兜底**。想缩小这个空档只能等更多商家在 CRM 侧积累广告数据，接口本身已无更多可挖。 |

原始分析脚本与落地数据：本机 `_hm_probe/`（`cmp_july.mjs`、`chk_cohort.mjs`、`chk_day1_signal.mjs`、`chk_cpc_curve.mjs`、`chk_maxcpc.sh`、`chk_samemerchant.sh`、`chk_tracking.sh`、`chk_multiaccount.sh`、`chk_expand2.sh`、`chk_softdel.sh`、`chk_expand_strict.sh`、`chk_gate50.sh`、`chk_cpc_truth.sh`、`chk_curve.sh`、`chk_lowcpc_volume.sh`、`chk_teamverify.sh`、`read_team_sheet.mjs`，输出同名 `.txt`）。

$6 总预算口径验证（12.5e）：`gate6_total.sh`（累计花费分布 / 花到 $6 时点击 / 天数）、`gate6_misfire2.sh`（误杀率 / 首单点击位置 / 组合规则）、`gate6_reconcile.sh`（核对 41.3% 与 14.3% 的口径差异）。**踩坑**：`campaigns` 表没有 `merchant_id`，商家关联走 `user_merchant_id`；`affiliate_transactions` 的日期列是 `transaction_time`（不是 `transaction_date`/`cst_date`）且同样有 `is_deleted`。

CPC 限额审计（12.5f）：Hermes 侧 `hm_cpc_audit.mjs`（出价分布 / 按状态 / 自动调参方向 / 在投实际成交价）、`hm_cpc_def.sh`（`settings.js` 定义 + `app_settings` 实际值 + 出价模式）、`hm_inflight_mids.sh`（导出在投系列 MID）；CRM 侧 `cpc_breakeven.sh`（盈亏平衡 CPC 分层 / 付超判定 / 高佣金商家清单）、`gen_mid_sql.mjs` + `run_mid_query.sh`（把在投 MID 对上团队真值）、`cpc_rule_sim.sh`（07 拍板后回溯模拟：新上限影响面 / 下界错误的代价 / 探测下限覆盖率 / `cpc_up` 冲突量化）。**踩坑**：Hermes 的库文件是 `data/hermes-ads.db`（**不是** `hermes.db`，后者是空壳）；配置表叫 `app_settings`（**不是** `settings`）。

Google Ads 真值链路（12.5d）：`yz04_gads.mjs`（在 CRM 服务器项目目录内跑，复用 `src/lib/google-ads/client.ts` 的 SA JWT + REST `searchStream`；拉 `keyword_view` + `campaign`）→ `yz04_keywords.tsv` / `yz04_campaigns.tsv`；`yz04_kwplan.mjs`（`generateKeywordHistoricalMetrics` 事前预估）→ `yz04_kwplan.tsv`；分析脚本 `ana_gads.mjs`、`ana_predictor.mjs`。**注意脚本必须放在 `crm-mvp/` 目录内执行**（放 `/tmp` 解析不到 `node_modules`），且含换行的 DB 字段一律走 `TO_BASE64()` + SQL 层 `REPLACE` 去换行。

### 12.8 一次没有任何报警的空转：审批环节死锁（2026-08-03 ~ 08-05，HM-D65）

07 在 08-05 下午问「为什么在跑广告没有补上」时，在投只剩 **30 条**，而两天前是 74 条。查下来**没有任何一个环节出故障**，四个各自正确的设计叠成了死锁。

**时间线**

| 时刻（北京） | 发生了什么 |
|---|---|
| 08-01 14:01 | 07 批准上一批任务（`approvals` 表最后一次 `approve`） |
| 08-03 06:45–08:07 | 名单补货建了 **52 条**新任务，合并发出一张批量审批卡（`cron.log` 里 `"sent": 52`） |
| 08-03 全天 | 08-01 批准的那批陆续发完，最后一条 `Barnesmith` 于 **23:15** 上线。当时 CID **74/74 全满** |
| 08-04 | 止损关停 **40 条**，发布 **0 条**。CID 开始空出来 |
| 08-05 上午 | 又关 4 条，发布仍是 **0 条**。在投 30 条，**44 个 CID 空转** |
| 08-05 14:09 | 07 收到重推的卡，一次批完 52 条 |
| 08-05 14:30 | 在投回到 **54 条**，另有 26 条 `approved` 在排队等 CID |

**四个环节当时各自的判断**

| 环节 | 判断 | 依据（都没错） |
|---|---|---|
| 名单补货 | 名单 52 > 低水位 50，跳过 | `waitlistCount()` = `pending_approval` + `approved`，**待批也算库存** |
| 发卡 | 这批已发过，不再发 | `approval_msg_sent=1` 后不进候选，HM-D29 防单卡轰炸 |
| 任务泵 | `approved` 为空，无事可做 | 只发已批准的 |
| 止损 | 照常关停 | 与审批无关 |

**根因是名单水位的口径**：`waitlistCount()` 把「待批」和「已批」加在一个数里。名单管的是「仓库里有没有货」，能不能上广告取决于「货有没有出库许可」，两件事被算成了一件——仓库 52 件、出库许可 0 张，系统看到 52 就安心了。HM-D42 的 `/api/waitlist` 注释已经警告过「三个概念别混：名单 / 配额 / CID 池」，但水位线自己仍然混着。

**一个反直觉的推论**：那两天**就算补货触发了也救不回来**。补货新建的任务同样是 `pending_approval`，只会把待批从 52 堆到 100，`approved` 依然是 0，一条也发不出去。补货解决的是「没货可批」，不是「批了没人点」。所以修法不是调水位线口径，而是补一条独立的「出货口堵塞」检测。

**HM-D65 的修法**：待批任务卡过 `approval_remind_hours`（默认 12h）就重推一次卡，并额外发一条文字消息说清代价（等了多久、在投多少条、多少个 CID 在空转）。**只重推卡片不够**——那张卡跟两天前那张长得一模一样，很容易被当成已经看过的；文字消息才是让人意识到「这两天没在投广告」的那一下。节流靠新列 `approval_reminded_at`，同一批任务每 12 小时最多提醒一次。

> ⚠️ HM-D65 只覆盖「待批无人处理」这一类空转。另一类——07 批了、但 CID 不够或发布连续失败导致的只出不进——它抓不到。07 在 08-05 的选项里选了「加超时提醒」而没选「盯在投数量告警」，故暂缺；若日后出现第二类空转，应补一条「在投数跌破阈值」的独立告警。

### 12.9 三个月没生效的抬价：用建单来源猜出价策略（2026-08-05，HM-D67）

HM-D66 上线当天，07 要求实地核查生产环境「真实情况和我们预想的一样吗？会不会有 BUG」。核查分两层做：先比对**库内值**与配置流水，再逐条**实调 Google** 取真值。前一层全部通过，后一层查出一个已经吃掉真金白银的 BUG。

**跟预想一致的部分**：三个改动文件的落盘时间都早于进程启动（代码确实加载了）、设置正确落库并留了 `settings_log` 流水、`inCooldown` 正常拦住重复抬价、`hard_cpc_usd` 从 $5 收到 $1 时没有误伤任何存量广告、抽查 5 条自建广告库内上限与 Google 真值全部一致。

**BUG 本体**：`adjustRun` 的 `set_cpc` 分支用 `run.queue_source === 'external'` 来判断一条广告是不是手动出价——这是 HM-D27 接管存量广告时留下的假设，当时 07 手工建的广告确实是 MANUAL_CPC。但**实调 5 条 external 广告，在 Google 侧全部已经是 TARGET_SPEND**，没有一条是手动的。于是每次调价都走 `setManualCpc` 去改广告组和关键词的 `cpc_bid_micros`，而那正是 HM-D57 白纸黑字写过的、在智能出价下**被 Google 完全忽略**的字段。唯一生效的 `campaign.target_spend.cpc_bid_ceiling_micros` 从来没被碰过。

**代价**：`#46 mmlafleur` 的三次抬价全部落空——7/30 `cpc_up_epc $0.25→$0.29`、7/31 `cpc_up_epc $0.29→$0.33`、8/05 HM-D66 首轮 `cpc_up $0.33→$0.66`，**Google 侧真实上限三个月纹丝不动停在 $0.50**。库内记的 `$0.33`、`$0.66` 都是幻影值。#47 `cariloha`（库 $1.50 / 真 $0.80）、#48 `fatandweirdcookie`（库 $0.18 / 真 $0.30）同样对不上，只有 #44/#45 因为从没被调过而碰巧一致。**这解释了 HM-D66 为什么「首轮生效」了却看不到任何量变**——它确实按设计算对了数、也确实写了库，只是写到了一个 Google 不看的地方。

**顺带挖到的旁证，比规则本身更有分量**：#46 近 7 天 `searchRankLostImpressionShare` 高达 **73%–86%**，而 `searchBudgetLostImpressionShare` 只有 0–10%（8/3 那天 53% 是唯一例外）。**七八成的展示机会输在排名不够、不是输在预算不够**。07 从 v1.9 起坚持的「贵的商家要去抢」到这里第一次拿到了 Google 侧的直接证据，不再是从 ROI 分档反推的推测。同期实付均价只有 $0.16–0.21、展示份额 12.8%–23.4%——上限 $0.50 之下 Google 只能捡到最便宜的那批竞价，贵的全部让掉。

**修法**：不再用建单来源当运行时状态的代理指标。`set_cpc` 每次调价前先 `gaql` 查当前 `bidding_strategy_type`，是 `MANUAL_CPC` 才走组层+关键词层，否则一律走 campaign 层 `setCpcCeiling`；查询失败时保守按智能出价处理并记一条 `api_call` 事件（实测 100% 的在投广告都是 TARGET_SPEND，这个降级方向是安全的）。**这类 BUG 的共性值得单独记住**：`queue_source` 是**建单那一刻**的事实，`bidding_strategy_type` 是**此刻**的事实，拿前者推断后者，只要中间有任何一次策略切换（07 手工改、或某次 `setCpcCeiling` 顺带把 MANUAL_CPC 切成 TARGET_SPEND——`google-ads.js` 的注释明说它会这么做）就会永久错下去，且**不报错、不告警、日志还写着「已调整」**。

**数据修正**：3 条幻影值同步成实调真值，并在 `run_events` 里写明原因，避免日后把 $0.66 当成基准再翻倍。修正后 auto-tune 按真实基准 $0.50 重算的目标是 `min($1.07 理想值, $0.50×2 步进封顶, $1.00 硬顶) = $1.00`。

**07 的两个拍板**：① `#46` **小步到 $0.75** 而不是直接跳 $1.00，观察一天再说；② 存量 72 条 $2 的在投广告**保持不动**——上限不等于花费（它们实付均价最高才 $0.88），且 mmlafleur 刚刚证明了压低上限会丢掉七八成展示，不该在刚吃过这个亏之后立刻重复同样的错。

**验证**：走修复后的完整链路推 `#46` 到 $0.75，实调 Google 确认 `cpcBidCeilingMicros: 750000` 真的落地，库内与真值一致，事件流水的文案也从「调手动出价（组+3 个关键词）」变成了「调出价上限（智能出价 cpc_bid_ceiling）」。**这是三个月来第一次真正生效的抬价**。

**观察基线（抬价前 7 天，明天用这组数对比）**：

| 日期 | 点击 | 花费 | 实付均价 | 展示份额 | 排名丢失 | 预算丢失 |
|---|---|---|---|---|---|---|
| 07-29 | 59 | $12.46 | $0.211 | 20.8% | 73.1% | 6.1% |
| 07-30 | 100 | $18.46 | $0.185 | 20.8% | 68.5% | 10.8% |
| 07-31 | 87 | $14.11 | $0.162 | 23.4% | 39.0% | 37.6% |
| 08-01 | 69 | $12.90 | $0.187 | 19.7% | 75.4% | 4.8% |
| 08-02 | 39 | $6.77 | $0.174 | 13.6% | 86.4% | 0.0% |
| 08-03 | 110 | $23.56 | $0.214 | 12.8% | 33.8% | 53.4% |
| 08-04 | 45 | $8.43 | $0.187 | 15.5% | 84.5% | 0.0% |

判读要点：**排名丢失率应该明显下降、展示份额上升、点击量上升**；实付均价会跟着涨（这是买量的代价，不是问题），要盯的是**盈利比是否还在目标比 2.05 以上**。如果预算丢失率转而飙高，说明瓶颈从出价换成了预算，那时该动的是 `hard_budget_usd` 而不是继续抬价。

> ⚠️ **还有一处没查**：#46 的广告组与 3 个关键词上留着 $0.66 的幻影值。在 TARGET_SPEND 下它们被 Google 忽略、无害，故未清理；但若日后有任何逻辑把该系列切回 MANUAL_CPC，这个值会突然生效。切策略的代码路径目前只存在于 `setCpcCeiling`（单向切向 TARGET_SPEND），暂无风险。

> ⚠️ **抬价后的 #46 不在自动止损覆盖范围内**。顺带核了 `monitor-runs.js` 里 `external` 的全部 6 处分支：接管广告**不参与止损**（401 行）、不参与出单调增预算（460 行）、不参与等待期考核（486 行）、不参与换后缀健康检查（494 行）——这些都是 HM-D27 接管时的有意设计，那批广告的风险由 07 手动管。但 `autoTuneRun` 的调用**不在** `!external` 分支里，所以接管广告**会**被自动调参动预算和出价（#46 的 $15 预算就是 7/29 `budget_up` 加上去的）。这个组合意味着：**系统能自动给 #46 加钱加价，却不会自动给它踩刹车**。在它盈利比 10.84 的当下风险很低，但抬价本身就是在改变它的经济结构，若明天数据转差，只能靠人看。是否要给接管广告补一条独立止损，留作待确认项。

**扫描同类隐患**：全库 `queue_source` 共 20 处用法，除本次修掉的一处外，其余都是**管辖权与统计口径**判断（换后缀只管自建、seq 编号不含接管的、merchant-gate 统计排除接管的、pause 时接管广告不回收 CID），用建单来源做依据是正当的——**判断「这条归谁管」可以用建单来源，判断「这条现在是什么状态」不行**。这条界线是本次教训的可复用部分。

### 12.10 收紧硬顶引发的连锁停摆：校验排在定稿前面（2026-08-06，HM-D68）

07 早上发来 CRM 数据中心两张截图，「今日投放数 0 条」。查下来是 **HM-D66 收紧 `hard_cpc_usd` 引入的回归**，而且当时正在持续恶化。

**故障链**：`advanceApproved()` 的顺序是「① 预占 CID → ② `assertLimits(run)` 校验 → ③ 取后缀 → ④ 按发布时默认值定稿预算/上限（HM-D61）→ ⑤ 发布」。校验排在定稿**前面**，校验的对象是建任务时盖的那个章。8/05 把 `hard_cpc_usd` 从 $5 收到 $1 之后，存量 `approved` 任务身上 $2 的旧章撞上新硬顶，`assertLimits` 抛异常——**而 CID 已经在上一步预占掉了，这个异常又没有任何一层接**，于是任务永久停在 `cid_reserved`：不发布、不失败、不归还 CID。

**为什么会啃光整个池子**：pump 每 5 分钟捞一条 `approved`，每捞一条就预占一个 CID 然后卡死。改设置后 5 条依次卡在 10:30、11:15、13:15、13:40、13:55，改设置**之前**发布的那批（#457–472）则全部正常——时间线严丝合缝。到 8/06 早上，71 个可用账号已被占满、**空闲为 0**，98 条待发布全部堵在门口。CRM 上看到的「今日投放 0 条」只是这条链子的最后一格。

**修法**：定稿必须先于校验，因为**校验的对象应该是「这一刻真正要发出去的值」，不是两三天前盖的章**。实现上不动第 3 步，只在校验处用 `effectiveBidding(run)` 的结果构造待校验对象；同时把 `assertLimits` 包进 try/catch，失败即归还 CID 并标 `failed`。**预占之后的每一条失败路径都必须归还 CID，不能有例外**——原代码里后缀失败、发布失败两条路径都记得还，唯独这条新出现的校验失败没人管。

**验证**：解卡 7 条退回 `approved` 并归还 CID，手动催一轮 pump，**7 条全部发布成功**，`cpc_usd` 由建任务时的 $2 正确定稿为 $1，在投从 62 回到 72。另 2 条是独立故障（#473 后缀卡在跳板域名 `go.ultrainfluence.com`、#474 Google Ads 未归类报错），与本 BUG 无关。

> ⚠️ **改全局风控参数时的连带检查清单**（这次教训的可复用部分）：`hard_*` 这类 Owner 上限不只作用于「将来新建的任务」，还会**回溯作用于所有尚未走完流程的存量任务**。收紧之前必须扫一遍处于中间态的任务（`approved` / `pending_approval` / `cid_reserved`）身上的旧值会不会撞线，以及撞线之后**抛出的异常有没有人接**。这次两样都没做。

> ⚠️ **CID 是当前唯一的硬瓶颈，且在净减少**。修完之后 CID 立刻又被占满（空闲 0、93 条排队）。更要紧的是账号损耗速度：07-29 至 08-05 一周之内 `5509319692` 被 CANCELED、另外 6 个被 SUSPENDED，合计**退役 7 个**，而池子总共才 80 个。产能已经不取决于名单、审批或规则，只取决于还有多少能用的账号。这一条需要 07 决策（加 MCC 子账号 / 查封号原因），代码层面无解。

### 12.11 决策底座换真数：从「商家合并」到「系列级」（2026-08-06，HM-D69）

07 定的三件事，A 换数据源、B 新广告基准回 $0.35、C 样本门槛按 AOV 分档。三件是一套：**A 保证算得准，B 保证攒得到样本，C 保证样本够了才敢下手**。

#### A 盈亏比改用 CRM 系列级真数

`auto-tune` 的每一条规则都以盈亏比为轴，可它拿到的从来不是这条广告自己的账。Hermes 的 `click_token` 归因只有 4% 命中率（12.7），系列级佣金拿不到，只能退而用「商家 + 联盟」把同一商家所有广告的佣金合并成一个数，去代表其中每一条。**一个商家只有一条广告时这么算没问题，有多条时就串味**。

而 CRM 那边本来就有系列级归因——不在 `affiliate_transactions.campaign_id`（那列全库 46.8 万笔只填了 1 笔，`team-verified-merchants` 接口的注释里据此判定「这条路走不通」，说的是另一条路），而在 `ads_daily_stats`：按 `campaign_id` 存了每天的佣金/订单/拒付，10.2 万行里 4.5 万行有佣金。Hermes 的 `campaign_resource` 里带的 Google campaign id 和 CRM 的 `campaigns.google_campaign_id` 是同一个值，**两边精确对齐，不需要任何模糊匹配**。

差多少，实测这 6 条在投有单的广告说得很清楚：

| 商家 | 合并口径 R | 系列级 R | 差 |
|---|---|---|---|
| mmlafleur | **16.98** | **10.25** | **−6.73** |
| budgetvetcare | 4.65 | 4.77 | +0.13 |
| lagos | 3.26 | 3.30 | +0.04 |
| 其余三条 | — | — | 完全一致 |

**只有 mmlafleur 差得离谱，而它恰好是唯一一个同商家多条广告的**。合并口径把别的系列的订单算了进来（278 单 vs 实际 125 单），花费又少算了 $206（$328 vs CRM 的 $534），一高一低叠加把 R 抬到 16.98。按抬价公式反推：用 16.98 该出 $1.36，用真实的 10.25 只该出 $0.82，**差 66%，等于会把价抬过头一大截**。

落地：CRM 侧新增 `POST /api/hermes/campaign-stats`（D-219，只读）；Hermes 侧新增 `crm_campaign_stats` 表 + `sync-crm-campaign-stats.js`，每小时 20 分整表替换（避开 monitor 的 12 分、佣金同步的 40 分，让调参每轮都拿到刚更新的数）。首轮拉到 431 条：66 trusted / 191 thin_sample / 174 no_data（当天刚发、CRM 还没同步），56 条有订单。

三处刻意的保守选择：

- **花费和点击取 CRM 与 Sheet 的较大值**。花费只增不减，取大的更接近真实；更要紧的是分母大 → 盈亏比小 → **只会少抬价不会多抬价**。CRM 同步有延迟、Sheet 回流又时不时读成 0（8/06 早上 11 条广告同时读 0，包括 #46），谁也不能单独信。
- **佣金一律用 CRM 系列级值**，这正是 Hermes 自己拿不到、要向 CRM 借的东西。
- **`inflated` 档传 `exclusive: false`**，复用现成的加码闸门把它挡在加预算/抬价之外，但保留减预算那条保护——这种系列的佣金掺了非广告流量（订单比点击还多）不能用来抬价，可它的花费是真的。实测 Hermes 自己 431 条**一条 inflated 都没有**（投的都是自己的品牌词，归因干净），这个污染只存在于团队其他人的广告里（`viagogo` 那条是 0 点击对 1712 单）。

调参流水里记下本次用的是哪套数（`数据源 CRM系列级` / `商家+联盟合并`）——同一条广告两套口径能算出差一倍的盈亏比，事后复盘必须能分辨。

#### B 新广告基准 CPC 回到 $0.35

⚠️ **这一条推翻了 HM-A08 的结论，也推翻了我在 8/06 上午给 07 的提醒**。当时依据 HM-A08「$0.8 上限那 72 条平均只跑到 10.7 个点击、$2 上限那 212 条平均 38.6 点击」判断「压低上限会丢量」，建议 07 不要降。**那份分析只看了点击量没看盈利结果，方向判反了**。用 CRM 真数按实付均价分档重算：

| 实付均价 | 系列数 | 出单率 | 花费 | 净佣金 | 整体盈亏比 |
|---|---|---|---|---|---|
| **< $0.35** | 133 | 28.6% | $1,640 | $6,259 | **3.82** |
| **≥ $0.35** | 124 | 14.5% | $905 | $335 | **0.37** |

贵流量那批**净亏 $570**，出单率只有便宜那批的一半。量确实会丢，但丢的是不赚钱的量。

**根因不在转化率，在样本**：≥$0.35 那 124 条的 quality 几乎全是 `thin_sample`，点击数普遍只有 11–25 个。$2 日预算按 $1 上限只够买 2 个点击，按 $0.35 能买 5.7 个——**贵的那批不是输在转化差，是输在还没攒够样本就被止损关掉了**。07 定的「新商家统一标准、交给止损阀判断」能成立，前提正是这个预算得先买得到足够的点击。

`default_cpc_usd` $1 → $0.35，`hard_cpc_usd` 保持 $1。两者分工：**$0.35 是新广告攒样本的价，$1 是决策池抬价的天花板**。在投的 72 条按 07 定的保持不动。

有 5 条例外值得记下：`budgetvetcare` 实付 $0.401 却 R=4.73、`hidock` $0.4379/R=2.79、`jymsupplement` $0.6181/R=2.28、`mapleparking` $0.4863/R=2.19、`calecim` $0.7454/R=2.02——**它们贵但赚钱**。这类不该靠放宽新广告基准去照顾，而应该走出单后的 `cpc_up` 抬上去，否则等于为了 5 条把 124 条的口子一起开了。

#### C 抬价样本门槛按 AOV 分档

原来的门槛只按盈利比分两档（≥2 倍目标比要 5 单，否则 2 单）。可真正决定「几单才算数」的是**每单佣金有多大**：单笔越大，抽样误差越大。团队 2849 条 trusted 系列实测，只看订单 1–2 单那批能算出的最高盈亏比：

| AOV | 1–2 单时最高 R | 1–2 单离散系数 | 整体达标率(R≥2) |
|---|---|---|---|
| < $2 | **0.78** | 1.21 | 13.3% |
| $2–5 | 2.14 | 0.73 | 24.2% |
| $5–10 | 2.82 | 0.74 | 41.1% |
| **≥ $10** | **25.19** | **1.47** | 53.6% |

AOV <$2 的广告**在 1–2 单时最高也只能算出 0.78，连抬价线都够不到**，门槛给多高都没意义；AOV ≥$10 的**1–2 单就能算出 R=25**，离散系数也是四档最差——**一笔运气单就能把 2 倍说成 25 倍，而这类广告恰恰因为盈利比亮眼最容易被抬价规则选中**。

门槛：AOV ≥$10 要 6 单、$5–10 要 4 单、$2–5 要 3 单、<$2 沿用 `tune_min_orders_up`。与原来那条「盈利比 ≥2 倍目标比要 5 单」**取严不取代**：AOV 分档管「单笔大不大」，盈利比翻倍管「算出来的空间离不离谱」，两件事。抬价理由里带上 AOV 与实际门槛，事后能一眼看出为什么放行或为什么没放行。

#### 干跑结果：本轮 0 条动作

| # | 商家 | 单 | 每单佣金 | AOV门槛 | 盈亏比 | 结果 |
|---|---|---|---|---|---|---|
| 46 | mmlafleur | 125 | $43.83 | 6 ✓ | 10.24 | 算出可出 $0.82，比现价 $0.75 只高 9.3%，卡在「涨幅不到 10% 不动」 |
| 331 | budgetvetcare | 9 | $7.79 | 4 ✓ | 4.60 | 算出只该出 $0.92，低于现上限 $2 |
| 127 | lagos | 3 | $44.58 | **6 ✗** | 3.26 | **被 AOV 门槛挡住** |
| 395 | oxiline | 1 | $10.66 | **6 ✗** | 2.08 | **被 AOV 门槛挡住** |
| 303 | calecim | 1 | $19.54 | **6 ✗** | 2.02 | **被 AOV 门槛挡住** |
| 278 | lookoptic | 11 | $4.20 | 3 ✓ | 1.47 | 够不到抬价线 |

被挡住的三条每单佣金都在 $10 以上却只有一到三单，正是 C 要防的组合。#46 卡在 10% 涨幅门槛下也合理——昨天刚抬到 $0.75，让它先跑满一天。

### 12.12 Google 弃用 v21：整个系统即将同时失去发布与止损能力（2026-08-06，HM-D70）

这一条是从 07 的一个小问题里岔出来的。07 问「359-711-5243、833-741-4380 属于 SUSPENDED 吗」，答案是不属于（两条独立路径实查都是 ENABLED）。但为了解释这两个账号名下的广告为什么花费为 0，去实查系列状态时，一次查询报了 `UNSUPPORTED_VERSION`，而紧挨着的另一次同样的查询却成功了。

翻生产日志里那批一直被归类成「Google Ads 报错（未归类）」的失败，完整报文是：

> `Version v21 is deprecated. Requests to this version will be blocked.`

**Google 已经在分批拒 v21 的请求了。**8/06 一个上午就有 14 条广告踩到，日志里躺着 9 条这个错误，受影响的动作包括换后缀（#426、#441、#452、#478）、出单调增（#457）、花费真值直查（#303）、发布（#474）。

#### 为什么这条比它看起来严重得多

不是「少发几条广告」的问题。**止损暂停和发布走的是同一个 `mutate` 通道。**全量阻断之后，Hermes 会同时失去两种能力：

- 发不出广告——只是少赚；
- **关不掉广告——一边烧钱，一边把广告记成已暂停**。

后者才是要命的形态：系统会认为止损已经生效，07 在看板上看到的也是「已暂停」，而 Google 那边广告还在跑。12.9（HM-D67）刚刚教过一次同样的教训——库里记的值和 Google 上的真值脱钩，代价是 #46 三次抬价全部落空、三个月纹丝不动。那次是读写了被智能出价忽略的字段，这次是整条通道要断，性质更重。

而这批错误**没有触发任何告警**，是手工翻日志才发现的。

#### 升到哪个版本：先验再换

`v22`–`v25` 实测都可用，`v26` 返回 404（尚不存在），所以最新是 v25。选版本前把 Hermes 用到的每一类操作在 v22 与 v25 上各验一遍——查询直接跑，写操作全部走 `validateOnly` 不落地：

| 验证项 | v22 | v25 |
|---|---|---|
| 查系列指标 + `primary_status` + 出价策略 | ✅ | ✅ |
| 查 `policy_summary`（拒登检测） | ✅ | ✅ |
| 查关键词 `cpc_bid_micros` | ✅ | ✅ |
| 列子账号（53 个） | ✅ | ✅ |
| 查 `search_rank_lost_impression_share`（该抢没抢的依据） | ✅ | ✅ |
| 改预算 / 改 CPC 上限 / 改后缀 | ✅ | ✅ |
| **暂停广告（止损命脉）** | ✅ | ✅ |
| **完整发布链路（63 个 operation）** | ✅ | ✅ |

**两版结果完全一致**，所以没有理由选跳跃小的那个——v22 大概率也在弃用队列里，几个月后还得再来一遍。取 v25，改动是一行常量。升级后实调验证：查账号、列子账号、查在投系列全部正常，一轮真实 monitor 检查 71 条，`UNSUPPORTED_VERSION` 一次未再出现。

#### 顺带补上的监控缺口

07 拍板给这类错误单独加飞书告警。要区分的是两类错误性质完全不同：

- 「这条广告没成」——写 `run_events` 就够，07 在看板上能看见；
- 「整个 Hermes 即将失去对 Google 的操作能力」——必须越过 `run_events` 直达 07。

新增 `alertFatal(kind, text)`，三个触发点：**API 版本被弃用**、**令牌池全部耗尽**、**CID 池归零**。6 小时静默沿用配额告警的做法（HM-D46）——这类事故一条提醒就够，每 5 分钟刷一遍只会让人把告警关掉。告警自身的任何失败都不许影响业务（整体 try/catch + `notifyFeishu().catch()`）。

告警文案写的是「影响什么 + 怎么处置」，不是错误码。CID 归零那条还会分辨是「被在投广告正常占满、等止损释放」还是「有任务卡在 `cid_reserved` 白占」——后者就是 8/06 早上那场事故的形态。

拿废弃的 v20 做端到端触发验证，第一次就暴露了文案里的一个缺陷：**Google 把有用的话埋在 `details` 里，外层 `message` 永远是那句没信息量的 `Request contains an invalid argument.`**，照原样报等于什么都没说。改成从报文里提取真实消息后重新触发，落库内容是 `Version v20 is deprecated. Requests to this version will be blocked.`。测试产生的静默记录已清除，免得真出事时被 6 小时静默挡住。

#### 同时纠正两个我前面说过头的判断

- **「CID 是唯一硬瓶颈、产能已经见顶」说重了**。pump 输出里的 `budgetExceeded: 93` 不是钱的闸门，是 HM-D43 的**每轮时间预算**——一轮只给几分钟，发完 7 条就收工，剩下的下一轮接着发，本就是设计如此。近 7 天真实发布量 24/26/25/39/37/45/46 条，产能正常；8/06 只有 10 条纯粹是早上那 8 小时卡死。真正该盯的是**流失趋势**：一周退役 7 个账号（1 CANCELED + 6 SUSPENDED），池子共 80 个、可用 71 个。
- **「38 条 monitoring 零花费」不是故障**。全部是 8/05–8/06 发的，没有一条超过 24 小时；实查 #463 在 Google 侧是 ENABLED / SERVING、广告 REVIEWED + APPROVED，主状态 `LEARNING`、原因 `BIDDING_STRATEGY_LEARNING`，就是智能出价还在学习期没起量。

另外把 `stale_read` 这个报错的两种情形分辨清楚了：**累计花费掉到 $0.00 基本等于账号被封**（#432 报 $0.00 后 1 小时确认 SUSPENDED，#396 报后第二天中午确认），而**小幅低于已知值只是回流延迟**（#46 是 $2.81 低于 $3.38）。两者现在混在同一条报错里，前者其实是可用的封号前兆信号——待做（HM-A 序列，尚未实现）。

### 12.13 禁投品类闸：账号是被商家品类打死的，事后暂停救不回来（2026-08-06，HM-D71）

07 核实了那七个被封账号，并指出其中 `761-316-6644` 的封停理由写得很明确——**违反「色情内容」政策**，随后给出 `ADFLY-广告禁投品类.pdf`，要求 PDF 里禁投的一律不能投。

#### 追到具体是哪个商家

`761-316-6644` 名下唯一的品类拒登来自 **`acmejoy.com`**（AcmeJoy，成人情趣用品）：8/03 05:15 发布，**58 分钟后**被 Google 判 `SEXUALLY_EXPLICIT_CONTENT` / `NON_FAMILY_SAFE` / `PORN`，类型 `PROHIBITED`。Hermes 当场自动入库并暂停了这条广告——**账号还是在次日 12:12 被封**。同一形态还有一例：`shopduer.com` 的 PROHIBITED 拒登（7/29）带走了 `795-035-8060`（8/05 封）。库里 12 条 PROHIBITED 拒登，对应两个已封账号。

**唯一能得出的结论：对 PROHIBITED 品类，事后暂停救不了账号，只能事前不投。** 一周退役 7 个账号，其中 `912-306-6476` 是跑过 7 条广告的成熟号；补一个新号的成本远高于放弃一个可疑商家。

#### 原来为什么拦不住——三个各自独立的缺口

| 本该拦住它的东西 | 为什么没拦住 |
|---|---|
| CRM 情报库 `crm_policy_flags`（910 条：87 prohibited + 823 restricted） | **只覆盖大麻/酒精/医疗保健，成人、武器、博彩、仿冒、烟草五类一条都没有**——恰好就是出事的那几类。acmejoy / wolftacticalusa / museumreplicas / beelivery 四个漏网的一个都查不到 |
| 联盟品类字段 `offers.category` | 填充率 100%，但 **65% 的值是 `Others>Others` / `Other`**，而上述高危商家的品类恰好全是 `Others>Others`。这个字段只能用来**平反**（Felina Intimates 标 `Clothing & Accessories`，据此排除误伤），不能用来定罪 |
| 关键词 | acmejoy、beelivery、wolftacticalusa 的**域名和商家名里一个敏感词都没有**。真正能识别 acmejoy 的是落地页标题那句「Shop Men & Women Adult Sex Toys」，正文里 `masturbator` 出现 123 次、`dildo` 120 次。beelivery 是英国送货服务顺带送烟酒，名字更干净，它吃的是 `TOBACCO` 拒登 |

#### 分三层，越靠前越便宜

| 层 | 判据 | 成本 | 拦的是哪一类 |
|---|---|---|---|
| **L1 词表** | 域名 + 商家名 | 零 | 名字就露痕迹的（`lavishsextoys`、`gentingcasino`） |
| **L2 落地页** | 首页标题 / 描述 / 正文词频 | 一次 HTTP | 名字干净但卖违规品的（acmejoy 这种） |
| **L3 拒登回灌** | Google 已判的品类违规 | 零 | 永久拉黑，不给第二次机会 |

L1 判 `blocked` 就不抓页面；只有 soft 词怀疑才往下走 L2。接入点选在 `create-runs.js` 的 SOP 深查**之前**、`build-queues.js` 建队列时用只读批量版，两处都不额外花钱。

#### 词表的边界全是踩出来的

第一版给所有词都加左边界，结果 `lavishsextoys.com` 直接漏了——`sextoys` 前面是 `lavish` 的 `h`，左边界把匹配挡掉。而 `gentingcasino.com` 当时能命中纯粹因为商家名里恰好有空格（"Genting Casino"）。**整张表在靠运气：商家名一旦写成连写就全部失效。** 改成两路并行：

- **`hardFlat`** —— 匹配「去掉所有分隔符」后的串，专抓长独特词，不加边界（`sextoy`、`gunbelt`、`concealedcarry`）；
- **`hardWord`** —— 匹配原文并要求两侧都不是字母或数字，给短词用（`cbd`、`thc`、`bong`、`ammo`）。

短词必须走后一路，因为 `cbd` 无边界会中 `abcbdesign`、`thc` 会中 `lengthchart`、`bong` 会中 `bongo`（邦戈鼓）、`weed` 会中 `tweed`（花呢面料）。同理 `sex` 不能单独用（essex/sussex/middlesex）、`anal` 不能（analytics/analog/canal）、`bet` 不能（betterhelp/bethesda）、`gun` 不能（gundam，但 `gunbelt`/`handgun` 可以）、`loan` 不能（sloane）、`holster` 必须排掉 `upholster`（否则家具软装全军覆没）。

#### 上线后跑了一轮真数，抓出四个自己的错误

第一轮扫全库把 358 个商家判进 verdict 表，看证据时发现的问题比预期多：

| 问题 | 表现 | 修法 |
|---|---|---|
| **正文整段 flatten 会跨词拼出假词** | `segurospromo.com.br`（巴西旅游险）葡语正文里的「por Nome」被拼成 `porn`；`axelhotels.com` 拼出 `bingo`、`sundaysinsurance.com` 拼出 `vape`。原以为「≥8 次才定性」的阈值兜得住，实测 `ktc` / `fwd` 都是被词频送进 blocked 的，**兜不住** | 正文改用只吃标点、保留空格的归一（连字符照样合并，`nylon-gun-belt` → `nylongunbelt`，wolftactical 的证据还在），跨词就拼不起来 |
| **`xxx` 是打码占位符** | `ktc.co.th`（泰国信用卡）把卡号打码成 `xxxx`，命中 8 次被判成人内容 | `xxx` 退出词表——全网通用写法，只会持续误伤 |
| **`insurance` 不在 PDF 的高风险清单里** | PDF 写的是「贷款广告、信用卡推广、加密货币交易」，保险不在其中。留着它一口气挂了 15 个保险商家（AXA / Allianz / 安盛旅游险），全是正规大牌 | `insurance` 退出金融可疑词 |
| **`armour` 与 `axe` 太脏** | Under Armour（运动服）AT / HK 两站、`axess-industries`（工业品）、`stand1armory`、`universalarmor` 全被误杀 | 退出词表，只留 `bodyarmor` / `bodyarmour` 在硬词里 |

反过来还有一个**漏网**方向的漏洞，性质更值得记：**落地页只看硬词，会把词表的 soft 怀疑无条件洗清。** `buckknives.com` 就是这么漏的——域名里 `knives` 是可疑词（本该转人工），但落地页扫不到任何硬词于是判 clean，直接洗成 cleared；可它的标题就写着「Buck® Knives OFFICIAL SITE - Quality Knives Since 1902」，卖的是猎刀，而 PDF 里 Google 禁投第一条就是「武器（枪械、刀具、炸药等）」。补法是**定向**的：只对词表已经怀疑过的那一类数它的 soft 词频，够密就维持待复核——不做全类扫描，否则任何服装站正文出现十次 `sexy` 都会被挂起。这一改把 35 个商家挂回待复核，其中 `buckknives`(46 次) / `hideawayknife`(55) / `kyokuknives`(82) / `losangeleslingerie`(127) / `thecryptomerch`(46) 都是商家自己首页确认了品类。

同时按 PDF 补进刀具硬词（`huntingknif` / `combatknif` / `tacticalknif` / `bowieknif` / `throwingknif` / `butterflyknif` / `survivalknif`）——厨具刀刻意不写进硬词。

#### 存量处置结果

| 判定 | 数量 | 处置 |
|---|---|---|
| **blocked** | 188 | 永不排任务。毒品用品 64 / 成人 38 / 烟草 26 / 博彩 21 / 金融 20 / 武器 10 / 保健 9 |
| **review** | 91 | 挂起等 07 裁定（07 拍板「判不了的保守拦下」）。金融 21 / 武器 12 / 成人 9 / 保健 7 |
| **cleared** | 79 | 放行，含 114 → 79 的两轮洗清（词表误伤 10 + 落地页复核 102，再挂回 35） |

**在投任务实查为 0 条命中禁投或待复核**，说明现在跑着的广告是干净的。`#427 Wolf Tactical` 已按 07 拍板暂停并永久拉黑（卖 concealed carry 枪带与 EDC 枪套，属 Google 武器政策的枪械配件）。

#### 一个反直觉的实现细节

`domain_blacklist` 表有 4316 条，但合规判定**一条都没往里写**，两表交集为 0。这是对的：那张表是**团队共享表格的镜像**，`sync-team-sheet.js` 每次同步先 `DELETE FROM domain_blacklist` 再整表重灌——写进去下次同步就被抹掉。合规判定的唯一落点是 `merchant_policy_verdicts`，`build-queues` 与 `create-runs` 都直接读它。

#### 判不了的怎么办

`review` 不是死胡同，新增两个接口给 07 裁：`GET /api/policy-reviews` 列队列（当前 91 条）、`POST /api/policy-reviews/decide` 拍板 blocked 或 cleared，人工裁定记 `layer=manual`，此后不再被自动逻辑翻案。已按 07 确认预置几笔：`acmejoy.com` / `wolftacticalusa.com` / `beelivery.com` 判 blocked，`felina.com`（正规女装内衣）/ `corkcicle.com`（保温杯，落地页里的 `rifle` 来自 Rifle Paper Co. 联名）判 cleared。

队列里还留着几类明显可以放但我不敢自己放的：厨刀（Kyoku / Kasumi / Nakano，Google 允许厨房刀具）、成人纸尿裤（`friendsdiaper.in`，是医疗护理不是成人内容）、`vaporfresh.com`（运动装备清洁剂，不是电子烟）、`medievalbrick.com`（中世纪主题积木，不是仿冒）。按 07 定的「判不了就挂起」它们现在是安全的，但确实是在少赚钱——**要不要批量放行这一类，等 07 拍**。

**07 于 8/06 傍晚拍板放行这四类**，执行时没有直接改判，而是逐个抓落地页要「正证据」：厨刀必须出现 kitchen/chef/santoku/gyuto 一类词 ≥3 次**且** tactical/hunting/EDC 一次都不许有。这个反向要求当场挡住一个：`kasumi-knives.com` 正证据只 5 次、反证据 10 次（folding、hunting、edc 都在），它不是纯厨刀店，维持挂起；其余 6 个正证据 10~85 次、反证据 0，写成 `layer=manual` 放行。如果只按「07 说厨刀可以放」批量改判，Kasumi 就跟着放出去了。

#### 抓不到页面的一律放行，这点不能反着做

正规站大量用 Cloudflare / Akamai 拦爬虫，在投的 `shoesensation.com` 直接返回 403。抓不到页面时若「保守拦下」，会大面积误杀正常商家、产能直接垮。**走到 L2 说明词表已经判它干净**——没有任何可疑信号，只是我们看不见页面，这不构成怀疑理由。真正可疑的（词表 review）压根走不到这一步，在 L1 就被拦了。这类商家的风险由词表 + CRM 情报库 + 拒登回灌三层兜。

#### L3：Google 判过的，当场永久拉黑并告警

`sync-remote-state.js` 原来对所有拒登一视同仁（记录 + 暂停）。现在区分两类，这个区分是必须的：库里 12 条 PROHIBITED 拒登里，`calvinklein.com` / `victoriassecret.cz` / `brooksbrothers.eu` 这些正规大牌吃的都是 `DESTINATION_MISMATCH`——联盟后缀在手机端或某地区跳去了别的域名，**是我们自己的后缀问题**，把商家拉黑纯属误杀。而 acmejoy 的 `SEXUALLY_EXPLICIT_CONTENT`、beelivery 的 `TOBACCO` 是商家卖什么的问题，换后缀、换文案都没用。

只有品类级政策码（成人 5 个 / 烟酒 2 个 / 博彩 / 武器 2 个 / 仿冒 / 药品 4 个 / 金融 2 个）会触发永久拉黑，同时推一条飞书告警，文案里直接写明**这类拒登是封号前兆**并点出对应 CID 需要盯、必要时主动申诉——因为 acmejoy 拒登次日 `761-316-6644` 就被封、shopduer 拒登后 `795-035-8060` 被封，这条时间关系是有实证的。

### 12.14 月度推荐表怎么真正跑起来：机制早就在，卡的是商家关系（2026-08-06，HM-D72）

07 选定 `LinkHaitao Recommended Merchant List August.xlsx`（199 个商家）要求「按顺序跑」，并要按团队要求在表上加「申请 / 拒付」两列、没上的备注淘汰原因，`pending` 的另出一张商家申请表。

#### 先确认口径，别自己假设

推荐表给了 `mcid` 和 `MID` 两个标识，`联盟` 列写的是上游网络（Impact 50 / Webgains 39 / PJ 25 / Rakuten 25…14 个网络）。乍看像是要按上游网络分别匹配，实测把 199 个 MID 打到 CRM 全库：**LH 平台命中 169 个（85%），PM 33、LB 22、RW 5** —— 后面几个是纯数字 ID 撞号。也就是说 `MID` 就是 **LinkHaitao 自己的商家 ID**，`联盟` 列只是告诉你这个商家的上游来源。跨平台只能靠域名精确匹配，不能用 `LIKE`：用 `LIKE '%art.com%'` 这种写法，199 个域名在 LB 匹出 580 个「商家」，全是子串误命中。

「申请」这一列的三个值 CRM 只能给出一个。`platform-api.ts` 里各平台同步全都带 `assumeAllJoined: true` / `relationship=Joined`，**只拉已加入的商家**，所以库里有记录＝`join`，没记录＝没加入。`pending`（申请中）和 `rejected`（联盟拒了）CRM 结构上拿不到，只能人工回填——这点写进了交付表的口径说明页，避免下个月有人以为空白就是没申请。

#### wj07 的实测结果

wj07 名下两个 LH 账号：`wenjun3`（5133 个商家，命中推荐表 77 个）、`novanest`（新号只有 25 个商家，命中 0）。淘汰原因取三处并集：团队共享黑名单 29 个（`domain_blacklist`，含录入人）、CRM 违规库 `violation_status='violated'` 4 个、禁投品类闸 7 个（3 烟草 + 3 金融 + 1 补剂待确认）。

一个容易出事的交叉情况：**77 个已加入里有 17 个属于淘汰品类**（Merrell、Saucony 两站、alibaba、American Eagle MX 在黑名单，`mycapnos.com` 是电子烟，`lolivier.fr` 是保险）。这些行「申请」列如实写 `join`——关系确实已建立——但备注前面加了 `⛔ 已加入但不投`。如果只按申请状态发下去，看表的人会直接把它们投出去。

拒付列本人本月为 0（wj07 八月 LH 只有 3 笔 pending）。全组口径有三笔：CRAVOT $20.10、Ethika $7.35、PETstock AU $2.12，写进备注，用来判断商家质量。

#### 「按顺序跑」这件事 Hermes 早就做了，卡在别处

`monthly_recommendations` 表已有 2029 行，这张 8 月 LH 表 199 条 rank 1-199 **早在库里**，`build-queues.js` 的三档优先级（白名单 40000 > 推荐表 20000-rank > 联盟平台）也在跑：队列里 `queued` 优先级最高的清一色是推荐表档。所以要做的不是加机制。

真正的瓶颈是商家关系：199 个里**只有 95 个在 Hermes 的商家池里**（Hermes 的 LH/PM 账号加入过），63 个已建过广告，**104 个池子里根本没有**——没加入就没有 tracking 链接，排队也没用。这跟前面那张申请表是同一件事的两面：**要让推荐表按顺序跑完，先得把申请表交上去、等联盟审。**

还有一个待拍的点：推荐表档是 6 个联盟混排的（MUI 302 / PM 261 / CG 208 / LH 199 / RW 190 / EV 26，各自 rank 从 1 起），LH 的 #1 和 MUI 的 #1 优先级完全相同。如果 07 要的是「这张 LH 表优先跑完」，需要给 LH 一个档内加成。→ **07 已拍板要 LH 压过其他联盟，实现见 12.15**。

#### 金融口径收紧时漏了非英语，差点白改

07 同一天拍板「金融连保险一并禁」，实现上除了把 `insurance` 加回可疑词，还给金融这一类加了 `softIsHard`：**只有金融是可疑即禁**，其他品类保留 soft→待复核的分级（`sexy` 之于服装、`tactical` 之于户外，误伤面太大）。`clearedBy` 平反照旧保留，联盟品类明确是 Tech/Home 的手机壳不会因为 crypto 联名被拦。存量 79 个判定升级为硬拦。

改完自查在投任务时抓到只写英语的问题：`unipol.it`（Unipol Assicurazioni，意大利保险）是 `approved` 待上线状态，词表一个都没命中——因为意语「保险」是 `assicurazioni`。我们商家池里欧洲站占三分之一，只拦英语等于没拦。补齐意/荷/北欧/波/土/捷/匈七个语种后，在线池里金融类硬拔到 182 个，并当场抓到两条要处理的在投任务：`freely.me`（已投 $4.63，Google 侧暂停后改本地状态——顺序不能反，否则库里停了 Google 还在烧钱）、`unipol.it`（还没拿 CID，直接取消，省下一个 CID）。两个域名都写进 `merchant_policy_verdicts`，下一轮不会再排进来。

### 12.15 让推荐表真的按顺序跑：优先级早就对了，是失败的行把商家永久挡在门外（2026-08-06，HM-D73/HM-D74）

07 拍两件事：LH 这张表要压过其他联盟先跑完；其余五个账号用各自平台的 8 月推荐表，同样出跟进表和申请表。

#### 五个平台的表长得不一样，口径得一张张对

`PM Recommended Advertiser List for August.xlsx`（261）和 `RW20260728Weekly Brand Recommendations.xlsx`（190）各是一张独立表，而 **`CZ 8月推荐商家列表.xlsx` 一张表里塞了三个平台**——首列 BU 分别是 `chuizhan_ui`（MUI 302）、`chuizhan_cg`（CG 208）、`chuizhan_ev`（EV 26），必须按 BU 拆开再各自匹配，否则会把 MUI 的商家算到 CG 账号头上。这五个平台**没有 LH 那种能直接对 CRM `merchant_id` 的 MID**，只能按标准化域名精确匹配，`LIKE` 的子串误命中问题（12.14 里 LB 匹出 580 个）在这里同样成立。

#### LH 档内加成：5000 这个数是挑过的，不是随手填的

新增设置 `rec_lead_network`（`none`/LH/PM/CG/MUI/RW/EV，默认 LH），`build-queues.js` 读到它就给该联盟的推荐表行加 `REC_LEAD_BONUS = 5000`。取 5000 的理由：推荐表档的分数是 `20000 − rank`、rank 封顶 9000，加成后落在 **24001–25000**，仍低于白名单档最低的 30001——**三档区间照旧不重叠**，不会出现「LH 的第 199 名压过白名单」这种越档。读设置失败或设成 `none` 就回到混排，绝不因为读不到设置而中断建队列。

干跑确认整段分离：LH 24808–24967，其他五个联盟全部落在 19703–19995。既在白名单又在推荐表里的（如 Hornby UK，LH rank 35）仍走白名单档 32297，这是既有 SOP，没动。

#### 真正的发现：干跑说 130 个命中，实际建队列只有 9 个

上线后正式重建队列，`monthlyRecHits` 从干跑的 **130 掉到 9**，队首清一色 MUI、**一条 LH 都没有**。差别在 `dryRun` 不写库：真正写库时先 `DELETE FROM test_queue WHERE status='queued'`，再 `INSERT ... ON CONFLICT(normalized_domain, country_code) DO NOTHING`——**只清 `queued`，但唯一键管着全表**。于是任何非 `queued` 状态的旧行都会让这个商家在后续每一次建队列里被静默跳过。

`sop_dropped` 被挡住是对的（SOP 判它不合格）。问题在另外两个状态：`suffix_failed`（后缀池空 / 取后缀报错）和 `copy_failed`（AI 文案生成失败）——**这两个是系统自己的临时故障，不是商家不合格**，却和永久淘汰享受同一种待遇。全库 **147 条**卡在这两个状态上（`suffix_failed` 64 + `copy_failed` 83，最早的到 7/20），其中 **11 个是 07 点名要跑的 LH 推荐表商家**：JB Tools（rank 33）、PETstock AU（63）、MGM Resorts（154）、Nutricost（192）……全都被丢在门外，日志里一句提示都没有。

修法（HM-D74）：建队列前先跑 `releaseTransientFailures()`，把冷却期（12 小时）之外的这两类行删掉让它们重新入队；为了不变成无限重试烧 SerpApi，用新表 `queue_retry_ledger`（域名+国家为主键）记次数，**最多放行 3 次**，之后不再管，并把放行结果写 `sync_logs`（`queue_retry_release`）留痕。冷却 12 小时是因为后缀池空这类故障通常几小时内会被补货任务修好，太快重试等于原地失败。

实测：一次重建放行 147 条、重试用尽 0 条，`monthlyRecHits` **9 → 53**，队首变成 JB Tools US（P=24967）、PETstock AU、Visión MX、DeepDiscount、MGM Resorts、Halo Sleep、SHOPVIDA、Nutricost——**8 条 LH 连成一段压在所有其他联盟前面**。取候选那条 SQL（`ORDER BY CASE WHEN priority >= 10000 THEN priority ELSE 0 END DESC, (country_code='US') DESC, priority DESC`）对 ≥10000 的档位直接按优先级排，所以这个顺序会原样传到建任务。

生效时点要说清楚：候补名单当前 61 条、低水位 50，手动催补货返回 `skipped: above_watermark`，**LH 要等候补掉到 50 以下的那一轮才真正开始建**，不是立刻。

#### 顺带把 12.14 的一个数说准了

12.14 写「199 个里 104 个池子里根本没有」，那是只拿域名比 `offers` 得到的。这次改成 `merchant_id OR domain` 并把已建过广告的先扣掉，逐个归因得到完整分布：

| 卡在哪 | 个数 | 明细 |
|---|---|---|
| 已建过广告 | 63 | paused 38 / approved 12 / monitoring 7 / cancelled 3 / failed 2 / rejected 1 |
| 商家池里没有（未加入联盟） | 49 | 就是申请表要解决的那批 |
| 在池里但被闸拦 | 57 | 团队黑名单 23、**offer 全部下线 19**、拒付率过高 4、亏损品类 4、无可投国家 3、禁投品类 2、禁止品牌词 1、限制投放·成人 1 |
| 卡在队列失败态 | 30 | `sop_dropped` 19（合理）+ `suffix_failed` 6 + `copy_failed` 5（**本次已修**） |

真正只差「加入联盟」的是 **49 个**而不是 104 个；另有 **19 个已加入但 offer 全下线**（库里有商家、没有一条在线 offer），这类申请表也解决不了，得让联盟那边重新上架。拒付率那 4 个数字很值得看一眼：MSC Cruises 57.81%（91 笔 / $6254 佣金基数）、Secret Flight Club 65.08%、AG Jeans 34.06%、Drivalia 33.53%——**推荐表推的商家不等于能赚钱的商家**，闸门拦下来是对的。

### 12.16 禁投品类判定回写 CRM：顺手堵掉一个会推翻 07 裁定的闭环（2026-08-07，HM-D75）

07 的要求是「我们查过的联盟品类，要回写到 CRM 的主营业务栏」。起因是前一天七个账号被封、其中一个死于成人内容，而 CRM `我的商家` 里那些商家的主营业务写着 `Others>Others`，谁看都不知道碰不得。

#### 先否掉一个想当然的做法

第一反应是「把 Hermes 的联盟品类同步给 CRM」，但数一遍就知道不行：Hermes `offers` 43,432 条里 **23,000 条同样是 `Others` 打头**（53%），CRM 那边 148 万行里 71% 是 `Others`/`Others>Others`/`Other`。两边是同一批联盟 API 的同一套分类，**互抄等于没做**。

真正新增的知识只有一处——三层禁投闸（HM-D71）抓落地页、数词频、回灌 Google 拒登得出的品类结论。374 条判定里，**276 条正好落在「联盟只给 Others、我们查出了真品类」** 的交集上：金融 88、CBD 58、成人 45、烟草 25、武器 25、博彩 18、补剂 16、仿冒 1。这 276 个对应 CRM **6,198 行、29 个用户**，且**没有一行被人工改过**，不抢人工修正。

#### 只写主营业务是「看得见」，拦不住

CRM 本来就有 `policy_status`/`policy_category_code`，而且领取接口对 `prohibited` 是**硬拦**。所以推过去的是两层：主营业务（只补空、不覆盖真分类和人工修正，写了打 `category_manual=1` 防被平台同步冲回）＋政策标记（`blocked → prohibited`、`review → restricted`）。

定级刻意用**我们的判定**而不是 CRM `ad_policy_categories.restriction_level`：后者把 adult / financial / gambling 都只算 `restricted`（领取仅弹警告），而被封的账号里就有一个踩了成人内容。品类代码映射到 CRM 已有的 8 个（`finance→financial`、`healthclaim→healthcare`），`counterfeit` CRM 表里没有，**不推 code**——凭空加一行品类配置会改到所有用户的领取规则。

金融那 88 个按 07 要求**按命中的原词细分**成 Insurance / Crypto / Finance（证据串里留了原词：allianz 命中 `Insurance`、100cashloans 命中 `Loans`、cryptocoindisplay 命中 `crypto`），分不出来才落回父类。证据串里也有我自己写的中文说明，所以正则同时认「保险 / 加密货币」这类中文词。

#### 差点自己给自己上锁

CRM 的 `GET /api/hermes/merchant-intelligence` 会把 `restricted/prohibited` 商家导回来变成 `crm_policy_flags`，而 `sop-screen.js` 对这张表里的域名是**无条件淘汰**的（`if (pol) return { pass:false, reason:'标签违规' }`，没有任何例外）。

于是链条会闭合成：我们把判定推给 CRM → CRM 又导回 Hermes → **07 以后通过 `POST /api/policy-reviews/decide` 手动放行的商家，会被我们自己种下的标签重新拦住**，表现为「明明放行了却就是不投」，而且日志里看不出是被哪一道闸拦的。

两头都堵：Hermes 侧 `policyFlag` 之前先查本地有没有 `verdict='cleared' AND layer='manual'`，07 亲手裁定的压过 CRM 标签；CRM 侧收到 `cleared + layer=manual` 时把 `policy_status` 解回 `clean`。这跟 HM-D71「合规判定不能写 `domain_blacklist`」、HM-D74「清空重灌的唯一键会变永久黑名单」是同一类坑的第三个方向——**任何跨系统回灌都要先问一句：它会不会把人的决定盖掉。**

#### 实测：一个自检点对上了，一个漏洞被抽查抓出来

第一轮只按 (平台, MID) 匹配：343 条判定命中 7,679 行、写 7,501 行，主营业务 **6,190** 条。这个数是自检点——事前按库里数预测 6,198，实际 6,190，**差的正好是 8 行本来就有真分类的**（Tech 6 + Fashion 2），守卫按设计跳过了它们。

但抽查 Acmejoy 时发现：这个 8/03 被 Google 判 `SEXUALLY_EXPLICIT`、直接导致账号封停的商家，除 MUI 之外还挂在 **BSH 平台另一个 MID** 上，那些行仍是 `Others>Others` + `clean`，**别人照样能领取去投**。判定按域名走、CRM 按平台+MID 存，两边口径差一层，只按 MID 写就必然漏。开域名传播后累计写 16,174 行，`prohibited` 从 1,257 行 / 187 商家涨到 **14,848 行 / 978 商家**；收益最大的反而是 Hermes 根本没接的 LB 平台（6,391 行 / 904 商家）。

**还剩一个已知缺口**：精确域名匹配按设计不跨 TLD，所以 `acmejoy.nl`/`.it`/`.fr` 至今仍是 `Others` + `clean`。按「域名首段品牌名」扩展能补上，但首段是 `shop`/`travel` 这类通用词时会误伤无关商家，不能无条件开——已列入 CRM 侧 D-221 的待 07 确认项。

## 十三、变更记录

### 13.1 编号约定与 2026-08-05 重编映射

07 于 2026-08-05 拍板（待确认项 #20），编号拆成两套，互不干扰：

| 前缀 | 含义 | 编号权威 |
|---|---|---|
| **`HM-D`** | **已落地生效的代码改动** | **Hermes 服务器代码**。本文档只记录，不自行分配新号 |
| **`HM-A`** | 分析、证据、待实现的设计决策 | 本文档独立编号 |

起因：服务器代码一直在自行编号且**已用到 HM-D63**，与本文档 8/04 起新增的记录在 D55～D61 七个号上完全重叠（服务器侧 D57 = 智能出价大改造，本文档原 D57 = 软删除修正）。**这不只是追溯不便——本文档因为缺了服务器侧 7/30-8/01 的九笔改动，12.5f 初稿把已在 7/31 撤除的 `cpc_up` 规则当成了现行规则。**

本次重编映射（本文档原编号 → 新编号，内容不变）：

| 原编号 | 新编号 | 内容 | 日期 |
|---|---|---|---|
| HM-D55 | **HM-A01** | yz04 vs wj07 七月对比，第十二章证据基线 | 2026-08-04 |
| HM-D56 | **HM-A02** | 「复投」口径纠正为跨联盟账号扩池 | 2026-08-04 |
| HM-D57 | **HM-A03** | `platform_connections` 软删除误算修正 | 2026-08-04 |
| HM-D58 | **HM-A04** | 判决矩阵（$6 / 50 点击 / 团队验证信号） | 2026-08-04 |
| HM-D59 | **HM-A05** | 选品门槛替换物：品牌词月搜索量 | 2026-08-04 |
| HM-D60 | **HM-A06** | 「$6 是总预算」口径重做验证 | 2026-08-05 |
| HM-D61 | **HM-A07** | CPC 限额按商家盈亏平衡值计算 | 2026-08-05 |

**纪律**：本文档若要新增 `HM-D` 号，必须先取服务器侧最大号再往后接——
`ssh hermes "grep -rho 'HM-D[0-9]\+' ~/hermes-ads/src | grep -o '[0-9]\+' | sort -n | tail -1"`

### 13.2 服务器侧已生效改动补记（HM-D55～D63，2026-07-30～08-01）

07 于 2026-08-05 拍板全部补齐。这九笔是**已在生产环境生效的代码改动**，此前本文档一笔未记，编号与内容以服务器代码为准。**其中 D55/D56/D57/D60 直接改变了决策逻辑，读第十二章的任何结论前都要先看这四笔。**

| 编号 | 日期 | 内容 | 与本文档结论的关系 |
|---|---|---|---|
| **HM-D55** | 07-30 | **自动调参判据从「流量信号」换成「盈利信号」**（采 kyads 口径）。原来只有点击/花费两个输入，加预算的唯一条件是「预算花满了」——实测 **113 次加预算里 101 次加在最终亏钱的广告上**，75 条零出单广告因此多烧 $539，「这条规则实际是亏损加速器」。现在每一条加减都以**盈利比 = EPC ÷ 实际均价 = 有效佣金 ÷ 花费**为轴：保护类减预算（盈利比 <1）→ ×0.7，地板 `default_budget_usd`；成长类加预算（点击≥20 且订单≥2 且盈利比≥1.2 且佣金归自己且预算撞线）→ ×1.25 | **「盈利比」与 12.5f 的「盈亏平衡 CPC」是同一个量**：盈利比 ≥1 ⟺ 实际 CPC ≤ 盈亏平衡 CPC。差别只在现规则调**预算**、12.5f 要求调**出价上限** |
| **HM-D56** | 07-30 | **复活双通路**（任一即可）：通路 A＝非拒付订单数达 `revive_min_orders` 且净 ROI 过 `revive_roi_min`；通路 B＝净 ROI 高于 `revive_roi_direct`(3，即佣金到花费 4 倍) 直通、不看单数。ROI 一律净口径 (佣金−花费)÷花费 | 修正了 HM-D32「复活即豁免考核」的老问题；也是 HM-D60 敢取消 CID 留位的前提 |
| **HM-D57** | 07-31 | **本轮最大的一次改造，含五件事**：① **出价改智能出价**（`TARGET_SPEND` 争取更多点击）+ **死上限 $2**（07 原话「智能出价，策略是争取更多次数，写一个死上限 2 刀」），起因 LAGOS #127 手动出价 $0.80、Google 要价 $1.84、质量分 10/10 却零展示；② **移除 `auto-tune.js` 全部 CPC 类规则**（`cpc_up`/`cpc_down`/`cpc_up_epc`），理由：智能出价下组层与关键词层的 `cpc_bid_micros` 被 Google 完全忽略，而那正是原规则在写的字段，「它们三天里一直在改一个不起作用的数」；且二价竞价下均价**永远**低于上限，`cpc_down` 依据的「均价只有出价 38% ⇒ 竞争弱」是错的推断（那两轮把出价砍到 $0.24、日点击从 33 掉到 8）；③ **净亏止损线按商家单笔佣金中位数分档**：`clamp(中位数 × stop_net_loss_scale, stop_net_loss_usd, stop_net_loss_cap_usd)`——「$4 这条线跟客单价完全无关：evryjewels 单笔 $0.25，$4 等于亏掉 16 单利润才停；LAGOS 单笔 $105.78，$4 是一单利润的 4%」；用中位数抗离群；④ **零流量判死**：智能出价下投满 N 整天累计花费仍 $0 → 当场关停（上限不够或品牌词没人搜，等天数闸是白等）；⑤ **「买不起就别测」**：均价高到花费闸买不够 `tune_min_clicks` 个点击 → 关停并给商家打「超出预算级别」标记入 `merchant_budget_skips`（实测 31/72 条在投累计花费 $0；能读到首页出价估算的 14 条里只有 4 条能在 $6 闸内买够 12 个点击，schoolyardsnacks 首页价 $11.40、**$6 买 0.5 个点击**）。标记记录判定当时的上限与花费闸，**抬高任一项自动解除**，不是永久拉黑 | **同时是 12.5f 的病因与药方**：①②是 12.5f 批评的对象（$2 成了全局默认、且再无任何加价路径）；③⑤ 却是 12.5f 建议的**同源先例**——「按商家属性分档而非一刀切」07 在 7/31 已做过一次，只是做在止损线上没做在出价上限上，`perOrderCommissionMedian()` 可直接复用；⑤ 已经实现了 07 选的「照投不拦、靠闸门兜」，只需把判定口径从 `tune_min_clicks` 扩到 50 个点击 |
| **HM-D58** | 07-31 前后 | 跟链失败计数与冷却（以前一次失败也不记，补货每 5 分钟无脑重来）；后缀生成时把链上抓到的**联盟自己的点击流水号**存进 `suffix_pool.click_token`，交易回来优先用它反查任务（改 HM-D52 的追踪反查口径） | 提升了佣金→系列的归因精度，但 CRM 侧仍只有 65/589,216 行有 `campaign_id`（见 12.7「佣金归因到商家级」） |
| **HM-D59** | 07-31 前后 | **通知降噪**：上线不再逐条推飞书（实测日均 27 条、7/28 一小时内多条）、巡检只推「实质性变化」（此前每小时雷打不动一条、7 天 130 条）、停广告改由 `pumpRuns` 收尾合并 | 与本文档结论无关，但解释了 07 感知到的「系统好像停摆」——被 HM-D62 部分回调 |
| **HM-D60** | 07-31 | ① **取消 CID 留位**（`cid_hold_hours_earning=0`，07 原话「干脆取消留位」）：留位是 HM-D38 加的，当时复活门槛很松，HM-D56 收紧到「≥2 单且净 ROI>1.5 或 ROI>3」后留 48h 不再划算；② **拒付折扣改回 1:1**（07 原话「不用算折扣，就按 1:1 算」，`pending_discount` 从 0.85 回到 1.0） | **②直接影响 ROI 口径**：Hermes 侧现按账面全额算佣金，与本文档第十二章「按账面拒付计算、实际会更低」一致（见 12.7「拒付未落定」） |
| **HM-D61** | 07-31 前后 | ① **预算/出价上限改在「发布那一刻」定稿**，不在建任务时锁死（队列深到 100 条以后建号到上线要隔两三天，HM-D57 把默认上限从 $0.5 提到 $2 后，队列里积压的 67 条还揣着 $0.5 陆续上线）；存量补齐把 19 条 $0.5 改成 $2；② **账户级停投识别**：`status=ENABLED` 但 `servingStatus=SUSPENDED` 是整个账户被停（多为付款问题），此前会被当成在投 | ①是 12.5f 里「$2 一刀切」覆盖面为何这么大的直接机制（发布时统一盖最新默认值）；实测 `pending_approval` 52 条全部 $2 即此故 |
| **HM-D62** | 08-01 | 补位上线通知合并成一条（每轮任务泵一条，不随广告条数涨）。回调 HM-D59 过度降噪——「只推止损不推上线，看起来像系统停摆」 | 无关 |
| **HM-D63** | 08-01 | ① 待投名单补货候选缓冲**按倍数给**而非写死 `need+20`；② 审批卡 `resend`（07 报案 08-01「待我审批没有给我发审批卡片」）：新建一批任务必须发新消息才会弹通知 | **08-05 的空转与这条同源**：D63 修的是「又建了一批新任务时卡不弹通知」，没覆盖「卡发出去之后没人理」——两次报案相隔 4 天，说明审批通道的兜底一直缺一半，见 12.8 与 HM-D65 |
| **HM-D65** | 08-05 | **待审批超时提醒**，起因是 08-03~08-05 一次没有任何报警的空转：52 条任务卡在待批两天半，在投从 74 掉到 30、44 个 CID 空转，而补货/发卡/任务泵/止损四个环节各自都「按设计正常工作」（详见 12.8）。落地：① `ad_runs` 加列 `approval_reminded_at`（幂等 ALTER）做节流，否则任务泵每 5 分钟会把卡重推一遍；② 新设置项 `approval_remind_hours`（默认 **12**，0=关闭，07 可在设置界面调）；③ `dispatchPendingApprovals()` 原来在没有未发卡任务时直接 `return {sent:0, channel:'none'}`，改为转入新增的 `remindStalePendingApprovals()`——捞出「卡已发 ≥12h、仍是 `pending_approval`、且距上次提醒 ≥12h」的任务，先用 `notifyFeishu` 发一条**文字**消息说清代价（等了多久 / 在投多少条 / 多少 CID 空转），再调 `dispatchApprovalCards` 重推卡片。**两条消息缺一不可**：只重推卡片的话，它跟两天前那张长得一模一样，很容易被当成已经看过的；文字消息才是让人意识到「这两天没在投广告」的那一下。提醒发送失败不阻断卡片重推（卡才是能操作的那个）| ✅ **已上线**（进程 14:12:03 启动、代码 14:10:34 落盘，新列已建、`approval_remind_hours=12`、`dispatchPendingApprovals` 空跑返回 `channel:none` 无报错）。**本次事故已人工解除**：手动把 52 条的 `approval_msg_sent` 置回 0 并重推批量卡（`{"sent":52,"channel":"batch"}`），07 于 **14:09:28** 一次批完，14:30 在投回到 54 条、另有 26 条 `approved` 排队。**⚠️ 只覆盖「待批无人处理」一类**，07 批了之后因 CID 不足或发布连续失败导致的空转仍无告警（07 未选「盯在投数量」那一项）|

> ⚠️ 本表由 2026-08-05 审计服务器代码注释与 `app_settings` 反推整理（`hm_dnum.sh`、`hm_d57_truth.sh`、`hm_d5x.sh`），**不是当时的原始记录**。日期栏凡标「前后」的是代码注释里未写明确日期、按相邻编号推断。若与 Hermes 侧实际记忆冲突，以服务器代码为准。

### 13.3 变更记录明细

| 记录编号 | 日期 | 内容 | 07 状态 |
|---|---|---|---|
| **HM-D75** | 2026-08-07 | **禁投品类判定回写 CRM 主营业务 + 政策标记**（详见 12.16，CRM 侧对应 D-221）。07 要求「我们查过的联盟品类回写到主营业务栏」。**先否掉了想当然的做法**：Hermes `offers` 43,432 条里 23,000 条同样是 `Others` 打头（53%），CRM 148 万行里 71% 是 `Others`/`Others>Others`/`Other`——两边同一批联盟 API 的同一套分类，互抄等于没做。真正新增的知识只有三层闸（HM-D71）查出的品类结论：374 条判定里 **276 条**落在「联盟只给 Others、我们查出真品类」的交集（金融 88、CBD 58、成人 45、烟草 25、武器 25、博彩 18、补剂 16、仿冒 1），对应 CRM **6,198 行 / 29 个用户**，且没有一行被人工改过。**实现**：新增 `src/jobs/push-policy-verdicts.js` + 路由 `POST /api/policy-verdicts/push`（支持 `dry_run`/`expand_domain`）与 `GET /api/cron/policy-verdicts-push`（每日 03:35），推给 CRM 新接口 `POST /api/hermes/policy-verdicts`。主营业务只补空、不覆盖联盟真分类（59 条因此只推政策标记）也不覆盖人工修正；政策标记按 `blocked→prohibited`、`review→restricted`，**刻意不用 CRM 的 `restriction_level`**（它把 adult/financial/gambling 只算 restricted，领取仅弹警告，而被封账号里就有一个踩成人内容）。金融按命中原词细分 Insurance/Crypto/Finance，正则兼收中文证据串。`counterfeit` CRM 品类表没有，不推 code。**顺手堵掉一个闭环**：CRM 的商家情报接口会把 restricted/prohibited 导回来变 `crm_policy_flags`，而 `sop-screen.js` 对它是**无条件淘汰**——不改的话 07 以后手动放行的商家会被我们自己种的标签重新拦住，且看不出被哪道闸拦。故 `policyFlag` 前先查本地 `cleared + layer=manual`，07 的裁定压过 CRM 标签；CRM 侧收到 manual 放行也把状态解回 `clean`。**可复用教训**：任何跨系统回灌都要先问「它会不会把人的决定盖掉」——这是 HM-D71（判定不能写会被整表重灌的表）、HM-D74（清空重灌的唯一键变永久黑名单）之后同类坑的第三个方向 | ✅ **已上线并实测**。Hermes 侧 systemd 重启、`/api/health` 200，组装出 **343 条**待推（276 条会写主营业务、59 条联盟已有真分类只推标记、8 条 manual 放行解锁）；金融细分抽样正确（cryptocoindisplay→Crypto、staysure→Insurance、ktc.co.th 命中 Credit→Finance）。**两轮回写**：先只按 MID 写 7,501 行（主营业务 6,190，与事前预测 6,198 差的正好是 8 行已有真分类的，守卫按设计跳过）；抽查发现 Acmejoy 在 BSH 平台另一个 MID 上漏写，开域名传播后累计 16,174 行，`prohibited` 由 1,257 行/187 商家 → **14,848 行/978 商家**，且最大收益来自 Hermes 没接的 LB 平台（6,391 行/904 商家）。底线复查：写过却仍是 Others 的 0 行、`skipped_manual` 0、未知品类代码 0。⚠️ **剩两件待 07 拍**：① `acmejoy.nl/.it/.fr` 这类同品牌不同 TLD 仍未覆盖（按品牌名扩展会误伤 `shop`/`travel` 这类通用首段）；② 回写后查出 14 行禁投商家已被组员领取、其中 **8 条广告正在投**（L'olivier 保险、Staysure、Travel Medical Insurance、Beelivery），领取接口只拦新的、不动已领的 |
| **HM-D74** | 2026-08-06 | **建任务的临时失败等于永久淘汰：唯一键把商家静默挡在门外**（详见 12.15）。发现路径是 HM-D73 上线后正式重建队列，`monthlyRecHits` 从干跑的 **130 掉到 9**、队首一条 LH 都没有。根因：写库时先 `DELETE FROM test_queue WHERE status='queued'` 再 `INSERT ... ON CONFLICT(normalized_domain, country_code) DO NOTHING`——**只清 `queued`，唯一键却管全表**，任何非 `queued` 的旧行都会让该商家在此后**每一次**建队列里被跳过。`sop_dropped` 被挡是对的（SOP 判它不合格），但 `suffix_failed`（后缀池空/取后缀报错）与 `copy_failed`（AI 文案生成失败）是**系统自己的临时故障**，却和永久淘汰同等待遇：全库 **147 条**（`copy_failed` 83 + `suffix_failed` 64，最早到 7/20），其中 **11 个是 07 点名要跑的 LH 推荐表商家**（JB Tools rank 33、PETstock AU 63、MGM Resorts 154、Nutricost 192…），且**没有任何日志或告警提示**。**修法**：建队列前 `releaseTransientFailures()` 把冷却期（12h）外的这两类行删掉让其重新入队，新表 `queue_retry_ledger`（域名+国家主键）记次数、**最多放行 3 次**防无限重试烧 SerpApi，放行结果写 `sync_logs`（`queue_retry_release`）。冷却取 12h 是因为后缀池空这类故障通常几小时内被补货任务修好，重试太快等于原地再失败。**可复用教训**：「清空后重灌」的表只要带唯一键，就必须同时想清楚**没被清掉的那些行会不会变成永久黑名单**——这跟 HM-D71 里「合规判定不能写 `domain_blacklist`（整表重灌会抹掉）」是同一个坑的两个方向 | ✅ **已上线并实测**。一次重建放行 147 条、重试用尽 0 条，`monthlyRecHits` **9 → 53**，队首 8 条 LH 连成一段（JB Tools US P=24967 → Nutricost P=24808）压在其他联盟前面。⚠️ **生效时点**：候补名单当前 61 条 > 低水位 50，手动催补货返回 `skipped: above_watermark`，LH 要等候补掉到 50 以下那一轮才真正开始建 |
| **HM-D73** | 2026-08-06 | **推荐表档内指定先跑哪个联盟 + 另五个平台的 8 月推荐表交付**（详见 12.15）。07 拍板 LH 这张压过其他联盟，并要求其余五个账号用各自平台的 8 月表。**表的形态先对清楚**：PM（261）、RW（190）各是独立表，而 **`CZ 8月推荐商家列表.xlsx` 一张表里塞了三个平台**——首列 BU 为 `chuizhan_ui`（MUI 302）/`chuizhan_cg`（CG 208）/`chuizhan_ev`（EV 26），必须按 BU 拆开再匹配，否则会把 MUI 的商家算到 CG 账号头上；这五个平台没有 LH 那种可直接对 CRM `merchant_id` 的 MID，只能按标准化域名精确匹配（`LIKE` 的子串误命中见 12.14）。**实现**：新增设置 `rec_lead_network`（`none`/LH/PM/CG/MUI/RW/EV，默认 LH），`build-queues.js` 读到即给该联盟的推荐表行加 `REC_LEAD_BONUS = 5000`。**5000 是挑过的数**：推荐表档分数为 `20000 − rank`、rank 封顶 9000，加成后落 **24001–25000**，仍低于白名单档最低 30001，**三档区间照旧不重叠**，不会出现「LH 第 199 名压过白名单」；读设置失败或设 `none` 即回混排，绝不因读不到设置而中断建队列。既在白名单又在推荐表的（Hornby UK，LH rank 35）仍走白名单档 32297，未改既有 SOP。**顺带把 12.14 的一个数说准**：原「104 个池子里没有」是只按域名比 `offers` 得到的，改成 `merchant_id OR domain` 并扣掉已建过广告的后逐个归因——已建过广告 63 / **未加入联盟 49** / 在池里被闸拦 57（团队黑名单 23、**offer 全部下线 19**、拒付率过高 4、亏损品类 4、无可投国家 3、禁投品类 2、禁止品牌词 1、成人 1）/ 卡队列失败态 30。真正只差「加入联盟」的是 **49 个**，另 19 个**已加入但 offer 全下线**，申请表解决不了、需联盟重新上架。拒付率拦下的 4 个值得单看：MSC Cruises **57.81%**（91 笔 / $6254 基数）、Secret Flight Club 65.08%、AG Jeans 34.06%、Drivalia 33.53%——**推荐表推的商家不等于能赚钱的商家** | ✅ **已上线并干跑+实测验证**。干跑确认整段分离（LH 24808–24967 vs 其他五联盟 19703–19995）；五个平台的跟进表与未加入审核申请表已按 `CRMyz04` 命名规范导出，MID/域名对齐逐张核过。⚠️ 上线后立刻撞到 HM-D74 那个 bug，配合修复后才真正见效 |
| **HM-D72** | 2026-08-06 | **月度推荐表跑单流程落地 + 金融口径收紧到多语种**（详见 12.14）。07 选定 LinkHaitao 8 月推荐表（199 商家）要求按顺序跑，并按团队要求出「申请/拒付/备注」三列跟进表 + `pending` 商家申请表。**口径先实测再动手**：199 个 MID 打到 CRM 全库，LH 命中 169（85%）、PM 33、LB 22——`MID` 就是 LinkHaitao 自己的商家 ID，`联盟` 列只是上游来源；跨平台只能按域名精确匹配，用 `LIKE` 会在 LB 匹出 580 个子串误命中。「申请」列三值 CRM 只给得出 `join`：各平台同步全带 `relationship=Joined`，库里有＝已加入，`pending`/`rejected` 结构上拿不到，需人工回填（已写进交付表的口径说明页）。**wj07 实测**：`wenjun3` 命中 77、待申请 99、淘汰 23；`novanest` 是新号（池内仅 25 商家）命中 0。淘汰原因取三处并集：团队黑名单 29 + CRM 违规库 4 + 禁投品类闸 7。**最容易出事的一类**：77 个已加入里 17 个属淘汰品类（Merrell/Saucony/alibaba/American Eagle 在黑名单、`mycapnos.com` 电子烟、`lolivier.fr` 保险），「申请」列如实写 `join` 但备注加 `⛔ 已加入但不投`，否则看表的人会直接投出去。**「按顺序跑」的机制早就在**：`monthly_recommendations` 已有这张表 199 条 rank 1-199，`build-queues` 三档优先级（白名单 40000 > 推荐表 20000-rank > 联盟平台）在跑，队列最高优先级清一色推荐表档；卡的是商家关系——199 个只有 95 个在 Hermes 商家池里、63 个已建过广告、**104 个池子里根本没有**，没加入就没有 tracking 链接。⚠️ 待 07 拍：推荐表档是 6 个联盟混排（MUI 302/PM 261/CG 208/LH 199/RW 190/EV 26，各自 rank 从 1 起），LH 的 #1 与 MUI 的 #1 优先级相同，若要这张表先跑完需给 LH 档内加成。**金融收紧**：07 拍板「连保险一并禁」，实现为金融独有的 `softIsHard`（只有金融是可疑即禁，其他品类保留 soft→待复核的分级，`sexy` 之于服装误伤面太大），`clearedBy` 平反保留，存量 79 个升级硬拦。自查在投任务时发现只写英语等于没拦——`unipol.it`（Unipol Assicurazioni）是 `approved` 待上线且词表零命中，意语「保险」是 `assicurazioni`；补齐意/荷/北欧/波/土/捷/匈七语种后在线池金融硬拦 182 个，并抓到两条在投：`freely.me`（已投 $4.63，Google 侧先暂停再改本地状态——顺序反了会「库里停了 Google 还在烧」）、`unipol.it`（未拿 CID 直接取消，省下一个 CID），两域名写入判定表。**四类误伤放行也是实测过的**：07 拍板放行厨刀/纸尿裤/清洁剂/积木，执行时要求落地页给正证据 ≥3 次且反证据为 0，当场挡住 `kasumi-knives.com`（正证据 5、反证据 10，folding/hunting/edc 都在，不是纯厨刀店），其余 6 个写 `layer=manual` 放行 | ✅ 已上线（`hermes-ads` 已重启、健康检查 200；判定表 268 blocked / 52 review / 39 cleared） |
| **HM-D71** | 2026-08-06 | **禁投品类闸（三层）：账号是被商家品类打死的，事后暂停救不回来**。起因是 07 核实七个被封账号后指出 `761-316-6644` 的封停理由是**违反「色情内容」政策**，并给出 `ADFLY-广告禁投品类.pdf` 要求一律不投。追到具体商家：该账号名下唯一品类拒登来自 **`acmejoy.com`**（成人情趣用品），8/03 05:15 发布、**58 分钟后**被判 `SEXUALLY_EXPLICIT_CONTENT`/`NON_FAMILY_SAFE`/`PORN`（PROHIBITED），Hermes 当场自动入库并暂停——**账号仍在次日 12:12 被封**；同形态 `shopduer.com`（7/29 PROHIBITED）带走 `795-035-8060`（8/05 封）。**结论：对 PROHIBITED 品类，事后暂停救不了账号，只能事前不投。** **原来三个缺口各自独立**：① CRM 情报库 `crm_policy_flags`（910 条）**只覆盖大麻/酒精/医疗保健，成人、武器、博彩、仿冒、烟草五类一条都没有**，恰好就是出事的；② 联盟品类字段填充率 100% 但 **65% 是 `Others>Others`**，且高危商家全落在这个值上——它只能用来**平反**（Felina 标 Clothing 据此排除误伤）不能定罪；③ 关键词也不行——acmejoy/beelivery/wolftacticalusa 的域名与商家名一个敏感词都没有，能识别 acmejoy 的只有落地页标题「Shop Men & Women Adult Sex Toys」（正文 `masturbator` 123 次、`dildo` 120 次）。**落地三层**：L1 词表（域名+商家名，零成本）→ L2 落地页（标题/描述/正文词频，一次 HTTP）→ L3 拒登回灌（Google 判过的品类违规永久拉黑）。接入 `create-runs.js` 的 SOP 深查**之前**、`build-queues.js` 用只读批量版；新表 `merchant_policy_verdicts`。**词表两路并行**：`hardFlat` 匹配去分隔符串抓长独特词（第一版给所有词加左边界导致 `lavishsextoys.com` 漏网——`sextoys` 前面是 `lavish` 的 `h`；而 `gentingcasino` 当时能中纯粹因为商家名里有空格，**整张表在靠运气**）、`hardWord` 匹配原文并要求两侧非字母数字给短词用（`cbd` 无边界会中 `abcbdesign`、`bong` 中 `bongo`、`weed` 中 `tweed`）。**L3 必须区分品类违规与技术拒登**：12 条 PROHIBITED 里 `calvinklein`/`victoriassecret.cz`/`brooksbrothers.eu` 吃的是 `DESTINATION_MISMATCH`（我们自己的后缀跳错域名），拉黑商家纯属误杀；只有品类级政策码永久拉黑并推飞书，文案直写「这类拒登是封号前兆」并点出对应 CID 要盯 | ✅ **已上线并跑过真数**。第一轮扫全库 358 条，**看证据时抓出自己四个错误**：① 正文整段 flatten 会跨词拼假词——`segurospromo.com.br` 葡语「por Nome」拼成 `porn`、`axelhotels` 拼出 `bingo`，原以为「≥8 次才定性」兜得住，实测 `ktc`/`fwd` 都是被词频送进 blocked 的，**兜不住**，改为只吃标点保留空格；② `xxx` 是打码占位符（`ktc.co.th` 卡号打码命中 8 次被判成人内容），退出词表；③ `insurance` 不在 PDF 的高风险清单里（PDF 只写贷款/信用卡/加密货币），留着挂了 15 个保险大牌，退出；④ `armour`/`axe` 太脏（Under Armour 两站、`axess-industries`、`stand1armory` 全误杀），只留 `bodyarmor` 在硬词。**还补了一个漏网方向的漏洞**：落地页只看硬词会把词表 soft 怀疑无条件洗清——`buckknives.com` 域名里 `knives` 该转人工，落地页无硬词判 clean 直接洗清，可它标题写着「Quality Knives Since 1902」卖猎刀，而 PDF 里 Google 禁投第一条就是刀具。补法**定向**：只对词表怀疑过的那一类数 soft 词频，够密维持待复核（不做全类扫描，否则服装站出现十次 `sexy` 都会被挂起），挂回 35 个（`buckknives` 46 次 / `hideawayknife` 55 / `kyokuknives` 82 / `losangeleslingerie` 127）。**最终 188 blocked / 91 review / 79 cleared**，在投任务实查 **0 条**命中禁投或待复核。**抓不到页面一律放行不能反着做**（在投的 `shoesensation.com` 直接 403，保守拦会大面积误杀、产能垮；走到 L2 说明词表已判它干净）。**合规判定刻意不写 `domain_blacklist`**——那表是团队共享表格镜像、每次同步 `DELETE` 后整表重灌，写进去会被抹掉。新增 `GET /api/policy-reviews`（91 条）与 `POST /api/policy-reviews/decide` 给 07 裁定，人工判定记 `layer=manual` 不再被自动翻案。⚠️ **队列里有几类明显能放但我不敢自己放**：厨刀（Kyoku/Kasumi/Nakano，Google 允许厨房刀具）、成人纸尿裤 `friendsdiaper.in`、`vaporfresh.com`（运动装备清洁剂不是电子烟）、`medievalbrick.com`（主题积木不是仿冒）——按 07「判不了就挂起」现在安全但在少赚钱，**要不要批量放行待 07 拍** |
| **HM-D70** | 2026-08-06 | **Google 弃用 v21，整个系统即将同时失去发布与止损能力——升 v25 + 系统级失能告警**。发现路径是 07 问「359-711-5243、833-741-4380 属于 SUSPENDED 吗」（答案：不属于，两条独立路径实查均 ENABLED），为解释这两个账号名下广告为何零花费而实查系列状态时，一次查询报 `UNSUPPORTED_VERSION`、紧挨着的同样查询却成功。翻那批一直被归类成「Google Ads 报错（未归类）」的失败，完整报文是 **`Version v21 is deprecated. Requests to this version will be blocked.`**——Google 已在**分批**拒 v21，8/06 一个上午 14 条广告踩到、日志里躺着 9 条，涉及换后缀（#426/#441/#452/#478）、出单调增（#457）、花费直查（#303）、发布（#474）。**严重性不在少发广告**：止损暂停与发布走同一条 `mutate` 通道，全量阻断后 Hermes 会同时失去发布与关广告的能力——前者只是少赚，**后者是一边烧钱一边把广告记成已暂停**，与 12.9/HM-D67「库内值与 Google 真值脱钩」同类但更重。而这批错误**没有触发任何告警**，是手工翻日志才发现的。**选版本**：v22–v25 实测均可用、v26 返回 404（尚不存在）。在 v22 与 v25 上各验 9 项核心操作（查系列指标/`primary_status`/拒登 `policy_summary`/关键词出价/子账号/`search_rank_lost_impression_share`，改预算/CPC 上限/后缀，**暂停广告**）加一次 **63-operation 完整发布干验**（写操作全部 `validateOnly` 不落地），**两版结果完全一致**——故取离下次弃用最远的 v25，v22 大概率也在弃用队列里。改动是一行常量。**告警**（07 拍板）：新增 `alertFatal(kind, text)`，触发点为**版本弃用 / 令牌池全部耗尽 / CID 池归零**；6 小时静默沿用 HM-D46 配额告警做法；告警自身失败绝不影响业务。文案写「影响什么 + 怎么处置」而非错误码，CID 归零那条还分辨是「被在投正常占满等释放」还是「卡在 `cid_reserved` 白占」（即 HM-D68 事故形态）。**端到端验证时第一次就抓到文案缺陷**：Google 把有用的话埋在 `details` 里、外层 `message` 永远是没信息量的 `Request contains an invalid argument.`，照原样报等于什么都没说，已改为从报文提取真实消息 | ✅ **已上线并验证**。v25 生效后实调：查账号/列子账号（53 个）/查在投系列全部正常，一轮真实 monitor 检查 71 条，`UNSUPPORTED_VERSION` 一次未再出现（唯一报错为 #226 老 `stale_read`）。告警用废弃 v20 端到端触发两轮，修复后落库内容为 `Version v20 is deprecated. Requests to this version will be blocked.`；测试静默记录已清除。**同时纠正两个我说过头的判断**：① 「CID 是唯一硬瓶颈、产能见顶」说重了——`budgetExceeded: 93` 是 HM-D43 的**每轮时间预算**不是钱的闸门，近 7 天真实发布量 24/26/25/39/37/45/46 条，8/06 只有 10 条纯因早上 8 小时卡死；真正该盯的是一周退役 7 个账号的**流失趋势**。② 「38 条 monitoring 零花费」不是故障——全部 8/05–8/06 发布、无一超 24 小时，实查 #463 在 Google 侧 ENABLED/SERVING、广告 REVIEWED+APPROVED，主状态 `LEARNING`（`BIDDING_STRATEGY_LEARNING`），即智能出价学习期未起量。**另辨明 `stale_read` 两种情形**：累计花费掉到 **$0.00 基本等于账号被封**（#432 报 $0.00 后 1 小时确认 SUSPENDED、#396 报后次日中午确认），小幅低于已知值只是回流延迟（#46 是 $2.81 低于 $3.38）——前者是可用的封号前兆信号，拆分告警待做 |
| **HM-D69** | 2026-08-06 | **决策底座换真数（07 拍板三件套 A/B/C）**。**A 盈亏比改用 CRM 系列级真数**：`auto-tune` 每条规则都以盈亏比为轴，但 Hermes 的 `click_token` 归因只有 4% 命中率，只能用「商家+联盟」合并值代表其中每一条广告——同商家多条时串味。实测 #46 mmlafleur 合并口径 278 单/R=16.98，系列级实际 125 单/R=10.25（合并把别的系列订单算了进来，花费还少算 $206），按抬价公式反推前者给 $1.36、后者给 $0.82，**差 66%**。CRM 的 `ads_daily_stats` 本就有系列级归因（10.2 万行里 4.5 万行有佣金），用 Google campaign id 与 `campaigns.google_campaign_id` 精确对齐，431 条匹配 420 条。落地：CRM 新增 `POST /api/hermes/campaign-stats`（D-219），Hermes 新增 `crm_campaign_stats` 表 + 每小时 20 分整表替换。三处保守取舍：花费/点击取 CRM 与 Sheet **较大值**（分母大→盈亏比小→只少抬不多抬；Sheet 回流当天有 11 条读成 0）、佣金一律用 CRM 系列级、`inflated` 档传 `exclusive:false` 复用加码闸门挡住抬价但保留减预算。调参流水记录本次数据源。**B 新广告基准 $1→$0.35**：⚠️ 推翻 HM-A08 与我当天上午给 07 的提醒——那份分析只看点击量没看盈利。CRM 真数按实付均价分档：<$0.35 那 133 条出单率 28.6%、整体盈亏比 **3.82**；≥$0.35 那 124 条出单率 14.5%、盈亏比 **0.37**、净亏 $570。根因在样本不在转化：贵的那批 quality 几乎全是 `thin_sample`、点击仅 11–25 个，$2 预算按 $1 上限只够买 2 个点击、按 $0.35 能买 5.7 个。`hard_cpc_usd` 保持 $1，两者分工为「$0.35 攒样本、$1 是决策池抬价天花板」。**C 抬价样本门槛按 AOV 分档**：原门槛只按盈利比分两档，但决定「几单才算数」的是单笔多大。团队 2849 条 trusted 实测 1–2 单时能算出的最高 R：AOV<$2 仅 0.78（够不到抬价线，门槛无意义）、$2–5 是 2.14、$5–10 是 2.82、**≥$10 高达 25.19 且离散系数 1.47 最差**——一笔运气单能把 2 倍说成 25 倍，而这类广告恰因盈利比亮眼最易被抬价规则选中。门槛改为 ≥$10 要 6 单 / $5–10 要 4 单 / $2–5 要 3 单，与原「≥2 倍目标比要 5 单」取严不取代 | ✅ **已上线并干跑验证**。CRM 接口经 GitHub Actions 部署，Hermes 首轮拉到 431 条（66 trusted / 191 thin_sample / 174 no_data / 56 条有订单），**一条 inflated 都没有**——Hermes 投的都是自己的品牌词、归因干净，该污染只存在于团队其他人的广告（`viagogo` 那条 0 点击对 1712 单）。干跑本轮 **0 条动作**且每条原因都对得上：#46 算出可出 $0.82 但比现价 $0.75 只高 9.3%，卡在「涨幅<10% 不动」（昨天刚抬，正好让它跑满一天）；#127 lagos（3 单/$44.58）、#395 oxiline（1 单/$10.66）、#303 calecim（1 单/$19.54）**全部被新 AOV 门槛挡住**，正是 C 要防的「高 AOV + 少订单」；#331 算出只该出 $0.92 低于现上限 $2；#278 盈亏比 1.47 够不到线 |
| **HM-D68** | 2026-08-06 | **发布前的上限校验排在定稿前面，收紧硬顶后把 CID 池啃光**。起因是 07 发来 CRM 截图「今日投放数 0 条」。`advanceApproved()` 的顺序是「预占 CID → `assertLimits(run)` → 取后缀 → 按发布时默认值定稿（HM-D61）→ 发布」，校验的是**建任务时盖的章**：8/05 把 `hard_cpc_usd` $5→$1 之后，存量 `approved` 身上 $2 的旧章撞上新硬顶抛异常，**而 CID 已在上一步预占、异常无人接**，任务永久停在 `cid_reserved`——不发布、不失败、不归还。pump 每 5 分钟捞一条就吃掉一个 CID，5 条依次卡在 8/05 的 10:30–13:55（改设置**之后**；之前发布的 #457–472 全部正常），到 8/06 早上 71 个可用账号被占满、**空闲 0**、98 条堵在门口。**修法**：① 校验对象改用 `effectiveBidding(run)` 的结果，即「这一刻真正要发出去的值」而非旧章（不动第 3 步，最小改动）；② `assertLimits` 包 try/catch，失败即归还 CID 并标 `failed` + 飞书告警——**预占之后的每条失败路径都必须归还 CID**，原代码后缀失败、发布失败两条都记得还，唯独新出现的校验失败没人管。**可复用教训**：`hard_*` 这类 Owner 上限会**回溯作用于所有中间态任务**，收紧前必须扫 `approved`/`pending_approval`/`cid_reserved` 身上的旧值会不会撞线，以及撞线后的异常有没有人接 | ✅ **已上线并验证**。解卡 7 条退回 `approved` 归还 CID，手动催一轮 pump：**7 条全部发布成功**，`cpc_usd` 由 $2 正确定稿为 $1，在投 62→72。另 2 条独立故障（#473 后缀卡在跳板域名 `go.ultrainfluence.com`、#474 Google Ads 未归类报错）与本 BUG 无关。⚠️ **CID 随即又被占满**（空闲 0、93 条排队），且一周内退役 7 个账号（1 CANCELED + 6 SUSPENDED），池子共 80 个——产能已不取决于名单/审批/规则，只取决于还有多少能用的账号，需 07 决策 |
| **HM-D67** | 2026-08-05 | **调价改哪个旋钮，不能靠建单来源猜**。起因是 07 要求实地核查「真实情况和我们预想的一样吗、会不会有 BUG」，实调 Google 后查出：`adjustRun` 的 `set_cpc` 用 `run.queue_source === 'external'` 判断是否手动出价，而 5 条接管广告在 Google 侧**全部已是 TARGET_SPEND**，于是每次调价都去改广告组与关键词的 `cpc_bid_micros`——正是 HM-D57 写明「被智能出价完全忽略」的字段。**代价**：`#46 mmlafleur` 三次抬价（7/30 `$0.25→$0.29`、7/31 `$0.29→$0.33`、8/05 HM-D66 首轮 `$0.33→$0.66`）**全部落空**，真实上限三个月停在 **$0.50**，库内是幻影值；#47（库 $1.50/真 $0.80）、#48（库 $0.18/真 $0.30）同样对不上。**这正是 HM-D66「首轮生效」却看不到量变的原因**——算对了、写库了，只是写到了 Google 不看的地方。**落地**：① `set_cpc` 调价前先 `gaql` 查 `bidding_strategy_type`，`MANUAL_CPC` 才走组层+关键词层，否则走 campaign 层 `setCpcCeiling`，查询失败保守按智能出价并记 `api_call` 事件；② 3 条幻影值同步成实调真值并在事件流水写明原因，避免日后拿 $0.66 当基准再翻倍。**共性教训**：`queue_source` 是建单那一刻的事实、`bidding_strategy_type` 是此刻的事实，拿前者推断后者，只要中间有过一次策略切换就会永久错下去，而且**不报错、不告警、日志还写着「已调整」**（`setCpcCeiling` 的注释明说它会顺带把 MANUAL_CPC 切成 TARGET_SPEND，切换路径一直存在）| ✅ **已上线并实调验证**。走修复后的完整链路推 `#46` 到 07 拍板的 **$0.75**（未直接跳 auto-tune 算出的 $1.00），Google 侧 `cpcBidCeilingMicros: 750000` 确认落地、库内一致，事件文案由「调手动出价（组+3 个关键词）」变为「调出价上限（智能出价 cpc_bid_ceiling）」——**三个月来第一次真正生效的抬价**。存量 72 条 $2 按 07 拍板保持不动。**顺带拿到「贵的要抢」的直接证据**：#46 近 7 天排名丢失 **73%–86%**、预算丢失仅 0–10%，七八成展示输在出价不够而非预算不够，v1.9 起的判断首次有 Google 侧硬数据支撑。观察基线见 12.9 |
| **HM-D66**<br>（服务器侧已生效）| 2026-08-05 | **决策池：把出价上限重新交给自动调参，并按拒付率调门槛**。起因是 07 划清了两段分工——「新商家都统一标准 $2 预算、止损阀判断要不要关停，**有出单才到我们的观察、决策池**」，随即指出真正的难题在后半段：出了单之后加多少、抬不抬价，现在没有规则。**① 决策池实测（54 条出过单广告 / 225 家已投放商家）**：按盈亏比分区，R<1 有 28 条（占池子一半）、R>4 只有 4 条却贡献池内 **88%** 的佣金；按 07 的净利润规模判据分档更极端——206 家亏损、17 家 <$50、1 家 $50-200、**1 家 >$2000**，`mmlafleur` 一家撑起全部利润，第二名 `lagos` 只有 $92.76，**中间是断层没有过渡带**。**② 病灶：上限与赚钱能力完全脱钩**。HM-D57 在 7/31 移除全部 CPC 规则时，把「组层/关键词层出价」和「出价上限」一起停了——前者被智能出价忽略（砍它是对的），可后者是 `cpc_bid_ceiling`，是唯一真正生效的价格字段。停掉之后上限成了建任务时盖的章，此后没有任何逻辑碰过：全系统最赚钱的 `#46 mmlafleur`（花 $319 收 $5,523、盈利比 17.3、5.8 个点击出一单）**上限只有 $0.33，而预算早在 $15 顶满**——它一直是被出价卡量、不是被预算卡量；同时一批亏损广告的上限是 $2。**③ 新规则 `cpc_up`**：新上限 = **实付均价 × (盈利比 ÷ 目标比)**。乘实付均价而不乘现上限——上限 $2 的广告实付才 $0.19，按上限翻倍会直接跳到 $4，等于把从没验证过的价一次性放出去；乘实付价则始终贴着真实竞价水位往上试。三道闸沿用成长类（佣金归自己、点击 ≥20、订单够），订单门槛按盈利比分级（≥2 倍目标比时要 5 单，一笔运气单能把 2 倍说成 8 倍）；单次涨幅封 2 倍 + 22h 冷却，分几轮爬到位，每轮拿新数据重验。**④ 拒付率进决策（07 拍板「抬高门槛而不是一刀切」）**：目标比 = `tune_cpc_target_ratio × (1 + 拒付率)`，拒付 40% 就要盈利比到 2.8 才准抬。拒付率在 `monitor-runs.js` 里按 **CRM 结算中心同口径**算好再传（`rejected ÷ 全状态佣金总额`，分母含拒付本身）——不在调参里算是因为分母要用未打折的 `effective`，调参拿到的 `effComm` 已过 `pending_discount`。**⑤ HM-D64 下线**：`effectiveBidding()` 不再调 `cpcCeilingFor()`，上线前一律 `min(default_cpc_usd, hard_cpc_usd)`。07 的原则是「没跑出订单的商家，别人的历史数据不该替它做主」，加价能力整体挪进决策池。`cpc-ceiling.js` 与 `crm_team_verified` 同步**保留不删**，留作将来的安全阀。**⑥ 配置**：`tune_profit_ratio_up` 1.2 → **1.5**（1.2 会给 6 条卡在基准线下的广告误加码）、`hard_cpc_usd` $5 → **$1**（07 原话「最高 CPC 只能到 1 刀」，`default_cpc_usd` 联动 2 → 1；实测均价 $0.19 不碰顶，对新广告无实际影响）、新增 `tune_cpc_target_ratio`(2) | ✅ **已上线**（15:38 落盘、16:07 重启、`/api/health` 200）。上线前干跑 74 条在投：**抬价 1 条、加预算 0、减预算 0**，行为极克制。首轮 monitor 实测生效：`#46 cpc_up: set_cpc $0.33 → $0.66（盈利比 10.84，166 单 / 实付均价 $0.2021 / 拒付率 2.7%，按目标比 2.05 可出到 $1.07）`——拒付率把目标比从 2.00 抬到 2.05、单次封 2 倍把 $1.07 压到 $0.66，两道保护都按设计动作。`lagos`/`budgetvetcare` 现上限 $2 已高于算出的价，规则不动它们。**⚠️ 确认率暂不入规则**（07 选「先只统计展示」）：全局确认率 **0.1%**、结算率 **0%**、待定率 92.6%，但那是因为系统才跑两个半月而 approved 平均要等 **70.6 天**，现在用它做权重会把所有商家一起判死；待跑满三个月再议 |
| **HM-D66 口径教训** | 2026-08-05 | **一次被 07 当场纠正的口径错误，记下来防止再犯**。我在干跑时发现 `pending_discount = 1`（待定佣金按全额计入决策），又查到 `mmlafleur` 已裁决的 16 笔**全部拒付、通过 $0**，据此判断「系统正拿未定的钱当已到手的钱，抬价是在虚高数字上加杠杆」，并建议把折扣降到 0.3。**07 的纠正**：「我们的佣金就是按照所有状态的佣金算，拒付、结算、通过都有各自的特殊显示……不然的话就是杞人忧天」，并给出正式公式 **净利润 = 佣金 − 拒付 − 成本**。**错在哪**：我用「已裁决样本的通过率」去外推未裁决部分，而已裁决样本只占 7.4%，且裁决速度极慢（approved 平均 70.6 天、下单超 60 天的老单也只有 46% 出结果）——`mmlafleur` 最老的单才 59 天，它的「100% 拒付」是**样本没到期**不是**被判死**。按 CRM 结算中心口径重算，它的拒付率只有 **6.8%**，属健康。**正确做法**：拒付是**已发生的事实**，从佣金里显式扣掉即可（Hermes 的 `effective = SUM(commission WHERE status != 'rejected')` 本就等于 approved+paid+pending，减成本正好是 07 的公式，`pending_discount = 1` 本来就对）；拒付率的正当用途是**当权重因子**（见 HM-D66 ④），不是当折算系数去猜没发生的事。**顺带记两个转义陷阱**（本次连踩两次）：PowerShell 的 `-replace` 是正则替换、替换串里 `$$` 折成一个 `$`；JS 的 `String.replace` 同理。往服务器传含 `$${...}` 模板字符串的补丁必须用 `.Replace()` 与 `split/join` 字面替换，否则代码里的货币符号会被静默吃掉 | ✅ 已按 07 口径修正，`pending_discount` 维持 1.0 未动 |
| **HM-D64**<br>（服务器侧已生效）| 2026-08-05 | **接口通路落地，不改任何决策行为**。新增本地缓存表 `crm_team_verified`（`(platform, merchant_id)` 主键 + `normalized_domain`/`merchant_id` 双索引）、同步任务 `jobs/sync-crm-team-verified.js`（首跑 **20 秒 / 3,104 条**，整表替换；**拉失败或返回 0 条时拒绝覆盖旧快照**——盈亏平衡值按累计交易算，隔一两天不更新远好过表被清空导致全员掉回探测下限）、路由 `GET /api/cron/crm-team-verified`（独立 `withLock`，与 `crm-intel` 隔离：该接口 26 秒，串在一起会把 2 秒的情报同步拖成半分钟）、crontab `55 7 * * *`（与 `crm-intel` 的 `50 7` 错开）。匹配采用「`merchant_id`+`domain` → 仅 `domain` → `merchant_id` 全库唯一」三级退让（CRM 侧 885 条无 domain，而 `merchant_id` 跨联盟撞号）。**定价规则分两批上线**：第一批只落数据通路、`lib/cpc-ceiling.js` 代码在位但**刻意不接** `effectiveBidding()`，留出干跑窗口——事后证明这一步很值，干跑推翻了 12.5f ⑥ 的两个参数（见 HM-A08）；07 重新拍板后第二批才接入，规则为「**$2 基准线 + 有证据才加价**」：`effectiveBidding()` 调 `cpcCeilingFor()`，新增设置项 `cpc_safety_trusted`(0.5) / `cpc_safety_inflated`(0.25) / `cpc_hard_inflated`($2)，`hard_cpc_usd` 由形同虚设的 100 收到 **$5** 充当 `trusted` 档天花板（走 `setSetting` 留流水；改动时旧代码仍在跑，`min(2,100)` 与 `min(2,5)` 同为 2，线上无感）。原文件备份 `/home/ubuntu/hm_backup_20260805/`（含 `.pre-d64` 两份）。**编号教训再次应验**：初版按文档推断用了 HM-D62，grep 服务器发现 D62/D63 已被占用（补位通知合并 / 名单缓冲倍数），已全部重编为 D64 | ✅ **已全量上线**。重启后 `/api/health` 200、启动日志无报错。上线前干跑全库 452 条 `ad_runs`：**降价 0 条**、待发布 52 条一字未变、仅 14 家 `trusted` 可加价（$2.13–$4.39）。上线后实调四条代表路径确认新逻辑生效：`caracaranyc.com` → $4.39、`tedbaker.com`（trusted 但平衡值 $0.0712）→ 维持 $2 不降、`mmlafleur.com`（inflated）→ 封 $2、快照外 → $2 |
| **HM-A08** | 2026-08-05 | **干跑 12.5f ⑥ 的四档规则，发现两个参数必须回调、一处实现偏离已修正——落成 12.5f-1；07 已据此重新拍板，规则已随 HM-D64 第二批上线**。**① 修正实现偏离**：初版 `cpc-ceiling.js` 写成 `clamp(raw, floor, hard)`，正是 ⑥ 上方已警告过的错误下界，改为只封顶不设下界（仅留 $0.01 的 Google 计价精度底）；修正后实测 **8 个 trusted 商家**落到 <$0.02（`Splash Wines` 平衡值 $0 → $0.01），此前会被错误抬回 $0.30。**② 覆盖率真相：待发布 52 条能按盈亏平衡定价的 0 条**——43 条快照里根本没有（MID 全在 `1865xxxx` 新号段）、6 条 `no_ad_data`、3 条 `thin_sample`。**不是匹配 bug**（域名模糊匹配命中 0、反向抽查口径一致）。根因是两池天然不相交：快照收「团队已出过单」的商家，队列排「还没投过」的新商家；天花板＝`offers` 21,831 域名与快照重叠仅 **1,092（5.0%）**。**③ 探测下限 $0.30 建立在口径错配上**——⑥ 用「全库 77.8% 成交 ≤$0.30」论证，但那是含已跑出量老系列的口径；**Hermes 自己 171 条有花费广告实际 CPC 中位 $0.3250、≤$0.30 仅占 45.0%**（CRM 团队快照 611 商家则是 $0.1433 / 85.1%）。这与 12.5a 已证的「测试期系列 CPC $0.3686 vs $0.2824」高度吻合——**$0.30 正好卡在测试期 CPC 中位数上，等于对一半新商家关掉竞价资格**，属 12.5a 踩过的「加权错觉」同类错误。**④ 上限压低不省钱只是拿不到量**（Hermes 367 条已发布实证）：$0.8 组 72 条平均仅 10.7 点击、**0 条跑到 50 点击**、实际均价被顶到 $0.6634；$2 组 212 条平均 38.6 点击、实际均价反而只 **$0.3283**——与 ⑥ 上方「成交价由二价竞价决定、上限只是天花板」一致，空间给足 Google 才能挑便宜机会成交。这正是服务器侧 HM-D57 在 7/31 把上限从 $0.5 提到 $2 的原因。**⑤ 代价具体形状**：17 条真正出过单的广告里 **8 条**实际 CPC >$0.30（该批中位 $0.2707、p75 $0.4032），含佣金最高的 `BudgetVetCare`（$0.403，花 $30.24 赚 $39.28）与 `Calecim`（$0.532，花 $11.71 赚 $19.54），两条都净赚。亏损上限本由 $6 花费闸兜住，出价上限再压一道管的不是风险而是「能否参与竞价」。**⑥ `hard` 收 $2 的两难**：撞顶 75 个（`trusted` 14 + `inflated` 61）；放宽到 $5 则撞顶降至 30 个但**会连带放开 31 个 `inflated`**（该档平均平衡值 **$12.594**，虚高源于 CRM `campaign_id` 零填充）——⑥ 原话「$2 硬顶恰好挡住最脏一档」在此成立，故若放开应**按档给天花板**而非抬全局 `hard`。**⑦ 顺带证实 12.5e ④ 的推论**：实际花到 $5–7 的 15 条广告平均只买到 **24.7 个点击**、跑到 50 的仅 1 条，按均价 $0.33 算 $6 只够买约 18 个点击，**「50 点击无单即 pass」在 $6 下是虚设**。**⑧ 建议改法**＝从「分档降价」改成「有证据才加价」：上限 = `max($2 基准, trusted 的平衡值 × 0.5)`，`inflated`/`thin_sample`/无数据一律维持 $2，`trusted` 天花板放宽（建议 $5）。代价收益明确：现有 52 条待发布**一条都不受影响**，14 个高价值 `trusted` 商家可突破到 $3–4.4。**这仍满足 07 原始要求**——「不一律 2 刀」的正解是给贵商家开口子，而非给便宜商家降上限（$2 对便宜商家实测不造成浪费，实付 $0.15–0.33）。**⑨ 与 ⑥ 的 `cpc_rule_sim` 效果表不矛盾**：那表算的是有 breakeven 数据商家按各自平衡值定价的效果（本地快照复算：382 个可比商家里 347 不受影响、35 被压住），本条质疑的只是**无数据商家**落在哪个探测下限；但被压住的 35 个里含明星商家 `PupLabs`（平衡 $0.4205、实付 $0.3274、新上限 $0.21），但这 35 个在 07 拍板后已不复存在——`max(base, …)` 使任何低于 $2 的结果退回基准线，`PupLabs` 现在维持 $2。代价是 ⑥ 预期的「压虚高档省钱」也一并没有了，而 ③④ 已证明那份预期本身不成立 | ✅ **07 于 2026-08-05 拍板 #23 = $2 / #24 = 按档给，⑧ 的建议已原样落地**（见 HM-D64 第二批与 12.5f-1 ⑦）。⑧ 的预测经实测坐实：待发布 52 条零影响、全库降价 0 条、14 家 `trusted` 突破至 $2.13–$4.39。**唯一未采纳的是 ① 的「只封顶不设下界」**——它是针对 ⑥ 的 $0.30 下限说的，07 把下限定回 $2 之后，「不设下界」反而会让 `Splash Wines` 这类平衡值近 $0 的商家掉到 $0.01 上，与「基准线不下移」冲突，故最终实现是 `max(base, min(raw, hard))`，$0.01 技术底不再有触发路径 |
| HM-A07 | 2026-08-05 | **07 指出「CPC 要有限额，现在 Hermes 完全搞反了……如果这个商家的 CPC 很贵，我们要去抢，才采用高 CPC，而不是直接拉 2 刀的 CPC」——审计生产库确认批评成立，落成 12.5f，并修正 12.5d/12.5e 里「选便宜 CPC」的表述**。**① 现状**：`app_settings` 里 `default_cpc_usd = 2` 是**全局默认**、`hard_cpc_usd = **100**`（风控上限形同虚设）。一刀切实证：`pending_approval` 52 条**全部 $2**，在投 34 条均值 $1.95，全库 ≥$1 的 272 条。来由在代码注释有据——服务器侧 HM-D57（07 于 7/31 拍板「智能出价、争取更多次数、写一个死上限 2 刀」，起因是 LAGOS #127 手动出价 $0.80 被 Google 要价 $1.84、质量分 10/10 却零展示，而 `cpc_down` 又按「均价只有出价 38%」把它砍到 $0.24 使日点击从 33 掉到 8）。**方向本来是对的，错在被写成全局默认且注释标注「不该被自动调参碰」**。**② 代价已发生**：在投 34 条里 **24 条(71%)已花超 $6，却只有 10 条(29%)攒到 50 点击**，**14 条卡在「花超 $6 且点击不足 50」的判决无效区、已烧 $172.24**——正是 12.5e ④ 预测的死区。个案：`340-tinyland` 花 **$8.94 只买 9 个点击**(CPC $0.993)、`256-wecreattechltd` $19.29/23 点击($0.839)、`194-nubeo` 花 **$25.79(预算 4 倍)才 49 点击**。**③ 正确依据＝盈亏平衡 CPC**（＝该商家有效佣金/点击＝单笔佣金×点击转化率×(1−拒付率)）。商家间**差 328 倍**：盈亏平衡 <$0.05 的 829 个商家实际付 $0.0531(**超 3.3 倍**)净亏 **$7,058**；≥$1.00 的 112 个只付 $0.2124(**用了 4%**)净赚 **$113,153**。直接按「有没有付超」分组判据最干净：付超的 361 条净亏 $2,447，未付超的 727 条**净赚 $270,714**。**④ 07 的「贵的要去抢」有硬数据，且方向与我此前结论相反**——按实际 CPC 分层，每点击收益与 ROI 随 CPC **单调上升**：<$0.08 → EPC $0.17/ROI 501%，$0.20-0.35 → $3.42/**1,242%**，≥$0.60 → **$16.89/1,897%**。贵的点击贵得有道理，那批商家单笔佣金极高（Hoteles Xcaret UK 单笔 $103.47、盈亏平衡 CPC **$47.55**；M.M.LaFleur $43.67/$1.42；PandaDoc $59.22；PupLabs $20.35/$30.55）——对它们 $2 上限甚至偏保守。**故 12.5d 的月搜门槛必须改口径**：它解决「买不买得到量」，盈亏平衡 CPC 解决「买来的量亏不亏」，**两个维度都要、不可互替**；「选便宜」应读作「选相对自己盈亏平衡值便宜」。**⑤ Hermes 在投实测两个方向的错同时在犯**：86 个在投/待批 MID 里 **69 个(80%)团队完全无数据**（拿 $2 上限投彻底未知的商家，实际付 $0.3514）；付超的如 `Noyz` 盈亏平衡 **$0.0232**（3 人跑过、5 单、883 点击）却付 $0.403＝**17 倍**、`ULANZI` $0.0789 付 $0.3973＝5 倍、`Ergomotion` 平衡 **$0** 仍在投；该抢没抢的如 `Slate Flosser` **$4.68**、`Oxiline` **$2.90**（实际仅 $0.347）、`BudgetVetCare` $1.83、`M.M.LaFleur` $1.42（实际 $0.33）。**⑥ 建议规则**：出价上限 = clamp(盈亏平衡CPC × 安全系数0.5, 探测下限$0.30, hard_cpc_usd)；取消 `default_cpc_usd` 全局默认、$2 只作天花板、`hard_cpc_usd` 从 100 收回 $2；**盈亏平衡 ≤ 探测下限的商家在建队列时就拦掉**（属选品问题不是限额问题）；盈亏平衡值走 12.5c 的 CRM 只读 API 取团队真值（该接口本已规划 `weighted_cpc`，扩一个 `breakeven_cpc` 即可）。**⑦ Hermes 里已有正确逻辑但被淹没**：`cpc_up_epc` 规则（样例 `set_cpc $0.25 → $0.29（每点击赚 $1.718、盈利比 8.15，小步加价抢量）`）正是 07 说的「值得抢才加价」，**但只触发 4 次，而一刀切的服务器侧 HM-D61 改了 19 条、服务器侧 HM-D57 迁移改了 54 条**。**⑧ 附带发现治理问题**：服务器代码/库里已有自己的 服务器侧 HM-D57 / 服务器侧 HM-D61 编号，与本文档撞号，登记为待确认项 #20。**⑨ 07 当日四项全部拍板**（安全系数 0.5 / 探测下限 $0.30 / `hard_cpc_usd` 收回 $2 / 低平衡值商家照投不拦），规则随即回溯验证（`cpc_rule_sim.sh`）：**新上限压住的量恰好是全部亏钱的量**——不受影响的 384 个商家 ROI **2,196.7%**（净赚 $475,732），被压住的 608 个 ROI **−22.8%**（净亏 $3,199）。**第四项选择暴露并修正了我给的公式错误**：原写 `clamp(盈亏平衡×0.5, 探测下限, hard)`，其下界会把 **365 个**新上限本应 <$0.02 的商家抬回 $0.30，使规则对最该管的那批完全失效（这批 7 月净亏 $4,399）；探测下限已改为**只用于「团队无数据」分支**。「照投不拦」经验证自洽：低平衡值商家拿 <$0.02 上限后必然零展示，现有零流量止损会清掉，不必改建队列逻辑；$0.30 探测下限覆盖全库 **77.8%** 的系列成交价。**同时揪出一处会抵消整个限额的冲突**：`cpc_up`「投放 ≥3~10 天零点击 → 出价 +50% 试探」会把被压住的商家一路提回去，必须把它的上界也改成 `盈亏平衡CPC × 安全系数`，让 `cpc_up_epc` 成为唯一的上调路径。本次仍未动任何线上配置 | ✅ 四项已拍板；待实现（须带 12.5f ⑥ 配套第 2 条） |
| HM-A06 | 2026-08-05 | **07 澄清「总预算就是 6 刀」（不是日预算）——按总预算口径重做全部验证，落成 12.5e，并修正 12.5a 的误杀曲线**。此前我拿 $6 与 yz04 的日预算中位 $4 作印证是错的口径：他日预算 $3-4 但系列跑几十天,单条累计花费远超 $6（`073-MUI1-puplabs` 8/4 一天花 $26.31、累计 $1,332.68）。重做后结论：**① $6 判决其实就是 yz04 的实操**——他 894 条系列里 **397 条(44.4%)总花费不到 $6 就停**,且这批 CPC 恰是最贵档 **$0.1935**;而 ≥$50 的 95 条 CPC 仅 $0.07、吃掉 74% 的点击。**「贵的早砍在 $6 内、便宜的放它长跑」与 07 的口径本质相同。② $6 够花,且比我原先说的宽松得多**——全库 7 月新建 1,448 条出单系列首单时**只花了 $2.67、累计 20.5 个点击**;$6 误杀率全库 **14.3%**(yz04 27.6%、wj07 16.1%),**而非 12.5a 旧表写的 41.3%**——旧数是 yz04 全期口径且未排零点击,已在 12.5a 就地标注修正。yz04 偏高是因为**他的首单来得晚**($4.48 vs 全库 $2.94),不是算错;按 CPC 分层看误杀率各档接近(10.7%-17.5%),**说明 $6 这条线本身不是区分器**。**③ 50 点击够判决,只漏 11%**——出单系列首单位置 81.4% 在 30 点击内、**90.1% 在 50 点击内**、96.1% 在 100 点击内,07「严一点没关系」的代价很小。**④ 真正的矛盾是 $6 能否买到那 50 个点击,完全由 CPC 决定**：CPC<$0.08 花完 $6 已有 **193.6** 个点击(97.4% 达 50)、$0.08-0.12 有 64.7(81.8%)、$0.12-0.20 只 43.1(23.5%)、$0.20-0.35 仅 28.4(1.8%)、**≥$0.35 只 12.8 个点击(0% 能达 50)**。**故「$6+50 点击」不是止损规则而是「判决有效性」规则**：贵商家花完 $6 只有十几个点击,此时零订单不能证明商家不行,pass 掉等于扔硬币——**这就是 12.5d 月搜门槛存在的意义**(月搜 ≥2 万 → CPC $0.0396 → 落进 <$0.08 档 → 97.4% 能在 $6 内攒够 50 点击)。**⑤ 组合实测**(全库 7 月新建 3,028 条)：CPC<$0.12 通过组 767 条命中 **58.3%**/CPC $0.0390,拒绝组 2,261 条命中 44.3%/$0.2228,**被拒那批吃掉 62% 总花费($24,155)命中率却更低**。**⑥ 日预算建议 $2-3**：实际日花 $2-3 时 $6 撑 2.7 天、$3-4 撑 2.3 天、$4-6 只 2.0 天(学习期不够,12.2 已证 2-3 天档 ROI −32.7%),日花 ≤$2 会拖到 6.7 天偏慢。**⑦ 判决规则因此可简化**：$11-14 从「已验证商家的默认额度」降级为「仅在 CPC 超预期、$6 买不到 50 点击时的补救额度」,绝大多数系列只花 $6 | 待 07 拍板（日预算取 $2 还是 $3；是否接受 12.5e ⑦ 的简化规则） |
| HM-A05 | 2026-08-04 | **07 定 HM-D48 选品门槛「完全更换」，为此拉取 yz04 的 Google Ads 关键词与出价真值，找出可替换的事前判别器——结论＝品牌词月搜索量 ≥2 万**（落成 12.5d）。数据来源：`yz04_gads.mjs` 复用 CRM 的 Service Account JWT + REST `searchStream` 直调，拉 8/4 全部 52 个有花费 CID 的 `keyword_view` 与 `campaign`（1152 条关键词行、78 条当天有花费），`yz04_kwplan.mjs` 再用 `generateKeywordHistoricalMetrics` 取同批词的事前预估。**① 他的出价是统一模板，成本根本不由他控制**：52/52 全部 `TARGET_SPEND`、关键词有效出价中位统一 **$0.500**，而实际成交 CPC 只用掉出价的 **27.8%**（p10 仅 4.4%，贴顶 >0.9 的只 8/78）——同一个 $0.50 上限下 `etsy` 成交 $0.0099、`mitoredlight` 成交 $0.4550，**差 46 倍**。展示份额丢失归因给出机制：便宜那批「因排名不足丢失」≈0%（Etsy 0%、American Eagle 0.2%，没人抢他吃满了），贵那批 90%（$0.50 仍排不上）。**这从第三个角度复证了 HM-A04 关于「CPC 不是出价技巧」的结论。② 07 定的 $6 预算与他的实操吻合**：日预算中位 **$4.00**，最常见 $3(16 条)/$4(10 条)，**38/52 条 ≤$6**——$6 不是保守而是他的正常水位偏上。质量分中位 **10 分**（446 个有值的里 337 个满分）故无区分力；有花费的词 EXACT 59 / PHRASE 19 / BROAD 0，词形就是品牌名变体。**③ 四个事前判别器只有一个可用**：品牌词月搜索量 Spearman **−0.578**，而规划器「预估首页低位出价」仅 0.341、「竞争度指数」仅 **0.146**——竞争度标签必须明确否掉，标为 `HIGH` 的 51 个词 CPC 从 $0.0091 到 $0.8400 **跨度 92 倍**（`etsy` HIGH/91 → $0.0099，`mito red light` HIGH/100 → $0.3700）。**④ 分界线在月搜 2 万，且与 12.5a 独立算出的 $0.12 CPC 线重合**（互为交叉验证）：<5 千 → CPC $0.2411/$6 买 25 个点击；5 千-2 万 → $0.1642/37 个；2 万-10 万 → $0.0620/**97 个**；10 万-50 万 → $0.0520/115 个；≥50 万 → $0.0097/617 个。系列级门槛实测 2 万线：通过 31 条 CPC **$0.0396**、拒绝 21 条 **$0.2145**（**差 5.4 倍**），单日达量率 54.8% vs 14.3%。**建议取 2 万而非 5 万/10 万**——更严门槛成本几乎不再改善（$0.0396→$0.0256）却把通过量从 31 条砍到 10 条。**⑤ 取法必须按系列所有品牌词变体取 max**：逐词判会误杀 `mgmresorts`(月搜 4400 但成交 $0.0148) 和 `jwpei`(1.21 万/$0.0430)，而这两条系列分别还挂着 `mgm rewards`(9.05 万) 和 `jw pei`(7.4 万)，取 max 即可救回；假阳性仅 2 个（`pique` 11 万却 $0.21、`rails` 6.05 万却 $0.1554，占通过组 6.5%，由 $6/50 点击闸兜住）。**⑥ 落地位置**＝SOP 阶段三原「单笔佣金 ≥$1.5」的槽位，API 已实跑通（78 词 8 国 **16 秒**，US 覆盖 93.4%、其余国家 100%），按商家缓存 30 天，查不到照 HM-D48 既定原则「放行并降分，不淘汰」。**踩坑记录**：`mysql -B` 批处理模式会把 `service_account_json` 里的真实换行转义成 `\n` 字面量导致 JSON 解析失败，改走 `TO_BASE64()` 传输；而 `TO_BASE64` 自带 76 字符换行同样会被转义，须在 SQL 层 `REPLACE` 掉——**以后从 CRM 取任何含换行的字段都走 base64** | 待 07 拍板（门槛取 2 万还是 5 万；是否同意删掉「单笔佣金 ≥$1.5」，见待确认项 #18） |
| HM-A04 | 2026-08-04 | **07 拍板判决口径：「少量预算($6)换一定量(50 点击),换不到订单就 pass,严一点没关系」+「被 CRM 其他人验证过的商家可以适当调高去做,没人验证过的宁愿保守」——落成 12.5a 判决矩阵**。拍板前先校正了两个我给错的前提：① **「CPC 差 3.4 倍」是加权错觉**——那是总花费/总点击口径，被 yz04 几条超大流量系列（Etsy 单日 1571 点击 / CPC $0.0099）拉低；换系列级中位数是 $0.3496 vs $0.1713（2.0 倍），只看测试期（≤50 点击的系列）是 **$0.3686 vs $0.2824，仅差 1.31 倍**。yz04 8/4 数据给出机制：CPC 与点击量单调负相关（≥500 点击档 $0.0165、<10 点击档 $0.2446，差 15 倍），**低 CPC 是「跑出量」的结果不是原因**，测试期起点两人一样贵。② **「$6 买 50 点击」物理上做不到**——买 50 点击的中位成本 $9.77（wj07 $9.52），$6 实际购买力 20-24 个点击；能在 $6 内做到的 yz04 仅 8.6%、wj07 2.2%。**但按 CPC 分层后该目标在低 CPC 商家上成立**：CPC<$0.12 的 86 条里 89.5% 能攒到 50 点击、平均 2.1 天，29.9% 真在 $6 内做到；CPC≥$0.12 的 322 条只有 30.7% 能攒到、**$6 内 0 条**。**另外验证并否掉了「调低出价上限换更多点击」的设想**：两组系列出价上限 $0.97/$1.16 而实际成交 $0.04-0.70，**上限高出一个数量级、压根没触发**，CPC 由拍卖与质量得分决定；yz04 低 CPC 系列 CTR 高达 18.7-53.6%（品牌词自投特征）、预算利用率 80-155%（跑满而非被卡）。**核心产出＝二维判决矩阵**（全库 7 月新建 2337 条实证）：已验证×低CPC 命中 65.2%/ROI 130.9%、已验证×高CPC 44.2%/50.2%、未验证×低CPC 44.8%/117.9%、**未验证×高CPC 29.3%/ROI −10.6%（768 条花 $7,311 净亏 $772，四格里唯一亏钱的）**。故规则比 07 原话更精确：**不是「未验证就保守」，而是「未验证＋高 CPC 才砍」**，未验证但低 CPC 的 306 条 ROI 117.9% 不该砍。「已验证」信号强度量化：命中 50.5% vs 33.7%、ROI 81.0% vs 38.2%（1.5 倍 / 2.1 倍），且**第一个验证人贡献最大**（0→1 人 33.7%→46.1%，3+ 人才 56.0%），判定用布尔值即可不必加权人数。闸门定价曲线定出硬顶拐点：$6→41.3% 误杀、$9→26.3%、$10→19.4%、**$11→14.4%**、$14→7.5%、$18→5.0%，$6→$11 每 $1 换回 5-7 个百分点、过 $14 只剩 0.6，**故已验证商家硬顶取 $11-14**；点击闸 50 个对应 22.5% 误杀，07 确认可接受。**实现前提暴露一个卡点（12.5c）**：矩阵依赖的「团队已验证」信号 Hermes 三个数据源全拿不到——共享表格「已测记录」374 行**全部 `source=hermes-auto`/`owner=wj07`**（其余 19 名员工走 CRM 不登记）、`result` 372 行全是 `observing`、`roi` 填充率 **0%**（等于只登记不结算的坏功能）；「白名单/推荐商家表」389 行人工维护质量不错但覆盖窄且**只有商家名没有域名列**。**07 拍板走 CRM 只读 API `/api/team/verified-merchants`、Hermes 每日 08:30 拉、本地缓存兜底**（走 HTTP 不直连库，不违背 v1.2 解耦初衷），字段与匹配键约定见 12.5c，新增待确认项 #16；「已测记录」回写修复 07 定为**先不做**、等通道落地一并处理。**遗留一处内部冲突（待确认项 #17）**：07 同时选了「CPC 硬限保持 $0.3」与「已验证商家只卡总花费不卡 CPC」，而高 CPC 商家实际成交常在 $0.3 以上（07 举例的 `231-MUI1-VisitorsCoverageInc` 实际 CPC $0.44-1.99，其 `max_cpc_limit` 被 07 本人设为 1.58、全库他人均 0.30），上限锁死则预算花不出去；暂按「未验证 $0.3 / 已验证放开至 $1.0」实现待确认。同一系列也给出放开的边界：7 月下旬 CPC 飙至 $1.30-1.99、7/26 花 $9.98 只买 6 个点击后连续零订单，wj02 用上限 $1.27-1.42 跑同商家三次全亏——**「已验证就能加价」必须有天花板**。本次仍未动任何线上配置 | 口径已拍板；#16/#17 待落实 |
| HM-A03 | 2026-08-04 | **07 质疑「这个你是不是把软删除也算上了？朱文欣没有这么多」——确实算上了，HM-A02 的「池架构缺口」整节作废**。`platform_connections` 有 `is_deleted`（tinyint 默认 0），我的查询只筛了 `status='connected'`。按 `is_deleted=0` 重算：**wj07 只有 7 个有效连接（CG#280 / EV#100 / LH#17 / LH#277 / MUI#83 / PM#171 / RW#20），恰好就是 Hermes v1.4 池表里的那 7 个——Hermes 一个都没漏，不存在「少接了池」**。被我误算成缺口的 15 个连接（CG ×7、RW ×4、PM ×1、CF ×2、AD ×1）7 月 `camps_spent_july` 全为 0、花费全为 $0.00，确系废弃记录。yz04 同样数错：真值 **10 个**有效连接，多账号结构是 **MUI ×2 / RW ×2 / LB ×2**，BSH/CG/LH/PM 各 1——不是原记的 MUI ×3 / RW ×3 / LB ×3 / LH ×2 / PM ×2。**顺带自查了一个更危险的连带风险并已排除**：「跨账号扩池」原本按 `platform_connection_id` 是否不同判定，而 yz04 有 5 组同名账号的新旧成对记录（LB/xukewei、LH/lanshao、MUI/haven-hearth、PM/yongpeilan、RW/thekhanhnguyen），完全可能把「同一账号的新旧两条记录」误计为跨账号，从而让 421.9% 这个核心数字失效。改用 `平台 + TRIM(account_name)` 严格重算（`chk_expand_strict.sh`）：**三组数字一字未变**（跨账号 11 商家/24 系列/ROI 421.9%，同账号 16/33/129.7%，单系列 238/121.5%），误判商家 **0 个**——因为那些 `is_deleted=1` 的旧记录 7 月一条系列都没跑过，yz04 7 月全部有花费系列都挂在 10 个有效连接上；全库统计（yz04 11 / wj07 5 / wj10 4）同样不变。**12.6 第 3~5 条结论全部成立。** **建议性质随之改变**：原以为「复制 api_key 即可补池」是本次唯一不需拍板、改配置就能做的动作，实际是**要 07 去联盟新开第二个账号**（成本与周期都高得多）。改后落地顺序：① 先只在 LH 上启用扩池（两池均在跑，$361.84 + $84.13）验证 #14 触发线；② 若推广，按 wj07 7 月花费排优先级 MUI($410.79) > EV($380.65) > PM($249.50) > RW($208.38) > CG($8.22)。12.5b 整节重写并保留错因说明，待确认项 #13 重写，12.7 新增「`platform_connections` 有软删除列」边界条——**这是本次分析第三次踩 CRM 字段语义的坑**（前两次：`max_cpc_limit` 全空被误读、队列不可比），已记为通用教训：查 CRM 主表前先 `SHOW COLUMNS` 确认软删除列。本次仍未动任何线上配置 | 待 07 确认更正 |
| HM-A02 | 2026-08-04 | **07 纠正 HM-A01 里「同商家复投」的误读：「蓝永培是同商家同联盟但是不同账号开……他的联盟账号也是不同的，只是联盟相同」**。原文把「一个商家开了 ≥2 条系列」笼统算成复投，得出「27 个商家贡献 67% 净利」——**这个口径把两种完全不同的动作混在了一起，已作废**。按第二条是否换 `platform_connection_id` 重新拆分（`chk_multiaccount.sh` / `chk_expand2.sh`）：**跨联盟账号** 11 商家 24 条系列，花 $3,345 → 净利 $14,114、**ROI 421.9%**；**同一账号再开** 16 商家 33 条，花 $1,883 → 净利 $2,442、ROI 129.7%；**只开一条** 238 商家，ROI 121.5%。**同账号再开（129.7%）与只开一条（121.5%）基本无差别——「复投」本身不产生增益，增益全部来自换账号**；11 个跨账号商家只占 yz04 7 月花费 27.7%，产出净利 56.7%。最干净的证据是 Contiki：第一条在 `xukewei` 账号跑 6 天花 $23.92 零出单（按任何止损线都该关），7/10 换 `dominickessaryiagdot` 账号重开，21 天赚 $1,056（ROI 1193%）——**「商家不行」与「商家在这个账号上不行」是两个判断，Hermes 现在当成一个**。另两条新发现：① **扩池触发线**——14 个被扩池商家的第一条平均净利 $1,127 / 203 单，473 个未扩池的仅 $37.6 / 22 单，差 30 倍，说明是「跑出量级才加开」而非「盈利就加开」；第二条不追求复制原成绩（12 组配对 8 赚 4 亏），但平均只投 $44、最大亏 $46 而最大赚 $1,056，是**投入受限、上限敞开的不对称赌注**，适合自动化。② **新账号冷启动**——`fortunavita`（yz04 于 7/24 新开的第 3 个 MUI 账号）7 月只建 5 条，全部同日建、全部是 MUI1 已验证商家、零新探索，4/5 出单、ROI 195.4%；同联盟老账号 `haven-hearth` 7 月新建 59 条命中 57.6%、ROI 仅 33.6%。**对 Hermes 的最重要含义**：5.1 选池规则第 3 条「高 ROI 扩池」写于 v1.1 双池时期，「另一个池」指的正是同联盟另一个账号，**机制方向本来就对**——我先前写「无复投机制」是错的；真正的缺口是 v1.4 全联盟入池时每个新联盟只复制了 1 个 api_key，**扩池在 LH 之外的 6 个联盟里架构性无法执行**。而 wj07 名下在 CRM 已有 connected 账号 **CG 8 个 / RW 4 个 / PM 2 个**，复制 api_key 即可补齐，无需新申请（缺口表见 12.5b，含平台段序号扩到两位数会撞 5.6 归因解析的风险提示）。全库看跨账号扩池几乎只有 yz04 在做（11 个商家，其余 15 名员工多为 0）。新增待确认项 #13/#14/#15，本次未动任何线上配置。**⚠️ 本条末段「CG 8 个 / RW 4 个 / PM 2 个、复制 api_key 即可补齐」已被 HM-A03 推翻作废**（漏过滤 `is_deleted`，真值 7 个且 Hermes 全已接入）；其余各条经严格复核成立 | 待 07 拍板（#14 触发线 / #15 新账号策略；#13 已按 HM-A03 重写） |
| HM-A01 | 2026-08-04 | **07 要求「对比 CRM 里佣金最高的蓝永培(yz04) 与 wj07 七月数据，把可用于决策重构的信息落进 Hermes 设计方案」——新增第十二章证据基线**。分析踩了两个坑并已修正，都写进第十二章的边界说明：① **`max_cpc_limit` 全空被误读成「不设出价上限」**——07 当场指出「不是真的为空，是表格数据没返回」；查证确认全表填充率仅 14.6%、11 个用户 100% 空，属字段未回流，原结论已撤回；顺带发现 yz04 的 `keywords` 0 条、`ad_creatives` 0 条、`cookie_duration` 0/295 同样是空洞，**关键词/文案策略在 CRM 侧查不了**。② **队列不可比**——yz04 名下 118 个 Google 账户的最早系列建立时间全部落在 2026-06-26 13:34 同一分钟（批量纳管已跑过的老系列），而 wj07 是 3 月起陆续建、7/28 单日新增 56 条；不分队列就是拿「已通过筛选的老系列」比「还在测试期的新系列」，会得出相反结论（例如单均佣金混看是 $4.95 vs $21.60，同队列下变成 $3.60 vs $4.47）。**核心发现**：（a）**8 天是分水岭**——只看 7 月新建队列，4-7 天档 ROI 仅 11.3%、8-14 天跳到 138.4%、15 天+ 达 356.7%；活过 8 天的 56 条贡献 yz04 新建队列净利的 98.8%；活过 8 天率 yz04 25.2% vs wj07 0.5%（差 50 倍）。（b）**Hermes 现值 `test_days_cap=2` / `test_spend_cap_usd=9` 回溯误杀 18.4% / 28.9%**（后者损失佣金 $26,976），而 3 天线只误杀 9.5%、损失 $637。（c）**参数组合自锁**——「$2 日预算 + CPC 硬限 $0.3 + 观察期 2 天」最多只能积累约 13 个点击，而首单前需要中位 32、p90 111 个点击；wj07 的真实结果印证：122/192 条系列累计点击不到 20 就停，该档出单率仅 12.3%——**Hermes 目前在复刻 wj07 的失败模式**。（d）**出单率是商家级属性**——11 个共同商家上两人转化率几乎相同（2.44% vs 2.33%），wj07 缺的是量不是转化能力。（e）首日 CPC 预测力很弱（五档出单率 69-84% 基本持平），不能当闸门。**建议改法七条见 12.5**（主判据改累计点击 ≥100~120、天数放宽到不早于 5 天、花费线降为硬顶、等待期考核后移到满 8 天、预算按 CPC 反推、单笔佣金 ≥$1.5 降级为参考分、增加已验证商家复投机制）——**均需 07 拍板后才改 env**，本次未动任何线上配置。CPC 差 3.4 倍的根因仍未查清（已排除出价策略/网络设置/学习期/质量分/追踪断链），要继续查须走 Google Ads API 拉关键词与实际出价。**注：最后一条「复投机制」的口径已被 HM-A02 纠正为「跨联盟账号扩池」，且 Hermes 本就有此机制，缺的是多账号池；12.5 建议现为八条** | 待 07 拍板（12.5 八条） |
| HM-D54 | 2026-08-03 | **07 反馈「Hermes 上飞书网关已经换人了，但是消息还是传给旧的飞书账号」——两个进程各一份 `.env`，换人只改了网关那一份**。Hermes 服务器上飞书出口有两条独立链路：Agent 网关（`~/.hermes/.env`，负责对话）已换成新应用 `cli_aae7288df4b81be8` + 新人 `ou_2d48d6f0c4fd2c572460d6e636c2d1a7`；而**审批卡、止损通知、每日复盘这些主动推送全部走 hermes-ads**（`~/hermes-ads/.env`，`lib/feishu.js` 以应用身份 `im/v1/messages` 私聊 `APPROVER_OPEN_IDS` 第一人），它仍是旧应用 `cli_aaa1c42aaef95bea` + 旧人 `ou_1355a3fca78447fcc1b54b605dac2f3e`。**先确认不是同一人换应用**：`contact/v3/users` 查两个 open_id 的 `union_id` 分别是 `on_e15f5db9…` / `on_34feffa1…`，确系两个不同的人（open_id 是应用维度的，所以 APP_ID/SECRET 与 open_id **必须成对更换**，只换其中一个必然发送失败）。**落地**：① `~/hermes-ads/.env` 三项同步为新账号（`FEISHU_APP_ID` / `FEISHU_APP_SECRET` / `APPROVER_OPEN_IDS`），备份 `~/hermes-ads/.env.bak.20260803_090851`；`APPROVER_OPEN_IDS` 同时是审批白名单，故旧账号即刻失去审批权、新账号获得。② **清空 `FEISHU_WEBHOOK`**（07 拍板）——原降级兜底指向旧群自定义机器人 `e43e1cd8-…`，主通道验证正常的前提下，留着它反而是消息漏回旧账号的口子。③ **存量待审任务的卡片要手动救回**：删掉 `app_settings._batch_approval_msg_id`（值 `om_x100b69c9f227fcb0b10b95ff34e6fac`，旧应用发的卡新应用 PATCH 不动）**还不够**——当时 52 个 `pending_approval` 任务的 `ad_runs.approval_msg_sent` 全是 1，`dispatchPendingApprovals()` 只捞 `approval_msg_sent=0` 的行，等一轮 pump 返回的是 `{"sent":0,"channel":"none","pending":0}`，即那批卡会永远留在旧账号、新人一个也看不到。须 `UPDATE ad_runs SET approval_msg_sent=0 WHERE status='pending_approval'` 后再 `POST /api/runs/dispatch-approval`，实测返回 `{"sent":52,"channel":"batch","cardMode":"sent"}`，新卡 `om_x100b69cb63cc90b8b11a46d814c119b` ✅。**换人操作清单里必须带这一步**。**实测**：新旧应用 token 均可取；用 hermes-ads 自己的 `config.js` + `im/v1/messages` 发测试消息返回 `code:0`（`chat_id=oc_fff4ac30…`），证明新应用具备 `im:message:send_as_bot` 权限 ✅；服务重启后 `/api/health` 正常 ✅。**顺带核查 CRM 侧**：`crm-mvp/.env` 无任何飞书配置，每日亏损提醒等不受影响。**教训（写进后续操作红线）**：以后换飞书接收人，`~/.hermes/.env` 与 `~/hermes-ads/.env` 两处必须一起改，且 APP_ID/SECRET 与 open_id 成对更换 | 待 07 验收 |
| HM-D51 | 2026-07-29 | **07 反馈「两个 MCC 的 CID 根本没有投满，但是 Hermes 那边却没有继续补」——CID 池与 MCC 实况长期失同步，三处根治**。先澄清一件事：Hermes **没有停止补量**，近 24h 发布了 71 条，CID 一释放 5 分钟内就有任务顶上（01:12 回收 3 个、01:15-01:16 补了 3 个）；卡点是它认为 79 个 CID 全占满、可用 0，89 个已批任务在排队。**「以为占着、实际空转」的三个来源**：① **池子只有一次性手动 seed，MCC 增减账号 Hermes 不知道**——老 MCC 下 `3597115243` 一直是 ENABLED 却从没进过池；反向更糟，`5509319692`（#25 hunderunde）在 Google 侧早已 **CANCELED**，Hermes 却还挂着 monitoring 占着名额，每 5 分钟报一次「回流表格里没有 campaign」没人看得见——这正是 HM-D38 遗留那条「403 无权限待 07 查账号」的真相，**不是没授权，是账号注销了**；老 MCC「Hermes 认为在投 25」减掉这个幽灵，恰好等于 07 截图里的 24 个系列。② **查 MCC 自身时 login-customer-id 解析不到**——`loginCustomerIdFor` 只按子账号查 `cid_registry.mcc_id`，拉子账号清单时 CID 就是 MCC 本身、表里查不到，回退成老 MCC 身份去查新 MCC，一律 403，等于新 MCC 的账号增减完全是盲区。③ **手动暂停的 CID 留位跟止损一样长**（48h）——07 手动关一般就是不打算再跑了，占 48h 是白占。**落地**：① 新增 `syncCidRegistry()`（`lib/cid.js`）+ `POST /api/cid/sync`，挂进每小时巡检（0.55 步，放在回池清扫之前，本轮新入池的 CID 当轮就能用）：按 MCC 拉 `customer_client` level=1，ENABLED 的自动入池、CANCELED/SUSPENDED/CLOSED 的摘出池（`status='retired'`），占用它的任务一并释放预占并转 paused（`fail_reason=account_<status>`），增减都进巡检汇总；一个 MCC 一次 GAQL，日耗约 48 次操作可忽略。**以后 07 在 MCC 里加子账号不用再通知 Hermes**。② `loginCustomerIdFor` 补一条回退：CID 不在 registry 但它出现在 `mcc_id` 列里，说明查的就是 MCC 自己，报自己。③ 留位时长改成**每条留位事件自带**——事件文本写 `留位 Nh`，`releaseStaleCids` 从文本里解析各自到期（没写数字的老记录按 `cid_hold_hours_earning` 算）；新设置键 `cid_hold_hours_manual`（默认 6h，设 0 = 立即回池）给手动暂停单独用。**部署实测（2026-07-29 09:4x CST）**：对账首轮 `3597115243` 入池、`5509319692` 摘出池且 #25 转 paused ✅；两个 MCC 全部查通、errors 空 ✅——老 MCC 39 个直属（ENABLED 30 / CANCELED 4 / SUSPENDED 5）、新 MCC 53 个直属（ENABLED 49 / CANCELED 2 / SUSPENDED 2），**两边 ENABLED 账号现已 100% 在池、无遗漏**（07 截图上新 MCC 的 50 vs 池里 49，差额是非 ENABLED 账号不是漏账号）；可用 CID 0→1、`bottleneck` 提示消失 ✅；新设置项生效 ✅。**顺带查清 #76 Evry Jewels**：归因佣金 $5.34/28 笔全 pending，最后一笔 07/26、最近一次拉到新单 07/27 04:40，已两天没动静，留位早已超期，下轮巡检自动回池（另注意归因是「商家 MID + 联盟池」口径，$5.34 含该商家其他来源的单，不全是这条广告的） | 待 07 验收 |
| HM-D50 | 2026-07-29 | **07 反馈「Hermes 有调整预算和 CPC，但是 CRM 这边看不到，无法同步」**。实例：lagos 那条 Google 后台已是 `101-MUI1-lagos-US-0728-8005348` / $8.00，CRM 界面仍显示 `393-MUI1-lagos-US-0726-8005348` / $2.00 / CPC $0.0000，而且 07-28 22:12 刚同步过——**同步跑了，字段没更新**。查下来问题全在 CRM 侧，匹配没坏（认 `google_campaign_id`，改名不影响），是更新语句里压根没带这几个字段：① 每天 06:00 的 daily-sync 对已存在的 campaign 只更新 `google_status`，`campaign_name` / `daily_budget` 一个字都不写；② 这两个字段只有手动点「同步MCC」走 Sheet 那条路才更新；③ `max_cpc_limit` 更彻底——**只有人在 CRM 界面点铅笔改过才有值**，CRM 跑的 GAQL 里没有任何出价字段（出价在 `ad_group.cpc_bid_micros` 上，要单独查一轮），实测全库 11647 条里 10199 条是 NULL，07 截图里的 $0.0000 就是这么来的。另外 CRM 拉数据的 GAQL 带 `metrics.cost_micros > 0`，当天零花费的连 API 这条路都拉不到。**两边都改**（07 拍板「两边都做」）：① **Hermes 主动推**（`jobs/push-crm-state.js`，`ad_runs` 加 `crm_push_fp` 列存「系列名\|预算\|CPC\|状态」指纹，只推变化过的，cron `3-59/10` 每 10 分钟一次、错开 pump 的 `*/5` 留出发布时间）——**用攒批定时推而不是每次 mutate 完就推**：改预算/CPC/暂停散落在 adjust-run、auto-tune、monitor-runs、pump-runs、sync-remote-state 五个地方，逐个挂钩子既容易漏、又会把 CRM 的可用性绑进 Hermes 的关键路径（CRM 挂了不该让止损失败）；攒批推只有一个入口，推失败不改指纹、下一轮原样重推，天然自愈。**只推名字/预算/CPC 不推状态**——状态有 CRM 自己的同步链路在管，两边都写会打架。② **CRM 新增 `POST /api/hermes/campaign-state`** 按 gcid 收单更新；查不到的 gcid 只回报不代建（代建要猜 `user_id`/`mcc_id`/`user_merchant_id`，属数据真实性规范里的臆测）。③ **CRM daily-sync 的 API 分支补写 `campaign_name` + `daily_budget` 做兜底**——这轮本来就把 `campaign.name` 和 `campaign_budget.amount_micros` 查回来了（`sync.ts` 的 GAQL 里有），只是没落库，补上零额外 API 成本，还顺带覆盖 07 直接在 Google 后台改预算/改名的情况。**实测**：179 条已发布广告，CRM 里能找到的 176 条名字/预算/CPC 三项零差异；另 3 条是 CRM 还没同步进来的新广告，daily-sync 建行后下一轮补上 | 待 07 验收 |
| HM-D49 | 2026-07-28 | **07 要求「批量审批要求放到一个批量审批页，不要分开」**——飞书里同时躺着一张 12 个任务的卡和一张 10 个任务的卡（#283-#292），得分两次点。两个原因叠在一起：① `dispatchApprovalCards` 里 `PER_CARD = 12` 硬切分页，22 个任务就是 12+10 两张；② 每轮名单补货（HM-D37）只对**这一轮新建的**发卡，上一轮没批完的那张还挂在那儿。分页本来是为了绕开飞书卡片体积上限，而撑大体积的是**每个任务占 5 行、还整条贴联盟链接**。落地：卡片改成**一行一个任务**（`#283 Cub Cadet　cubcadet.com · US · RW1　$2/天`），完整文案/系列名/联盟链接/追踪后缀都在审批页上看——实测 200 个任务的卡片才 13KB（上限 30KB）、按钮链接 553 字符（nginx 上限 8k），**分页整个去掉**；卡片覆盖的改成**当前全部待审**而不只是新建的；`message_id` 存进 `app_settings`（`_batch_approval_msg_id`），已有卡就 PATCH 原地刷新、没有才新发，飞书里永远只有一张；审批动作之后自动刷卡（`refreshBatchApprovalCard`）——还有剩的改成剩下的、清空了改成「已全部处理」，批量页按钮、单条按钮、回复「批准 #N」三条路都挂上了，不然 07 批完一部分卡上还挂着已处理的任务，看着像没生效。`feishu.js` 的 `sendAsApp`/`sendFeishuCard` 改为返回 `message_id`，新增 `updateFeishuCard()`；webhook 降级通道拿不到 message_id，那种情况退回重发。**顺带发现一个部署事故**：Hermes 是 **systemd**（`hermes-ads.service`）托管的，而我这一天用的是 `pm2 restart hermes-ads`——pm2 根本没装，命令被 `>/dev/null 2>&1` 吞掉了，服务从 12:06 起就没重启过，**HM-D45b 的改动推上去后一直没生效**，本次一并 `systemctl restart` 才真正加载 | 待 07 验收 |
| HM-D45b | 2026-07-28 | **07 看审批页「系列名怎么还是这样」——页面上是 526/527，而当天实际发布出去的是 163**。查下来 HM-D45 的发布改号是对的（152-163 严格连号、日期都是 0728），坏的是**发布之前**那个占位号：建任务时用 `nextSeq()` = 全表 `MAX(seq)+1`，起点是 HM-D27 接管的 CRM 号段（310），再随建任务一路爬，名单深到 92 个时占位号已经飙到 536——等于给 07 看了个跟上线号差三百多的假号；日期段同理是建号那天（0723 的名字挂到 0728 还在）。**发布顺序是 FIFO，所以真号在建任务时就算得准**：`forecastSeq(runId)` = 已发布最大号 + 排在自己前面的待发布任务数；`nextSeq()` 改用它，新任务的占位号不再乱爬；`previewCampaignName(run)` 给发布前的展示用，序号与日期段一起刷成「按当前排队位置 + 今天」，已发布的原样返回不动。审批页（`approve-server.js`）、飞书单条卡、批量卡三处都改成显示预估名并注明「按当前排队位置预估，发布那一刻重定」——**之前只有飞书卡带这句提示，审批页是裸贴库里的名字**，而 07 看的正是审批页。存量 78 条待发布任务的占位号一并刷成 164-241 | 待 07 验收 |
| HM-D48 | 2026-07-28 | **07 给了《选商家 —— 业务流程拆解》，要求「让 Hermes 按照这个来筛选」**。SOP 五个阶段二十个检查点，逐条对下来 Hermes 缺三样数据：商家级拒付率、ATC 在投同行数、客单价——**这三样全在 CRM 手里**（拒付商家页、ATC 快照表、政策标签表），而 Hermes 在另一台机器上、CRM 的 MySQL 只监听 127.0.0.1。落地：① **开只读快照通道**（CRM 侧新增 `/api/hermes/merchant-intelligence`，Bearer 鉴权，一次全量返回政策标签 3193 条 + 拒付率 5202 条 + ATC 快照 341 条；Hermes 侧 `sync-crm-intel.js` 整表替换落进本地 SQLite，每天 07:50 cron 跑，比队列重建早 10 分钟）——**不直连库**是为了 CRM 挂了不影响 Hermes 选品。② **阶段二初筛**（`lib/sop-screen.js`，队列重建时对全池 1.9 万个商家跑，只查本地表）：政策标签违规、拒付率 >25%（口径与 CRM 拒付页一致——按佣金金额不是按笔数）、ATC >10 个同行、商家名是货号。③ **阶段三三关**（`lib/sop-deep.js`，只在建任务前对少数候选跑）：SerpApi 验品牌搜索量（第一条自然结果是不是官网 + 第一屏有没有第三方声量）、联盟条款允不允许投品牌词、单笔佣金 ≥$1.5（推荐表有带单佣金就直接用，没有就 puppeteer-core 抓官网首页价格算，结果缓存 30 天）。**07 拍板「查不到就放行并降分，不淘汰」**——现查失败不该让商家背锅。④ **阶段一取号顺序按 SOP 改成白名单 → 推荐表 → 联盟平台三档严格不重叠**（改前是「推荐表 5000-rank 压过白名单 +1000」，与 SOP 刚好反了），平台内 MUI→LH→LB→EV→CG→RW 也改成档位而非加权分。⑤ **阶段四国家档位对齐**：新增 `countryFromName()`（SOP 4.1.1「先看商家名里的国家词」——Hornby UK 的域名是 hornby.com，只看后缀会漏判成 ES），「不要投」档的主场国按 SOP 4.2 换同语言邻国（NL→BE、BR→ES/US、TH→PH/HK、SE→DE/PL）而不是整个放弃。⑥ **阶段五筛选台账**（`screen_ledger` 表按 SOP 的 A-T 二十列记，通过和淘汰都记，一天一个「域名+国家」一行，重跑只更新）——SOP 原话「淘汰的也要登记，不然下个月这个商家又会被翻出来重判一遍」；回写 Google 工作表**只回写走完三关的那批**，阶段二一天判 2.2 万行是机器扫全池的量，匿名写通道逐格键入写不动也没人看，其余 `GET /api/screen-ledger` 随时查。**两处纠偏**：`looksLikeCodeName` 初版把 Booking.com、PROMOFARMA 一起杀了，拆成 `isObviousCode`（ASIN/货号硬淘汰）+ `needsBrandCheck`（像域名的强制走阶段三品牌搜索验证）；`Revshare 70%` 这类分成比例被当成佣金率会算出天价单笔佣金再打上「优先」标签，>50% 一律当未知（数据真实性规范里记过这个坑）。**07 拍板的三件事**：CRM 违规名单 854 个先报给 07 复核再拉黑（**实测发现全部已被团队黑名单挡住，无敞口**）、缺数据现场查、台账本地表 + 回写工作表。**实测**：全池 22350 行判定，通过 17266、淘汰 5084（无可投国家 2497、标签违规 1511、亏损品类 840、团队已测过 122、拒付率过高 87、禁止品牌词 22、无品牌词 5）| 待 07 验收 |
| HM-D47 | 2026-07-28 | **07 反馈「这个拒登的信息返回要是能让我看懂的」**。起因是令牌池（HM-D46 后续）把发布打通后，飞书连着弹三条 `GoogleAds 400: Request contains an invalid argument. [{"@type":"type.googleapis.com/google.ads.googleads.v21.errors.GoogleAdsFailure","errors":[{"errorCode":{"policyFindingError":"POLICY_FINDING"},…}]}]`——整段 JSON，看不出哪个商家、为什么被拒、要不要管。拆开真实报文发现关键信息埋在第四层：`details.policyFindingDetails.policyTopicEntries[].topic` = `DESTINATION_MISMATCH`，`evidences[].destinationMismatch.urlTypes` = `FINAL_MOBILE_URL`——**不是商家违规，是联盟后缀在手机端把 calvinklein.com 跳去了别的域名**，Google 判网址不一致。落地：① 新增 `src/lib/ads-error.js`，`explainAdsError()` 把政策条目 + 证据 + requestId 拆出来翻成「哪个环节、什么原因、Hermes 怎么处理」，非政策类错误（账号停用、没权限、没绑卡、重名、认证失败、令牌被禁）走 `ERROR_HINTS` 词表各配一句人话与处置建议，认不出来的至少区分「Google 侧」还是「网络/代理侧」；`failureLine()` 补上商家/国家/域名/系列名，飞书里一眼定位。② **建广告当场被拒不再算「发布失败」**——`POLICY_FINDING` 直接进拒登库（HM-D40），同域名今后不再排期，避免同一个商家换个国家再撞一次墙（#163/#164/#165 就是 Calvin Klein 的 HK/TW/JP 各撞一次）；报错落库也存人话，后台列表和日报同样看得懂。③ **通知按域名合并**，一轮多个任务撞同一个坑只讲一遍、列出受影响任务号。④ 政策词表从 13 条补到 38 条（处方药、门票转售、烟草、假冒等），`recordRejection` 去重改为「有 campaignId 按 campaign 认、没有按域名认」（建广告时就被拒压根没有 campaignId，原逻辑会一个国家躺一条）。补录历史 5 条政策失败入库、合并 Calvin Klein 的 3 条重复、旧记录英文政策名刷成中文 | 待 07 验收 |
| HM-D45 | 2026-07-28 | **07 反馈「广告系列名都乱七八糟的，完全没有按照顺序来。要求系列名的顺序和任务顺序一样，我发布的 001 任务序号就是 001，任务序号只认发布的序号」+「任务发布为什么一下发 100 多号、一下发 200 多号，是不是队列弄错了」**。两件事其实是一根因：序号在**建任务**时分配，而 HM-D42 把发布顺序改成了**按推荐表名次抢 CID**，于是 #151 和 #227 交替上线，号码看着毫无规律。落地（07 逐项拍板）：① **序号改在发布那一刻给**——`nextPublishSeq()` 只数已发布的、Hermes 自成一套从 001 连起，`finalizeCampaignName()` 同时重写首段序号与倒数第二段 MMDD（与 HM-D44 合并成一次改名），发布成功才写回、失败不占号不留空号；建任务时的号退化为占位，审批卡标注「序号/日期发布时重定」；顺带砍掉每建一个任务就拉一次 CRM sheet 对齐号段的网络往返。② **发布顺序改回按任务号 FIFO**（撤销 HM-D42 的 priority 排序）——推荐表名次在「建哪些任务」那步已经生效，到发布这步 07 要的是先来后到、好对账。③ **与 CRM 重号 07 拍板接受**（两边靠 campaignId 匹配数据，平台段 + CID 能分清）。④ **历史 140 条按发布时间重排 001-140**（`tools/renumber-published.mjs`，跑前停服务防 pump 抢号），**HM-D27 接管的 5 条 CRM 存量不动**（它们的号属于 CRM 号段，改了会动到 07 自己建的系列）；Google 后台同步改名时**打爆了配额**（BASIC token 15,000 次/天，当天已被 HM-D43 那场 403 风暴烧掉大半），34 条成功、106 条 429，遂改为**巡检自愈**：状态同步（HM-D39）顺带比对远端名字，不一致就推本地名过去，`rename_sync_per_round`(15) 限量摊到每轮，约十轮补齐。**排查中挖到一个比编号严重得多的问题**：新 MCC `988-504-7597` 名下 **37 个在投广告一个都不在回流表格里**，而花费哨兵读表判 $9 线、读不到直接 return——**这批广告的止损从上线起就是失明的，烧到多少都不会自动关**。一开始判断成「MCC Ads Script 只装在老 MCC 上」，**07 指出「每个 MCC 都有一个表格」并给了新 MCC 的表**（`1sL9EZBP…`，结构与老表一致、公开可读、正好盖住那 37 个）——真因是 **Hermes 只读了一张表**。落地：`config.crmMetricsSheetIds`（`CRM_SHEET_IDS` 逗号分隔，默认老表+新表）+ `loadOne()` 逐表读、按 campaignId 合并，一张挂了不连累其他 MCC，全挂才抛错；同时保留 `guardStatsWhenSheetBlind()` 作二道兜底（表里没有就直查 Ads 喂哨兵，`guard_api_interval_min`(15) 节流防吃光配额）。**实测**：覆盖 361 个 campaign，在投 63 个命中 62（仅剩 #25 那个 403 老账户），当时零出单超线 0 个、最高 $8.63 正在逼近线上 ✅。**运维铁律：以后每加一个 MCC，必须把它的表格 ID 追加进 `CRM_SHEET_IDS`，否则该 MCC 全体广告止损失明** | 待 07 验收 |
| HM-D44 | 2026-07-28 | **07 需求「系列名是审批的时候就建好的，但发布可能要两三天后，我希望广告系列里面的日期是发布日期」**。名单模式（HM-D42）下建名与上线彻底解耦——名单 100 深、CID 30-79 个，任务躺两三天很正常，`0726` 的名字挂到 0728 才上线，回头对账分不清哪天开的。落地：`withPublishDate()` 只替换 6 段命名里倒数第二段的 MMDD（序号/平台/商家/国家/MID 全不动，从后往前定位避免商家名含横杠），pump 发布前改名并写回本地、记一条事件流水；审批卡的系列名后面标注「日期段以实际发布当天为准」。**未动序号**：首段 seq 也是建任务时按「本地历史 max+1 与 CRM 在投 max+1 取大」分配的，同理会有滞后甚至与 07 手工建的撞号，但改它要在发布时再查一次 CRM sheet（每条一次网络往返），先留给 07 决定 | 待 07 验收 |
| HM-D43 | 2026-07-28 | **07 反馈「新 MCC 可以投放了，但是没有用 100 个待投放任务，而是重新建任务」——根因是新 MCC 的 CID 全部 403，把任务泵卡死 5 小时**。表象是 Agent 又去手工批量建任务（还撞上 create-run 互斥锁全失败），但名单其实有 112 个任务、CID 池 49 个空位，谁也没接上。**真因链**：① `login-customer-id` 全局写死 wj07 MCC `941-949-6301`，07 新加的 MCC `988-504-7597` 名下 49 个子账号每次调用都 `403 The caller does not have permission`——实测 SA `qmy123@` 对新 MCC **是有权限的**（`listAccessibleCustomers` 里有 9885047597），换成新 MCC 的 login id 立刻 200，纯粹是请求头报错了 MCC；`cid_registry.mcc_id` 本来就记着归属，改成按 CID 查 MCC（10 分钟缓存）即通。② `reserveCid` 对每个任务都把所有候选 CID 挨个远程实查，49 个必 403 × 94 个待投任务 = 四千多次注定失败的 API 调用，**一轮 pump 跑了 5 个多小时**；改为 403 即标记 `is_available='N'`（带 note，走 6h 自愈复查）+ 单次预占最多试 15 个候选。③ **最危险的一环**：pump 是锁内任务，卡住后每 5 分钟的 cron 全被「pump 正在运行中」挡回（cron.log 里 23 条无人看），**花费哨兵连续 5 个多小时没跑**，零出单超线的广告没人管；新增 `pump_round_budget_min`（默认 4 分钟）超时收工、剩下的下一轮接着做，并给互斥锁加看门狗——被挡回时算持锁时长，超 15 分钟发飞书报警（每锁 30 分钟一条）。**修复后实测**：新 MCC CID 直查 200 ✅；4 分钟内自动接位发布 10 条（#131-#140，全部落在新 MCC CID），在投 27→37，名单 102/100 自动补齐 ✅。**遗留**：老 MCC 的 CID `5509319692`（#25 hunderunde）仍 403，是该账号本身没授权给 SA，需 07 在 Google Ads 后台处理 | 待 07 验收 |
| HM-D42 | 2026-07-27 | **07 反馈「我让 Hermes 把审批后待投放的广告队伍扩充到 100，Hermes 这么回我，估计是这个待投放的队伍没做好流程，才让 Hermes 有误解」——待投名单概念澄清 + 补货分轮**。07 说「现在把等待队列数拓张到 100 个」，Agent 改的是 `hard_daily_runs`/`daily_runs`（15→100，每日新建配额），`waitlist_target` 仍是 30，等于**答非所问且动了 Owner 风控栏杆**；随后 07 问「有多少广告缺少」，Agent 又只报了 `pending_approval`=2，实际名单是 29（2 待批 + 27 已批等 CID），听着像名单见底、其实是快满了。**根因是三个数长得像**：待投名单（库存）/ 每日新建配额（速度）/ CID 池（真正能同时在投几条），skill 的设置映射表里只有后两个，没收录「等待队列」这个说法。落地：① `GET /api/waitlist` 一次给全——名单 count/target/低水位、待批与已批等 CID 的拆分、`cid_pool` 总量/可用/占用、要改哪个 key，以及 `bottleneck` 直接把「CID 池已满、N 个已批任务发不出去、扩容要往 CID_List 加子账号」写成人话；② skill v1.13.0 第 5 节加「三个数别搞混」对照表 + 判断口诀（说库存→名单、说速度→配额、说投不出去→先看 CID 池），设置项 desc 里互相点名（`daily_runs` 注明「不是待投名单」、`waitlist_target` 注明 07 的原话说法）；③ 新增 `waitlist_max_build`（默认 30）——名单目标调到 100 后缺口有七十个，一个任务要 1-2 分钟、一轮全建完要两个多小时，会把下一轮 :45 的 cron 撞锁挡掉，改成每轮最多建 30、分几轮爬到 target，返回带 `remaining`。**已按 07 本意改到位**：`waitlist_target` 30→100、`waitlist_low` 10→50、`waitlist_max_build` 30。**同时要交代的事实**：名单是库存不是产能，CID 池只有 30 个（当前 0 可用），名单堆到 100 也只有 30 条能同时在投，07 新加的 MCC 988-504-7597 子账号 CID 填进 CID_List 表后才是真扩容。**07 追加定调「待投名单不预占 CID，有 CID 被释放时就从名单里选一个投，所以名单有多少个都无所谓」**——查证后现状确实如此（CID 只在 pump 发布那一刻 `reserveCid`，`approved` 任务不持有任何 CID，池空就留在 approved 每 5 分钟重试），按这个模型补了四处：① **发布顺序改按价值**——名单深了以后「谁抢到刚释放的 CID」才是真正决定测什么的地方，原来是 `id ASC` 先建先上，等于几天前建的低价值任务永远插在新推荐商家前面；新增 `ad_runs.queue_priority`（建任务时从队列带过来，推荐表层 5000−rank），pump 改 `queue_priority DESC, id ASC`，已回填 43 个存量任务（当前队首是 rank 28 的 lagos-US）；② **出库前复查拒登库**——任务可能在名单里躺几天，期间同域名在别国被拒进了库，不复查就会拿刚回收的 CID 去投注定被拒的广告，命中即 cancelled 不占 CID；③ **删掉「CID 不足已排队」飞书提醒**——名单常态就是排队等 CID（100 深时七十个在等），这个提醒本身成了刷屏源；改为只报**卡住**（池子有空位却发不出去=故障，6h 一条），排队状态每任务只记一条事件流水；④ 每日 09:30 复盘卡加一行名单/CID 池现状（待批、已批等 CID、池容量与空闲、昨日接位发布数） | 待 07 验收 |
| HM-D40 | 2026-07-27 | **07 需求「会有广告因为各种原因被拒登，我需要能给 Hermes 发这个图片，能自动放到拒登库，之后每次做广告避开这些问题」——拒登库**（07 附 311-MUI1-brooksbrothers 的 Google 拒登邮件截图：政策「目标页面不匹配」）。落地时先验证了一件比截图更值钱的事：**拒登状态可以直查**——`ad_group_ad.policy_summary` 带 `approval_status` 与 `policy_topic_entries`，截图上的信息 API 里全有（brooksbrothers 实测返回 `DISAPPROVED` / `DESTINATION_MISMATCH` / 证据 `FINAL_MOBILE_URL`）。所以做成两条入口一个库：① **自动发现**——每小时巡检按 CID 分组查一次（与 HM-D39 状态同步合并成同一条 GAQL，零额外请求），`DISAPPROVED` 即入库 + 暂停 + 回收 CID + 汇总通报；② **07 发截图**——Agent 读图取「系列名 + 政策名 + 说明」调 `POST /api/rejections`（中英文政策名归一到 topic code，默认顺手暂停，`pause:false` 可只入库）。**规避按域名整体生效**（同一域名换国家重测大概率还是拒）：`build-queues` 排队时过滤、`createRunFromQueue` 建任务时拦截（放在最前面，连 SerpApi 文案和后缀验证的钱都省），07 说「拒登 #N 解除」→ `POST /api/rejections/clear` 恢复。新表 `ad_rejections`（系列+政策唯一，含证据/截图路径/来源/解除备注）；skill v1.12.0 第 11 节。**上线首轮实测（2026-07-27 09:33 CST）**：**11 个拒登入库，其中 7 个还开着在跑**——ticombo 一家占 7 国（TICKET_RESALE 票务转售，属需资质的受限类目，US/ES/FR/DE/SG/AU/GB 全军覆没）已全部自动暂停 ✅；另有 beelivery（TOBACCO）、nexcess/mingwangknits/brooksbrothers（DESTINATION_MISMATCH，联盟跳板导致展示网址与到达网址不一致）已是暂停状态，只入库 ✅；5 个域名（ticombo/beelivery/nexcess/mingwangknits/brooksbrothers）从此不再排新任务 | 待 07 验收 |
| HM-D39 | 2026-07-27 | **07 需求「我在 CRM 手动的操作，要回传给 Hermes，两个系统要同步数据」——远端真值同步**。此前 Hermes 只认自己库里的状态，07 在 CRM 数据中心或 Google Ads 后台一手动开关，两边就分叉：Hermes 以为在投的其实早被关了（还在等它出单、占着名额），Hermes 以为关了的其实又被开起来了（脱离止损管理一路亏）。落地：每轮巡检开头按 CID 分组拉 Google 真值（一个 CID 一条 GAQL，顺带取拒登状态，见 HM-D40），**Google 为准回写本地**——① 本地在投 / Google 已关 → 跟随暂停（零出单回收 CID，出过单的留位等佣金回流）；② 本地已停 / Google 在投 → 跟随续投并**重新纳管止损**，同时记「**净亏基线**」：07 明知在亏还要开是他的判断，Hermes 下一轮秒关等于跟他打架，所以要在续投当时的净亏之上**再亏满 `stop_net_loss_usd`** 才自动关（中途被自动止损过则基线作废）；手动续投也不再受运营期 ROI<0 那条闸门约束，只受净亏额度约束。反向不需要同步——CRM 数据中心读的就是 Google 真实状态。**首轮实测**：#47 Cariloha、#48 FatAndWeirdCookie、#76 evryjewels 三个 07 在后台关掉的跟随暂停 ✅（都出过 16-76 笔单，CID 按 HM-D38 规则留位）；#75 mapleparking（07 在后台重开）跟随续投、基线 $4.67 ✅。**上线路上补了一刀**：#75 首轮没纳管成功——它昨天被止损关掉时 CID 已回收给 #100，`reacquireCid` 拒绝后原逻辑直接跳过；但广告在 Google 上照样花钱，不认它只会让它彻底无人管，改为**占不回 CID 也照常纳管**，只记「CID 占位冲突」事件并在通报里标注该账户下有两条 Hermes 系列 | 待 07 验收 |
| HM-D38 | 2026-07-27 | **07 要求「按规则有些不该停、有些过晚停，先做数据检查、系统检查」——全量对账后修三大破口**。**数据检查结论**：钱主要漏在「没人停」而非「停错」——误杀合计仅 $42（CRM 哨兵 6 个，佣金滞后 12-48h 到账），而 CRM 侧 119 个「已出单但持续净亏」的系列仍 ENABLED，累计净亏 **$1,754**（piquelife 一个 -$387），两系统都没有这条规则；另有 87 个哨兵想停停不掉（jy 组 MCC 无 SA 凭据，已花 $1,106、超线浪费 $323）、164 个窗口外零出单 ≥$9 老系列（约 $4,500）、10 个因「同商家有单就豁免」漏判（$228）。**系统检查发现的根因与落地**：① **运营期没有净亏上限**——只有断流规则，持续出单的亏损系列断流天数永远归零，#22 hobobags 复活后从 -$4 一路亏到 -$23.10 才被 07 手动停；新增净亏 ≥ `stop_net_loss_usd` 或 ROI < `operate_min_roi`(0) 立即关，安全区（ROI>200%）按 07 要求仍豁免；② **「止损复活」被当作已通过等待期考核**（HM-D32 原设计），复活门槛只是 ROI 转正，等于给 ROI<100% 的亏损系列开永久绿灯——移除该豁免，复活门槛提到 `revive_roi_min`(0.2) 防关-开振荡；③ **Sheet 花费系统性低估**——比对 27 个系列的 Google 真值，Hermes 记 $6.88 时实际已 $9.06，偏差 $0.5-$2.31，$9 的线实际执行在 $9.5-$10.1（即 07 说的「实际 10 刀以上才关」，HM-D36 压缩的是巡检间隔、没解决数据源滞后）；monitor 与 5 分钟哨兵在花费 ≥ 线×`cost_verify_ratio`(0.8) 时直查一次 Ads API 取真值再判，只在临界点花配额；④ **Sheet 里没有该 campaign 时止损完全失明**（近 24h 434 次「无 campaign」），原逻辑直接 return——改为 Ads 直查兜底，两路都失败才告警；⑤ **出过单的任务止损后 CID 留位 48h**（`cid_hold_hours_earning`）等佣金回流判复活——#35 evryjewels ROI 转正要复活时原 CID 已被 #92 抢走；零出单仍即关即回收（07「热窗不用特意留」不变）；⑥ `pending_discount` 默认 1 → 0.85（hobobags 的 12 笔佣金全是 pending，靠未确认佣金把系列放回去继续烧）。**07 拍板口径**：ROI=(佣金−花费)/花费；净亏闸门除 ROI>200% 安全区外全适用；CRM 侧不做自动止损（「CRM 都是自己手动关停」）。**部署实测（2026-07-27 09:0x CST）**：新规则首轮即命中 #22 hobobags（运营期净亏 $27.81 → 关）✅；预演其余在投任务无误伤（#76 等待期净亏 $0.94 继续）✅；复活门槛 + pending 折算后 7 个 paused 任务全部不够格复活（ROI -5% ~ -89%，确为沙子）✅。**顺带发现待处理**：CID 5509319692（#25 hunderunde）Ads API 返回 403 无权限、Sheet 也无该 campaign，Hermes 既读不到也停不了，需 07 检查该账号；#79 rajapackde 后缀连续 219 次 stuck_on_tracker（tradedoubler/linksprf 跳板跟不动），广告在跑旧后缀 | 待 07 验收 |
| HM-D29 | 2026-07-23 | **07 反馈「建广告只要让我批量通过，不要一个一个通过」——HM-D26 批量卡有路径漏洞，根治**。排查：HM-D26 的批量卡只挂在 09:00 `planExecute`；07 平时在飞书让助手建任务走的是 `POST /api/runs/create`，每建一个即时发一张单卡——实锤当日 #40-#43、#49-#52 共 8 个任务全走单卡（#49-52 一分钟一张）。落地（不依赖 Agent 自觉，双保险）：① `create` 接口**默认缓发卡**（`defer_card=false` 才即时发），任务停在 pending_approval 且 approval_msg_sent=0；② 新增聚合器 `dispatchPendingApprovals` + 接口 `POST /api/runs/dispatch-approval`——把**所有待审未发卡任务合并成一张批量卡**（≥2 批量卡带全部批准/全部拒绝按钮，1 个则单卡），Agent 建完一批调一次；③ **任务泵每 5 分钟兜底**自动聚合发卡（120s 静默窗：最新任务刚建好可能同批未建完，等下轮，防一批被切成两张卡）——就算 Agent 忘了触发，卡最迟约 7 分钟合并送达、绝不会丢；④ skill v1.9.1 建任务章节改红线：建完一批必须调 dispatch-approval 一次、中途不许调。**服务器实测**：把当日被单卡轰炸的 #49-#52 重置未发卡 → dispatch-approval 返回 `{sent:4, channel:'batch'}`，一张 4 任务批量卡已发 07 私聊 ✅；pump 空跑 approvalDispatch=none ✅；4 任务 run_events 各一条「批量审批卡片已发送（4 个任务同卡）」✅ | 待 07 验收 |
| HM-D37 | 2026-07-25 | **07 需求「Hermes 的换链接也要用 CRM 现在的换链接方式」——移植 CRM 前一日跟链降耗修复（CRM commit 40979cf7）**。背景：Hermes 跟链与 CRM **共用同一 kookeey 账号**（HM-D06），CRM 侧排查发现 fetchChain 两个流量放大器（120KB 上限只停缓存不断 socket，多 MB 落地页全量流经代理；Accept-Encoding:identity 拒绝压缩放大 3~6 倍），修复后 CRM 实测单跳 -94~95%、全天消耗 11.3GB→2.1GB。Hermes 的 `suffix-follow.js` requestOnce 正是从 CRM 老版移植的，带同款问题。落地（对齐 CRM 实现）：① 攒够 120KB 立即 destroy 断开 socket；② identity→gzip + zlib 流式解压（gzip/br/deflate 兜住，主动截断触发的解压尾错按已够用完成）；③ 非 HTML 响应直接断开（旧 resume() 全量拉完）。浏览器兜底（suffix-browser.js）已有 CRM 两阶段拦截无需改；接口/判定语义不变，调用方（create-runs / suffix-pool）零改动。**追加（07 拍板「加」）**：冒烟顺带发现存量问题——run#81（ediblearrangements）落地停在 `r.linksprf.com/v2/go?t=<混淆参数>`（secprf.com 同族二段跳板）但该域名不在跳板名单，系统一直把跳板自身的 t= 混淆参数当后缀采信入池（改动前后行为相同，非本次回归）；已将 `linksprf.com` 加入 TRACKER_HOST_PATTERNS——浏览器兜底会等它跳离拿商家真参数，跳不走则如实报 stuck_on_tracker 触发换后缀健康告警，不再产可疑后缀。**加名单后实测**：受影响任务共 2 个——① #81 立竿见影，浏览器等到跳离拿到真实 Impact 参数（`im_ref=…&irgwc=1&utm_source=impact`）；池内 9 条 t= 垃圾后缀已回填过期时间作废，补货 cron 已为 #81 重新生成 5 条真后缀（在投广告下次点击增长换后缀时自动换上）；② #79 Rajapack DE 如实暴露为 stuck_on_tracker（停在 gutscheine.tradedoubler.com，HTTP+浏览器均跟不动）——它从发布起所有后缀都是垃圾、点击从未可归因，属 tatrck 同类「suffix 模式无法归因」商家，监控将按换后缀健康告警上报，是否提前暂停待 07 定 | 待 07 验收 |
| HM-D30 | 2026-07-24 | **07 反馈「到了告警线但同时在出单的，Hermes 直接关了，不看出单情况」（#35 evryjewels 凌晨 03:12 被止损暂停，当时已 6 笔佣金回流）——止损改为出单感知**。根因：HM-D28 佣金数据已入库，但 monitor 止损判断仍是毛口径（累计花费 ≥ spend_cap 即关），佣金没参与决策。落地：① **花费止损改净损口径**：净损 = 累计花费 − 有效佣金（approved+pending+paid，剔 rejected），净损 ≥ spend_cap 才自动暂停——有出单的任务天然多跑，ROI 回正的几乎不会被止损；② **天数止损只关零出单任务**：days ≥ days_cap 且有效佣金=0 才关；有出单的过测试期继续投（它已证明能出单，不是死测试）；③ **止损豁免通知**：毛花费/天数到线但被佣金豁免时，巡检汇总加「🟢 止损豁免（有出单继续投）」名单，**每任务只提醒一次**（run_events 查重），07 可随时「#N 暂停」「#N 止损改 $X」；④ **monitor 每轮开头先刷近 3 天佣金增量**（防「花费先到线、佣金还没回流」窗口期误关；失败不阻塞用库存数据）；⑤ `resume` 校验同步改净损口径（否则 #35 毛花费 $10.42 ≥ $10 恢复会被拒）；⑥ 暂停通知/事件流水带「花费 − 佣金 = 净损」明细；skill v1.9.2 加「止损是出单感知的」说明。**实测（2026-07-24）**：#35 佣金已累计 $10.16/33 笔 vs 花费 $10.42（净损 $0.26），已 resume 续投 ✅；**顺手排查出 #22 hobobags 同样被冤关**（佣金 $10.21 vs 花费 $6.38，净损 −$3.83 已盈利，0721 被天数线关），一并 resume ✅；其余 17 个 paused 任务佣金均为 0，属正常止损。已知边界：#35 刚恢复后 CRM Sheet 尚无当日点击行，monitor 报 stats_failed 只跳过不动作，出点击后自愈 | 待 07 验收 |
| HM-D31 | 2026-07-24 | **07 拍板完整止损标准 + 复活机制**（07 反馈「关停时确实没出单、后面出单 ROI 转正了却没有下一步操作」+ 给出关停标准：到达 9 刀/3 天/佣金−费用=−4/连续 7 天只有花费没佣金总 ROI<1.5/连续 3 天总 ROI<1；ROI 口径 = (佣金−花费)/花费）。**联网核对业内做法后确认 07 标准与主流框架一致**（零出单硬线 ≈ 业内「3×目标 CPA 零转化必杀」金标准；3 天 ≈ 72h 学习期共识；断流+ROI 双条件 ≈ 业内「CPA 连续 5 天超线才杀，单信号杀是最大误杀来源」；补了业内两点：pending 佣金拒付率 5-15% → `pending_discount` 折扣系数可调、复活门槛必须比关停严格防振荡）。落地：① `stopLossDecision`：零出单=花费 ≥ $9（`test_spend_cap_usd`）或 ≥3 天（`test_days_cap`）；有出单=净损 ≥ $4（`stop_net_loss_usd`）/ 断流 ≥3 在投日且 ROI<100%（`stop_dry_short_*`）/ 断流 ≥7 在投日且 ROI<150%（`stop_dry_long_*`）——断流天数=最后一笔非拒付佣金后的**在投日数**（run_daily_stats 行数，暂停期不累积，续投后重新计）；② **止损复活** `reviveStoppedRuns`（`auto_revive` 默认 on）：仅对「止损自动暂停」的任务（07 手动暂停的绝不碰），双条件=暂停后有新佣金（first_seen_at > 暂停时刻）且 ROI 转正 → 自动 resume + 巡检汇总「♻️ 止损复活」；off 时只提案（每暂停周期一次）；③ 全部阈值进 settings 注册表（07 飞书一句话可改）；④ resume 校验同口径（零出单看花费线/出单看净损线）；⑤ 存量 31 个任务线 10/2 → 9/3（07 手动改过线的不动），设置默认值同步 9/3；skill v1.9.3。**实测（2026-07-24 巡检）**：#39 truskin 出 5 笔 $2.15 vs 花费 $6.66 净损 $4.51 ≥ $4 → 关 ✅（成为复活规则天然观察对象）；#40 brooksbrothers 零出单 $9.06 ≥ $9 → 关 ✅（旧线 $10 昨晚放过了它）；#22/#35 有出单未触线继续投 ✅。**已知特性（书面告知 07）**：出单任务净损线 $4 比零出单线 $9 紧——出一小笔单反而更早触线（07 标准字面如此，如需改说「净损线改 $X」） | 被 HM-D32 细化取代 |
| HM-D32 | 2026-07-24 | **07 拍板分阶段生命周期模型（经 AskQuestion 逐项确认口径）**：① **观察期**（发布起 2 天，`test_days_cap` 3→2）：零出单满 2 天或花费 ≥ $9 → 关；**一出单 → 预算自动翻倍**（≤ $10 `boost_cap_usd`，仍受 hard 约束，一次性、run_events 去重，巡检汇总「📈 出单调增」）→ 进等待期、不再看花费线；② **等待期**（首笔非拒付佣金 cst_date 起 2 个 CST 日 `wait_days`，期内再出单**不重置**）：只看净亏 ≥ $4（`stop_net_loss_usd`）→ 关；③ **期满考核**（一次性）：ROI < 100%（`wait_roi_min`）→ 关，达标记「等待期考核通过」进运营期——**曾止损复活/被 07 手动续投的任务跳过考核**（复活与续投本身就是新准入判断，防振荡）；④ **运营期**：ROI > 200%（`roi_safe_threshold`）→ 本月豁免一切自动关（07：「ROI>2 的持续到本月结束再观察」）；断流 ≥3 在投日且 ROI<100% → 关；断流 ≥7 在投日且 ROI<150% → 关；**ROI 每轮实时算、到了谁的管理区间就由谁管理**（07 原话）；⑤ 复活机制（HM-D31）保留：暂停后出新单且 ROI 转正 → 自动续投。**顺带答疑**：07 问「默认预算说了 $4 为什么还是 $2」——settings_log 实锤 default_budget_usd=4 自 07-23 01:15 起生效、新任务（#49-53）都是 $4/$5；$2 是 07-22 之前建的老任务（如 #22），设置不回溯旧广告；已把 #22 预算手动补到 $4。**部署实测**：15 在投任务无误关，#22/#35（有续投记录）正确跳过考核走断流管理，存量广告不参与阶段规则；settings `test_days_cap=2`、存量 31 任务 days_cap 3→2 | 待 07 验收 |
| HM-D37 | 2026-07-25 | **07 规格「30 个等待投放的名单，低于 10 个商家就补齐名单给我批准，不然总是一个一个给我太麻烦」——待投名单水位线补货，取代 HM-D34 逐个关停补位**。旧模式痛点：关一个补一个，每次补位都随一张（往往单任务的）审批卡来，07 被小卡零敲碎打。新模式：**待投名单** = 建好待批（pending_approval）+ 已批排队等 CID（approved）的任务总数；目标 30（`waitlist_target`）、低水位 10（`waitlist_low`）——名单跌破 10 → 每小时补货 cron（:45，`POST /api/cron/waitlist-replenish`）从新测队列按推荐表序一次性补齐到 30，合并批量审批卡让 07 一次批完（**超 12 个自动分页多张卡**，防撞飞书卡片体积上限，每张仍是一键全批）。配套改动：① monitor 止损、pump 花费哨兵、手动暂停三处的 `backfillOne` 逐个补位全部下线（回收的 CID 由 pump 5 分钟内自动发布名单里的下一个任务——名单是常备缓冲，不需要现建）；② `auto_backfill` 语义改为名单补货开关；③ 09:00 plan-execute 无显式 daily_plans 时委托水位线（不再每天固定建满配额，名单封顶 30 自节流，补货豁免每日配额）；④ 新接口 `GET /api/waitlist`（现量/水位）、`?force=1` 忽略水位立即补满（07 说「把名单补满」）；⑤ skill v1.10.0。**上线路上补了一刀**：补货建任务一个要 1-2 分钟，pump 每 5 分钟的兜底聚合（120s 静默窗）会把建到一半的批次拆成零碎单卡——加 `replenishActive` 护栏，补货进行中兜底发卡一律按住，建完由补货自己统一扫尾（`dispatchPendingApprovals`，含历史中断遗留）。**部署实测（2026-07-25 16:04 CST）**：force 补满一轮——名单 14 → 30 ✅，16 个任务建成（#95-#110，Corkcicle/Movado/RAD 三国/Bluetti/Brevo 双国/GOG 等，严格推荐表序），3 个候选建失败自动跳过换下一个 ✅，最后合并 **2 张批量卡（12+4）** 同时发出、全程零零碎单卡 ✅；crontab 每小时 :45 已挂 | 待 07 验收 |
| HM-D36 | 2026-07-24 | **07 反馈「#49 polarde 实际 $10.97 才关（线是 $9）」——止损反应粒度太粗，加花费哨兵**。根因链：Ads 实际花费 → MCC 脚本写 CRM Sheet（07 侧两个脚本错开、各半小时一跑，Sheet 本身保鲜 ≈15-30 分钟）→ Hermes 整点 :12 巡检才做止损判断——超线后最多再跑 1 小时，$9 线冲到 $10.97。07 拍板**不直查 Ads API，调整消费节奏**（「可以用脚本实现实时数据的」）。落地：pump（每 5 分钟）本来就在读 Sheet 做点击换后缀，`campaignMetricsFromSheet` 返回值里 costUsd 一直在手没用——加 `spendGuard`：**零出单（有效佣金≤0，含 pending 折扣口径）且花费 ≥ spend_cap → 立即暂停 + CID 即关即回收 + 补位 + 飞书即时通知**，零新增数据请求；出过单的任务不越权，留给整点 monitor 做三阶段完整决策（净亏线/断流/豁免）；事件前缀保持「止损自动暂停」→ 哨兵关的任务日后出单照常走自动复活；熔断期跳过（monitor 兜底）。止损延迟从「Sheet 保鲜 + ≤60 分钟」压到「Sheet 保鲜 + ≤5 分钟」。**部署实测**：17:00 pump 轮 `guarded:[]` 正常出现（当轮无任务触线）✅ | 待 07 验收 |
| HM-D35 | 2026-07-24 | **07 需求：「每个月我都能拿到推荐表，我其实更推荐从推荐表上按顺序测试」（提供 5 份 7 月推荐 xlsx）——月度推荐表成为队列最高优先层**。表源分析：PM 表 71 家 / LH 表 208 家 / RW 表 95 家 / CZ 表 644 家（「新平台」与「CZ」两份内容完全相同只导一份；按 BU 列拆——chuizhan_ui→MUI 272、chuizhan_cg→CG 204、chuizhan_ev→EV 27，cf/bsh/df 共 141 家无对应池跳过），合计 877 行入库。落地：① 新表 `monthly_recommendations`（source_month+network+MID 唯一，含 rank/域名/国家主场/EPC/佣金率）+ `POST /api/recommendations/import`（replace 全量换月）+ `GET /api/recommendations`；② 本地解析脚本 `tools/import-recommendations.py`（表头关键词自动定位列，支持 --out 出 JSON → scp → 服务器 curl 导入；**每月新表到手跑一次即可**）；③ 队列构建：推荐商家 **priority = 5000−rank 纯表序**（不叠加白名单/佣金分——首版叠加导致 rank7 排到 rank1 前，已修正），绝对高于团队白名单层（≤1200）；多联盟同 rank 并列交错测试；黑名单/团队已测去重照常过滤；**国家优先用表内商家主场**（offer 支持时，如 Rails 用 CA 不强扭 US）；④ 取队排序统一改「推荐层严格表序 → 非推荐层 US 优先+分数」（planExecute/backfill/create Top1 三处）。**实测**：877 行导入 ✅（LH 176/PM 69 表内重复 MID 合并属正常）；队列重建 18741 排队、推荐命中 450；队首 rank 序列 1,1,3,4,4,4,4,5,7,7,8,9 严格表序 ✅（rank 缺号=该商家不在池/被过滤）。**D35b 追加（07 拍板「丢给 Hermes 他能看懂」）**：导入全程 Hermes 自主——飞书网关本就把 07 发的文件缓存到服务器本地（`~/.hermes/cache/documents/doc_*_原名.xlsx`），Agent 拿路径跑 `python3 tools/import-recommendations.py --rebuild <文件...>` 一条龙（联盟自动识别：CZ 看 BU 列、LH/PM/RW 看标题/文件名；同内容文件去重；token 自动读 .env；月份默认当月 CST；识别不出明确报错不猜）；API replace 改**按联盟局部替换**（RW 周更重发只刷 RW 不动别家）；服务器 apt 装 python3-openpyxl 3.1.2。端到端实测：4 份模拟网关缓存名文件 → 识别 4/4、导入 877、重建命中 450，与首轮一致（幂等）✅；skill v1.9.6 | 待 07 验收 |
| HM-D34 | 2026-07-24 | **07 需求：「关掉广告，就对应补上广告」——关停补位保持测试管道满载**。落地：① `backfillOne`（create-runs）：从新测队列取队首（HM-D35 后即推荐表顺位）缓发卡建任务，`auto_backfill` 开关默认 on；② 挂两个触发点：**止损自动暂停**（monitor 暂停成功后）与 **07 手动暂停**（adjust pause 后，接管的存量广告不触发——那不是测试位）；③ 补位任务进批量审批卡等 07 批（HM-D29 通道），巡检汇总新增「🔄 关停补位」节；④ skill v1.9.5/1.9.6。**上线当天 07 反馈「暂停后没有及时补货」（14:12 止损关了 #32/33/34/37 无补位）→ 连修两刀**：❶ 配额语义错——首版补位受每日新建配额约束，当天 15/15 已满被静默挡住；修正为**补位豁免每日配额（是替换不是新增，关 N 补 N 净在投数不变），当天补位总数自身 ≤ daily_runs 防失控**；且跳过/失败不再静默，巡检汇总明说原因（配额满/队列空/连败）；❷ 补位死循环——队首商家 filter 文案样本不足（品牌词真实广告太少）时建任务抛错、队列行不消耗，补位反复撞同一家白烧 SerpApi；修正为**样本不足行标 `copy_failed` 静默排除**（同 suffix_failed 先例，07 指定 ai_generate 仍可建）+ backfillOne 单候选失败自动试下一个（最多 5 个）。**补发实测**：4 个位全补上——#69 Cara Cara New York（EV 推荐榜 #1，单卡）、#70 Music and Arts（MUI #4）、#71/72 The Designer Box US/AU（PM #4，批量卡 3 合 1）；07 全批，#69 已上线；Rails（MUI #1 加拿大）、Yumi Kim（EV #3）因样本不足被排除 | 待 07 验收 |
| HM-D33 | 2026-07-24 | **07 反馈「Hermes 总是 CID 领取失败」（#61-68 连续 8 个预占失败）——CID 池只进不出，根治**。根因：`releaseCid` 只挂在发布失败路径，任务被止损暂停后 CID 永远占着；加上远程实查发现 enabled campaign 就把 CID 标 N **且永不复查**——30 个 CID 的池 26 个被占（含 9 个 paused 任务）+4 个拉黑 = 可用 0。与设计语义自相矛盾（远程双验只挡 enabled campaign，paused 广告的 CID 本该可复用）。落地四件套：① **热窗回池** `releaseStaleCids`：paused/failed 任务的 CID 保留 `cid_hold_hours`（默认 24h，复活主要发生在佣金滞后的 24h 内——#39 案例关停到出新单仅 10h）后自动释放，monitor 每轮执行；② **复活安全网** `reacquireCid`：CID 已释放的任务复活前校验——原 CID 被别的任务占用或远程有 enabled campaign → 不硬来，飞书通知 07 决定（每暂停周期一次）；否则重新占回，**保证一 CID 永不两个活广告**；③ **拉黑自愈**：reserveCid 池空时自动复查拉黑名单（6h 冷却），远程 enabled=0 就解禁回池；④ **排队不弃单**：CID 不足时任务留在 approved 由 pump 每 5 分钟自动重试（飞书只在首次短缺提醒一次），不再直接 failed 轰炸 07。**救急实测**：#61-68 重置回 approved；#61 即由拉黑复查解禁的 CID 3574434429 自动发布 ✅；8 个零出单 paused 任务的 CID 提前手动释放（#39 truskin 已出 9 笔单留在热窗内等复活）→ #62-68 七个全部自动发布上线 ✅。**排查小坑**：pump 路由是 `GET /api/cron/pump`（不是 POST）；run_events 时间为 UTC，算热窗年龄别按北京时间想当然。**次日修正（07 反馈「又出现 CID 不足」）**：热窗与 HM-D34 关停补位相撞——14:12 止损关 4 个零出单任务，CID 全被 24h 热窗扣住，补位任务 #70-72 批准后无 CID 排队。救急：手动释放 #32/33/34/37 四个 CID → pump 一轮 #70/71/72 全部发布上线 ✅。**随后 07 拍板「热窗不用特意留，我们就是疯狂投放、筛选，只找金子不找沙子」→ 热窗机制整体取消**：止损/手动暂停一律**即关即回收**（暂停成功即 releaseCid + monitor 每轮清扫兜底），复活/手动续投前 `reacquireCid` 现场校验占回（被占/远程有 enabled 会拦下），`cid_hold_hours` 设置删除；#39 truskin 的 CID 也已释放（池空闲 +2）。**07-25 再修（07 反馈「怎么又这样了」——09:00 批量建 13 个任务，#78-#90 逐条各发一条「CID 不足已排队」刷屏 13 条）**：核查确认排队本身是设计内（池 27 个可用 CID 全部被 27 个在投任务占着=满载，等止损/关停回收后按队列自动发布），毛病在「首次短缺提醒一次」做成了**每任务一次**——批量建任务场景 N 个任务 = N 条。已改为 **pump 整轮聚合一条**：只在有任务首次进入短缺时发，一条列全所有新排队任务号 + 池子满载状态（27/27 在投）+「无需处理」，后续轮次静默重试；发布成功本就有单独通知。部署实测：修复后手动 pump 一轮，13 个任务全部 firstShortage=false → 零飞书消息 ✅ | 待 07 验收 |
| HM-D28 | 2026-07-23 | **07 反馈「Hermes 没有拿到联盟商家的佣金」→ P3 联盟佣金回流 + ROI 归因上线**。落地：① 新增 `lib/affiliate-txn.js`——移植 CRM 生产 `platform-api.ts` 的 `fetchAllTransactions` 全套口径（LH cashback2 GET/4s 限频/30 天切片/满页续翻；CG/EV/MUI/PM POST JSON 必带 `dataScope:"user"`；RW form limit=30/14 天切片防 504；MUI/EV 订单→items 展平；行级 ID/金额非零优先/状态归一 approved·pending·rejected·paid/paid_date 点亮；时区=LH CST 钟面、其余 Unix 秒真 UTC，`cst_date` 在解析层算好）；② 新表 `affiliate_transactions`（UNIQUE pool+transaction_id upsert）+ `jobs/sync-commissions.js`；③ **归因链按设计 5.6**：订单数字 MID → 商家；系列名池段+末段 MID → 同一任务；LH 双池精确匹配不串池，其余联盟一账号一池按 network 匹配（存量接管广告池序号解析偏差不影响归因）；起投日起、CST 切日；④ ROI=(佣金−拒付−花费)/花费；**复盘卡片每任务加「佣金 当日/累计（含待定/拒付）｜ROI」行** + 💡 新增「ROI 回正→建议放量」；新转化归因到任务时整轮一条「💰 联盟佣金回流」飞书汇总（无新转化静默，对齐通知纪律）；⑤ cron：每小时 :40 增量（近 14 天）+ 09:10 全量对账（最早起投日前 3 天起，封顶 90 天，赶在 09:30 复盘前）；⑥ 接口 `POST /api/cron/sync-commissions`（days/full/notify）、`GET /api/commissions[?run_id=]`；skill v1.9.0 加「佣金/ROI 查询」章节（出单滞后要如实解释，无法归因如实转达）。**首轮全量实测（2026-07-23）**：7 池 867 笔交易入库（LH1 121/LH2 14/EV 447/MUI 189/PM 58/RW 38/CG 0），283 笔归因到任务；**存量广告真实 ROI 浮出——#46 MMLaFleur 佣金 $1217.86 vs 花费 $123.15（ROI +889%）、#47 Cariloha +57%、#48 FatAndWeirdCookie +27%（拒付 $45.93 已剔）**，直接修正了此前把这三个当「严重超止损应暂停」的误判（当时只有花费没佣金）；#35 evryjewels、#39 truskin 等新测任务也已见单（均 pending 待联盟确认）。**顺手排除次生事故**：`cron-env.sh` CRLF 复发（根因=仓库 blob 本身是 CRLF，每次部署 checkout 还原坏行尾，HM-D19 同源）——已重写 LF+`tr -d '\r"'` 双保险，并加 `.gitattributes`（*.sh 强制 LF）根治 | 待 07 验收 |
| HM-D27 | 2026-07-23 | **07 两点反馈：① 巡检通知「这种也是批量通道查看，不要一大堆消息发来」；② 「现在 Hermes 只管自己发的，之前我创建的在投广告他就不管了」**。落地：①【通知整轮汇总】自动调参/止损暂停/告警不再逐条推飞书——monitor 整轮收集后合并成**一条「Hermes 巡检汇总」**（分节：🛑 止损 N 个 / 🤖 调参 N 个 / 📥 新接管 N 个 / 告警），无事发生则静默；`autoTuneRun` 只执行+记流水，通知职责上收 monitor；②【接管存量广告】新增 `jobs/adopt-campaigns.js`，每小时巡检开头扫 CRM 绑定 Sheet 的 Campaigns 表（MCC 脚本维护，含 campaignId/cid/finalUrl/mid/trackingUrl），凡 Hermes 不认识的在投系列：一次性 GAQL 补齐 budget/ad_group 资源与当前预算/CPC（仅 ENABLED 的收），落 ad_runs（`queue_source='external'`，published_at=DailyData 最早日期），此后数据快照+自动调参一视同仁。**存量广告红线**：不设测试止损（caps=NULL，monitor 跳过止损判断，07 显式说「#N 止损改 $X」才会设线）；不参与 Hermes 换后缀/后缀池（kylink/MCC 脚本在管，双写会打架，pump/replenish 均排除 external）；复盘卡片对无止损线任务展示「存量广告（无测试止损）」。**首轮实测**：接管 5 个（#44-#48，EV Desire/Bloom、MMLaFleur、Cariloha、FatAndWeirdCookieCo，预算 CPC 从 GAQL 实读）✅；踩坑并当场修复——**接管首轮无昨日快照，累计花费被当成当日花费**，#46/47/48（$123/$55/$234 累计）误判「撞预算线」加了预算 $8.88→$11.1，已手动改回 $8.88 并加防护：无昨日基线且非当日起投的任务，按日规则（撞线/均价）本轮跳过，等次日有基线再算；复跑巡检 0 误动作 ✅ | 待 07 验收 |
| HM-D26 | 2026-07-23 | **07 需求：「批量创建广告的时候默认给我批量审批通道，而不是单个审批卡」**。落地：① 新设置项 `approval_mode`（batch/single，**默认 batch**，07 说「审批改回单卡」即切）；② 批量建任务（09:00 plan-execute，≥2 个成功创建）时 `createRunFromQueue` 缓发单卡（deferCard），统一走 `dispatchApprovalCards`——batch 模式发**一张批量卡片**：每任务紧凑摘要（商家/国家/池/预算/CPC/止损/系列名/关键词/完整联盟链接）+ 合计预算 + 「✅ 全部批准 / ❌ 全部拒绝」按钮；仅 1 个任务或 single 模式仍走原单卡；③ 批量签名链接：HMAC 覆盖整批 ID（`hermes-approve-batch:1,2,3:action`，ID 规范化去重升序防重排/注入），确认页 `/approve?ids=...` GET 列出全部任务表格+可展开完整文案（已处理的标注跳过），POST 一次对所有仍 pending 的逐个执行并汇总结果页（批准 N/跳过 M/失败 K）；**部分单独处理过的任务再点批量按钮自动跳过，不会重复审批**；④ skill v1.7.0：第 2 节加批量审批话术（「全部批准/全部拒绝」→ Agent 查 pending 逐个调接口；「批准 #12 #13，拒绝 #14」逐条执行），设置表加 approval_mode 行。**服务器实测**：approval_mode 默认 batch ✅；批量链接确认页 200（列出 #14/#18 并标注已处理）✅；篡改签名 403 ✅ | 待 07 验收 |
| HM-D25 | 2026-07-23 | **07 需求：「Hermes 要帮我实时监控在投数据，适当加减预算、CPC」——从 HM-D22 的「只建议、07 确认才执行」升级为自动执行**。落地：① 新增 `jobs/auto-tune.js`，挂进每小时监控巡检（monitor-runs 第 4 步，止损未触发才轮到调参），4 条保守规则（kyads 决策树按现有点击/花费信号裁剪）：**加预算**=当日花费 ≥ 日预算 95%（撞线卡量）→ +25%；**降 CPC**=当日 ≥5 点击且实际均价 ≤ 出价一半 → 降到均价×1.5（绝对下限 $0.05）；**提 CPC**=投放 ≥3 天累计 0 点击 → +50% 试探；**减预算**=近 3 天日均花费 ≤ 预算 20% 且预算高于默认值 → -20% 回收敞口（下限=默认预算）。② 风控继承既有体系：预算/CPC 永远 ≤ Owner 上限 hard_*（走 adjustRun 同一校验），每任务每类动作 **22h 冷却**（run_events event=auto_tune 判重），单轮巡检每任务最多 1 个动作，Ads 熔断时静默跳过下轮再试；③ **事后通知制**（非事前请示）：每次动作即时飞书通知 07（含依据 + 「不认可直接说 #N 改回 $X」），09:30 复盘卡片再兜一遍「近 24h 自动调参汇总」；④ 新设置项 `auto_tune`（on/off，默认 on），07 说「自动调参关掉」全停，止损暂停等既有保护不受影响；⑤ skill v1.6.0：解释 4 条规则 + 开关话术 + 「为什么自动改了」查 run_events 依据；Agent 本身仍不得自作主张调价（自动调参是 cron 干的）。**生产实测（2026-07-23 首轮巡检 23 个在投）**：12 个动作全部按规则执行——run#18/#22 降 CPC $0.5→$0.08（当日均价仅 $0.05-0.06）✅；run#24/#26 预算 $3→$3.8、run#29/30/32/34/35/37/39 预算 $2→$2.5（当日花费全部撞线）✅；每笔 adjust+auto_tune 双流水 ✅；飞书通知 0 失败 ✅ | 待 07 验收 |
| HM-D24 | 2026-07-22 | **07 两项发文规格：① 允许「给我 MUI xxx 商家的文章链接」当提示词；② 违规链接绝不发联盟绑定站点，必须发 draftify.sbs**。落地：①【按联盟池查商家】`publish-article.js` 加 `findOfferByNetwork`（network+商家名/域名模糊查 offers 池；命中多个不同域名商家时报歧义列候选让 07 选）；**正常发文选站升级为「如实发站点」= 该联盟连接绑定的站**（CRM publish-sites 接口新增 `bindings`——wj07 的 platform_connections.publish_site_id：CG/MUI→allurahub.top、EV→mevora.top、LH→aura-bloom.top/novanest.one、PM→keymint.co、RW→parcelnplate.top），无绑定才落节奏轮换；**同商家已有已发布文章直接回链接（reused=true）不重发**，07 明确说重新发才 `force_new`；②【违规链接隔离】`violation:true` 模式：强制选隔离站（`VIOLATION_SITE_DOMAIN` 默认 draftify.sbs，= CRM publish_sites id=44），忽略 site_id 覆盖 + 发布前硬断言兜底（选站结果≠隔离站直接拒发），隔离站不参与正常文章轮换，违规/正常文章分开去重；`article_publishes` 加 `network/is_violation` 列；③ skill v1.5.0：两种新说法的调用模板 + 红线（07 说「违规」必带 violation:true；就算指定别的站也会被隔离站覆盖）。**实测（2026-07-22）**：bindings 7 条 ✅；「RW 的 plarium」→ 如实报「RW 池里找不到」（plarium 实际在 LH/PM 池）✅；「PM 的 plarium 违规链接」→ 发布至 `draftify.sbs/post-plarium-vs-other-free-to-play-platforms-three-honest-differences`（8 图全本地化，对比型标题/问题开篇/多小标题结构）✅；同参数二次调用秒回 reused=true 同链接 ✅。注意：首次全流程（抓图+LLM+发布）可能超 10 分钟（plarium 首跑 >600s，二跑约 4.5 分钟），Agent 侧建议 nohup/耐心等待勿重复触发 | 待 07 验收 |
| HM-D23 | 2026-07-22 | **07 需求：Hermes 跳板宝塔发布文章（配图 5-8 张，质量对齐 draftify.sbs 参考文）**。07 拍板执行方式=**委托**（Hermes 生成内容与编排，真正写宝塔走 CRM 久经考验的发布链路），保留 Humanizer AI 痕迹门禁，站点共享 CRM 站群，飞书指令触发。落地分两端：①【CRM 端】新增 `/api/hermes/*` 三个 Bearer 接口（独立 `HERMES_API_TOKEN`，非 CRON_SECRET；未配置整体 503 禁用）——`GET publish-sites`（活跃站点清单+各商家最近发布日，供节奏校验）、`POST crawl-images`（复用 CRM 本地爬虫主页+产品子页+搜索兜底抓图，出口统一 isQualityImageUrl，返回 ≤40 候选）、`POST publish-article`（接收 Hermes 成品文章：**配图硬校验 5-8 张 img** → Humanizer 门禁（不过检自动清洗一次，仍不过 422 拒发）→ articles 落库（归属 `HERMES_ARTICLE_USERNAME` 默认 wj07，CRM 文章列表可见）→ 同站同 slug 409 防重 → `publishArticleToSite`（SSH 宝塔 + 图片下载本地化 `/images/articles/{id}_{n}.webp` + 索引/静态页 + CDN））；②【Hermes 端】新增 `jobs/publish-article.js` + `POST /api/articles/publish`（传 `run_id` 给广告任务配软文，或 merchant_name+merchant_url 独立发文）+ `article_publishes` 本地表：**选站**=同品牌同站 ≥7 天硬约束、优先最久未发站（CRM 历史+本地记录双查）；**配图**=CRM 抓图候选 → HEAD 体积探测 ≥20KB 过滤图标/占位（draftify 参考图 40-120KB webp 口径），目标张数 5-8 随机（防固定张数指纹），达标 <5 张如实拒发；**生成**=LLM（gemai claude-opus-4-6）按 `seo-article-style.mdc` 反指纹轮换：标题结构 10 池（同商家 90 天不重复）、开篇钩子 5 型（不连续同型）、正文结构 6 型（与上一篇错开）、年份出现率控制、字数 750-1150 随机；system prompt 内置 Humanizer 禁词表（em dash/delve/seamless 等）降低被门禁拒率；嵌图不达标自动重试 1 次；**登记**=article_publishes 记录轮换参数（title_structure/hook_type/body_layout 支撑后续去重）+ 发布成功飞书通知（助手身份）；③ skill v1.4.0 加「发布软文文章」章节（红线：配图不足/节奏冲突不硬发、Humanizer 连拒 2 次停下问 07）。**实测端到端（2026-07-22）**：run#35（Evry Jewels/evryjewels.com）72 秒完成——自动选站 aurislane.top、标题结构=数字明确型/钩子=失败教训/结构=决策清单型、`After 3 Returns and $200 Wasted…` 发布成功，**8 张图全部本地化**（4589_0~7.webp，19-80KB 实拍级商品/模特图，人工抽查 2 张质量达标）、Humanizer 一次过检、文内 3 处追踪链接 ✅ | 待 07 验收 |
| HM-D22 | 2026-07-22 | **07 规格：AI 日常运营（加预算/调 CPC 等）——SSH 探查 kyads 生产实现后复刻**。kyads 闭环＝cron 5:00 LLM 分析（决策树提示词 v5.0）→ 建议进 ai_recommendations/decision_journal → **人工在 UI 确认** → apply-actions → v23 REST mutate（预算=campaign_budget.amount_micros；CPC=组层 cpc_bid_micros 或 TARGET_SPEND 出价上限；只有 pause 无 resume；无自动执行）。Hermes 落地（对话代替 UI）：① `google-ads.js` 加 `setCampaignBudget`（同 kyads 口径）+ `setManualCpc`（**与 kyads 的差异**：Hermes 发布时关键词层也写了出价会覆盖组层，故组+全部关键词 criteria 一次原子 mutate）+ `resumeCampaign`（kyads 没有；止损暂停后续投需要）；② 新增 `jobs/adjust-run.js` + `POST /api/runs/adjust`：`set_budget / set_cpc / pause / resume / set_caps`，仅 monitoring/paused 可操作，预算/CPC 受 Owner 上限（hard_*）拒超，resume 时若累计花费仍超止损线则拒绝并要求带新线（防续投即被 monitor 再暂停），全部动作记 run_events(event=adjust) 流水；③ **每日复盘卡片加 💡 操作建议**（kyads 决策树按现有信号裁剪：预算撞线 95%→建议加预算；实际均价≤出价一半→建议降 CPC；3 天 0 点击→建议提价或停投；累计达止损线 80%→提前问续投），**只建议不自动执行**，07 回「#N 预算改 $X」即落地（对齐 kyads 人工确认制；ROI 归因 P3 接入后升级完整决策树）；④ skill v1.3.0 加「日常运营」+「每日运营纪律」章节，红线=不自作主张调价。**服务器实测**：run#14 validateOnly 干跑调预算 $2.5/调 CPC $0.35（组+1 关键词）均 200 ✅；set_caps 真执行 $10/2天↔$15/7天 往返 ✅（流水两条）；预算 $99 超上限被拒 ✅；新版复盘卡片已发 07 ✅ | 待 07 验收 |
| HM-D21 | 2026-07-22 | **07 纠正 HM-D20：回流不该靠 Ads API 节流，应读已有公开 Google Sheet**。CRM 表 `1UtzNj_Zei…` 的 **Campaigns.todayClicks**（换后缀水位）+ **DailyData**（按日 Clicks/Cost micros → SUM 累计止损/回流）已由 MCC Ads Scripts 写入，Hermes 原先用 GAQL `campaignStats` 重复烧 BASIC token 才导致 429 风暴。落地：新增 `crm-metrics-sheet.js`（公开 gviz CSV + 90s 缓存）；pump/monitor **读路径全面改 Sheet**；Ads API **仅保留** mutate（换后缀 / 止损暂停 / 发布 / CID 实查）。HM-D20 的点击 15 分钟节流与「熔断整轮跳过回流」已撤销（读不再耗配额）；mutate 侧熔断仍保留。口径：换后缀用今日点击（跨日回落重置水位）；止损/复盘用 DailyData 累计。 | 待 07 验收 |
| HM-D20 | 2026-07-22 | **07 飞书告警「回流断流 / 换后缀异常」排查并修复**。根因：Hermes 单 Developer Token（BASIC）被 pump（每 5 分钟 × 全部 monitoring CID GAQL）打爆日配额 → 持续 Google Ads **429 RESOURCE_EXHAUSTED**；monitor 再按任务刷「回流断流」飞书噪音。修复：① `google-ads.js` 进程级熔断（日配额 → 等到太平洋时区次日 00:05；短时 429 → 3–30 分钟）；② pump 点击查询默认 **15 分钟** 节流（`ADS_CLICK_POLL_MIN_SEC`，14 CID×5min 约 168 次/时 → 约 56 次/时）；熔断期间跳过点击查询/暂缓 approved 推进，不再逐任务写 429 error；③ monitor 熔断整轮跳过 + **全局「配额熔断」告警一条**（run_id=0 去重），换后缀/回流告警排除 429 噪音。广告不停投；熔断结束后自动恢复。后续可选：多 token 池（对齐 CRM） | ⚠️ 已被 HM-D21 纠正（节流治标；正确读源=Sheet） |
| HM-D01 | 2026-07-13 | 需求澄清完成（11 项结论），方案 v1.0 定稿；核实服务器白纸状态、wj07 LH 凭证可用、CRM 黑名单表为空 | ✅ 已批准（07：先开工） |
| HM-D02 | 2026-07-13 | **P0 完成**：① gemai 模型接通（provider=custom + base_url=api.gemai.cc，默认 claude-opus-4-6；密钥经实测有效，注：gemai 已下架 claude-sonnet-4-5，旧文档记录过期）；② 飞书原生机器人接入（旧应用凭证，websocket 已 connected，白名单仅 07 的 open_id）；③ 网关常驻化（systemd 用户服务 hermes-gateway + linger，开机自启/崩溃自拉）；④ 实测 `hermes -z` 中文对话正常、`hermes send` 已向 07 飞书私聊发送上线通知。配置变更均有备份（.env.bak.p0 / config.yaml.bak.p0）。**待 07 验证**：在飞书私聊回一条消息确认收发闭环 | 待 07 飞书验证 |
| HM-D03 | 2026-07-16 | **第二轮需求澄清定稿（v1.1）**：20 项新结论（见 2.2）。要点：① 一 Hermes 一员工，先做 wj07、可复制部署；② CID 改用 wj07 名下 MCC 941-949-6301（39 CID，实查 CRM 库）；③ LH 双池（LH1 wenjun3 / LH2 novanest）+ 选池/双开规则；④ 团队去重改共享谷歌表格（替代 SSH 读库），结构草案见 5.4；⑤ 建广告规格参照 kyads 提炼完成（5.5：1系列1组1RSA、仅搜索、Manual CPC、品牌词 PHRASE、6 段命名）；⑥ 归因参照 CRM 提炼完成（5.6：cashback2 拉单、商家+池维度对齐、ROI 口径）；⑦ 监控每小时巡检、止损即自动暂停；⑧ 放量前期提案制、后期自主；⑨ 每晚 21:00 问明日计划；⑩ 交互全走私聊。 | 待 07 验收 |
| HM-D04 | 2026-07-16 | **07 修正落地 URL 规格**：final URL 必须是**商家域名**（优先首页，支持 deeplink 可用商家内页），不能照抄 kyads 的「验证落地页当 final URL」；追踪后缀 = 解析 LH tracking_url 跳转后落地页的 query 参数。已深挖 CRM 后缀体系（suffix_pool 36h 过期 + 每5分钟补货 cron + MCC 脚本点击增长换链 + 住宅代理跟链），Hermes 侧后缀维护深度（完整复刻/简化版/调 CRM 接口）及代理来源列为待确认项 #10 | ✅ 已拍板（见 HM-D05） |
| HM-D05 | 2026-07-17 | **07 定后缀方案 = A（完整复刻 CRM 后缀池体系）**：新增 5.7 节规格——住宅代理跟链生成后缀入池（36h 过期）、每 5 分钟补货 cron、任务泵内查点击增量换后缀（Hermes 直连 Google Ads API 替代 CRM 的 MCC 脚本）、断链告警不停投、发布前置生成首条后缀。数据模型加 `suffix_pool` 表、cron 编排加 suffix-replenish。遗留：跟链代理来源（待确认项 #11） | ✅（#11 见 HM-D06） |
| HM-D06 | 2026-07-17 | **07 定跟链代理 = 复用 CRM 同款 kookeey**。实查 CRM 生产库：kyads_proxies 仅 kookeey（gate.kookeey.info:1000，socks5/http 双协议，username 模板 {COUNTRY}+session+life-5m）为 active（cliproxy/ipip 已禁用），wj07 绑定 kookeey。凭证开工时从 CRM 库解密复制到 Hermes 本地 .env，运行时不依赖 CRM。待确认项 #11 关闭 | ✅（07：开工） |
| HM-D07 | 2026-07-17 | **P1 完成并部署上线**：① hermes-ads 后端（Node 22 + 内置 SQLite 零依赖，127.0.0.1:8787，Bearer 鉴权）部署为 systemd `hermes-ads.service`（内存限 512M）；② LH 双池全量同步实测通过——LH1(wenjun3) 5063 条 + LH2(novanest) 4165 条 ≈ 6 分钟，`restricted_keywords/support_deeplink/promotion_area/comm_rate` 等字段全落库；③ 筛选管道+队列重建实测：可测池 9220 → 排禁品牌词 80 / 无可投国家 267 / 双池合并 4207 → 新测队列 **4666**（优先级=佣金+明确国家+EPC+deeplink）；④ 去重/黑名单抽查通过：向已跑记录和黑名单各注入 1 条队列 Top 域名，重建后两者均被排除（零漏），清理后还原；⑤ 团队表格同步器+登记接口就绪，未配置表格时降级本地缓存+飞书告警；⑥ cron：08:00 同步+重建、08:30 表格同步；⑦ Agent skill `hermes-ads/offer-pipeline` 已装、网关已重载；⑧ 代码入服务器本地裸库 `~/hermes-ads.git` + 本机克隆备份。**遗留阻塞**：kyads SA 所在 GCP 项目未启用 Sheets/Drive API 且 SA 无权自启用，共享表格暂不能建（见待确认项 #12） | 待 07 验收 P1 |
| HM-D09 | 2026-07-20 | **P2 完成并部署上线**：任务状态机全链路打通。① 数据模型加 `ad_runs / run_events / cid_registry / cid_reservations / approvals / suffix_pool / run_click_state` 七表；② Google Ads REST 直连（wj07 MCC 941-949-6301 的 SA `qmy123@qimanyan` + dev token + login-customer-id，均从 CRM 库 google_mcc_accounts id=1 复制本地），发布器对齐 kyads mutate 结构（1 系列 + 1 组 + 1 RSA、Manual CPC、仅 Google 搜索、PRESENCE、品牌词 PHRASE、公共否定词、6 段命名），实测 SA 换 token + GAQL 查 CID 均 200；③ CID 池播种 30 个（28 可用），预占＝本地可用 + 远程实查无 enabled campaign 双验 + 唯一约束一 CID 一系列；④ 后缀池：kookeey socks5 跟链取落地页 query（socks-proxy-agent 纯 JS，36h 过期、低水位补货、点击增量换后缀），复用 CRM 同款 kookeey 凭证；HTTP 跟不动（lhdeal 延迟 JS 跳转）时自动无头 Chrome 兜底（服务器已装 google-chrome-stable 150 + puppeteer-core，http 代理同凭证、两阶段请求拦截省流量、全局串行防低配机爆内存），并识别 LH 新增二段跳板 secprf.com——实测 hornby lhdeal 链 14s 跟到 `uk.hornby.com/?clickref=1101lDC7iLIp`（真实 Partnerize clickref）；⑤ LLM 改用 gemai `claude-opus-4-6`（claude-sonnet-4-5 已下架），生成 RSA 文案/品牌词 + 预算/CPC 建议（硬限 clamp）；⑥ 飞书审批：白名单（07 open_id）+ 机器身份永不可自审；⑦ cron 加 09:00 plan-execute、每 5min pump、每 5min suffix-replenish；⑧ Agent skill `ad-runs`（建任务/审批/查状态/每日计划）+ `/api/daily-plan` 接口。**实测验收**：四道硬限全部被拒（预算 $10>5 拒、CPC $0.5>0.3 拒、日新建 >5 拒、机器/非白名单审批全拒）；建成 1 个真实任务 run#7（Hornby/hornby.com/US，$3/$0.15，15 标题 3 关键词 10 否定词，6 段名 001-LH1-hornbyuk-US-0720-54696）止于 pending_approval，审批卡片已推 07 私聊，**等 07 在飞书批准后任务泵才会真实 CID 预占→生成后缀→发布上线**（真实花钱，故未自动执行）。测试周期/止损数值见待确认项 #7（默认 7 天/$15）。遗留：P3 monitor（止损自动暂停 + ROI 归因 + 每日复盘）+ 21:00 主动问计划（Agent 侧定时） | 待 07 验收 P2 |
| HM-D10 | 2026-07-20 | **07 纠正：默认文案改 kyads filter 模式，不 AI 生成**。SSH 实查 kyads 生产库确认：`ad_creation_drafts.generation_mode` 默认 `filter`（代码 `parseGenerationMode` 不传即 filter；生产 4118 草稿 = filter 1181 / ai_generate 2937），filter = 不生成新文案，SerpApi 抓真实在投广告样本，LLM 只当「筛选官」原文照搬挑选（kyads `prompts/filter.ts`）；关键词=域名品牌词单个（如 `["tous"]`）；预算/CPC 固定默认 $2/$0.3；公共否定词是 55 词固定表。Hermes 落地：① 新增 `src/lib/serpapi.js`（三路样本：google SERP desktop+mobile、google_ads 六变体（brand/official/coupon/discount/sale/online）、透明中心 `text=整域名`——kyads 2026-05-16 实测必须整域名；约 $0.135/任务，key 复用 kyads 生产同款）+ `src/lib/rsa-filter.js`（标题按 `\|/–/—` 拆、描述按句号拆、去重、合规长度过滤 → LLM 筛选 → **代码层强制输出⊆候选池**防改写 → LLM 挂了本地兜底品牌自投优先）；② `ad_runs` 加 `generation_mode` 列，`/api/runs/create` 加 `mode` 参数（默认 filter，07 说「用 AI」才 ai_generate）；③ 否定词表替换为 kyads 生产 55 词；④ 审批卡片加「文案模式」行。**实测**：AI 版 run#7 已删；hornby.com 三路样本全空（透明中心 100 条创意全是图片渲染无文字）→ 如实拒建「样本不足」✅；tous.com 建成 **run#8**（001-LH1-tous-US-0720-161594，$2/$0.3，真实广告 17 条 → 候选 53 标题/45 描述 → 筛出 15 标题 2 描述全部原文照搬，卡片已发 07 私聊待批）✅ | 待 07 验收 |
| HM-D11 | 2026-07-20 | **审批升级为交互式卡片（07 要求）**：① 审批消息改飞书 interactive 卡片（蓝头 + 任务信息 + 文案逐条 + ✅批准/❌拒绝按钮），发送失败自动降级文本；② 按钮走 **HMAC 签名链接** → 确认页（GET 展示确认、POST 执行、已处理幂等提示），审批人记白名单 07 open_id + 备注「飞书卡片按钮审批」；③ 公网链路：Hermes 腾讯云安全组仅放行 22（实测 80/443/8788 均不通、无腾讯云 API 凭证、CF token 无隧道权限），故借道 CRM——CRM nginx 加 `location /hermes-approve/` 反代 127.0.0.1:18788，Hermes 起 `hermes-approve-tunnel.service` 反向 SSH 隧道（-R 18788:127.0.0.1:8788，crm-xlx0310.pem，断线自动重连），审批页进程只听回环；④ 实测：`/hermes-approve/healthz` 公网 200、run#8 确认页渲染正常（h1「确认批准发布任务 #8？」+ 表单）、新卡片已发 07 私聊。指令审批通道保留不变 | 待 07 验收 |
| HM-D12 | 2026-07-20 | **07 反馈「suffix 跟踪链接没加上」→ 后缀前置验证变建任务硬门槛**。① 卡片/确认页新增「追踪后缀 + 完整联盟链接」两行（`final_url + ? + suffix`）供 07 审批前核对；② 建任务顺序重排：**先跟链验证 suffix → 再 SerpApi 文案 → 落库 → 发卡片**，验证失败的 offer 标 `test_queue.status=suffix_failed` 排除 + 飞书告知（不烧 SerpApi、不占每日 5 个额度、不留垃圾 run）；③ 发布泵改为优先复用预生成后缀（池空才现生成）；④ 移植 CRM D-182「落地洗参」兜底：浏览器跟链记录逐跳 chain，落地无 query 时回溯「同根域名 + 带联盟追踪键（clickref/cjevent/utm_source 等 33 键）」那一跳取 query；⑤ **发现 LH 新跳板 tatrck.com（cookie 归因）**：lhdeal → `tatrck.com/h/xx?url=<商家>&s=lh_xx` 302 → 商家落地**无任何参数**，追踪靠 tatrck 302 时种 cookie——Google 广告直跳商家域名不经跳板，这类 offer 用 suffix 模式**根本无法归因**，实测 tous/thriftbooks/airalo/canvasondemand/suppliesoutlet 全中招，已入跳板名单；⑥ **run#8（tous）判 failed**（无法产 suffix），替补 **run#12 Adheart（adheart.ru/US，002-LH1-adheart-US-0720-18885）**建成：真实广告 18 条→筛出文案，后缀 `utm_source=admitad&utm_medium=affiliate&utm_campaign=591217`（admitad 链路参数落到商家域名，可正常追踪），卡片已发 07 私聊待批 | 待 07 验收 |
| HM-D13 | 2026-07-20 | **07 两项修正**：① **跳过通知静默化**——「拿不到追踪后缀已排除」这类建任务跳过信息不再推飞书（07：得静默在后台），只记 `test_queue.status=suffix_failed` + `sync_logs(kind=suffix_pregate_skip)`，07 要看随时可查；② **系列序号以 07 实际发布为准**——新增 `src/lib/crm-sheet.js` 读 CRM 绑定的公开 Google Sheet（`1UtzNj_Zei…gid=0`，列含 campaignName/cid/mccId/trackingUrl 等），取表内系列名首段最大序号，建任务序号 = max(本地历史, CRM 在投)+1，sheet 拉不到时退回本地序号。实测 CRM 当前最大 228 → run#14（Dog is Good，dogisgood.com/US）已由 004 改名 **229-LH1-dogisgood-US-0720-39572** 重发卡片。同批清理：run#12 Adheart 07 已拒、#13（learnhowtorap，курс类可疑且后缀取到的是 lhdeal 自身参数）与 #15（dogisgood 重复）已取消，当前待批仅 #14。**Hermes 首个真实广告已上线（2026-07-20 15:42）**：07 卡片批准 run#14 → 泵自动走完 CID 预占（1467922493，远程实查无在投）→ 消费预生成后缀 → 发布，campaign `24048569684` 已 ENABLED，Google Ads 侧核验：预算 $2/天、Manual CPC、final URL `https://www.dogisgood.com`、suffix `cid=335&mid=10258&…uid=v0304…`、关键词 `"dogisgood"` PHRASE、8 标题 RSA、campaign 级否定词 56 条——进入 monitoring（每 5 分钟点击增量换后缀 + 止损盯守） | ✅ 已上线 |
| HM-D14 | 2026-07-20 | **07 明确：换后缀与花费回流由 Hermes 自己盯（不靠人看）→ 监控巡检上线**。新增 `src/jobs/monitor-runs.js` + `POST /api/cron/monitor`（crontab 每小时 :12，走 cron-env.sh）：① **花费/点击回流**——GAQL 拉各在投 campaign 累计点击/花费，落 `run_daily_stats`（run_id×日期快照，`GET /api/stats` 可查近 14 天，是 P3 ROI/复盘的数据底座）；② **止损双卡控**——累计花费 ≥ spend_cap（$15）或投放天数 ≥ days_cap（7）任一到线 → 自动 pauseCampaign + 状态转 paused + 飞书通知 07 复盘；暂停失败单独告警要人工；③ **换后缀健康**——近 6h 换后缀/后缀生成失败 ≥3 次告警（不停投，跑旧后缀）；池空且有点击流量告警；④ **回流断流**——GAQL 连续失败 ≥3 轮告警；⑤ 告警同类 6h 去重（run_events event=alert）。实测首轮：run#14 回流成功（0 点击/$0，快照已落库）；顺手补货后缀池 4 条可用（发布消费 1 条后池曾为 0） | 待 07 验收 |
| HM-D19 | 2026-07-21 | **07 问「昨天新广告为什么没有分析报告」→ 排查出双重原因并修复**。①【事故】cron 全线停摆约 20 小时：7-20 10:38 部署时 `cron-env.sh` 被 Windows 端以 CRLF 覆盖，行尾 `\r` 被拼进 Bearer token（65 字符），Node HTTP 拒绝含 `\r` 的请求头 → 所有 cron 请求（pump/monitor/suffix-replenish/sync）返回 400 空响应且无日志，syslog 显示 cron 每 5 分钟都在触发但全部无效——monitor 停摆导致 run#14 只有发布当天 07:48 一条快照。修复：服务器端重写 cron-env.sh（LF），token 提取加 `tr -d '\r"'` 双保险；实测 pump/monitor 恢复 200，cron.log 恢复增长。②【功能缺失】「每日复盘报告」此前本就未实现（P3 遗留），补齐：新增 `src/jobs/report-daily.js` + `POST /api/cron/report-daily`（body 可传 date 补发指定日）+ crontab 每天 09:30——汇总所有已发布任务（在投/已暂停）的当日增量（相邻快照差值）与累计点击/花费、止损进度（$X/$cap、第 N/M 天），以智能助手身份发卡片（卡片挂了降级纯文本，报告不能断）；无已发布任务时静默跳过。ROI/佣金归因列待 P3 联盟返现回流接入后追加。已补发 7-20 复盘卡片（run#14：0 点击/$0，第 1/7 天）；skill 补「补发昨天的报告」用法，网关已重载 | 待 07 验收 |
| HM-D18 | 2026-07-20 | **07 规格：三条硬限也要能改——权限模型改为「对话使用者 = Owner，权限最大」**（智能助手之后可批量复制，每个实例以其对话使用者为 Owner）。① 原三条代码硬限（日预算 $5 / CPC $0.3 / 日建 5）**从代码天花板降级为 Owner 级设置项** `hard_budget_usd / hard_cpc_usd / hard_daily_runs`，07 一句话即可改；仅保留防手误的宽松绝对界限（$10000 / $100 / 1000 个，非风控，07 要多大给多大）；② 日常规则联动：`default_budget_usd / default_cpc_usd / daily_runs` 的上限从写死数值改为动态引用对应 hard_*；**下调 hard_* 时自动联动压低已超线的日常规则**（返回带 `adjusted`，助手须一并复述）；③ 强制点全部改读设置中心：建任务显式传参校验、发布泵二次校验（pump-runs assertLimits）、ai_generate 预算建议 clamp（rsa-gen）、每日新建额度（effectiveDailyRuns = min(daily_runs, hard_daily_runs)），config.js 去掉 env 的 Math.min 硬夹；④ ad-runs skill 升 v1.2.0：11 项规则对照表 + 操作红线（改 hard_* 必须 07 本人明确说出数字，助手不得为放行某次建任务自行上调上限；改前复述数字）；⑤ 服务器实测全链路：上限 5→8 ✅ → 默认预算改 $6（旧硬限下不可能）✅ → 上限下调 4 联动压低 default 6→4 ✅ → 99999 超绝对界限被拒 ✅ → 日建上限改 8 ✅ → 全部恢复默认 ✅，settings_log 流水 10 条完整可溯 | 待 07 验收 |
| HM-D17 | 2026-07-20 | **07 规格：智能助手要能自主对话 + 发消息改规则**。对话本身已通：网关（websocket，claude-opus-4-6）在线、白名单=07 open_id、带本地终端能力和 hermes-ads skills。补上「改规则」正式入口：① 新增 `src/lib/settings.js` 设置中心 + `app_settings/settings_log` 两表 + `GET/POST /api/settings`——**8 项可调规则**：默认预算(0.5~5)/默认CPC(0.05~0.3)/每日新建(0~5，0=暂停)/止损天数(1~30)/止损金额($1~100)/默认文案模式(filter/ai_generate)/后缀池目标水位(1~20)/低水位(0~19)；改动写库覆盖 .env 默认（重启不丢）、留流水（谁改的/从几到几）、超范围 400 拒绝、可一键恢复默认；三条代码硬限（$5/$0.3/5个）是天花板任何人改不了；② `create-runs/suffix-pool/planExecute` 全部改读设置中心（改动对之后新建的任务生效，在投任务止损沿用建时写入值）；③ ad-runs skill 升 v1.1.0：加「07 说法→key」对照表与操作规范（改完复述、超限如实解释），网关已重载；④ 服务器实测：list 8 项 ✅、改 daily_runs 5→3 ✅、改 9 被拒「超出允许范围 [0,5]」✅、恢复默认 ✅；已用助手身份发使用说明卡片给 07 | 待 07 验收 |
| HM-D16 | 2026-07-20 | **07 反馈：消息应由「智能助手」发出，不是 webhook 机器人**（07 开始完全交由 Hermes 自主沟通）。`src/lib/feishu.js` 重写为双通道：主通道=飞书应用 im API（tenant_access_token + open_id 私聊 07，应用与 Agent 网关同一个 = 智能助手身份），webhook 自定义机器人降级兜底。审批卡片/止损通知/监控告警/发布成功全部走助手身份。服务器实测：文本+交互卡片均以助手身份私聊送达 ✅。注意：网关（websocket 收消息）与 hermes-ads（HTTP 发消息）共用同一应用，互不冲突；07 在助手私聊里回「批准 #N」由网关 Agent 处理（skills/ad-runs），卡片按钮走签名链接不依赖网关 | 待 07 验收 |
| HM-D15 | 2026-07-20 | **07 三项规格：全联盟入池 + 白名单严格优先 + 发布即登记已测记录**。① **全联盟入池**——新增通用适配器 `src/adapters/generic.js`（monetization JSON 系 CG/EV/MUI/PM + Rewardoo form 系 RW，端点/分页/限流照抄 CRM 生产 platform-api.ts），wj07 名下 7 个联盟账号全部接入：LH1/LH2 + CG1(allurahub)/EV1(mevora)/MUI1(allurahub)/PM1(keymint)/RW1(parcelandplate)，key 复制自 CRM `platform_connections`；`offers.network` 参数化，5.1 更新至 v1.4；各联盟跳板域名入 `TRACKER_HOST_PATTERNS`；② **黑名单不测**——原有硬排除逻辑不变（黑名单命中直接不入队，从不测试）；**白名单优先测试**升级为严格优先：入队打分重点分享 +1200 / 普通分享 +1000（非白名单打分上限约 150），白名单商家必然整体置顶先测；③ **发布即登记**——任务泵发布成功（进 monitoring）当场 `registerTestedOffer` 写「已测记录」+本地缓存。写 sheet 通道：两个 SA 的 GCP 项目 Sheets API 均 403 且 SA 无权自助启用；**07 拍板不开 API** → 新增匿名浏览器写通道 `sheet-anon-write.js`（表格公开可编辑，无头 Chrome 匿名会话 Name Box 跳格键入 + CSV 复核，双通道自动降级 + pending 兜底 + `flushPendingSheetWrites` 每日补写 + 缓存回填防去重丢失）。**实测**：run#14（dogisgood.com/US）经浏览器通道 19s 写入共享表格成功，公开 CSV 复核在表 ✅。**部署实测（2026-07-20）**：全量同步入池 CG 79 / EV 215 / MUI 1631 / **PM 19847**（RW 首页即 504——RW 服务端慢，CRM 侧同账号连败 3 次同因，已放宽 120s 超时+首页重试，每日 08:00 cron 自动再试）；offers 在线 **30195**（原 8439 的 3.6 倍）；队列重建：候选 30182 → 黑名单排除 1678 → 新测队列 **20582**（LH1 3501 / LH2 12 / CG1 15 / EV1 105 / MUI1 1225 / PM1 15724），白名单命中 40 条全部置顶（Top1 Hornby UK 1125 分起，第一条非白名单排在 40 名之后）✅ | 待 07 验收 |
| HM-D08 | 2026-07-17 | **团队共享表格接入完成**：07 提供团队现有公开编辑表格（ID `1wQE3ieaVJhkDPvbpn2y6ldThh6gtowQuhW0Tzlm6p7Q`）。① 实测表内两工作表：「黑名单」（商家名称/平台/域名/下架时间/原因/来源，约 4800 行）与「白名单/推荐商家表」（商家名称/ROI参考/佣金率/结算率/标记/分享时间/备注，约 390 行），结构与原 5.4 草案不同，按团队现状适配（5.4 更新至 v1.2）；② 读通道改走**公开 CSV（gviz）**，绕开 Sheets API 未启用问题；写通道保留 SA API + `pending_sheet_writes` 兜底；③ 新增本地缓存表 `team_whitelist`，白名单命中商家入队加分（重点分享 +50 / 普通 +30）；④ 服务器实测：黑名单 4802 / 白名单 387 同步入库，队列重建 9220 候选 → 黑名单排除 **695**、白名单命中 16、新测队列 4313（较接入前 4666 净减 353，团队黑名单生效）；⑤ 「已测记录」工作表已由 Hermes 经浏览器匿名编辑通道自建（tab 名「已测记录」，表头 A1:J1 = normalized_domain/country_code/network/network_account/owner/source/tested_at/result/roi/note），服务器实测 `testedTabMissing:false` 读取正常。待确认项 #9 关闭、#12 转半开（仅写通道） | 待 07 验收 |
